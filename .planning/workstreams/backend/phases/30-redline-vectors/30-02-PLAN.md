---
phase: 30-redline-vectors
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - services/review_service.py
  - services/version_service.py
autonomous: true
requirements:
  - VEC-02
  - VEC-03
  - VEC-04

must_haves:
  truths:
    - "mark_contract_as_template сохраняет full_text документа (не только subject) и его embedding в template_embeddings"
    - "match_template использует кэшированные embeddings из template_embeddings, не пересчитывает каждый раз"
    - "Порог автоподбора шаблона поднят до 0.70 (был 0.60)"
    - "compute_embedding передаёт полный текст без среза [:8000] или [:3000]"
  artifacts:
    - path: "services/review_service.py"
      provides: "Исправленный mark_contract_as_template + кэшированный match_template"
      contains: "template_embeddings"
    - path: "services/version_service.py"
      provides: "Убранный truncation в compute_embedding"
      contains: "model.encode(text,"
  key_links:
    - from: "services/review_service.py"
      to: "modules/database.py template_embeddings"
      via: "INSERT OR REPLACE INTO template_embeddings"
      pattern: "template_embeddings"
    - from: "services/review_service.py"
      to: "contracts.full_text"
      via: "SELECT full_text FROM contracts"
      pattern: "full_text"
---

<objective>
Исправить векторный слой — full_text в шаблонах, кэш embeddings, правильный порог.

Purpose: Сейчас `mark_contract_as_template` сохраняет только поле `subject` (строка 69 review_service.py) — это 3-10 слов вместо полного договора. Из-за этого автоподбор шаблона работает только случайно. После исправления юрист помечает документ как шаблон и следующий похожий договор подбирается автоматически.

Output: Исправленные `review_service.py` и `version_service.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/30-redline-vectors/30-CONTEXT.md
@.planning/workstreams/backend/phases/30-redline-vectors/30-01-SUMMARY.md

<interfaces>
<!-- Текущий баг в mark_contract_as_template (review_service.py:58–71): -->
```python
row = db.conn.execute(
    "SELECT contract_type, filename, subject, original_path FROM contracts WHERE id=?",
    (contract_id,),
).fetchone()
# ...
content = subject or filename  # БАГ: subject = 3-10 слов, не полный текст
return add_template(db, contract_type or "Прочее", name, content, original_path)
```

<!-- Что нужно: добавить full_text в SELECT и сохранять embedding -->
<!-- Таблица contracts содержит колонку full_text (проверить через grep) -->

<!-- Текущий truncation в version_service.py:44: -->
```python
def compute_embedding(text: str) -> np.ndarray:
    model = get_embedding_model()
    return model.encode(text[:8000], normalize_embeddings=True)
    # БАГ: передаём ~1500-2000 токенов модели с лимитом 128 токенов
    # Решение: убрать срез полностью — модель сама усечёт до max_seq_length
```

<!-- Текущий порог в version_service.py:22: -->
```python
TEMPLATE_MATCH_THRESHOLD = 0.60  # нужно изменить на 0.70 (per CONTEXT.md решение)
```

<!-- template_embeddings создана в Plan 01: -->
```sql
template_embeddings (
    template_id INTEGER PRIMARY KEY REFERENCES templates(id) ON DELETE CASCADE,
    file_hash   TEXT NOT NULL,
    vector      BLOB NOT NULL,
    model_version TEXT NOT NULL,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Исправить mark_contract_as_template и добавить кэш embeddings в match_template</name>
  <files>services/review_service.py</files>
  <read_first>
    - services/review_service.py (строки 48–167) — mark_contract_as_template и match_template
    - modules/database.py (grep "full_text") — убедиться что колонка full_text существует в contracts
  </read_first>
  <action>
**Изменение 1: mark_contract_as_template — сохранять full_text + embedding**

Изменить SELECT в строке 59:
```python
# Было:
"SELECT contract_type, filename, subject, original_path FROM contracts WHERE id=?"
# Стало:
"SELECT contract_type, filename, subject, original_path, full_text FROM contracts WHERE id=?"
```

Изменить распаковку результата:
```python
# Было:
contract_type, filename, subject, original_path = row
content = subject or filename
# Стало:
contract_type, filename, subject, original_path, full_text = row
content = full_text or subject or filename  # per VEC-02: полный текст приоритетнее
```

После вызова `add_template(...)` получить template_id и сохранить embedding:
```python
template_id = add_template(db, contract_type or "Прочее", name, content, original_path)
if template_id and content:
    from services.version_service import compute_embedding, EMBEDDING_MODEL
    import io as _io
    import numpy as np
    vector = compute_embedding(content)
    buf = _io.BytesIO()
    np.save(buf, vector)
    file_hash = ""  # нет file_hash у шаблона из реестра — пустая строка как sentinel
    with db._lock:
        db.conn.execute(
            """INSERT OR REPLACE INTO template_embeddings
               (template_id, file_hash, vector, model_version)
               VALUES (?, ?, ?, ?)""",
            (template_id, file_hash, buf.getvalue(), EMBEDDING_MODEL),
        )
        db.conn.commit()
return template_id
```

**Изменение 2: match_template — использовать кэшированные embeddings**

В `match_template()` перед циклом `for tmpl in templates` добавить загрузку кэшированных векторов:
```python
# Загрузить кэшированные embeddings для всех шаблонов
cached_vectors: dict[int, np.ndarray] = {}
with db._lock:
    rows = db.conn.execute(
        "SELECT template_id, vector, model_version FROM template_embeddings"
    ).fetchall()
for row in rows:
    if row[2] == EMBEDDING_MODEL:  # пропустить если модель изменилась
        cached_vectors[row[0]] = np.load(io.BytesIO(row[1]))
```

В цикле заменить `compute_embedding(tmpl.content_text)` на:
```python
if tmpl.id in cached_vectors:
    tmpl_vector = cached_vectors[tmpl.id]
else:
    tmpl_vector = compute_embedding(tmpl.content_text)
    # (опционально: сохранить в кэш здесь же)
```

Добавить импорты в начало файла (если не присутствуют): `import io`, `import numpy as np`.
Добавить в import из version_service: `EMBEDDING_MODEL`.
  </action>
  <acceptance_criteria>
grep -n "full_text" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/review_service.py | wc -l
# >= 1

grep -n "template_embeddings" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/review_service.py | wc -l
# >= 2 (INSERT и SELECT)

grep -n "cached_vectors" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/review_service.py | wc -l
# >= 2
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from pathlib import Path
from modules.database import Database
from services.review_service import mark_contract_as_template, match_template
import tempfile, os

with tempfile.NamedTemporaryFile(suffix='.sqlite', delete=False) as f:
    tmp = f.name
db = Database(Path(tmp))

# Вставить тестовый contract с full_text
db.conn.execute('''INSERT INTO contracts
    (original_path, filename, file_hash, status, contract_type, subject, full_text)
    VALUES (?,?,?,?,?,?,?)''',
    ('/tmp/test.docx', 'test.docx', 'abc123', 'done',
     'Аренда', 'Аренда офиса', 'Настоящий договор аренды заключён между сторонами. Арендодатель передаёт помещение. Арендатор обязуется оплачивать.')
)
db.conn.commit()
contract_id = db.conn.execute('SELECT id FROM contracts WHERE file_hash=?', ('abc123',)).fetchone()[0]

tmpl_id = mark_contract_as_template(db, contract_id)
assert tmpl_id is not None, 'FAIL: mark_contract_as_template вернул None'

# Проверить что embedding сохранён
row = db.conn.execute('SELECT template_id FROM template_embeddings WHERE template_id=?', (tmpl_id,)).fetchone()
assert row is not None, 'FAIL: embedding не сохранён в template_embeddings'

os.unlink(tmp)
print('OK: mark_contract_as_template сохраняет full_text + embedding')
" 2>&1 | tail -5</automated>
  </verify>
  <done>mark_contract_as_template сохраняет full_text (не subject) и записывает embedding в template_embeddings. match_template использует кэш при повторных вызовах.</done>
</task>

<task type="auto">
  <name>Task 2: Убрать truncation в compute_embedding и поднять порог до 0.70</name>
  <files>services/version_service.py</files>
  <read_first>
    - services/version_service.py (строки 20–44) — TEMPLATE_MATCH_THRESHOLD и compute_embedding
  </read_first>
  <action>
**Изменение 1: убрать [:8000] в compute_embedding (строка ~44)**

```python
# Было:
def compute_embedding(text: str) -> np.ndarray:
    """Вычисляет 384-мерный вектор для текста. Использует первые 8000 символов (~512 токенов)."""
    model = get_embedding_model()
    return model.encode(text[:8000], normalize_embeddings=True)

# Стало:
def compute_embedding(text: str) -> np.ndarray:
    """Вычисляет 384-мерный вектор для текста. Передаёт полный текст — модель усечёт до max_seq_length."""
    model = get_embedding_model()
    return model.encode(text, normalize_embeddings=True)
```

**Изменение 2: поднять TEMPLATE_MATCH_THRESHOLD (строка ~23)**

```python
# Было:
TEMPLATE_MATCH_THRESHOLD = 0.60  # sim >= 0.60 → подходящий шаблон
# Стало:
TEMPLATE_MATCH_THRESHOLD = 0.70  # sim >= 0.70 → подходящий шаблон (per VEC-03)
```

Это два однострочных изменения. VERSION_LINK_THRESHOLD (0.85) не трогать.
  </action>
  <acceptance_criteria>
grep -n "TEMPLATE_MATCH_THRESHOLD = 0.70" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/version_service.py | wc -l
# должно быть 1

grep -n "text\[:8000\]" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/version_service.py | wc -l
# должно быть 0 (убрано)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from services.version_service import TEMPLATE_MATCH_THRESHOLD, compute_embedding
assert TEMPLATE_MATCH_THRESHOLD == 0.70, f'FAIL: threshold={TEMPLATE_MATCH_THRESHOLD}, expected 0.70'
# compute_embedding должен принять длинный текст без обрезки
import inspect
src = inspect.getsource(compute_embedding)
assert '[:8000]' not in src and '[:3000]' not in src, 'FAIL: truncation still present'
print(f'OK: threshold={TEMPLATE_MATCH_THRESHOLD}, no truncation in compute_embedding')
"</automated>
  </verify>
  <done>TEMPLATE_MATCH_THRESHOLD = 0.70. compute_embedding не обрезает текст. pytest проходит без регрессий.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# Проверить оба изменения
python -c "
from services.version_service import TEMPLATE_MATCH_THRESHOLD, compute_embedding
import inspect
assert TEMPLATE_MATCH_THRESHOLD == 0.70
src = inspect.getsource(compute_embedding)
assert '[:8000]' not in src
print('version_service: OK')
"

# Прогнать существующие тесты
python -m pytest tests/ -m "not slow" -x -q 2>&1 | tail -10
```
</verification>

<success_criteria>
1. `mark_contract_as_template` извлекает `full_text` из `contracts` (не только `subject`)
2. Embedding сохраняется в `template_embeddings` при пометке как шаблон
3. `match_template` читает кэш из `template_embeddings` — не пересчитывает каждый вызов
4. `TEMPLATE_MATCH_THRESHOLD` = 0.70
5. `compute_embedding` не содержит `[:8000]` или любого другого среза
6. Тест-сюит проходит без регрессий
</success_criteria>

<output>
После завершения создать `.planning/workstreams/backend/phases/30-redline-vectors/30-02-SUMMARY.md`
</output>
