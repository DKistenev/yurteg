---
phase: 30-redline-vectors
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - services/version_service.py
  - services/review_service.py
autonomous: true
requirements:
  - RED-02
  - RED-03

must_haves:
  truths:
    - "generate_redline_docx в version_service.py делегирует в redline_service.generate_redline_docx"
    - "review_service.py экспортирует get_redline_for_template(db, contract_id, template_id) → bytes"
    - "Старый sentence-level код generate_redline_docx в version_service.py удалён"
    - "Оба use case (версия vs версия, документ vs шаблон) используют один движок"
  artifacts:
    - path: "services/version_service.py"
      provides: "generate_redline_docx делегирует в redline_service"
      contains: "from services.redline_service import generate_redline_docx"
    - path: "services/review_service.py"
      provides: "get_redline_for_template() — redline документа против шаблона"
      exports: ["get_redline_for_template"]
  key_links:
    - from: "services/version_service.py"
      to: "services/redline_service.py"
      via: "re-export generate_redline_docx"
      pattern: "from services.redline_service import"
    - from: "services/review_service.py"
      to: "services/redline_service.py"
      via: "generate_redline_docx вызов"
      pattern: "redline_service"
---

<objective>
Переключить version_service и review_service на единый redline-движок из Plan 01.

Purpose: Сейчас есть два несовместимых redline пути — sentence-level в version_service (сломан) и никакого в review_service (вообще не подключён к редлайну). После этого плана оба пути используют один word-level движок из `redline_service.py`.

Output: Старый sentence-level код удалён, оба сервиса вызывают `redline_service.generate_redline_docx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/30-redline-vectors/30-CONTEXT.md
@.planning/workstreams/backend/phases/30-redline-vectors/30-01-SUMMARY.md

<interfaces>
<!-- services/redline_service.py (создан в Plan 01): -->
```python
def generate_redline_docx(text_old: str, text_new: str, title: str = "Редлайн") -> bytes:
    """Word-level redline. Возвращает байты .docx с track changes."""
```

<!-- services/version_service.py — текущий generate_redline_docx (строки 231-306): -->
```python
def generate_redline_docx(text_old: str, text_new: str, title: str = "Редлайн") -> bytes:
    """СТАРЫЙ: sentence-level, теряет rPr. Нужно удалить и заменить делегированием."""
    import difflib, re
    # ... 75 строк sentence-level кода ...
```

<!-- review_service.py импортирует generate_redline_docx из version_service: -->
```python
from services.version_service import (
    compute_embedding,
    generate_redline_docx,   # ← нужно перенаправить на redline_service
    TEMPLATE_MATCH_THRESHOLD,
    _cosine_sim,
)
```

<!-- Нужен новый публичный метод в review_service: -->
```python
def get_redline_for_template(
    db: Database,
    contract_id: int,
    template_id: int,
) -> Optional[bytes]:
    """Генерирует редлайн документа против шаблона. Возвращает bytes .docx или None."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Заменить generate_redline_docx в version_service.py делегированием</name>
  <files>services/version_service.py</files>
  <read_first>
    - services/version_service.py (строки 231–307) — весь старый generate_redline_docx
    - services/redline_service.py — убедиться что экспортирует generate_redline_docx
  </read_first>
  <action>
**Шаг 1:** Удалить весь блок функции `generate_redline_docx` (строки ~231–306 включительно) из `services/version_service.py`. Это ~75 строк с sentence-level логикой.

**Шаг 2:** Добавить в начало файла (после существующих import-ов):
```python
from services.redline_service import generate_redline_docx  # noqa: F401 — re-export
```

Расположить после `from modules.models import DocumentVersion` — это последний import в блоке.

`noqa: F401` нужен потому что функция re-экспортируется, а не вызывается напрямую в этом файле.

**Проверить после изменения:** `review_service.py` импортирует `generate_redline_docx` из `version_service` — этот импорт должен продолжать работать без изменений в review_service, потому что мы делаем re-export.

**Что НЕ трогать:** все остальные функции в файле (`compute_embedding`, `find_version_match`, `link_versions`, `diff_versions`, `get_version_group`, `ensure_embedding`).
  </action>
  <acceptance_criteria>
grep -n "def generate_redline_docx" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/version_service.py | wc -l
# должно быть 0 (старая реализация удалена)

grep -n "from services.redline_service" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/version_service.py | wc -l
# должно быть 1
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from services.version_service import generate_redline_docx
from services.review_service import generate_redline_docx as rdl2
assert generate_redline_docx is rdl2, 'FAIL: разные функции'
result = generate_redline_docx('старый текст договора', 'новый текст договора')
assert isinstance(result, bytes) and len(result) > 0, 'FAIL: не bytes'
print('OK: generate_redline_docx работает через redline_service')
"</automated>
  </verify>
  <done>version_service.py не содержит sentence-level кода. generate_redline_docx re-экспортируется из redline_service. Импорт из review_service работает без изменений.</done>
</task>

<task type="auto">
  <name>Task 2: Добавить get_redline_for_template в review_service.py</name>
  <files>services/review_service.py</files>
  <read_first>
    - services/review_service.py (строки 1–30, 123–167) — импорты и match_template
  </read_first>
  <action>
Добавить в конец `services/review_service.py` новую публичную функцию:

```python
def get_redline_for_template(
    db: Database,
    contract_id: int,
    template_id: int,
) -> Optional[bytes]:
    """Генерирует редлайн документа против шаблона-эталона.

    Извлекает тексты из БД (templates.content_text, contracts.full_text).
    Возвращает bytes .docx с word-level track changes или None если данные не найдены.
    """
    with db._lock:
        tmpl_row = db.conn.execute(
            "SELECT content_text, name FROM templates WHERE id=? AND is_active=1",
            (template_id,),
        ).fetchone()
        contract_row = db.conn.execute(
            "SELECT full_text, filename FROM contracts WHERE id=?",
            (contract_id,),
        ).fetchone()

    if tmpl_row is None or contract_row is None:
        logger.warning(
            "get_redline_for_template: не найден template_id=%d или contract_id=%d",
            template_id, contract_id,
        )
        return None

    template_text, template_name = tmpl_row
    contract_text, contract_filename = contract_row

    if not template_text or not contract_text:
        logger.warning(
            "get_redline_for_template: пустой текст (template=%s, contract=%s)",
            bool(template_text), bool(contract_text),
        )
        return None

    title = f"Редлайн: {contract_filename} vs {template_name}"
    return generate_redline_docx(template_text, contract_text, title)
```

Убедиться что `generate_redline_docx` уже импортирован в этом файле (через `from services.version_service import ..., generate_redline_docx`).

Проверить что импорт `Optional` присутствует в `from typing import Optional`.
  </action>
  <acceptance_criteria>
grep -n "def get_redline_for_template" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/review_service.py | wc -l
# должно быть 1

grep -n "full_text" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/review_service.py | wc -l
# >= 2 (в mark_contract_as_template и get_redline_for_template)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from pathlib import Path
from modules.database import Database
from services.review_service import get_redline_for_template, add_template
import tempfile, os, zipfile, io as _io

with tempfile.NamedTemporaryFile(suffix='.sqlite', delete=False) as f:
    tmp = f.name
db = Database(Path(tmp))

# Вставить contract с full_text
db.conn.execute('''INSERT INTO contracts
    (original_path, filename, file_hash, status, full_text)
    VALUES (?,?,?,?,?)''',
    ('/tmp/c.docx', 'c.docx', 'h1', 'done',
     'Договор аренды. Арендодатель передаёт помещение по адресу Москва.')
)
db.conn.commit()
cid = db.conn.execute('SELECT id FROM contracts WHERE file_hash=?', ('h1',)).fetchone()[0]

# Добавить шаблон
tid = add_template(db, 'Аренда', 'Эталон аренды',
    'Договор аренды. Арендодатель передаёт помещение по адресу Санкт-Петербург.')

result = get_redline_for_template(db, cid, tid)
assert result is not None, 'FAIL: вернул None'
assert isinstance(result, bytes), 'FAIL: не bytes'

# Проверить XML
with zipfile.ZipFile(_io.BytesIO(result)) as z:
    xml = z.read('word/document.xml').decode()
assert 'w:del' in xml or 'w:ins' in xml, 'FAIL: нет track changes'

os.unlink(tmp)
print('OK: get_redline_for_template возвращает валидный DOCX с track changes')
" 2>&1 | tail -5</automated>
  </verify>
  <done>get_redline_for_template экспортирован из review_service.py. Принимает db + два ID, возвращает bytes .docx с word-level track changes. None если данные отсутствуют.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# Убедиться что оба сервиса используют один движок
python -c "
from services.version_service import generate_redline_docx as v_rdl
from services.redline_service import generate_redline_docx as rdl
assert v_rdl is rdl, 'Разные функции!'
print('OK: единый движок')
"

# Прогнать тест-сюит
python -m pytest tests/ -m "not slow" -x -q 2>&1 | tail -10

# Итоговая проверка: оба use case
python -c "
from services.version_service import generate_redline_docx
from services.review_service import get_redline_for_template
print('version use case: imports OK')
print('template use case: imports OK')
"
```
</verification>

<success_criteria>
1. `services/version_service.py` не содержит sentence-level реализации `generate_redline_docx`
2. `version_service.generate_redline_docx` is `redline_service.generate_redline_docx` (один объект)
3. `review_service.get_redline_for_template(db, contract_id, template_id)` → `bytes` работает
4. Тест-сюит (`pytest -m "not slow"`) проходит без регрессий
5. Оба вызова производят DOCX с `w:del`/`w:ins` в XML
</success_criteria>

<output>
После завершения создать `.planning/workstreams/backend/phases/30-redline-vectors/30-03-SUMMARY.md`
</output>
