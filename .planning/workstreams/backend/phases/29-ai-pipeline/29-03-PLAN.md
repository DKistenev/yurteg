---
phase: 29-ai-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/postprocessor.py
autonomous: true
requirements:
  - AI-03
must_haves:
  truths:
    - "NDA не удаляется из contract_type при cyrillic_only профиле"
    - "SLA, GPS, ИНН, МРОТ, НДС, ООО, ИП, ЗАО, ОАО сохраняются в cyrillic_only полях"
    - "Остальные латинские символы по-прежнему удаляются из cyrillic_only полей"
    - "cyrillic_latin профиль не затронут"
  artifacts:
    - path: "modules/postprocessor.py"
      provides: "ABBREVIATION_WHITELIST + _protect_abbreviations() + _restore_abbreviations()"
  key_links:
    - from: "_sanitize_string() cyrillic_only ветка"
      to: "_RE_CYRILLIC_ONLY.sub()"
      via: "аббревиатуры защищаются до regex и восстанавливаются после"
---

<objective>
Починить постпроцессор — whitelist латинских аббревиатур в cyrillic_only профиле.

Purpose: Текущий _RE_CYRILLIC_ONLY удаляет все латинские символы, включая NDA, SLA, GPS, ИНН — юридически важные сокращения в типах договоров.
Output: postprocessor.py с protect/restore механизмом для whitelist аббревиатур.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/29-ai-pipeline/29-CONTEXT.md

<interfaces>
<!-- modules/postprocessor.py — текущий _sanitize_string() cyrillic_only ветка: -->
if profile == "cyrillic_only":
    cleaned = _RE_CYRILLIC_ONLY.sub("", value).strip()
    return cleaned if cleaned else None

# _RE_CYRILLIC_ONLY удаляет [^\u0400-\u04FF\u0451\u0401\s\d.,;:!?()«»"—–-/№%₽]
# то есть любые латинские символы включая NDA, SLA, GPS

<!-- Whitelist из CONTEXT.md: NDA, SLA, GPS, ИНН, МРОТ, НДС, ООО, ИП, ЗАО, ОАО -->
<!-- Подход: protect/restore через placeholder до/после regex -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Whitelist аббревиатур в postprocessor.py</name>
  <files>modules/postprocessor.py, tests/test_postprocessor.py</files>
  <read_first>
    - modules/postprocessor.py — полный файл перед изменениями
    - tests/test_postprocessor.py — если существует, читать перед добавлением тестов
  </read_first>
  <behavior>
    - sanitize_metadata({"contract_type": "NDA о конфиденциальности"})["contract_type"] == "NDA о конфиденциальности"
    - sanitize_metadata({"contract_type": "Договор SLA с поддержкой"})["contract_type"] == "Договор SLA с поддержкой"
    - sanitize_metadata({"contract_type": "Соглашение NDA и GPS трекинг"})["contract_type"] == "Соглашение NDA и GPS трекинг"
    - sanitize_metadata({"contract_type": "Договор abc def"})["contract_type"] == "Договор "  # не в whitelist — удаляется
    - sanitize_metadata({"contract_type": "Договор NDA abc"})["contract_type"] == "Договор NDA "  # NDA сохранён, abc удалён
    - sanitize_metadata({"special_conditions": ["Штраф при нарушении NDA"]})["special_conditions"] == ["Штраф при нарушении NDA"]
    - cyrillic_latin профиль (counterparty, subject) не затронут — test: sanitize_metadata({"counterparty": "ООО Alpha"})["counterparty"] == "ООО Alpha"
  </behavior>
  <action>
В modules/postprocessor.py:

1. Добавить ABBREVIATION_WHITELIST после константы _NULL_STRINGS:
```python
# Аббревиатуры, которые НЕ удаляются из cyrillic_only полей
# Источник: CONTEXT.md Phase 29 — юридически важные сокращения
ABBREVIATION_WHITELIST: tuple[str, ...] = (
    "NDA", "SLA", "GPS",
    "ИНН", "МРОТ", "НДС", "ООО", "ИП", "ЗАО", "ОАО",
)
```

2. Добавить функции protect/restore после ABBREVIATION_WHITELIST:
```python
def _protect_abbreviations(value: str) -> tuple[str, dict[str, str]]:
    """Заменяет аббревиатуры из whitelist на placeholder-ы перед regex.

    Returns:
        (protected_value, {placeholder: original_abbr})
    """
    placeholders: dict[str, str] = {}
    for i, abbr in enumerate(ABBREVIATION_WHITELIST):
        # Граница слова: аббревиатура не должна быть частью другого слова
        pattern = re.compile(r'(?<![A-Za-zА-Яа-яЁё])' + re.escape(abbr) + r'(?![A-Za-zА-Яа-яЁё])')
        placeholder = f"\x00ABBR{i}\x00"
        if pattern.search(value):
            value = pattern.sub(placeholder, value)
            placeholders[placeholder] = abbr
    return value, placeholders


def _restore_abbreviations(value: str, placeholders: dict[str, str]) -> str:
    """Восстанавливает аббревиатуры из placeholder-ов."""
    for placeholder, abbr in placeholders.items():
        value = value.replace(placeholder, abbr)
    return value
```

3. Обновить ветку cyrillic_only в _sanitize_string():
```python
if profile == "cyrillic_only":
    # Защищаем аббревиатуры из whitelist перед regex
    protected, placeholders = _protect_abbreviations(value)
    cleaned = _RE_CYRILLIC_ONLY.sub("", protected).strip()
    # Восстанавливаем аббревиатуры
    if placeholders:
        cleaned = _restore_abbreviations(cleaned, placeholders)
        # Почистить возможные двойные пробелы после удаления не-whitelist латиницы
        cleaned = re.sub(r' {2,}', ' ', cleaned).strip()
    return cleaned if cleaned else None
```

4. В tests/test_postprocessor.py добавить тесты (если файл не существует — создать):
```python
"""Тесты постпроцессора."""
import pytest
from modules.postprocessor import sanitize_metadata, ABBREVIATION_WHITELIST


class TestAbbreviationWhitelist:
    def test_nda_preserved_in_contract_type(self):
        result = sanitize_metadata({"contract_type": "NDA о конфиденциальности"})
        assert result["contract_type"] == "NDA о конфиденциальности"

    def test_sla_preserved(self):
        result = sanitize_metadata({"contract_type": "Договор SLA с поддержкой"})
        assert result["contract_type"] == "Договор SLA с поддержкой"

    def test_multiple_abbreviations_preserved(self):
        result = sanitize_metadata({"contract_type": "Соглашение NDA и GPS трекинг"})
        assert result["contract_type"] == "Соглашение NDA и GPS трекинг"

    def test_non_whitelist_latin_removed(self):
        result = sanitize_metadata({"contract_type": "Договор abc def"})
        # abc и def — не в whitelist, удаляются
        assert "abc" not in (result["contract_type"] or "")
        assert "def" not in (result["contract_type"] or "")

    def test_mixed_whitelist_and_other_latin(self):
        result = sanitize_metadata({"contract_type": "Договор NDA abc"})
        r = result["contract_type"] or ""
        assert "NDA" in r
        assert "abc" not in r

    def test_special_conditions_nda_preserved(self):
        result = sanitize_metadata({"special_conditions": ["Штраф при нарушении NDA"]})
        assert result["special_conditions"] == ["Штраф при нарушении NDA"]

    def test_cyrillic_latin_profile_unchanged(self):
        # counterparty использует cyrillic_latin — не затронут
        result = sanitize_metadata({"counterparty": "ООО Alpha Ltd"})
        assert result["counterparty"] == "ООО Alpha Ltd"

    def test_inn_abbreviation_preserved(self):
        # ИНН — кириллица, должна работать и без whitelist, но test на всякий случай
        result = sanitize_metadata({"contract_type": "Договор ИНН стороны"})
        assert "ИНН" in (result["contract_type"] or "")
```
  </action>
  <verify>
    <automated>pytest tests/test_postprocessor.py -v -q</automated>
  </verify>
  <done>Все 8 тестов TestAbbreviationWhitelist проходят; NDA/SLA/GPS сохраняются в cyrillic_only полях; остальная латиница удаляется</done>
</task>

</tasks>

<verification>
```bash
# Тесты постпроцессора
pytest tests/test_postprocessor.py -v

# Быстрая проверка вручную
python -c "
from modules.postprocessor import sanitize_metadata
r1 = sanitize_metadata({'contract_type': 'NDA о конфиденциальности'})
assert r1['contract_type'] == 'NDA о конфиденциальности', f'FAIL: {r1}'
r2 = sanitize_metadata({'contract_type': 'Договор ABC неизвестный'})
assert 'ABC' not in (r2['contract_type'] or ''), f'FAIL: ABC не удалён: {r2}'
print('OK: whitelist работает корректно')
"

# Общий suite
pytest -x -q
```
</verification>

<success_criteria>
- ABBREVIATION_WHITELIST = (NDA, SLA, GPS, ИНН, МРОТ, НДС, ООО, ИП, ЗАО, ОАО)
- _protect_abbreviations() и _restore_abbreviations() реализованы
- cyrillic_only ветка в _sanitize_string() использует protect/restore
- Все 8 тестов TestAbbreviationWhitelist проходят
- pytest -x -q без новых падений
</success_criteria>

<output>
После выполнения создать `.planning/workstreams/backend/phases/29-ai-pipeline/29-03-SUMMARY.md`
</output>
