---
phase: 29-ai-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/contract.gbnf
  - services/llama_server.py
autonomous: true
requirements:
  - AI-01
must_haves:
  truths:
    - "Грамматика не содержит поля confidence — модель не генерирует это поле"
    - "Поле contract_number присутствует в грамматике как опциональное"
    - "Месяцы в date ограничены 01-12, а не 00-19"
    - "llama-server запускается без флага --grammar-file"
    - "Грамматика передаётся через тело запроса, что позволяет отключать её для logprobs-запроса"
  artifacts:
    - path: "data/contract.gbnf"
      provides: "Обновлённая GBNF грамматика без confidence, с contract_number"
    - path: "services/llama_server.py"
      provides: "start() без параметра grammar_path и флага --grammar-file"
  key_links:
    - from: "services/llama_server.py start()"
      to: "subprocess cmd_base"
      via: "отсутствие --grammar-file в команде"
      pattern: "grammar-file"
---

<objective>
Доработать GBNF-грамматику под актуальную схему и убрать grammar-file из server flag.

Purpose: GBNF + logprobs несовместимы в b5606 при server-level grammar. Перенос грамматики в request body позволяет делать два запроса: один с grammar (структурированные данные), второй без (logprobs).
Output: Обновлённый data/contract.gbnf + llama_server.py без --grammar-file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/29-ai-pipeline/29-CONTEXT.md
@.planning/workstreams/backend/ROADMAP.md

<interfaces>
<!-- Текущий root в data/contract.gbnf: -->
root ::= "{" ws (
    "\"contract_type\""    ws ":" ws str-or-null ws ","  ws
    "\"counterparty\""     ws ":" ws str-or-null ws ","  ws
    "\"subject\""          ws ":" ws str-or-null ws ","  ws
    "\"date_signed\""      ws ":" ws date-or-null ws "," ws
    "\"date_start\""       ws ":" ws date-or-null ws "," ws
    "\"date_end\""         ws ":" ws date-or-null ws "," ws
    "\"amount\""           ws ":" ws str-or-null ws ","  ws
    "\"special_conditions\"" ws ":" ws str-array ws ","  ws
    "\"parties\""          ws ":" ws str-array ws ","    ws
    "\"confidence\""       ws ":" ws confidence ws ","   ws   # <-- УДАЛИТЬ
    "\"is_template\""      ws ":" ws boolean ws ","      ws
    "\"payment_terms\""    ws ":" ws str-or-null ws ","  ws
    "\"payment_amount\""   ws ":" ws number-or-null ws "," ws
    "\"payment_frequency\"" ws ":" ws payment-frequency ws "," ws
    "\"payment_direction\"" ws ":" ws payment-direction
) ws "}"

# Текущий date-or-null — месяцы 00-19 (ошибка):
date-or-null ::= "\"" [0-9] [0-9] [0-9] [0-9] "-" [0-1] [0-9] "-" [0-3] [0-9] "\"" | "null"

<!-- LlamaServerManager.start() принимает grammar_path: Optional[Path] = None -->
<!-- При grammar_path is not None добавляет --grammar-file в cmd_base -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить GBNF грамматику</name>
  <files>data/contract.gbnf</files>
  <read_first>
    - data/contract.gbnf — полный файл, читать перед любыми изменениями
  </read_first>
  <action>
Переписать data/contract.gbnf со следующими изменениями:

1. Удалить поле "confidence" из root — убрать строку:
   `"\"confidence\""       ws ":" ws confidence ws ","   ws`
   и саму rule `confidence ::= ...`

2. Добавить опциональное поле "contract_number" после "amount":
   `"\"contract_number\""  ws ":" ws str-or-null ws ","  ws`

3. Ужесточить месяцы в date-or-null: заменить [0-1][0-9] на (("0" [1-9]) | ("1" [0-2])):
   БЫЛО: `date-or-null ::= "\"" [0-9] [0-9] [0-9] [0-9] "-" [0-1] [0-9] "-" [0-3] [0-9] "\"" | "null"`
   СТАЛО: `date-or-null ::= "\"" [0-9] [0-9] [0-9] [0-9] "-" (("0" [1-9]) | ("1" [0-2])) "-" (("0" [1-9]) | ([1-2] [0-9]) | ("3" [0-1])) "\"" | "null"`

4. Обновить комментарий в шапке файла — отразить что confidence удалён, добавлен contract_number.

Сохранить все остальные правила без изменений (str-or-null, str-array, boolean, number-or-null, payment-frequency, payment-direction, string, char, escape, ws).
  </action>
  <verify>
    <automated>python -c "
content = open('data/contract.gbnf').read()
assert 'confidence' not in content, 'confidence всё ещё есть в грамматике'
assert 'contract_number' in content, 'contract_number отсутствует'
assert '0-1' not in content or '\"0\" [1-9]' in content, 'месяцы не ужесточены'
print('OK: GBNF обновлён корректно')
"
    </automated>
  </verify>
  <done>data/contract.gbnf не содержит "confidence", содержит "contract_number", месяцы 01-12 вместо 00-19</done>
</task>

<task type="auto">
  <name>Task 2: Убрать --grammar-file из llama_server.py</name>
  <files>services/llama_server.py</files>
  <read_first>
    - services/llama_server.py — полный файл, читать перед изменениями
  </read_first>
  <action>
В services/llama_server.py:

1. Изменить сигнатуру метода `start()` — убрать параметр `grammar_path`:
   БЫЛО: `def start(self, grammar_path: Optional[Path] = None) -> None:`
   СТАЛО: `def start(self) -> None:`

2. Удалить весь блок условного добавления грамматики в команду:
   БЫЛО:
   ```python
   if grammar_path is not None and grammar_path.exists():
       cmd_base += ["--grammar-file", str(grammar_path)]
   ```
   СТАЛО: этот блок удалить полностью.

3. Убрать импорт Optional из typing если он больше не используется — проверить другие использования Optional в файле перед удалением. Если Optional используется в `self._process: Optional[subprocess.Popen]` — оставить импорт.

4. Обновить docstring метода start() — убрать упоминание grammar_path из Args.

Найти все вызовы start() в кодовой базе и убрать аргумент grammar_path:
- grep -r "start(grammar_path" . --include="*.py"
- grep -r "\.start(" . --include="*.py" | grep -v test | grep -v ".pyc"
  </action>
  <verify>
    <automated>python -c "
import ast, sys
with open('services/llama_server.py') as f:
    src = f.read()
assert 'grammar_path' not in src, 'grammar_path ещё остался в файле'
assert '--grammar-file' not in src, '--grammar-file ещё в команде'
# Проверить что файл валидный Python
ast.parse(src)
print('OK: grammar_path и --grammar-file удалены, файл синтаксически корректен')
"
    </automated>
  </verify>
  <done>services/llama_server.py не содержит grammar_path и --grammar-file; start() вызывается без аргументов во всей кодовой базе</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:

```bash
# Грамматика обновлена
python -c "
content = open('data/contract.gbnf').read()
assert 'confidence' not in content
assert 'contract_number' in content
print('GBNF OK')
"

# llama_server.py чист
python -c "
src = open('services/llama_server.py').read()
assert 'grammar_path' not in src
assert '--grammar-file' not in src
print('llama_server OK')
"

# Нет вызовов start() с grammar_path
grep -r "start(grammar" . --include="*.py" && echo "FAIL: found start(grammar calls" || echo "OK: no start(grammar calls"

# Тесты проходят
pytest -x -q
```
</verification>

<success_criteria>
- data/contract.gbnf: нет "confidence", есть "contract_number", месяцы строго 01-12
- services/llama_server.py: нет grammar_path, нет --grammar-file
- pytest -x -q проходит без новых падений
</success_criteria>

<output>
После выполнения создать `.planning/workstreams/backend/phases/29-ai-pipeline/29-01-SUMMARY.md`
</output>
