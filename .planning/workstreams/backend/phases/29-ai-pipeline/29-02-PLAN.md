---
phase: 29-ai-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - providers/ollama.py
  - modules/ai_extractor.py
autonomous: true
requirements:
  - AI-02
must_haves:
  truths:
    - "Первый запрос к llama-server идёт с grammar в теле (структурированные данные)"
    - "Второй запрос без grammar возвращает logprobs на ключевых полях"
    - "Confidence в ContractMetadata вычислен из logprobs, а не из GBNF-вывода"
    - "Документы с подозрительными null-полями проходят второй запрос"
    - "Документы без подозрительных null-полей второй запрос не делают (экономия)"
    - "Низкий confidence (logprobs < -2.0 на ключевых полях) помечает документ needs_review"
  artifacts:
    - path: "providers/ollama.py"
      provides: "complete() с grammar в request body; get_logprobs() для второго запроса"
    - path: "modules/ai_extractor.py"
      provides: "Двухзапросный flow в extract_metadata() + вычисление confidence из logprobs"
  key_links:
    - from: "modules/ai_extractor.py extract_metadata()"
      to: "providers/ollama.py complete()"
      via: "grammar передаётся как kwargs['grammar'] в request body"
    - from: "modules/ai_extractor.py _compute_confidence()"
      to: "providers/ollama.py get_logprobs()"
      via: "второй запрос без grammar, возвращает dict[field, logprob]"
    - from: "ContractMetadata.confidence"
      to: "modules/database.py"
      via: "значение logprobs-confidence сохраняется в колонку contracts.confidence"
---

<objective>
Реализовать confidence через logprobs — два запроса к llama-server, grammar только в первом.

Purpose: GBNF + logprobs несовместимы на уровне сервера. Передача grammar через request body (параметр "grammar") позволяет делать второй запрос без grammar и получать осмысленные logprobs.
Output: OllamaProvider с grammar-in-body и get_logprobs(); extract_metadata() с двухзапросным flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/phases/29-ai-pipeline/29-CONTEXT.md
@.planning/workstreams/backend/phases/29-ai-pipeline/29-01-SUMMARY.md

<interfaces>
<!-- providers/ollama.py — текущий complete() без grammar и logprobs: -->
def complete(self, messages: list[dict], **kwargs) -> str:
    response = self._client.chat.completions.create(
        model="local",
        temperature=0.05,
        max_tokens=512,
        messages=messages,
    )
    content = response.choices[0].message.content
    ...

<!-- modules/ai_extractor.py — текущий extract_metadata() вызывает _try_provider(): -->
raw_text = _try_provider(provider, messages, config.ai_max_retries)

<!-- ContractMetadata в modules/models.py: -->
@dataclass
class ContractMetadata:
    ...
    confidence: float = 0.0
    ...

<!-- КРИТИЧЕСКОЕ РЕШЕНИЕ из CONTEXT.md:
- grammar передаётся через тело запроса: параметр "grammar" в JSON body (llama-server extension)
- НЕ json_schema, именно "grammar" (сырой GBNF текст)
- Второй запрос только если есть подозрительные null-поля (contract_type, counterparty, amount)
- Порог: logprobs < -2.0 → needs_review
- Ключевые поля для logprobs: contract_type, counterparty, amount
- ВАЖНО: перед реализацией — curl-тест logprobs против работающего llama-server b5606
-->

<!-- Как получить logprobs через OpenAI-compatible API (llama-server b5606):
POST /v1/chat/completions
{
  "model": "local",
  "messages": [...],
  "logprobs": true,
  "top_logprobs": 1
}
Response: choices[0].logprobs.content[i].logprob  # для каждого токена
-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Curl-тест logprobs против живого llama-server</name>
  <what-built>Ничего — это проверка совместимости API перед реализацией</what-built>
  <how-to-verify>
Перед написанием кода ОБЯЗАТЕЛЬНО убедиться, что llama-server b5606 поддерживает logprobs и grammar в request body.

Шаг 1 — Запустить llama-server (если не запущен):
```bash
python -c "from services.llama_server import LlamaServerManager; m = LlamaServerManager(); m.start()"
```
Подождать ~30 сек до старта.

Шаг 2 — Тест grammar в request body:
```bash
curl -s http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "local",
    "messages": [{"role": "user", "content": "Ответь JSON: {\"test\": \"ok\"}"}],
    "grammar": "root ::= \"{\" ws \"\\\"test\\\"\" ws \":\" ws \"\\\"ok\\\"\" ws \"}\" ws\nws ::= [ \\t\\n\\r]*",
    "max_tokens": 50
  }' | python -m json.tool | grep -A2 '"content"'
```
Ожидаемый результат: {"test": "ok"}

Шаг 3 — Тест logprobs без grammar:
```bash
curl -s http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "local",
    "messages": [{"role": "user", "content": "Скажи одно слово: договор"}],
    "logprobs": true,
    "top_logprobs": 1,
    "max_tokens": 5
  }' | python -m json.tool | grep -A5 '"logprobs"'
```
Ожидаемый результат: choices[0].logprobs.content — массив токенов с logprob значениями (отрицательные числа).

Если grammar работает и logprobs возвращаются → типизируй "approved".
Если logprobs отсутствуют (null) → опиши формат ответа для корректировки реализации.
  </how-to-verify>
  <resume-signal>Напиши "approved" если оба теста прошли, или опиши что вернул сервер</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Добавить grammar и get_logprobs в OllamaProvider</name>
  <files>providers/ollama.py</files>
  <read_first>
    - providers/ollama.py — полный файл перед изменениями
    - data/contract.gbnf — нужен для get_grammar_content() хелпера
  </read_first>
  <action>
В providers/ollama.py:

1. Обновить complete() — передавать grammar из kwargs в request body через extra_body:
```python
def complete(self, messages: list[dict], **kwargs) -> str:
    extra_body: dict = {}
    if "grammar" in kwargs:
        extra_body["grammar"] = kwargs.pop("grammar")

    response = self._client.chat.completions.create(
        model="local",
        temperature=kwargs.get("temperature", 0.05),
        max_tokens=kwargs.get("max_tokens", 512),
        messages=messages,
        extra_body=extra_body if extra_body else None,
    )
    content = response.choices[0].message.content
    if not content:
        raise RuntimeError("llama-server вернул пустой ответ")
    return content
```

2. Добавить метод get_logprobs() — второй запрос без grammar для получения logprobs:
```python
def get_logprobs(
    self,
    messages: list[dict],
    fields_to_check: list[str],
) -> dict[str, float]:
    """Второй запрос без grammar — возвращает min logprob по ключевым полям.

    Просит модель повторить значения ключевых полей. По logprobs токенов
    вычисляет уверенность модели.

    Returns:
        dict field -> min_logprob (отрицательное число, ближе к 0 = увереннее)
        Пустой dict если logprobs недоступны.
    """
    # Промпт: попросить повторить конкретные поля
    field_list = ", ".join(fields_to_check)
    probe_messages = messages + [{
        "role": "assistant",
        "content": None,  # prefill — пусть модель продолжает
    }]
    # Упрощённый подход: один запрос с probe-промптом
    probe_user = (
        f"Из этого документа верни ТОЛЬКО JSON с полями: {field_list}. "
        "Без объяснений, только JSON."
    )
    probe_messages = [messages[0], {"role": "user", "content": probe_user}]

    try:
        response = self._client.chat.completions.create(
            model="local",
            temperature=0.0,  # детерминировано для logprobs
            max_tokens=100,
            messages=probe_messages,
            logprobs=True,
            top_logprobs=1,
        )
    except Exception as exc:
        logger.warning("get_logprobs: запрос не удался — %s", exc)
        return {}

    # Парсим logprobs из ответа
    logprobs_content = getattr(
        getattr(response.choices[0], "logprobs", None), "content", None
    )
    if not logprobs_content:
        logger.debug("get_logprobs: logprobs отсутствуют в ответе")
        return {}

    # Берём минимальный logprob по всем токенам — грубая оценка уверенности
    all_logprobs = [token.logprob for token in logprobs_content if token.logprob is not None]
    if not all_logprobs:
        return {}

    # Возвращаем агрегированный результат: среднее и минимум
    return {
        "_min": min(all_logprobs),
        "_mean": sum(all_logprobs) / len(all_logprobs),
    }
```

3. Добавить импорт logging если не добавлен (уже есть в файле).
  </action>
  <verify>
    <automated>python -c "
import ast
src = open('providers/ollama.py').read()
ast.parse(src)
assert 'extra_body' in src, 'extra_body отсутствует'
assert 'get_logprobs' in src, 'get_logprobs метод отсутствует'
assert 'logprobs=True' in src, 'logprobs=True отсутствует в get_logprobs запросе'
print('OK: OllamaProvider обновлён')
"
    </automated>
  </verify>
  <done>OllamaProvider.complete() принимает grammar в kwargs и передаёт через extra_body; get_logprobs() делает второй запрос с logprobs=True</done>
</task>

<task type="auto">
  <name>Task 2: Двухзапросный flow и confidence в ai_extractor.py</name>
  <files>modules/ai_extractor.py</files>
  <read_first>
    - modules/ai_extractor.py — полный файл перед изменениями
    - providers/ollama.py — интерфейс get_logprobs() (после Task 1)
    - data/contract.gbnf — содержимое для grammar kwarg
  </read_first>
  <action>
В modules/ai_extractor.py:

1. Добавить функцию _load_grammar() в начало файла после импортов:
```python
def _load_grammar() -> str | None:
    """Загружает GBNF грамматику из data/contract.gbnf."""
    grammar_path = Path(__file__).parent.parent / "data" / "contract.gbnf"
    try:
        return grammar_path.read_text(encoding="utf-8")
    except OSError as exc:
        logger.warning("Не удалось загрузить GBNF грамматику: %s", exc)
        return None
```

2. Добавить функцию _has_suspicious_nulls() для определения нужен ли второй запрос:
```python
_KEY_FIELDS = ("contract_type", "counterparty", "amount")

def _has_suspicious_nulls(metadata: ContractMetadata) -> bool:
    """True если хотя бы одно ключевое поле пустое — требуется logprobs-проверка."""
    return any(
        getattr(metadata, field) is None
        for field in _KEY_FIELDS
    )
```

3. Добавить функцию _compute_confidence_from_logprobs():
```python
_LOGPROB_THRESHOLD = -2.0  # порог из CONTEXT.md

def _compute_confidence_from_logprobs(
    provider: "LLMProvider",
    messages: list[dict],
) -> float:
    """Вычисляет confidence через logprobs второго запроса.

    Returns:
        float 0.0..1.0 — confidence из logprobs.
        0.0 если get_logprobs недоступен у провайдера.
    """
    if not hasattr(provider, "get_logprobs"):
        return 0.0

    try:
        lp = provider.get_logprobs(messages, list(_KEY_FIELDS))
    except Exception as exc:
        logger.warning("logprobs запрос не удался: %s", exc)
        return 0.0

    if not lp:
        return 0.0

    min_lp = lp.get("_min", 0.0)
    mean_lp = lp.get("_mean", 0.0)

    # Нормализация: -2.0 → 0.5, 0.0 → 1.0, -4.0 → 0.0
    # Линейная нормализация от диапазона [-4, 0] → [0, 1]
    normalized = max(0.0, min(1.0, (mean_lp + 4.0) / 4.0))

    logger.debug(
        "logprobs confidence: min=%.3f mean=%.3f → confidence=%.3f",
        min_lp, mean_lp, normalized,
    )
    return normalized
```

4. Обновить блок extract_metadata() — добавить grammar в первый запрос и confidence через logprobs для подозрительных документов:

В блоке `if provider is not None:` после `messages = [...]` добавить передачу grammar:
```python
# Загрузить грамматику для первого запроса (только для OllamaProvider)
grammar_content: str | None = None
if config.active_provider == "ollama":
    grammar_content = _load_grammar()

# В _try_provider call добавить grammar kwarg:
raw_text = _try_provider(
    provider, messages, config.ai_max_retries,
    grammar=grammar_content,
)
```

5. Обновить _try_provider() — передавать extra kwargs в provider.complete():
```python
def _try_provider(
    provider: "LLMProvider",
    messages: list[dict],
    max_retries: int,
    **kwargs,  # передаётся в provider.complete()
) -> str:
    ...
    raw_text = provider.complete(messages, **kwargs)
    ...
```

6. После получения result (ContractMetadata) добавить confidence-запрос:
```python
# После строки: result = _json_to_metadata(json_data)
# Для Ollama — вычислить confidence через logprobs если нужно
if (
    isinstance(result, ContractMetadata)
    and config.active_provider == "ollama"
    and _has_suspicious_nulls(result)
):
    confidence = _compute_confidence_from_logprobs(provider, messages)
    result.confidence = confidence
    if confidence < _LOGPROB_THRESHOLD / -4.0:  # порог: нормализованное < 0.5
        logger.info(
            "Документ помечен для проверки: logprobs confidence=%.3f",
            confidence,
        )
```

ВАЖНО: не трогать sanitize_metadata, fallback-логику и verify_metadata.
  </action>
  <verify>
    <automated>python -c "
import ast
src = open('modules/ai_extractor.py').read()
ast.parse(src)
assert '_load_grammar' in src, '_load_grammar отсутствует'
assert '_has_suspicious_nulls' in src, '_has_suspicious_nulls отсутствует'
assert '_compute_confidence_from_logprobs' in src, '_compute_confidence_from_logprobs отсутствует'
assert '_LOGPROB_THRESHOLD' in src, '_LOGPROB_THRESHOLD отсутствует'
assert 'grammar=grammar_content' in src or 'grammar_content' in src, 'grammar не передаётся в запрос'
print('OK: ai_extractor обновлён')
"
    </automated>
  </verify>
  <done>extract_metadata() передаёт grammar в первый запрос; для документов с пустыми ключевыми полями делает второй запрос get_logprobs(); confidence = logprobs-значение, не из GBNF</done>
</task>

</tasks>

<verification>
После выполнения всех задач:

```bash
# Синтаксис файлов корректен
python -c "
import ast
for f in ['providers/ollama.py', 'modules/ai_extractor.py']:
    ast.parse(open(f).read())
    print(f'OK: {f}')
"

# Тесты не сломаны
pytest -x -q

# Проверка наличия ключевых функций
grep -n "_load_grammar\|_has_suspicious_nulls\|_compute_confidence_from_logprobs\|get_logprobs\|extra_body" \
  providers/ollama.py modules/ai_extractor.py
```
</verification>

<success_criteria>
- OllamaProvider.complete() передаёт grammar через extra_body при наличии в kwargs
- OllamaProvider.get_logprobs() делает запрос с logprobs=True
- extract_metadata() для ollama: первый запрос с grammar, второй (только при suspicious nulls) через get_logprobs()
- ContractMetadata.confidence = нормализованный logprobs, не из GBNF
- pytest -x -q проходит без новых падений
</success_criteria>

<output>
После выполнения создать `.planning/workstreams/backend/phases/29-ai-pipeline/29-02-SUMMARY.md`
</output>
