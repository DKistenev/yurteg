---
phase: 28-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/ai_extractor.py
  - config.py
  - app/pages/settings.py
autonomous: true
requirements: [CLEAN-03, CLEAN-04, CLEAN-05]

must_haves:
  truths:
    - "python -c 'from modules.ai_extractor import extract_metadata' завершается без ошибок"
    - "grep '_create_client\\|_try_model' modules/ai_extractor.py возвращает только определения в _try_provider (если есть) или 0"
    - "grep 'ai_disable_thinking' config.py возвращает 0 совпадений"
    - "Страница настроек рендерится без раздела Thinking mode"
    - "from openai import OpenAI, APIError... в ai_extractor.py удалён (не нужен без _create_client/_try_model)"
  artifacts:
    - path: "modules/ai_extractor.py"
      provides: "AI extractor без _create_client, _try_model, без мёртвых облачных imports"
    - path: "config.py"
      provides: "Config без поля ai_disable_thinking"
    - path: "app/pages/settings.py"
      provides: "Settings UI без строки Thinking mode"
  key_links:
    - from: "modules/ai_extractor.py"
      to: "providers/base.py"
      via: "_try_provider — остаётся (это живой код)"
      pattern: "_try_provider"
    - from: "app/pages/settings.py"
      to: "config.save_setting"
      via: "thinking mode toggle — УДАЛИТЬ"
      pattern: "ai_disable_thinking"
---

<objective>
Удалить deprecated _create_client/_try_model из ai_extractor.py, убрать thinking mode из UI и config, вычистить мёртвые облачные imports.

Purpose: _create_client и _try_model были legacy-путём для ZAI/OpenRouter — теперь всё идёт через provider.complete(). Thinking mode — ZAI-специфичная настройка, Ollama её не поддерживает.
Output: ai_extractor.py без устаревшего кода; config.py без ai_disable_thinking; settings.py без Thinking mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/ROADMAP.md
@.planning/workstreams/backend/REQUIREMENTS.md
@.planning/workstreams/backend/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить _create_client, _try_model и мёртвые imports из ai_extractor.py</name>
  <read_first>
    - modules/ai_extractor.py (полный файл — особенно строки 1-30 imports, 190-240 _create_client, 407-486 _try_model, 286-314 legacy-путь в extract_metadata)
  </read_first>
  <files>modules/ai_extractor.py</files>
  <action>
    1. Удалить функцию `_create_client` (строки ~190-239) целиком вместе с docstring.

    2. Удалить функцию `_try_model` (строки ~407-486) целиком вместе с docstring.

    3. В функции `extract_metadata` найти и удалить legacy-путь (строки ~285-287):
       ```python
       else:
           # Legacy путь для обратной совместимости (прямые вызовы без провайдера)
           result = _try_model(config, messages, config.active_model, use_fallback=False)
       ```
       Заменить на исключение, если provider is None:
       ```python
       else:
           raise ValueError("provider обязателен — legacy _try_model удалён в v0.9")
       ```

    4. В функции `extract_metadata` также удалить fallback через `_try_model` (строки ~332-343):
       ```python
       elif config.model_fallback and os.environ.get("OPENROUTER_API_KEY"):
           logger.info("Основная модель недоступна, пробую fallback: %s", config.model_fallback)
           # Некоторые бесплатные модели не поддерживают system role —
           # вклеиваем system prompt в начало user-сообщения для надёжности
           fallback_messages = _merge_system_into_user(messages)
           fallback_result = _try_model(
               config, fallback_messages, config.model_fallback, use_fallback=True
           )
           if isinstance(fallback_result, ContractMetadata):
               return fallback_result
           last_error = fallback_result
       ```
       Удалить этот elif-блок целиком.

    5. Удалить мёртвые imports в начале файла:
       - Удалить: `from openai import OpenAI, APIError, APITimeoutError, RateLimitError`
         (эти классы использовались только в _create_client/_try_model)
       - Удалить: `import os` — если os.environ.get больше не используется нигде в файле
         (проверить grep — если есть другие использования os, оставить)
       - Удалить: `import time` — если time.sleep больше не используется нигде
         (проверить — _try_provider использует time.sleep, если _try_provider остаётся — оставить time)
       - Сохранить: `from providers.openrouter import _merge_system_into_user` — используется для fallback_provider

    6. Проверить что `_try_provider` НЕ удалена — это живой код для Ollama.

    7. Запустить: `python -c "from modules.ai_extractor import extract_metadata, verify_api_key, verify_metadata"` — должно пройти без ошибок.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from modules.ai_extractor import extract_metadata, verify_api_key, verify_metadata; print('OK')"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "_create_client\|_try_model" modules/ai_extractor.py || echo "OK: deprecated functions removed"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "from openai import" modules/ai_extractor.py || echo "OK: openai direct import removed"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_ai_extractor_wiring.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from modules.ai_extractor import extract_metadata"` — код 0, нет вывода ошибок
    - `grep "def _create_client\|def _try_model" modules/ai_extractor.py` — пустой вывод
    - `grep "from openai import OpenAI" modules/ai_extractor.py` — пустой вывод
    - `grep "_try_provider" modules/ai_extractor.py` — возвращает строки (функция НЕ удалена)
    - `pytest tests/test_ai_extractor_wiring.py -x` — код 0 (если тест существует и не патчит _try_model)
  </acceptance_criteria>
  <done>_create_client и _try_model удалены, мёртвые imports убраны, ai_extractor.py работает через provider.complete()</done>
</task>

<task type="auto">
  <name>Task 2: Удалить thinking mode из config.py и settings.py</name>
  <read_first>
    - config.py (строка 25: `ai_disable_thinking: bool = True`; строка 432-435 в _try_model — уже удалена Task 1)
    - app/pages/settings.py (строки 221-226 — "Row 3: Thinking mode toggle"; строка 63 в _render_summary_cards если есть упоминание)
  </read_first>
  <files>config.py, app/pages/settings.py</files>
  <action>
    1. В config.py удалить поле `ai_disable_thinking`:
       - Найти строку: `ai_disable_thinking: bool = True  # отключить thinking mode у GLM (5-7x ускорение)`
       - Удалить её полностью

    2. В app/pages/settings.py удалить Row 3 "Thinking mode toggle":
       - Найти блок:
         ```python
         # Row 3: Thinking mode toggle
         def _thinking_control():
             sw = ui.switch(value=s.get("ai_disable_thinking", True)).props("dense")
             sw.on_value_change(lambda e: save_setting("ai_disable_thinking", e.value))

         _settings_row("Thinking mode", "Отключить для 5-7x ускорения", control_fn=_thinking_control)
         ```
       - Удалить этот блок (5-6 строк) целиком

    3. Проверить grep по обоим файлам что `ai_disable_thinking` нигде не осталось.

    4. Проверить что в settings.py нет других мест с упоминанием thinking mode.

    5. Запустить smoke-test: `python -c "from config import Config; c = Config(); print('ai_disable_thinking' in vars(c))"` — должно выдать `False`.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from config import Config; c = Config(); assert not hasattr(c, 'ai_disable_thinking'), 'field still exists'; print('OK: field removed')"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "ai_disable_thinking\|thinking_mode\|Thinking mode" config.py app/pages/settings.py || echo "OK: thinking mode fully removed"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from app.pages.settings import build; print('OK: settings imports')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from config import Config; c = Config(); assert not hasattr(c, 'ai_disable_thinking')"` — код 0
    - `grep "ai_disable_thinking" config.py` — пустой вывод
    - `grep "ai_disable_thinking\|Thinking mode" app/pages/settings.py` — пустой вывод
    - `python -c "from app.pages.settings import build"` — код 0
    - `python -c "from config import Config"` — код 0
  </acceptance_criteria>
  <done>ai_disable_thinking удалён из Config и UI; settings.py импортируется без ошибок</done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# Нет мёртвого кода
grep -rn "_create_client\|_try_model\|ai_disable_thinking" . --include="*.py" | grep -v "__pycache__"

# Все импорты рабочие
python -c "
from modules.ai_extractor import extract_metadata, verify_api_key, verify_metadata
from config import Config
from app.pages.settings import build
print('All imports OK')
"

# Тесты проходят
python -m pytest tests/test_ai_extractor_wiring.py tests/test_settings_templates.py -x -q 2>&1 | tail -10
```
</verification>

<success_criteria>
- _create_client и _try_model удалены из ai_extractor.py
- Мёртвые openai/os imports удалены (если не используются в другом месте)
- ai_disable_thinking удалён из Config
- Thinking mode toggle удалён из settings.py
- Все модули импортируются без ошибок
</success_criteria>

<output>
После выполнения создать `.planning/phases/28-cleanup/28-02-SUMMARY.md`
</output>
