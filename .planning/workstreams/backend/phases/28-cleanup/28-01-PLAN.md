---
phase: 28-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - controller.py
  - modules/validator.py
  - modules/reporter.py
  - tests/test_controller.py
  - tests/test_reporter.py
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "python -m app запускается без ImportError или NameError"
    - "grep -r 'validator' controller.py возвращает 0 совпадений"
    - "grep -r 'reporter' controller.py возвращает 0 совпадений"
    - "pytest tests/test_controller.py проходит без патчей на validator/reporter"
    - "process_archive() возвращает dict с ключом report_path=None"
  artifacts:
    - path: "modules/validator.py"
      provides: "DELETED — файл не существует"
    - path: "modules/reporter.py"
      provides: "DELETED — файл не существует"
    - path: "controller.py"
      provides: "Оркестратор пайплайна без validator/reporter"
    - path: "tests/test_reporter.py"
      provides: "DELETED — тесты удалённого модуля"
  key_links:
    - from: "controller.py"
      to: "modules/validator.py"
      via: "import — ДОЛЖЕН БЫТЬ УДАЛЁН"
      pattern: "from modules.validator import"
    - from: "controller.py"
      to: "modules/reporter.py"
      via: "import — ДОЛЖЕН БЫТЬ УДАЛЁН"
      pattern: "from modules.reporter import"
---

<objective>
Удалить устаревшую L1-L5 валидацию и Excel reporter из кодовой базы.

Purpose: Валидация L1-L5 была спроектирована для облачных моделей — с GBNF-грамматикой Ollama она не нужна и создаёт мёртвый код. Excel reporter заменён встроенным реестром в UI.
Output: modules/validator.py и modules/reporter.py удалены; controller.py работает без них; тесты обновлены.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/ROADMAP.md
@.planning/workstreams/backend/REQUIREMENTS.md
@.planning/workstreams/backend/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить validator.py и все точки вызова</name>
  <read_first>
    - controller.py (строки 1-40 — imports; строки 226-268 — validate_metadata вызовы; строки 331-337 — validate_batch)
    - modules/validator.py (полный файл — понять что именно удаляется)
    - tests/test_controller.py (полный файл — найти mock-патчи на validator)
  </read_first>
  <files>controller.py, modules/validator.py, tests/test_controller.py</files>
  <action>
    1. Удалить файл modules/validator.py целиком (Path.unlink или просто удалить).

    2. В controller.py:
       - Удалить строку импорта: `from modules.validator import validate_batch, validate_metadata`
       - Удалить строки 227-268 в _run_pipeline — весь блок "Валидация L1–L3" и блок "AI-верификация L5":
         ```
         # Валидация L1–L3
         validation = validate_metadata(metadata, self.config)
         result.validation = validation

         # AI-верификация L5 (selective: только для проблемных)
         if (
             self.config.validation_mode == "selective"
             ...
         ) or self.config.validation_mode == "full":
             ... весь if-блок до строки 268 ...
         ```
       - Убедиться что `result.validation` больше не присваивается (если ProcessingResult.validation существует — оставить поле, просто не присваивать)
       - Удалить строки 331-337: весь блок "6. Перекрёстная валидация L4":
         ```
         # 6. Перекрёстная валидация L4
         if results:
             _notify(on_progress, total, total, "Перекрёстная валидация...")
             results = validate_batch(results, self.config)
             for r in results:
                 if r.status == "done":
                     db.save_result(r)
         ```
       - Обновить комментарий docstring _run_pipeline — убрать упоминание "C) validate"

    3. В tests/test_controller.py:
       - Найти все строки с `patch` или `MagicMock` для `modules.validator`, `validate_metadata`, `validate_batch`
       - Удалить эти mock-патчи
       - Если тесты проверяли наличие `result.validation` — убрать эти assertions или заменить на `assert result.validation is None` (если поле существует) или удалить assertion
       - Запустить pytest tests/test_controller.py после правки и починить любые оставшиеся ошибки импорта

    ВАЖНО: validate_mode в Config (строка `validation_mode: str = "off"`) — НЕ трогать, это поле может использоваться в тестах. Просто не вызывать валидацию.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from controller import Controller" && echo "OK: controller imports without validator"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "validate_metadata\|validate_batch\|from modules.validator" controller.py || echo "OK: no validator references"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_controller.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from controller import Controller"` завершается без ошибок
    - `grep "from modules.validator" controller.py` возвращает пустой вывод
    - `grep "validate_metadata\|validate_batch" controller.py` возвращает пустой вывод
    - Файл `modules/validator.py` не существует: `test -f modules/validator.py && echo EXISTS || echo DELETED` выводит DELETED
    - `pytest tests/test_controller.py -x` завершается с кодом 0
  </acceptance_criteria>
  <done>validator.py удалён, controller.py работает без него, тесты проходят</done>
</task>

<task type="auto">
  <name>Task 2: Удалить reporter.py и все точки вызова</name>
  <read_first>
    - controller.py (строки 1-35 — imports; строки 339-351 — generate_report вызов и stats dict)
    - modules/reporter.py (полный файл)
    - tests/test_reporter.py (полный файл — весь файл удалить)
    - tests/test_controller.py (найти mock-патчи на reporter после Task 1)
  </read_first>
  <files>controller.py, modules/reporter.py, tests/test_reporter.py, tests/test_controller.py</files>
  <action>
    1. Удалить файл modules/reporter.py целиком.

    2. В controller.py:
       - Удалить строку импорта: `from modules.reporter import generate_report`
       - Найти строки 339-342 (блок "7. Генерация Excel-реестра") и заменить:
         ```python
         # 7. Генерация Excel-реестра
         _notify(on_progress, total, total, "Генерация Excel-реестра...")
         all_data = db.get_all_results()
         report_path = generate_report(all_data, output_dir)
         ```
         На:
         ```python
         report_path = None
         ```
       - Убедиться что в return stats по-прежнему есть `"report_path": report_path` (теперь всегда None) — это сохраняет обратную совместимость контракта process_archive()
       - Обновить docstring process_archive() — в описании Returns убрать упоминание что report_path это реальный путь, написать: `report_path: None (Excel reporter удалён в v0.9)`

    3. Удалить файл tests/test_reporter.py целиком.

    4. В tests/test_controller.py:
       - Найти все mock-патчи на `modules.reporter` и `generate_report`
       - Удалить эти патчи
       - Если тест проверял что `stats["report_path"]` это Path — изменить на `assert stats["report_path"] is None`
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from controller import Controller" && echo "OK"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "generate_report\|from modules.reporter" controller.py || echo "OK: no reporter references"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_controller.py -x -q 2>&1 | tail -20</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from unittest.mock import patch, MagicMock
from config import Config
# Verify report_path=None is returned
print('reporter cleanup verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from modules.reporter\|generate_report" controller.py` возвращает пустой вывод
    - Файл `modules/reporter.py` не существует
    - Файл `tests/test_reporter.py` не существует
    - `python -c "from controller import Controller"` завершается без ошибок
    - `pytest tests/test_controller.py -x` завершается с кодом 0
    - В controller.py присутствует строка `report_path = None` (grep подтверждает)
  </acceptance_criteria>
  <done>reporter.py удалён, controller.py возвращает report_path=None, тесты обновлены и проходят</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:

```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg
# Нет мёртвых импортов
python -c "import controller; import app.pages.settings" && echo "All imports OK"

# Тесты проходят
python -m pytest tests/test_controller.py -x -q

# Нет ссылок на удалённые модули
grep -rn "from modules.validator\|from modules.reporter\|validate_batch\|validate_metadata\|generate_report" . --include="*.py" | grep -v "__pycache__" | grep -v ".pyc"
```
</verification>

<success_criteria>
- modules/validator.py удалён
- modules/reporter.py удалён
- tests/test_reporter.py удалён
- controller.py не импортирует validator или reporter
- controller.py возвращает report_path=None в stats
- pytest tests/test_controller.py проходит без ошибок
</success_criteria>

<output>
После выполнения создать `.planning/phases/28-cleanup/28-01-SUMMARY.md`
</output>
