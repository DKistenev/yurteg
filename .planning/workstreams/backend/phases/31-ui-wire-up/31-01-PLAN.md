---
phase: 31-ui-wire-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/document.py
autonomous: true
requirements:
  - WIRE-01
  - WIRE-04

must_haves:
  truths:
    - "Кнопка «Открыть файл» в action bar карточки документа открывает оригинальный PDF/DOCX в системном приложении"
    - "Кнопка «Сохранить как шаблон» в action bar вызывает mark_contract_as_template() и показывает подтверждение"
    - "Если original_path не существует — показывается ui.notify negative, краш не происходит"
  artifacts:
    - path: "app/pages/document.py"
      provides: "Action bar с двумя новыми кнопками: открыть файл + сохранить как шаблон"
      contains: "subprocess.Popen"
  key_links:
    - from: "app/pages/document.py (кнопка Открыть файл)"
      to: "subprocess.Popen(['open', path])"
      via: "platform.system() == 'Darwin'"
      pattern: "subprocess\\.Popen"
    - from: "app/pages/document.py (кнопка Сохранить как шаблон)"
      to: "services/review_service.mark_contract_as_template"
      via: "run.io_bound"
      pattern: "mark_contract_as_template"
---

<objective>
Добавить две кнопки в action bar карточки документа (app/pages/document.py):
1. «Открыть файл» — открывает оригинальный PDF/DOCX в системном приложении через subprocess
2. «Сохранить как шаблон» — вызывает mark_contract_as_template() и показывает ui.notify

Purpose: Подключить реализованный backend к UI — пользователь может открыть файл нативно и пометить документ как шаблон прямо из карточки.
Output: app/pages/document.py с двумя новыми кнопками в action bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/STATE.md
@.planning/workstreams/backend/ROADMAP.md
@.planning/workstreams/backend/phases/31-ui-wire-up/31-CONTEXT.md

<interfaces>
<!-- Из app/pages/document.py: action bar находится в строках ~241-289 -->
<!-- Action bar — ui.row с кнопками flat dense no-caps, класс ACTION_BTN -->
<!-- Кнопки уже есть: Назад, Скачать PDF, Переобработать -->
<!-- contract.get("original_path") возвращает строку-путь или None/""  -->

<!-- Из services/review_service.py: -->
```python
def mark_contract_as_template(
    db: Database,
    contract_id: int,
    template_name: Optional[str] = None,
) -> Optional[int]:
    """Returns: template id или None если contract не найден."""
```

<!-- Из app/state.py: -->
<!-- state.current_client — текущий клиент для get_db -->
<!-- _client_manager.get_db(state.current_client) → Database -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить кнопку «Открыть файл» в action bar</name>
  <files>app/pages/document.py</files>
  <read_first>
    - app/pages/document.py строки 240-290 (action bar + _reprocess handler)
  </read_first>
  <action>
В файле app/pages/document.py, после кнопки «Скачать PDF» (строка ~247) и перед `_reprocess`, добавить:

1. Импорт в верхнюю часть файла (после `import logging`):
   ```python
   import platform
   import subprocess
   ```

2. Функцию `_open_file()` прямо перед кнопкой `_reprocess` в action bar (определить как async def внутри блока `with ui.row()`):
   ```python
   async def _open_file() -> None:
       from pathlib import Path as _Path
       path_str = contract.get("original_path", "")
       if not path_str:
           ui.notify("Путь к файлу не найден", type="negative")
           return
       p = _Path(path_str)
       if not p.exists():
           ui.notify("Файл не найден на диске", type="negative")
           return
       system = platform.system()
       try:
           if system == "Darwin":
               subprocess.Popen(["open", str(p)])
           elif system == "Windows":
               import os
               os.startfile(str(p))
           else:
               subprocess.Popen(["xdg-open", str(p)])
       except Exception:
           logger.exception("Не удалось открыть файл")
           ui.notify("Не удалось открыть файл", type="negative")
   ```

3. Добавить кнопку в action bar (после кнопки «Скачать PDF», перед кнопкой «Переобработать»):
   ```python
   ui.button("Открыть файл", icon="open_in_new", on_click=_open_file).props(
       "flat dense no-caps"
   ).classes(ACTION_BTN)
   ```

Стиль: flat dense no-caps + ACTION_BTN — точно как остальные кнопки в action bar. Иконка `open_in_new` из Material Icons.
  </action>
  <verify>
    <automated>grep -n "subprocess.Popen\|open_in_new\|_open_file" app/pages/document.py | wc -l | grep -E "^[3-9]|^[0-9]{2}"</automated>
  </verify>
  <done>
    В action bar появилась кнопка «Открыть файл». Клик при существующем файле открывает его через системное приложение. Клик при отсутствующем файле показывает ui.notify negative.
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить кнопку «Сохранить как шаблон» в action bar</name>
  <files>app/pages/document.py</files>
  <read_first>
    - app/pages/document.py строки 1-35 (импорты)
    - app/pages/document.py строки 240-290 (action bar)
  </read_first>
  <action>
В файле app/pages/document.py:

1. Добавить в строку импорта из review_service:
   ```python
   from services.review_service import mark_contract_as_template, match_template, review_against_template, list_templates
   ```
   (Сейчас импортируется: `match_template, review_against_template, list_templates`)

2. Добавить async handler `_save_as_template` внутри action bar (после `_open_file`, перед `ui.element("div").classes("flex-1")`):
   ```python
   async def _save_as_template() -> None:
       save_btn.disable()
       try:
           db = _client_manager.get_db(state.current_client)
           template_id = await run.io_bound(
               mark_contract_as_template, db, int(doc_id)
           )
           if template_id is None:
               ui.notify("Не удалось сохранить шаблон — документ не найден", type="negative")
           else:
               ui.notify("Документ сохранён как шаблон", type="positive")
       except Exception:
           logger.exception("Ошибка при сохранении шаблона")
           ui.notify("Ошибка при сохранении шаблона", type="negative")
       finally:
           save_btn.enable()

   save_btn = ui.button("Сохранить как шаблон", icon="bookmark_add", on_click=_save_as_template).props(
       "flat dense no-caps"
   ).classes(ACTION_BTN)
   ```

Стиль и паттерн совпадают с `_reprocess` — disable/enable кнопки во время запроса, try/except/finally. Иконка `bookmark_add`.
  </action>
  <verify>
    <automated>grep -n "mark_contract_as_template\|bookmark_add\|_save_as_template\|save_btn" app/pages/document.py | wc -l | grep -E "^[4-9]|^[0-9]{2}"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "mark_contract_as_template" app/pages/document.py возвращает 2 строки (импорт + вызов)
    - grep -n "bookmark_add" app/pages/document.py возвращает 1 строку
    - python -c "import ast; ast.parse(open('app/pages/document.py').read())" завершается без ошибок
  </acceptance_criteria>
  <done>
    В action bar появилась кнопка «Сохранить как шаблон». Клик вызывает mark_contract_as_template() через run.io_bound и показывает ui.notify positive при успехе.
  </done>
</task>

</tasks>

<verification>
```bash
# Синтаксис
python -c "import ast; ast.parse(open('app/pages/document.py').read())" && echo OK

# Наличие всех ключевых элементов
grep -n "subprocess.Popen\|open_in_new\|mark_contract_as_template\|bookmark_add" app/pages/document.py
```
</verification>

<success_criteria>
- python -c "import ast; ast.parse(open('app/pages/document.py').read())" → OK
- В action bar три рабочие кнопки: «Открыть файл», «Сохранить как шаблон», «Переобработать»
- WIRE-01: subprocess.Popen(['open', path]) на macOS, os.startfile на Windows, xdg-open на Linux
- WIRE-04: mark_contract_as_template() вызывается через run.io_bound, результат показывается через ui.notify
</success_criteria>

<output>
После завершения создать `.planning/workstreams/backend/phases/31-ui-wire-up/31-01-SUMMARY.md`
</output>
