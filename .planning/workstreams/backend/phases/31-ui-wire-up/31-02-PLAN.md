---
phase: 31-ui-wire-up
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/registry.py
autonomous: true
requirements:
  - WIRE-02
  - WIRE-03

must_haves:
  truths:
    - "Виджет дедлайнов отображает список документов из get_attention_required() под stats bar"
    - "Bulk delete удаляет документы из БД — после перезагрузки страницы они не появляются"
    - "Виджет дедлайнов скрыт если get_attention_required() возвращает пустой список"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "Deadline widget под stats bar + подтверждение что bulk delete работает"
      contains: "get_attention_required"
  key_links:
    - from: "app/pages/registry.py (deadline widget)"
      to: "services/lifecycle_service.get_attention_required"
      via: "run.io_bound"
      pattern: "get_attention_required"
    - from: "app/pages/registry.py (_delete_bulk)"
      to: "db.conn.execute DELETE FROM contracts"
      via: "run.io_bound"
      pattern: "DELETE FROM contracts"
---

<objective>
Добавить виджет дедлайнов в registry.py — collapsible amber-блок под stats bar, показывающий документы из get_attention_required(). WIRE-02 (bulk delete) уже реализован через inline SQL в _delete_bulk — нужно только убедиться что он корректен и добавить метод db.delete_contract() в database.py для чистоты (опционально).

Purpose: Подключить lifecycle_service.get_attention_required() к UI — пользователь видит срочные документы прямо в реестре.
Output: registry.py с deadline widget после stats bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/STATE.md
@.planning/workstreams/backend/ROADMAP.md
@.planning/workstreams/backend/phases/31-ui-wire-up/31-CONTEXT.md

<interfaces>
<!-- Из services/lifecycle_service.py: -->
```python
def get_attention_required(db: Database, warning_days: int) -> list[DeadlineAlert]:
    """Возвращает истёкшие + истекающие документы без manual_status."""
```

<!-- DeadlineAlert fields: contract_id, filename, counterparty, contract_type,
     date_end (ISO), days_until_expiry (int, отрицательное = истёк), computed_status -->

<!-- Из app/pages/registry.py: -->
<!-- stats bar находится в строках ~483-495 (ui.row с STATS_BAR) -->
<!-- trust_banner_container создаётся ПОСЛЕ stats bar (строка ~498) -->
<!-- state.warning_days_threshold — порог дней из AppState -->
<!-- _client_manager.get_db(state.current_client) → Database -->
<!-- WIRE-02 (_delete_bulk) уже реализован: строки 1082-1091 через inline SQL -->

<!-- Из app/styles.py: -->
<!-- Использовать inline style для amber-цветов — Tailwind dynamic классы не работают -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить import get_attention_required и deadline widget в registry.py</name>
  <files>app/pages/registry.py</files>
  <read_first>
    - app/pages/registry.py строки 1-45 (импорты)
    - app/pages/registry.py строки 479-570 (build() function, stats bar, containers)
    - app/pages/registry.py строки 580-590 (_refresh_stats helper)
  </read_first>
  <action>
**Шаг 1 — Импорт:**
В строке импорта из lifecycle_service (строка ~42) добавить `get_attention_required`:
```python
from services.lifecycle_service import MANUAL_STATUSES, STATUS_LABELS, set_manual_status, get_attention_required
```

**Шаг 2 — Контейнер виджета:**
В функции `build()`, сразу после `trust_banner_container` (строка ~499), добавить:
```python
# ── Deadline widget placeholder (WIRE-03) ─────────────────────────────────
deadline_container = ui.column().classes("w-full")
deadline_container.set_visibility(False)
```

**Шаг 3 — Функция рендера виджета:**
Добавить функцию `_render_deadline_widget` внутри `build()`, рядом с `_refresh_stats`:

```python
async def _refresh_deadline_widget() -> None:
    """Загружает и отображает виджет документов, требующих внимания (WIRE-03)."""
    db = _client_manager.get_db(state.current_client)
    alerts = await run.io_bound(get_attention_required, db, state.warning_days_threshold)
    deadline_container.clear()
    if not alerts:
        deadline_container.set_visibility(False)
        return
    deadline_container.set_visibility(True)
    with deadline_container:
        with ui.element("div").style(
            "background:#fffbeb;border:1px solid #fcd34d;border-radius:8px;"
            "margin:0 24px 8px 24px;padding:10px 14px;"
        ):
            # Header row с toggle
            _expanded = {"v": False}
            with ui.row().classes("items-center gap-2 cursor-pointer w-full").on(
                "click", lambda: _toggle_deadline()
            ) as header_row:
                ui.html(
                    '<svg width="16" height="16" viewBox="0 0 24 24" fill="none"'
                    ' stroke="#d97706" stroke-width="2">'
                    '<circle cx="12" cy="12" r="10"/>'
                    '<path d="M12 6v6l4 2"/></svg>'
                )
                ui.label(f"Требуют внимания: {len(alerts)}").style(
                    "font-size:13px;font-weight:600;color:#92400e;"
                )
                expand_icon = ui.icon("expand_more").style("color:#d97706;font-size:18px;margin-left:auto;")

            # Expandable list — скрыт по умолчанию
            items_col = ui.column().classes("w-full gap-1 mt-2")
            items_col.set_visibility(False)

            def _toggle_deadline():
                _expanded["v"] = not _expanded["v"]
                items_col.set_visibility(_expanded["v"])
                expand_icon.set_name("expand_less" if _expanded["v"] else "expand_more")

            with items_col:
                for alert in alerts:
                    days = alert.days_until_expiry
                    if days < 0:
                        days_text = f"истёк {abs(days)} дн. назад"
                        row_color = "#fef2f2"
                        text_color = "#991b1b"
                    else:
                        days_text = f"через {days} дн."
                        row_color = "#fffbeb"
                        text_color = "#92400e"

                    counterparty = alert.counterparty or "—"
                    contract_type = alert.contract_type or "Документ"

                    with ui.element("div").style(
                        f"background:{row_color};border-radius:6px;padding:6px 10px;"
                        "display:flex;align-items:center;gap:8px;cursor:pointer;"
                    ).on("click", lambda cid=alert.contract_id: ui.navigate.to(f"/document/{cid}")):
                        ui.label(f"{contract_type} · {counterparty}").style(
                            "font-size:12px;color:#334155;flex:1;"
                            "overflow:hidden;text-overflow:ellipsis;white-space:nowrap;"
                        )
                        ui.label(days_text).style(
                            f"font-size:11px;font-weight:500;color:{text_color};white-space:nowrap;"
                        )
```

**Шаг 4 — Вызов при инициализации:**
Найти место где вызывается `_refresh_stats()` при инициализации страницы (в конце build() или в on-page-ready блоке). Добавить рядом:
```python
await _refresh_deadline_widget()
```

Паттерн поиска: найти `await _refresh_stats()` и после него добавить `await _refresh_deadline_widget()`.
  </action>
  <verify>
    <automated>grep -n "get_attention_required\|deadline_container\|_refresh_deadline_widget" app/pages/registry.py | wc -l | grep -E "^[4-9]|^[0-9]{2}"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "get_attention_required" app/pages/registry.py возвращает 2 строки (импорт + вызов)
    - grep -n "_refresh_deadline_widget" app/pages/registry.py возвращает минимум 2 строки (def + вызов)
    - python -c "import ast; ast.parse(open('app/pages/registry.py').read())" завершается без ошибок
  </acceptance_criteria>
  <done>
    Виджет дедлайнов отображается под stats bar. При 0 алертах скрыт. При N алертах показывает amber-блок с числом. Клик по заголовку раскрывает список; клик по строке переходит на /document/{id}.
  </done>
</task>

<task type="auto">
  <name>Task 2: Верификация WIRE-02 — убедиться что bulk delete корректен</name>
  <files>app/pages/registry.py</files>
  <read_first>
    - app/pages/registry.py строки 1082-1091 (_delete_bulk function)
  </read_first>
  <action>
Проверить существующую реализацию `_delete_bulk` (строки 1082-1091).

Текущий код:
```python
async def _delete_bulk(doc_ids):
    db = _client_manager.get_db(state.current_client)
    for doc_id in doc_ids:
        await run.io_bound(lambda did=doc_id: db.conn.execute("DELETE FROM contracts WHERE id = ?", (did,)))
    await run.io_bound(lambda: db.conn.commit())
    _clear_bulk_selection()
    if grid_ref["grid"]:
        await load_table_data(grid_ref["grid"], state, active_segment["value"])
    await _refresh_stats()
    ui.notify(f"Удалено {len(doc_ids)} документов", type="positive")
```

Этот код корректен. Однако нужно добавить вызов `_refresh_deadline_widget()` после `_refresh_stats()` чтобы виджет обновлялся после удаления:

```python
    await _refresh_stats()
    await _refresh_deadline_widget()   # ← добавить эту строку
    ui.notify(f"Удалено {len(doc_ids)} документов", type="positive")
```

Больше никаких изменений в _delete_bulk не нужно — inline SQL корректен, commit вызывается, grid обновляется.
  </action>
  <verify>
    <automated>grep -n "_refresh_deadline_widget" app/pages/registry.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "_refresh_deadline_widget" app/pages/registry.py возвращает минимум 3 строки (def + вызов в init + вызов в _delete_bulk)
    - python -c "import ast; ast.parse(open('app/pages/registry.py').read())" без ошибок
  </acceptance_criteria>
  <done>
    Bulk delete обновляет виджет дедлайнов после удаления. Удалённые документы не появляются после перезагрузки — grid перестраивается через load_table_data.
  </done>
</task>

</tasks>

<verification>
```bash
# Синтаксис
python -c "import ast; ast.parse(open('app/pages/registry.py').read())" && echo OK

# Ключевые символы
grep -n "get_attention_required\|deadline_container\|_refresh_deadline_widget" app/pages/registry.py
```
</verification>

<success_criteria>
- python -c "import ast; ast.parse(open('app/pages/registry.py').read())" → OK
- WIRE-03: get_attention_required() вызывается при загрузке страницы и после bulk delete
- WIRE-02: _delete_bulk корректно удаляет из БД, делает commit, обновляет grid и виджет дедлайнов
- Виджет скрыт при пустом списке алертов, показывает amber collapsible блок при наличии
</success_criteria>

<output>
После завершения создать `.planning/workstreams/backend/phases/31-ui-wire-up/31-02-SUMMARY.md`
</output>
