---
phase: 09-document-detail-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/database.py
  - app/state.py
  - app/pages/document.py
  - app/components/registry_table.py
  - tests/test_document_card.py
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-06]

must_haves:
  truths:
    - "Юрист видит карточку документа с метаданными при клике по строке реестра"
    - "Кнопка Назад к реестру возвращает на главную страницу"
    - "Prev/next переключают документы без возврата в реестр"
    - "Заметка юриста автосохраняется при blur"
    - "Статус документа отображается с бейджем и может быть изменён вручную"
  artifacts:
    - path: "modules/database.py"
      provides: "get_contract_by_id method"
      contains: "def get_contract_by_id"
    - path: "app/state.py"
      provides: "filtered_doc_ids field in AppState"
      contains: "filtered_doc_ids"
    - path: "app/pages/document.py"
      provides: "Full document card page with metadata, status, notes, nav"
      min_lines: 120
    - path: "tests/test_document_card.py"
      provides: "Tests for get_contract_by_id and prev/next logic"
      min_lines: 40
  key_links:
    - from: "app/pages/document.py"
      to: "modules/database.py"
      via: "run.io_bound(db.get_contract_by_id, int(doc_id))"
      pattern: "run\\.io_bound.*get_contract_by_id"
    - from: "app/pages/document.py"
      to: "services/lifecycle_service.py"
      via: "set_manual_status / clear_manual_status"
      pattern: "set_manual_status|clear_manual_status"
    - from: "app/components/registry_table.py"
      to: "app/state.py"
      via: "state.filtered_doc_ids = [r['id'] for r in rows]"
      pattern: "filtered_doc_ids"
---

<objective>
Data layer and document card scaffold — header with back/prev/next, metadata grid, status badge with manual override, lawyer notes with autosave on blur.

Purpose: Establishes the document card page that all other sections (review, versions) build upon. Юрист может открыть документ из реестра и видеть все основные метаданные.
Output: Working document card page with navigation, metadata, status, and notes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-document-detail-card/09-RESEARCH.md

@app/pages/document.py
@app/state.py
@app/main.py
@app/components/registry_table.py
@modules/database.py
@services/lifecycle_service.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From modules/database.py:
```python
class Database:
    def __init__(self, db_path: Path) -> None: ...
    conn: sqlite3.Connection  # public, row_factory=sqlite3.Row
    _lock: threading.Lock
    def get_all_results(self) -> list[dict]: ...  # JSON deserializes special_conditions, parties, validation_warnings
    def update_review(self, file_hash: str, review_status: str, comment: str) -> None: ...
```

From services/lifecycle_service.py:
```python
MANUAL_STATUSES = frozenset({"terminated", "extended", "negotiation", "suspended"})
STATUS_LABELS = {
    "active": ("✔", "Действует", "#22c55e"),
    "expiring": ("⚠", "Скоро истекает", "#f59e0b"),
    "expired": ("✗", "Истёк", "#ef4444"),
    "unknown": ("?", "Нет даты", "#9ca3af"),
    "terminated": ("✖", "Расторгнут", "#6b7280"),
    "extended": ("↻", "Продлён", "#3b82f6"),
    "negotiation": ("~", "На согласовании", "#a855f7"),
    "suspended": ("⏸", "Приостановлен", "#f97316"),
}
def get_computed_status_sql(warning_days: int) -> str: ...
def set_manual_status(db: Database, contract_id: int, status: str) -> None: ...
def clear_manual_status(db: Database, contract_id: int) -> None: ...
```

From app/state.py:
```python
@dataclass
class AppState:
    current_client: str = "Основной реестр"
    selected_doc_id: Optional[int] = None
    warning_days_threshold: int = 30
    filter_search: str = ""
    # ... other fields
def get_state() -> AppState: ...
```

From services/client_manager.py:
```python
class ClientManager:
    def get_db(self, client_name: str) -> Database: ...
```

From app/components/registry_table.py:
```python
def _fetch_rows(client_name, segment, search, warning_days) -> list[dict]: ...
async def load_table_data(grid, state, segment="all") -> None: ...
```

From app/main.py:
```python
# _STATUS_CSS already defines .status-active, .status-expiring, etc. CSS classes
# Route already exists: "/document/{doc_id}": document.build
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer — get_contract_by_id + AppState.filtered_doc_ids + tests</name>
  <read_first>
    - modules/database.py (full file — understand get_all_results JSON deserialize pattern)
    - app/state.py (full file — AppState fields)
    - app/components/registry_table.py (load_table_data function — where to add filtered_doc_ids)
  </read_first>
  <files>modules/database.py, app/state.py, app/components/registry_table.py, tests/test_document_card.py</files>
  <action>
**1. Add `get_contract_by_id` to `Database` class in `modules/database.py` (per D-21):**

Add this method after `get_all_results`:

```python
def get_contract_by_id(self, contract_id: int) -> dict | None:
    """Возвращает один контракт по ID с десериализованными JSON-полями."""
    with self._lock:
        row = self.conn.execute(
            "SELECT * FROM contracts WHERE id = ?", (contract_id,)
        ).fetchone()
    if row is None:
        return None
    d = dict(row)
    for field in ("special_conditions", "parties", "validation_warnings"):
        raw = d.get(field)
        if raw:
            try:
                parsed = json.loads(raw)
                d[field] = parsed if isinstance(parsed, list) else []
            except (json.JSONDecodeError, TypeError):
                d[field] = []
        else:
            d[field] = []
    d.setdefault("review_status", "not_reviewed")
    d.setdefault("lawyer_comment", "")
    d.setdefault("manual_status", None)
    return d
```

**2. Add `filtered_doc_ids` field to `AppState` in `app/state.py`:**

Add after `selected_doc_id`:
```python
filtered_doc_ids: list = field(default_factory=list)
```
Import `field` from `dataclasses` (already imported as `dataclass` is used — add `field` to the import).

**3. Update `load_table_data` in `app/components/registry_table.py` (per Pitfall 5 from RESEARCH):**

After `rows = build_version_rows(rows, db)`, before `grid.options["rowData"] = rows`, add:
```python
# Per D-20, Pitfall 5: sync filtered_doc_ids on every data load, not just on click
state.filtered_doc_ids = [r['id'] for r in rows if not r.get('is_child')]
```

**4. Create `tests/test_document_card.py`:**

```python
"""Tests for document card data layer (Phase 9, DOC-01, DOC-02, DOC-03)."""
import json
import sqlite3
import sys
import tempfile
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from modules.database import Database


def _make_db():
    """Create temporary Database with one test contract."""
    tmp = tempfile.mkdtemp()
    db = Database(Path(tmp) / "test.sqlite")
    db.conn.execute(
        "INSERT INTO contracts (id, filename, file_hash, status, contract_type, "
        "counterparty, subject, date_start, date_end, amount, special_conditions, "
        "parties, review_status, lawyer_comment) "
        "VALUES (1, 'test.pdf', 'abc123', 'done', 'Аренда', 'ООО Тест', "
        "'Аренда помещения', '2025-01-01', '2026-01-01', '100 000 руб', "
        "?, ?, 'not_reviewed', '')",
        (json.dumps(["Условие 1", "Условие 2"]), json.dumps(["Сторона А", "Сторона Б"])),
    )
    db.conn.commit()
    return db


def test_get_contract_by_id():
    db = _make_db()
    result = db.get_contract_by_id(1)
    assert result is not None
    assert result["contract_type"] == "Аренда"
    assert result["counterparty"] == "ООО Тест"
    assert isinstance(result["special_conditions"], list)
    assert len(result["special_conditions"]) == 2
    assert isinstance(result["parties"], list)
    assert result["lawyer_comment"] == ""
    db.close()


def test_get_contract_by_id_none():
    db = _make_db()
    result = db.get_contract_by_id(999)
    assert result is None
    db.close()


def test_prevnext_logic():
    """Test prev/next index computation from filtered_doc_ids list."""
    doc_ids = [10, 20, 30, 40, 50]
    current_id = 30
    idx = doc_ids.index(current_id)
    prev_id = doc_ids[idx - 1] if idx > 0 else None
    next_id = doc_ids[idx + 1] if idx < len(doc_ids) - 1 else None
    assert prev_id == 20
    assert next_id == 40

    # Edge: first element
    idx = doc_ids.index(10)
    assert (doc_ids[idx - 1] if idx > 0 else None) is None

    # Edge: last element
    idx = doc_ids.index(50)
    assert (doc_ids[idx + 1] if idx < len(doc_ids) - 1 else None) is None
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_document_card.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "def get_contract_by_id" modules/database.py returns exactly 1 line
    - grep -n "filtered_doc_ids" app/state.py returns at least 1 line
    - grep -n "filtered_doc_ids" app/components/registry_table.py returns at least 1 line
    - python -m pytest tests/test_document_card.py -x -q shows 3 passed, 0 failed
  </acceptance_criteria>
  <done>get_contract_by_id returns dict with deserialized JSON for existing ID and None for missing ID. AppState has filtered_doc_ids field. Registry table updates filtered_doc_ids on every data load. All 3 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Document card page — header, metadata grid, status, notes, prev/next navigation</name>
  <read_first>
    - app/pages/document.py (current stub)
    - app/main.py (routing, _STATUS_CSS classes)
    - services/lifecycle_service.py (STATUS_LABELS, MANUAL_STATUSES, set_manual_status, clear_manual_status, get_computed_status_sql)
    - modules/database.py (update_review signature — takes file_hash not id)
    - .planning/phases/09-document-detail-card/09-RESEARCH.md (patterns 1-7, pitfalls 1-6, code examples)
  </read_first>
  <files>app/pages/document.py</files>
  <action>
Replace the stub in `app/pages/document.py` with the full document card implementation. Per D-01: single column layout, top-to-bottom: header, metadata grid, status, notes. AI review and versions sections will be added in Plan 02.

**Structure (per D-01):**

```python
"""Карточка документа — полная информация, статус, заметки, навигация.

Per D-01: Single column, sections top-to-bottom.
Per D-02: Header: ← Назад слева, contract_type по центру, ◀ ▶ справа.
Per D-03: Prev/next переключают doc_id в URL.
"""
import logging

from nicegui import run, ui

from app.state import get_state
from services.client_manager import ClientManager
from services.lifecycle_service import (
    STATUS_LABELS,
    MANUAL_STATUSES,
    get_computed_status_sql,
    set_manual_status,
    clear_manual_status,
)

logger = logging.getLogger(__name__)
_client_manager = ClientManager()
```

**`build(doc_id)` function — async, per Pattern 1 from RESEARCH:**

1. Get `state = get_state()`, validate `doc_id` is non-empty (else `ui.navigate.to('/')`).
2. Load contract: `contract = await run.io_bound(_client_manager.get_db(state.current_client).get_contract_by_id, int(doc_id))`.
3. If contract is None: show "Документ не найден" label and return.
4. Load computed_status via separate SQL query (per Pattern 3 from RESEARCH):
   ```python
   db = _client_manager.get_db(state.current_client)
   status_row = await run.io_bound(
       lambda: db.conn.execute(
           f"SELECT {get_computed_status_sql(state.warning_days_threshold)} AS computed_status FROM contracts WHERE id = :contract_id",
           {"warning_days": state.warning_days_threshold, "contract_id": int(doc_id)}
       ).fetchone()
   )
   computed_status = dict(status_row)["computed_status"] if status_row else "unknown"
   ```

**Header (per D-02):**
```python
with ui.row().classes('w-full items-center justify-between px-6 py-4 border-b'):
    ui.button('← Назад к реестру', on_click=lambda: ui.navigate.to('/')).props('flat no-caps').classes('text-gray-600')
    ui.label(contract.get('contract_type') or 'Документ').classes('text-xl font-semibold text-gray-900')
    with ui.row().classes('gap-2'):
        # Prev/next buttons (per D-03, D-20)
        doc_ids = state.filtered_doc_ids
        current_idx = doc_ids.index(int(doc_id)) if int(doc_id) in doc_ids else -1
        prev_id = doc_ids[current_idx - 1] if current_idx > 0 else None
        next_id = doc_ids[current_idx + 1] if current_idx < len(doc_ids) - 1 else None
        ui.button('◀', on_click=lambda: ui.navigate.to(f'/document/{prev_id}')).props('flat dense').classes('text-gray-500').set_enabled(prev_id is not None)
        ui.button('▶', on_click=lambda: ui.navigate.to(f'/document/{next_id}')).props('flat dense').classes('text-gray-500').set_enabled(next_id is not None)
```

NOTE on prev/next lambdas: capture `prev_id`/`next_id` as default args to avoid closure issue:
```python
ui.button('◀', on_click=lambda pid=prev_id: ui.navigate.to(f'/document/{pid}')).props(...)
```

**Metadata grid (per D-04, D-05 — 7 fields, NOT showing parties/technical):**

Use helper `_render_metadata(contract)` following the code example from RESEARCH (2-column grid with label+value pairs). Use `ui.grid(columns=2).classes('gap-x-6 gap-y-3')`. Special_conditions as bulleted list below the grid.

**Status section (per D-06, D-07):**

Show computed_status badge using STATUS_LABELS. Use the existing CSS classes (`status-active`, etc.) from `_STATUS_CSS` in main.py. Render with `ui.html(f'<span class="{cls}">{icon} {label}</span>')`.

"Изменить" button toggles a `ui.select` with MANUAL_STATUSES options. On change: `await run.io_bound(set_manual_status, db, int(doc_id), value)` then refresh page.

"Сбросить" button: `await run.io_bound(clear_manual_status, db, int(doc_id))` then refresh.

**Lawyer notes (per D-08, D-09):**

Single `ui.textarea` with placeholder "Добавьте заметку..." and 3-4 rows. Bind `on_blur` to autosave:
```python
async def _save_comment(e):
    comment_text = e.sender.value or ""
    file_hash = contract.get('file_hash', '')
    if file_hash:
        await run.io_bound(db.update_review, file_hash, contract.get('review_status', 'not_reviewed'), comment_text)

comment_area = ui.textarea(value=contract.get('lawyer_comment', '')).props('outlined rows=4').classes('w-full')
comment_area.props('label="Пометки юриста"')
comment_area.on('blur', _save_comment)
```

IMPORTANT per Pitfall 6: `update_review` takes `file_hash` not `contract_id`. Read `contract['file_hash']` from the loaded contract dict.

**Placeholders for Plan 02 sections:**
At the bottom, add two commented markers:
```python
# ── AI Review section (Plan 02) ──
# ── Version History section (Plan 02) ──
```

**Wrap all content in `ui.column().classes('max-w-4xl mx-auto px-6 py-6 gap-6')`** for Notion-style clean layout.

ALL DB and service calls MUST use `await run.io_bound(...)` per Pitfall 2.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from app.pages.document import build; print('import OK')" && grep -c "run.io_bound" app/pages/document.py</automated>
  </verify>
  <acceptance_criteria>
    - python -c "from app.pages.document import build; print('OK')" prints OK without errors
    - grep -n "def build" app/pages/document.py returns async function signature
    - grep -n "run.io_bound" app/pages/document.py returns at least 3 lines (get_contract_by_id, set_manual_status/clear_manual_status, update_review)
    - grep -n "get_contract_by_id" app/pages/document.py returns at least 1 line
    - grep -n "STATUS_LABELS\|MANUAL_STATUSES" app/pages/document.py returns imports
    - grep -n "navigate.to.*/" app/pages/document.py returns back-to-registry navigation
    - grep -n "filtered_doc_ids" app/pages/document.py returns prev/next logic
    - grep -n "update_review\|file_hash" app/pages/document.py returns autosave logic using file_hash
    - grep -n "blur" app/pages/document.py returns at least 1 line (autosave on blur per D-08)
    - wc -l app/pages/document.py shows at least 120 lines
    - python -m pytest tests/test_document_card.py -x -q still passes (regression)
  </acceptance_criteria>
  <done>Document card page renders header with back/prev/next buttons, metadata grid with 7 fields (per D-04), status badge with manual override (per D-06/D-07), lawyer notes with autosave on blur (per D-08). All DB calls use run.io_bound. No Streamlit imports.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_document_card.py -x -q` — 3 tests pass
- `python -m pytest tests/ -x -q` — full suite green
- `python -c "from app.pages.document import build"` — no import errors
- `grep -c "run.io_bound" app/pages/document.py` — at least 3 (all DB calls wrapped)
</verification>

<success_criteria>
1. Карточка документа открывается при клике по строке реестра (DOC-01)
2. Метаданные отображаются в 2-column grid: тип, контрагент, предмет, даты, сумма, особые условия (DOC-02)
3. Prev/next кнопки переключают документы, disabled на краях списка (DOC-03)
4. Заметка автосохраняется при blur через update_review с file_hash (DOC-06)
5. Статус отображается с CSS-бейджем, можно изменить вручную и сбросить (DOC-06 adjacent)
</success_criteria>

<output>
After completion, create `.planning/phases/09-document-detail-card/09-01-SUMMARY.md`
</output>
