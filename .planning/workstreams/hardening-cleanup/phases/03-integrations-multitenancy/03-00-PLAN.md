---
phase: 03-integrations-multitenancy
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_notifications.py
  - tests/test_telegram_bot.py
  - tests/test_client_manager.py
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-04
  - PROF-01

must_haves:
  truths:
    - "Test skeletons exist for all phase 3 requirements before implementation begins"
    - "All xfail tests pass collection (no import errors)"
  artifacts:
    - path: "tests/test_notifications.py"
      provides: "INTG-04 test stubs"
      contains: "xfail"
    - path: "tests/test_telegram_bot.py"
      provides: "INTG-01, INTG-02 test stubs"
      contains: "xfail"
    - path: "tests/test_client_manager.py"
      provides: "PROF-01 test stubs"
      contains: "xfail"
  key_links: []
---

<objective>
Wave 0: Create xfail test skeletons for all phase 3 requirements before any implementation begins.

Purpose: Establish test contracts so implementation tasks have clear targets. Following the Wave 0 skeleton pattern from Phase 2.
Output: 3 test files with xfail-marked tests covering INTG-01, INTG-02, INTG-04, PROF-01.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integrations-multitenancy/03-CONTEXT.md
@.planning/phases/03-integrations-multitenancy/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create xfail test skeletons for all phase 3 requirements</name>
  <files>tests/test_notifications.py, tests/test_telegram_bot.py, tests/test_client_manager.py</files>
  <read_first>
    - tests/test_lifecycle.py (reference pattern for xfail skeletons from Phase 2)
    - tests/conftest.py (existing fixtures)
    - .planning/phases/03-integrations-multitenancy/03-RESEARCH.md (test map section)
  </read_first>
  <action>
Create 3 test files following the Phase 2 xfail skeleton pattern (pytest.mark.xfail with strict=False).

**tests/test_notifications.py** (INTG-04):
- `test_startup_toast_flag`: Assert that a startup notification check function returns alerts list when expiring docs exist. Import from `services.lifecycle_service` (get_attention_required already exists). Mark xfail.
- `test_startup_toast_only_once`: Assert session_state flag prevents repeated calls. Mark xfail.
- `test_no_toast_when_no_alerts`: Assert empty list when no expiring docs. Mark xfail.

**tests/test_telegram_bot.py** (INTG-01, INTG-02):
- `test_file_queue_enqueue`: Assert enqueue_file inserts row into file_queue table. Mark xfail.
- `test_file_queue_fetch`: Assert fetch_queue returns unfetched files and marks them fetched. Mark xfail.
- `test_binding_code_generation`: Assert 6-digit code is generated and saved to pending_bindings. Mark xfail.
- `test_binding_code_expiry`: Assert expired code returns None on consume. Mark xfail.
- `test_binding_code_consume`: Assert valid code returns chat_id and deletes pending entry. Mark xfail.
- `test_deadline_alerts`: Assert get_alerts_for_user returns contracts with date_end within threshold. Mark xfail.
- `test_push_deadlines_minimal_data`: Assert only date_end, filename, counterparty, status are serialized. Mark xfail.

**tests/test_client_manager.py** (PROF-01):
- `test_add_client`: Assert add_client creates entry in clients.json with correct db path. Mark xfail.
- `test_get_db`: Assert get_db returns Database instance with correct path. Mark xfail.
- `test_list_clients`: Assert list_clients returns all added names. Mark xfail.
- `test_default_client`: Assert default "Основной реестр" exists without any add_client calls. Mark xfail.
- `test_fuzzy_match_exact`: Assert find_client_by_counterparty matches exact name. Mark xfail.
- `test_fuzzy_match_reorder`: Assert "Рога и Копыта ООО" matches "ООО Рога и Копыта". Mark xfail.
- `test_fuzzy_match_no_match`: Assert unrelated name returns None. Mark xfail.
- `test_switch_client`: Assert switching client changes active db path. Mark xfail.

Each test function body: simple assert False or a placeholder assertion that will fail. Include docstring with the requirement ID (e.g., """INTG-04: ...""").

Add `import pytest` at top. Do NOT import actual services yet (they don't exist). Use conditional imports with try/except ImportError where needed.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_notifications.py tests/test_telegram_bot.py tests/test_client_manager.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_notifications.py contains `@pytest.mark.xfail` and `def test_startup_toast_flag`
    - tests/test_telegram_bot.py contains `@pytest.mark.xfail` and `def test_file_queue_enqueue` and `def test_binding_code_expiry`
    - tests/test_client_manager.py contains `@pytest.mark.xfail` and `def test_add_client` and `def test_fuzzy_match_reorder`
    - `pytest --collect-only` exits 0 and shows 18 tests collected
  </acceptance_criteria>
  <done>18 xfail test stubs across 3 files, all collected without import errors</done>
</task>

</tasks>

<verification>
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_notifications.py tests/test_telegram_bot.py tests/test_client_manager.py -v
All tests show as xfail (not xpass, not error).
</verification>

<success_criteria>
- 3 test files created with xfail stubs
- pytest collects all tests without errors
- No import failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-integrations-multitenancy/03-00-SUMMARY.md`
</output>
