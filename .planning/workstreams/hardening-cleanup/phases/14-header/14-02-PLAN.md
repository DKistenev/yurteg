---
phase: 14-header
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - app/components/header.py
autonomous: false
requirements: [HEAD-01, HEAD-02, HEAD-03, HEAD-04]

must_haves:
  truths:
    - "Header визуально тёмный — фон bg-slate-900 (#0f172a) виден на всех страницах"
    - "Лого-марка: indigo квадрат rounded-lg с белой «Ю» слева, затем «рТэг» белым текстом"
    - "CTA кнопка «+ Загрузить документы» — filled indigo (bg-indigo-600 text-white), НЕ flat/outline"
    - "Активная вкладка имеет видимый индикатор (подчёркивание или фон), отличный от неактивных"
    - "Все nav-ссылки имеют hover state — цвет меняется при наведении"
    - "Навигация «Документы» переименована в «Реестр»"
    - "Callback _on_upload_click работает — pick_folder() вызывается при клике на CTA"
  artifacts:
    - path: "app/components/header.py"
      provides: "Dark chrome header component"
      contains: "bg-slate-900"
  key_links:
    - from: "app/components/header.py"
      to: "pick_folder()"
      via: "_on_upload_click callback на upload_btn"
      pattern: "pick_folder"
    - from: "header.py _nav_link"
      to: "ui.link path routing"
      via: "active indicator detection через ui.navigate.target"
      pattern: "_nav_link"
---

<objective>
Визуально перестроить header компонент — тёмный chrome band как визуальный якорь, лого-марка «Ю» квадрат, filled indigo CTA, active tab indicator, hover states на всех nav-ссылках.

Purpose: Header виден на каждой странице — это главный визуальный якорь приложения. Без него все остальные страницы будут выглядеть дисжойнтно.
Output: Переработанный app/components/header.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-header/14-CONTEXT.md
@.planning/phases/14-header/14-01-SUMMARY.md

<interfaces>
<!-- Текущий header.py — прочитать ПОЛНОСТЬЮ перед правками -->

app/components/header.py — КРИТИЧЕСКИЕ callbacks (НЕ ЛОМАТЬ):
- _on_upload_click() → pick_folder() → on_upload(source_dir) — вся цепочка должна работать
- _switch_client() → ui.navigate.to("/") — переключение рабочего пространства
- _show_add_dialog() → ClientManager.add_client() — добавление клиента

Текущая структура header:
- ui.header().classes("bg-white border-b border-slate-200 px-6 py-0 flex items-center gap-8 h-12")
- Лого: ui.label("ЮрТэг").classes("text-base font-semibold text-slate-900 shrink-0")
- Nav: _nav_link("Документы", "/"), _nav_link("Шаблоны", "/templates"), _nav_link("⚙", "/settings")
- Upload btn: props("flat no-caps").classes("text-sm text-slate-700 shrink-0")
- Client dropdown: profile_btn + client_menu

app/styles.py (после Plan 01):
- BTN_ACCENT_FILLED = "px-5 py-2 bg-indigo-600 text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 transition-colors duration-150"

tokens.css (после Plan 01):
- --yt-color-accent: var(--yt-p-indigo-600)  → #4f46e5
- --yt-color-hero-bg: var(--yt-p-slate-900)  → #0f172a
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переработать header.py — dark chrome band, лого-марка, filled CTA, active indicator</name>
  <files>app/components/header.py</files>
  <read_first>
    - app/components/header.py (прочитать ВЕСЬ файл — сохранить все callbacks)
    - app/styles.py (BTN_ACCENT_FILLED и другие константы)
    - .planning/phases/14-header/14-CONTEXT.md (решения по header: лого, CTA, nav)
    - .planning/research/PITFALLS.md (Pitfall 2 — Quasar !important: НЕ использовать .props("color=primary"), использовать .classes("bg-indigo-600 text-white"))
  </read_first>
  <action>
Переписать app/components/header.py сохранив ВСЮ бизнес-логику (pick_folder, _switch_client, _show_add_dialog, ClientManager). Изменить только визуальный слой.

**1. Основной контейнер header:**
Заменить `bg-white border-b border-slate-200` на тёмный:
```python
ui.header().classes(
    "px-6 py-0 flex items-center gap-6 h-14"
).style("background: #0f172a; border-bottom: 1px solid #334155; box-shadow: 0 1px 3px rgb(0 0 0 / 0.2);")
```
Высота h-14 (56px) вместо h-12 (48px) — даёт пространство для логомарки.

**2. Лого-марка «Ю» — заменить простой label:**
```python
# Logo mark: indigo square + wordmark (как Slack/Notion pattern)
with ui.row().classes("items-center gap-2 shrink-0 no-underline"):
    # Indigo квадрат с белой «Ю»
    ui.element('div').classes(
        "w-7 h-7 rounded-lg flex items-center justify-center text-white text-sm font-bold"
    ).style("background: #4f46e5; line-height: 1;").text = "Ю"
    # Wordmark
    with ui.row().classes("gap-0 items-baseline"):
        ui.label("р").classes("text-base font-semibold text-white")
        ui.label("Тэг").classes("text-base font-normal text-slate-400")
```

НО у NiceGUI ui.element('div').text не работает напрямую. Использовать:
```python
ui.html('<div class="w-7 h-7 rounded-lg flex items-center justify-center text-white text-sm font-bold" style="background: #4f46e5; line-height: 1; flex-shrink: 0;">Ю</div>')
ui.label("рТэг").classes("text-base font-semibold text-white tracking-tight")
```
Обернуть оба элемента в `with ui.row().classes("items-center gap-2 shrink-0"):`

**3. Nav tabs с active indicator:**

Заменить _nav_link функцию. Активная вкладка определяется через `ui.context.client.url` или через JavaScript. Простое рабочее решение — добавить active state через data-path и JS:

```python
def _nav_link(label: str, path: str, aria_label: str = "") -> None:
    """Render nav link with active indicator for dark header."""
    link = ui.link(label, path).classes(
        "text-sm font-medium no-underline pb-1 transition-colors duration-150"
        " text-slate-300 hover:text-white"
        " border-b-2 border-transparent"
    ).props(f'data-path="{path}"')
    if aria_label:
        link.props(f'aria-label="{aria_label}"')
```

Добавить JS для active indicator — после рендера nav row, добавить:
```python
ui.add_body_html("""
<script>
(function() {
  function updateNav() {
    var path = window.location.pathname;
    document.querySelectorAll('a[data-path]').forEach(function(el) {
      var elPath = el.getAttribute('data-path');
      var isActive = (path === elPath) || (path === '/' && elPath === '/');
      el.style.color = isActive ? '#ffffff' : '';
      el.style.borderBottomColor = isActive ? '#4f46e5' : 'transparent';
      el.style.fontWeight = isActive ? '600' : '500';
    });
  }
  updateNav();
  window.addEventListener('popstate', updateNav);
  // NiceGUI SPA navigation
  document.addEventListener('nicegui:navigate', updateNav);
})();
</script>
""")
```

**4. CTA кнопка — filled indigo, НЕ flat:**
```python
upload_btn = ui.button(
    "+ Загрузить документы",
    on_click=_on_upload_click,
).classes(
    "px-4 py-1.5 bg-indigo-600 text-white text-sm font-semibold rounded-lg"
    " hover:bg-indigo-700 transition-colors duration-150 shrink-0"
).props("no-caps")
```
НЕ ИСПОЛЬЗОВАТЬ .props("flat") и НЕ ИСПОЛЬЗОВАТЬ .props("color=primary") — они эмитируют !important.

**5. Клиентский dropdown — адаптировать цвет под тёмный header:**
```python
profile_btn = ui.button(
    f"📁 {state.current_client}",
    on_click=lambda: client_menu.open(),
).props('flat no-caps aria-label="Рабочее пространство"').classes(
    "text-sm text-slate-400 hover:text-slate-200 transition-colors duration-150"
)
```

**6. Nav: переименовать «Документы» → «Реестр» (решение из CONTEXT.md):**
```python
_nav_link("Реестр", "/")
_nav_link("Шаблоны", "/templates")
_nav_link("⚙", "/settings", aria_label="Настройки")
```

**Полная структура файла:**
- Сохранить все imports (run, ui, pick_folder, AppState, ClientManager)
- Сохранить _cm и _header_refs module-level singletons
- render_header сигнатура не меняется: (state: AppState, on_upload: Optional[Callable])
- _switch_client — сохранить без изменений
- _show_add_dialog — сохранить без изменений
- _nav_link — переработать для тёмного header + active indicator
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from app.components.header import render_header; print('header import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - header.py импортируется без ошибок: `python -c "from app.components.header import render_header"` → exit 0
    - Тёмный фон header: `grep "0f172a\|slate-900\|slate_900" app/components/header.py` находит в ui.header() строке
    - Лого-марка «Ю»: `grep "\"Ю\"\|'Ю'" app/components/header.py` находит строку
    - CTA кнопка filled: `grep "bg-indigo-600.*text-white\|text-white.*bg-indigo-600" app/components/header.py` находит
    - CTA кнопка НЕ flat: `grep "upload_btn.*flat\|flat.*upload" app/components/header.py` → ничего НЕ находит (не должно быть .props("flat") на upload_btn)
    - pick_folder сохранён: `grep "pick_folder" app/components/header.py` находит
    - Nav переименован: `grep '"Реестр"' app/components/header.py` находит строку
    - Active indicator JS: `grep "data-path\|borderBottomColor" app/components/header.py` находит
    - _switch_client сохранён: `grep "def _switch_client" app/components/header.py` находит
  </acceptance_criteria>
  <done>Header визуально тёмный (bg-slate-900), лого-марка «Ю» indigo квадрат + wordmark «рТэг», CTA filled indigo без Quasar color prop, active nav tab с indigo underline indicator, hover states на всех nav items, «Реестр» вместо «Документы», все callbacks сохранены</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - tokens.css с 40+ --yt-* переменными (Plan 01)
    - app.colors() с indigo-600 primary (Plan 01)
    - IBM Plex Sans 300–700 (Plan 01)
    - Тёмный chrome header, лого-марка «Ю», filled CTA, active tab indicator (Plan 02)
  </what-built>
  <how-to-verify>
    1. Запустить приложение: `streamlit run main.py` или `python main.py`
    2. Проверить header: фон тёмный (slate-900), лого «Ю» indigo квадрат слева, «рТэг» белым текстом
    3. Нажать «+ Загрузить документы» — должен открыться диалог выбора папки (pick_folder)
    4. Перейти между вкладками Реестр / Шаблоны / ⚙ — активная вкладка имеет indigo underline
    5. Открыть DevTools → Console: `getComputedStyle(document.documentElement).getPropertyValue('--yt-color-accent')` → должен вернуть `#4f46e5` или ` #4f46e5`
    6. Переключить рабочее пространство (dropdown 📁) — должно работать без ошибок
    7. Открыть страницу реестра → переключить на Calendar view → убедиться что FullCalendar не сломан (нет пустого экрана, нет JS ошибок в консоли)
    8. Проверить фон страницы — не чисто белый (должен быть slate-100, #f1f5f9)
  </how-to-verify>
  <resume-signal>Напиши "approved" если всё выглядит правильно, или опиши проблемы для фиксов</resume-signal>
</task>

</tasks>

<verification>
Финальная проверка Phase 14:

1. `python -c "from app.components.header import render_header; from app.styles import TEXT_HERO, BTN_ACCENT_FILLED; print('ok')"` → ok
2. `grep -c "\-\-yt-" app/static/tokens.css` → >= 40
3. `grep "app\.colors(" app/main.py` → находит строку
4. `grep "tokens" app/main.py` → находит строку с tokens.css до design-system.css
5. `grep "@layer" app/static/design-system.css` → находит минимум 2 строки
6. Визуальный smoke-test: тёмный header, slate-100 фон, FullCalendar работает
</verification>

<success_criteria>
- HEAD-01: Header тёмный (bg-slate-900) на всех страницах
- HEAD-02: Лого-марка «Ю» — indigo квадрат + «рТэг» wordmark
- HEAD-03: CTA «+ Загрузить документы» filled indigo, клик открывает pick_folder
- HEAD-04: Active tab indicator (indigo underline), hover states на всех nav items
- DSGN-01: tokens.css создан, 40+ --yt-* переменных
- DSGN-02: app.colors() вызван, Quasar primary = #4f46e5
- DSGN-03: IBM Plex Sans 300/400/500/600/700 загружены
- DSGN-04: @layer components + @layer overrides в design-system.css
- DSGN-05: --yt-surface-bg (#f1f5f9) установлен как фон; --nicegui-default-padding: 0
- XCUT-03: Все цвета в tokens.css через --yt-* переменные (consistent spacing/color foundation)
</success_criteria>

<output>
После завершения создать `.planning/phases/14-header/14-02-SUMMARY.md`
</output>
