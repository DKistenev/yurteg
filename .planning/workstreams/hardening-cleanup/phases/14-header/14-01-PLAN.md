---
phase: 14-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/static/tokens.css
  - app/static/design-system.css
  - app/styles.py
  - app/main.py
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, XCUT-03]

must_haves:
  truths:
    - "В DevTools: :root содержит все --yt-* CSS переменные (цвета, типографика, spacing, shadows, radii)"
    - "app.colors() вызван в main.py — Quasar primary = #4f46e5 совпадает с --yt-color-accent"
    - "IBM Plex Sans загружается с weights 300/400/500/600/700 (открыть Network tab → fonts)"
    - "Фон страницы не белый — body/nicegui-content получает --yt-surface-bg (#f1f5f9)"
    - "--nicegui-default-padding: 0 и --nicegui-default-gap: 0 видны в :root DevTools"
    - "Все кастомные правила в design-system.css обёрнуты в @layer components или @layer overrides"
    - "FullCalendar calendar view открывается без артефактов после добавления токенов"
  artifacts:
    - path: "app/static/tokens.css"
      provides: "CSS custom properties — single source of truth"
      contains: "--yt-color-accent"
    - path: "app/static/design-system.css"
      provides: "Behavioral CSS — animations + Quasar overrides with @layer discipline"
      contains: "@layer components"
    - path: "app/styles.py"
      provides: "Python-side Tailwind constants + HEX dict for AG Grid"
      contains: "TEXT_HERO"
    - path: "app/main.py"
      provides: "Entrypoint — loads tokens.css first, calls app.colors()"
      contains: "app.colors("
  key_links:
    - from: "app/main.py"
      to: "app/static/tokens.css"
      via: "ui.add_head_html loading tokens.css before design-system.css"
      pattern: "tokens\\.css"
    - from: "app/main.py"
      to: "app.colors()"
      via: "module-level app.colors() call with #4f46e5"
      pattern: "app\\.colors\\("
---

<objective>
Создать дизайн-систему ЮрТэг v0.7 — файл tokens.css с полным набором --yt-* CSS переменных, обновить main.py для правильного порядка загрузки, настроить @layer дисциплину, расширить шрифт до weights 300–700, установить фон контентных зон.

Purpose: Это фундамент — все последующие фазы (header, splash, registry, card) наследуют токены. Без него визуальная работа бессмысленна — значения будут дрейфовать.
Output: tokens.css (новый файл), обновлённые design-system.css / styles.py / main.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-header/14-CONTEXT.md

<interfaces>
<!-- Текущие файлы, которые будут изменены. Прочитай их ВСЕ перед любыми правками. -->

app/main.py — текущий порядок загрузки CSS:
1. Google Fonts (400;600 только)
2. design-system.css
3. calendar tooltip div + calendar.js
4. Status badge Tailwind @layer block

app/static/design-system.css — существующий файл с анимациями (row-in, page-fade-in, dialog-in, badge-in, toast-in), hover actions, FullCalendar overrides. CSS написан БЕЗ @layer.

app/styles.py — существующие константы:
- HEX dict: slate_300..indigo_50 (используется в AG Grid JS cellRenderer — НЕ УДАЛЯТЬ)
- BTN_PRIMARY, SEG_ACTIVE/INACTIVE, TEXT_HEADING* и др.

CRITICAL: Функциональные CSS классы в design-system.css — НЕ ПЕРЕИМЕНОВЫВАТЬ:
- actions-cell, action-icon, expand-icon (AG Grid cellRenderer API)
- status-active, status-expiring, status-expired, status-unknown, status-terminated, status-extended, status-negotiation, status-suspended (в main.py @layer + AG Grid JS)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать tokens.css с полным набором --yt-* переменных</name>
  <files>app/static/tokens.css</files>
  <read_first>
    - app/static/design-system.css (какие hex-значения уже используются — вынести как токены)
    - app/styles.py (HEX dict — должны совпадать с primitives в tokens.css)
    - .planning/research/ARCHITECTURE.md (полная спецификация токенов, раздел Pattern 1)
    - .planning/research/STACK.md (раздел Layer 2 — CSS custom properties design tokens)
  </read_first>
  <action>
Создать новый файл `app/static/tokens.css` с двухуровневой системой токенов.

ВАЖНО: Использовать ТОЛЬКО prefix `--yt-` для всех переменных проекта (избегаем коллизии с --fc-* FullCalendar).

Содержимое файла:

```css
/* ЮрТэг Design Tokens v0.7
   Prefix: --yt-* (избегаем коллизии с --fc-* FullCalendar)
   Порядок загрузки: ПЕРВЫМ в main.py, до design-system.css
*/

:root {
  /* ── Primitives — палитра ─────────────────────────────────────── */
  --yt-p-indigo-600: #4f46e5;
  --yt-p-indigo-700: #4338ca;
  --yt-p-indigo-50:  #eef2ff;
  --yt-p-slate-900:  #0f172a;
  --yt-p-slate-800:  #1e293b;
  --yt-p-slate-700:  #334155;
  --yt-p-slate-600:  #475569;
  --yt-p-slate-500:  #64748b;
  --yt-p-slate-400:  #94a3b8;
  --yt-p-slate-300:  #cbd5e1;
  --yt-p-slate-200:  #e2e8f0;
  --yt-p-slate-100:  #f1f5f9;
  --yt-p-slate-50:   #f8fafc;
  --yt-p-green-600:  #059669;
  --yt-p-red-600:    #dc2626;
  --yt-p-amber-600:  #d97706;

  /* ── Semantic — цвета по назначению ─────────────────────────── */
  --yt-color-accent:        var(--yt-p-indigo-600);
  --yt-color-accent-hover:  var(--yt-p-indigo-700);
  --yt-color-accent-subtle: var(--yt-p-indigo-50);
  --yt-color-text-primary:  var(--yt-p-slate-900);
  --yt-color-text-secondary:var(--yt-p-slate-600);
  --yt-color-text-muted:    var(--yt-p-slate-400);
  --yt-color-surface:       #ffffff;
  --yt-surface-bg:          var(--yt-p-slate-100);   /* фон контентных зон — не белый */
  --yt-color-surface-raised:var(--yt-p-slate-50);
  --yt-color-border:        var(--yt-p-slate-200);
  --yt-color-border-strong: var(--yt-p-slate-300);
  --yt-color-hero-bg:       var(--yt-p-slate-900);   /* тёмный фон hero/splash */

  /* ── Типографика ─────────────────────────────────────────────── */
  --yt-font-family: 'IBM Plex Sans', system-ui, sans-serif;

  /* Размеры */
  --yt-text-hero:    clamp(2.5rem, 5vw, 3.5rem);   /* splash hero headline */
  --yt-text-h1:      1.75rem;    /* 28px — page title */
  --yt-text-h2:      1.25rem;    /* 20px — card heading */
  --yt-text-body:    0.875rem;   /* 14px — body */
  --yt-text-small:   0.75rem;    /* 12px — labels, badges */

  /* Веса */
  --yt-weight-light:    300;
  --yt-weight-regular:  400;
  --yt-weight-medium:   500;
  --yt-weight-semibold: 600;
  --yt-weight-bold:     700;

  /* ── Spacing ─────────────────────────────────────────────────── */
  --yt-space-1:  0.25rem;
  --yt-space-2:  0.5rem;
  --yt-space-3:  0.75rem;
  --yt-space-4:  1rem;
  --yt-space-6:  1.5rem;
  --yt-space-8:  2rem;
  --yt-space-12: 3rem;
  --yt-space-16: 4rem;

  /* ── Shadows ─────────────────────────────────────────────────── */
  --yt-shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --yt-shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.07), 0 2px 4px -2px rgb(0 0 0 / 0.05);
  --yt-shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.08), 0 4px 6px -4px rgb(0 0 0 / 0.05);
  --yt-shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.06);
  --yt-shadow-header: 0 1px 3px rgb(0 0 0 / 0.04);

  /* ── Radii ───────────────────────────────────────────────────── */
  --yt-radius-sm: 0.375rem;   /* 6px */
  --yt-radius-md: 0.5rem;     /* 8px */
  --yt-radius-lg: 0.75rem;    /* 12px */
  --yt-radius-xl: 1rem;       /* 16px */

  /* ── Transitions ─────────────────────────────────────────────── */
  --yt-ease-out:     cubic-bezier(0.25, 1, 0.5, 1);
  --yt-duration-fast:150ms;
  --yt-duration-base:200ms;
  --yt-duration-slow:300ms;

  /* ── NiceGUI spacing reset ───────────────────────────────────── */
  /* Предотвращает коллизию --nicegui-default-padding с Tailwind p-* классами */
  --nicegui-default-padding: 0;
  --nicegui-default-gap: 0;
}

/* ── FullCalendar CSS переменные (задокументированы при smoke-test) ── */
/* --fc-border-color, --fc-button-bg-color, --fc-button-hover-bg-color,
   --fc-event-bg-color, --fc-event-border-color, --fc-today-bg-color
   Эти переменные НЕ конфликтуют с --yt-* префиксом. */
```

Файл создаётся с нуля — он не существовал ранее.
  </action>
  <verify>
    <automated>grep -c "\-\-yt-" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/static/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - tokens.css существует: `ls app/static/tokens.css` выводит файл
    - Содержит минимум 40 --yt-* переменных: `grep -c "\-\-yt-" app/static/tokens.css` >= 40
    - Содержит --yt-color-accent: `grep "\-\-yt-color-accent:" app/static/tokens.css` находит строку
    - Содержит --yt-surface-bg: `grep "\-\-yt-surface-bg:" app/static/tokens.css` находит строку
    - Содержит --nicegui-default-padding: 0: `grep "nicegui-default-padding: 0" app/static/tokens.css` находит строку
    - НЕТ переменных без --yt-* prefix (кроме --nicegui-*): все кастомные переменные начинаются с --yt-
  </acceptance_criteria>
  <done>tokens.css создан с двухуровневой системой: primitives (--yt-p-*) + semantic (--yt-color-*, --yt-text-*, --yt-space-*, --yt-shadow-*, --yt-radius-*, --yt-duration-*), --nicegui-default-padding/gap сброшены в 0</done>
</task>

<task type="auto">
  <name>Task 2: Обновить main.py и design-system.css — порядок загрузки, @layer дисциплина, шрифт, app.colors()</name>
  <files>app/main.py, app/static/design-system.css, app/styles.py</files>
  <read_first>
    - app/main.py (текущий порядок загрузки CSS — нужно вставить tokens.css ПЕРВЫМ)
    - app/static/design-system.css (нужно обернуть существующие правила в @layer components/@layer overrides)
    - app/styles.py (добавить v0.7 константы, не ломать существующие)
    - .planning/research/STACK.md (раздел Layer 1 — app.colors() API и Layer 4 — @layer overrides)
    - .planning/research/PITFALLS.md (Pitfall 1 — @layer, Pitfall 2 — Quasar !important, Pitfall 8 — NiceGUI padding)
  </read_first>
  <action>
**1. app/main.py — три изменения:**

A) Расширить Google Fonts URL — добавить weights 300 и 500 и 700. Заменить:
```
family=IBM+Plex+Sans:wght@400;600
```
На:
```
family=IBM+Plex+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700&display=swap&subset=cyrillic
```
Также добавить в body style: `color: var(--yt-color-text-primary);`

B) Добавить загрузку tokens.css ПЕРВЫМ — до design-system.css. Вставить после _STATIC = ... строки:
```python
# tokens.css — ПЕРВЫМ (CSS custom properties должны быть до всех потребителей)
ui.add_head_html(f'<link rel="stylesheet" href="/_nicegui/static/tokens.css">', shared=True)
```
НО поскольку NiceGUI не раздаёт app/static/ напрямую — использовать inline:
```python
ui.add_head_html(f'<style>{(_STATIC / "tokens.css").read_text()}</style>', shared=True)
```
Вставить ЭТУ строку ПЕРЕД строкой с design-system.css.

C) Добавить `app.colors()` вызов на уровне модуля — после импортов, до `@ui.page` декоратора. Вставить сразу после строки `app.on_disconnect(_stop_llama)`:
```python
# Quasar color bridge — синхронизировать --q-primary с --yt-color-accent
app.colors(
    primary='#4f46e5',    # --yt-p-indigo-600
    secondary='#64748b',  # --yt-p-slate-500
    accent='#4f46e5',
    positive='#059669',   # green-600
    negative='#dc2626',   # red-600
    warning='#d97706',    # amber-600
    info='#3b82f6',       # blue-500
    dark='#0f172a',       # --yt-p-slate-900
    dark_page='#0f172a',
)
```

D) Добавить фон контентных зон — после status badge CSS блока, добавить:
```python
# Content area background — не белый (DSGN-05)
ui.add_head_html("""
<style>
  body { background: var(--yt-surface-bg) !important; }
  .nicegui-content { background: var(--yt-surface-bg); }
</style>
""", shared=True)
```

**2. app/static/design-system.css — @layer дисциплина:**

Обернуть существующие правила в @layer. Структура:

- Типографический baseline (h1-h6, p, li) → поместить вне layer (глобальный reset, безопасно)
- AG Row анимация и hover actions → обернуть в `@layer components { ... }` НО блок `.ag-row` оставить БЕЗ layer prefix (AG Grid живёт вне NiceGUI layer system — Pitfall 3)
- `.nicegui-content` → оставить вне layer (глобальный)
- Кнопки `.q-btn*` → обернуть в `@layer overrides { ... }`
- Dialog transitions → обернуть в `@layer overrides { ... }`
- `.q-card` hover → обернуть в `@layer components { ... }`
- Badge animations → оставить вне layer (работают через attribute selector)
- Toast, expansion, progress → обернуть в `@layer overrides { ... }`
- Link underline → обернуть в `@layer components { ... }`
- `@media (prefers-reduced-motion)` → оставить как есть (глобальный)
- FullCalendar overrides → оставить вне layer (они уже специфичны через .fc prefix)

ВАЖНО: НЕ ПЕРЕИМЕНОВЫВАТЬ классы: actions-cell, action-icon, expand-icon, status-*.

Также добавить в конец design-system.css новый раздел:
```css
/* ── Hero entrance animation (v0.7) ────────────────────────────── */
@keyframes hero-slide-up {
  from { opacity: 0; transform: translateY(24px); }
  to   { opacity: 1; transform: translateY(0); }
}
@layer components {
  .hero-enter {
    animation: hero-slide-up var(--yt-duration-slow) var(--yt-ease-out) both;
  }
  .hero-enter:nth-child(1) { animation-delay: 0ms; }
  .hero-enter:nth-child(2) { animation-delay: 100ms; }
  .hero-enter:nth-child(3) { animation-delay: 200ms; }
  .hero-enter:nth-child(4) { animation-delay: 300ms; }
}
```

**3. app/styles.py — добавить v0.7 константы:**

В конец файла добавить:
```python

# ── v0.7 Design System constants ─────────────────────────────────────────────

# Hero / dark surface typography
TEXT_HERO = "font-bold text-white tracking-tight leading-tight"
TEXT_HERO_SUB = "text-lg font-light text-slate-300"
TEXT_EYEBROW = "text-xs font-semibold text-slate-400 uppercase tracking-widest"

# Stats bar (Phase 16)
STAT_NUMBER = "text-2xl font-bold tabular-nums"
STAT_LABEL = "text-xs uppercase tracking-wide"

# Section structural
SECTION_LABEL = "text-xs font-semibold uppercase tracking-wider"
DIVIDER = "border-t border-slate-200 my-4"

# Template card (Phase 17)
TEMPLATE_CARD = "bg-white border border-slate-200 rounded-xl p-5 cursor-pointer"

# Accent CTA (filled, per D-decision — не Quasar color prop, а Tailwind класс)
BTN_ACCENT_FILLED = "px-5 py-2 bg-indigo-600 text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 transition-colors duration-150"
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "import app.styles; print('styles ok')" && grep -c "tokens.css\|tokens" app/main.py && grep "app\.colors(" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py содержит строку с tokens.css до строки с design-system.css: `grep -n "tokens\|design-system" app/main.py` показывает tokens на меньшей строке
    - app/main.py содержит app.colors(: `grep "app\.colors(" app/main.py` находит строку
    - app/main.py содержит primary='#4f46e5': `grep "primary='#4f46e5'" app/main.py` находит
    - Google Fonts URL содержит weights 300 и 700: `grep "300.*700\|wght@.*300" app/main.py` находит
    - app/static/design-system.css содержит @layer: `grep -c "@layer" app/static/design-system.css` >= 2
    - app/static/design-system.css содержит hero-slide-up: `grep "hero-slide-up" app/static/design-system.css` находит
    - app/styles.py содержит TEXT_HERO: `grep "TEXT_HERO" app/styles.py` находит строку
    - app/styles.py содержит BTN_ACCENT_FILLED: `grep "BTN_ACCENT_FILLED" app/styles.py` находит строку
    - Python импортирует без ошибок: `python -c "import app.styles"` завершается с кодом 0
  </acceptance_criteria>
  <done>tokens.css загружается первым в main.py; app.colors() вызван с indigo-600 primary; шрифт 300–700; design-system.css использует @layer; styles.py расширен v0.7 константами; фон контентных зон установлен в --yt-surface-bg</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:

1. Запустить приложение и открыть DevTools (Inspect Element)
2. В консоли: `getComputedStyle(document.documentElement).getPropertyValue('--yt-color-accent')` → должен вернуть `#4f46e5`
3. Проверить `--nicegui-default-padding` → должен вернуть `0`
4. Убедиться что фон страницы — slate-100 (#f1f5f9), не белый
5. Открыть calendar view и убедиться что FullCalendar не сломан
6. Smoke-test: функциональные кнопки, AG Grid статусы, навигация табов — всё работает
</verification>

<success_criteria>
- tokens.css создан, содержит 40+ --yt-* переменных (primitives + semantic)
- --nicegui-default-padding: 0 установлен
- tokens.css загружается ПЕРВЫМ в main.py
- app.colors() вызван с primary=#4f46e5
- IBM Plex Sans загружается с weights 300/400/500/600/700
- design-system.css содержит @layer components и @layer overrides
- styles.py содержит новые v0.7 константы (TEXT_HERO, BTN_ACCENT_FILLED и др.)
- Приложение запускается без ошибок
- FullCalendar calendar view без артефактов
</success_criteria>

<output>
После завершения создать `.planning/phases/14-header/14-01-SUMMARY.md`
</output>
