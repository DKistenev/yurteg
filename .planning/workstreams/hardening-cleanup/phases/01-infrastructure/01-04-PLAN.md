---
phase: 01-infrastructure
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - modules/ai_extractor.py
  - tests/test_date_normalization.py
autonomous: true
requirements:
  - FUND-04

must_haves:
  truths:
    - "Дата «31 декабря 2025 г.» сохраняется в БД как «2025-12-31», не как raw строка"
    - "Дата «31.12.25» нормализуется в «2025-12-31» (двузначный год)"
    - "Строка «бессрочный» возвращает None — не вызывает исключение"
    - "Строка «2025» (год без дня/месяца) возвращает None — не создаёт «2025-01-01»"
    - "Уже правильная дата «2025-12-31» проходит fast-path без вызова dateutil"
  artifacts:
    - path: "modules/ai_extractor.py"
      provides: "_normalize_date() функция + её применение в _json_to_metadata()"
      contains: "_normalize_date"
    - path: "tests/test_date_normalization.py"
      provides: "6 unit-тестов: russian_full, short_year, iso_passthrough, unparseable, year_only, none_input"
      exports: ["test_russian_full", "test_short_year", "test_iso_passthrough", "test_unparseable", "test_year_only_returns_none", "test_none_input"]
  key_links:
    - from: "modules/ai_extractor.py _json_to_metadata()"
      to: "_normalize_date()"
      via: "date_signed=_normalize_date(data.get('date_signed'))"
      pattern: "_normalize_date"
---

<objective>
Добавить функцию `_normalize_date()` в `ai_extractor.py` и применить её в `_json_to_metadata()` для всех трёх дат (date_signed, date_start, date_end). Использовать `python-dateutil` (уже установлен, версия 2.9.0.post0).

Purpose: FUND-04 — даты, извлечённые AI, нормализуются к ISO 8601 перед сохранением. AI возвращает «31 декабря 2025 г.», «31.12.25», «January 1, 2025» — всё должно стать «2025-12-31».
Output: `_normalize_date()` в ai_extractor.py, обновлённый `_json_to_metadata()`, тесты.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Из modules/ai_extractor.py — _json_to_metadata() которую нужно изменить (строки 470-512) -->

Текущий _json_to_metadata() — строки с датами (будут изменены):
```python
def _json_to_metadata(data: dict) -> ContractMetadata:
    # ... list/confidence parsing ...
    return ContractMetadata(
        contract_type=data.get("document_type") or data.get("contract_type"),
        counterparty=data.get("counterparty"),
        subject=data.get("subject"),
        date_signed=data.get("date_signed"),   # ← raw строка от AI, заменить
        date_start=data.get("date_start"),     # ← raw строка от AI, заменить
        date_end=data.get("date_end"),         # ← raw строка от AI, заменить
        amount=data.get("amount"),
        ...
    )
```

Уже установленные зависимости (подтверждено в RESEARCH.md):
- python-dateutil 2.9.0.post0
- Ключевой вызов: dateutil.parser.parse(raw, dayfirst=True)
- Исключение при ошибке парсинга: dateutil.parser.ParserError

ВАЖНО — pitfall из RESEARCH.md:
- dateutil.parser.parse("2025") возвращает datetime(2025, current_month, current_day)
  → нужно защититься: if raw.isdigit() and len(raw) <= 4: return None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать тест-файл нормализации дат (Wave 0 — тесты перед реализацией)</name>
  <files>tests/test_date_normalization.py</files>
  <read_first>
    - tests/conftest.py
    - modules/ai_extractor.py (строки 470-512 — _json_to_metadata, понять откуда экспортировать)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 5 — точное поведение _normalize_date)
  </read_first>
  <behavior>
    - test_russian_full: _normalize_date("31 декабря 2025 г.") → "2025-12-31"
    - test_short_year: _normalize_date("31.12.25") → "2025-12-31"
    - test_iso_passthrough: _normalize_date("2025-12-31") → "2025-12-31" (fast path, без dateutil)
    - test_unparseable: _normalize_date("бессрочный") → None
    - test_year_only_returns_none: _normalize_date("2025") → None (год-без-даты отклоняется)
    - test_none_input: _normalize_date(None) → None
  </behavior>
  <action>
Создать `tests/test_date_normalization.py`. Содержание:

```python
"""Тесты нормализации дат из AI-ответа (FUND-04).

_normalize_date() конвертирует произвольные строки дат в ISO 8601 (YYYY-MM-DD).
Возвращает None для неразбираемых строк и year-only значений.
"""
import pytest

# _normalize_date будет добавлена в ai_extractor.py
from modules.ai_extractor import _normalize_date


def test_russian_full():
    """Полная русская дата нормализуется в ISO 8601."""
    result = _normalize_date("31 декабря 2025 г.")
    assert result == "2025-12-31", f"Ожидалось '2025-12-31', получено {result!r}"


def test_short_year():
    """Дата с двузначным годом нормализуется корректно."""
    result = _normalize_date("31.12.25")
    assert result == "2025-12-31", f"Ожидалось '2025-12-31', получено {result!r}"


def test_iso_passthrough():
    """Дата уже в ISO 8601 проходит fast-path и возвращается без изменений."""
    result = _normalize_date("2025-12-31")
    assert result == "2025-12-31", f"Ожидалось '2025-12-31', получено {result!r}"


def test_unparseable():
    """Неразбираемая строка возвращает None, не поднимает исключение."""
    result = _normalize_date("бессрочный")
    assert result is None, f"Ожидался None, получено {result!r}"


def test_year_only_returns_none():
    """Строка из одного года возвращает None (dateutil даёт misleading результат)."""
    result = _normalize_date("2025")
    assert result is None, (
        f"Ожидался None для year-only строки '2025', получено {result!r}. "
        "dateutil.parser.parse('2025') = datetime(2025, current_month, current_day) — это неверно."
    )


def test_none_input():
    """None на входе возвращает None."""
    result = _normalize_date(None)
    assert result is None, f"Ожидался None для None-входа, получено {result!r}"
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_date_normalization.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл `tests/test_date_normalization.py` существует и содержит 6 test-функций
    - Все 6 тестов падают с ImportError (`cannot import name '_normalize_date'`) — RED state
  </acceptance_criteria>
  <done>6 тестов в RED state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Реализовать _normalize_date() и применить в _json_to_metadata()</name>
  <files>modules/ai_extractor.py</files>
  <read_first>
    - modules/ai_extractor.py (ПОЛНОСТЬЮ — понять все импорты и расположение _json_to_metadata)
    - tests/test_date_normalization.py (знать точные тест-случаи для реализации)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 5 — точный код _normalize_date)
  </read_first>
  <behavior>
    - _normalize_date(None) → None
    - _normalize_date("") → None
    - _normalize_date("null") → None
    - _normalize_date("2025-12-31") → "2025-12-31" (fast path: len==10 и формат YYYY-MM-DD)
    - _normalize_date("2025") → None (year-only guard: isdigit() and len <= 4)
    - _normalize_date("31 декабря 2025 г.") → "2025-12-31" (через dateutil)
    - _normalize_date("31.12.25") → "2025-12-31" (через dateutil с dayfirst=True)
    - _normalize_date("бессрочный") → None (ParserError → return None, лог WARNING)
    - Год вне диапазона 1990-2099 → None (санитарная проверка)
    - _json_to_metadata: date_signed, date_start, date_end обёрнуты в _normalize_date()
  </behavior>
  <action>
Изменить `modules/ai_extractor.py`:

**1. Добавить импорты** в начало файла (после `from openai import ...`):
```python
from dateutil import parser as dateutil_parser
from dateutil.parser import ParserError
```

**2. Добавить функцию `_normalize_date()`** — разместить ПЕРЕД `_merge_system_into_user`, то есть сразу после блока промптов (после строки VERIFY_PROMPT):

```python
def _normalize_date(raw: str | None) -> str | None:
    """Нормализует строку с датой в формат YYYY-MM-DD (ISO 8601).

    Возвращает None если строка непарсируема, год-only, или None на входе.
    Логирует оригинальное значение при неудаче.

    Примеры:
        "31 декабря 2025 г." → "2025-12-31"
        "31.12.2025"         → "2025-12-31"
        "31.12.25"           → "2025-12-31"
        "January 1, 2025"    → "2025-01-01"
        "2025-12-31"         → "2025-12-31"  (fast path)
        "бессрочный"         → None
        "2025"               → None  (year-only: dateutil даёт misleading результат)
        None                 → None
    """
    if not raw:
        return None
    raw = raw.strip()
    if not raw or raw.lower() in ("null", "none", ""):
        return None

    # Fast path: уже ISO 8601
    if len(raw) == 10 and raw[4] == "-" and raw[7] == "-":
        return raw

    # Защита от year-only строк: dateutil.parse("2025") → datetime(2025, today.month, today.day)
    # что создаёт ложную дату. Любая валидная дата содержит день и месяц.
    if len(raw) <= 4 and raw.isdigit():
        logger.warning("Отклонена year-only строка даты: %r", raw)
        return None

    try:
        dt = dateutil_parser.parse(raw, dayfirst=True)
        normalized = dt.strftime("%Y-%m-%d")
        # Санитарная проверка: договоры только в диапазоне 1990–2099
        if not (1990 <= dt.year <= 2099):
            logger.warning("Дата вне допустимого диапазона: %r → %s", raw, normalized)
            return None
        return normalized
    except (ParserError, ValueError, OverflowError):
        logger.warning("Не удалось нормализовать дату: %r", raw)
        return None
```

**3. Изменить `_json_to_metadata()`** — обернуть три даты в `_normalize_date()`:

Найти в `_json_to_metadata()` блок `return ContractMetadata(...)` и заменить строки с датами:
```python
# БЫЛО:
date_signed=data.get("date_signed"),
date_start=data.get("date_start"),
date_end=data.get("date_end"),

# СТАЛО:
date_signed=_normalize_date(data.get("date_signed")),
date_start=_normalize_date(data.get("date_start")),
date_end=_normalize_date(data.get("date_end")),
```

ВАЖНО:
- `_normalize_date` добавляется как МОДУЛЬНАЯ функция (не метод класса)
- Импорты dateutil добавляются ВВЕРХУ файла, не внутри функции
- `dayfirst=True` обязателен для корректной обработки российских дат (dd.mm.yyyy)
- Функция возвращает None при любой ошибке, не поднимает исключений
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_date_normalization.py -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "_normalize_date" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/modules/ai_extractor.py` выдаёт минимум 4 строки (def + 3 применения в _json_to_metadata)
    - `grep "from dateutil" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/modules/ai_extractor.py` выдаёт строку с импортом
    - `python -c "from modules.ai_extractor import _normalize_date; print(_normalize_date('31 декабря 2025 г.'))"` выводит `2025-12-31`
    - `python -c "from modules.ai_extractor import _normalize_date; print(_normalize_date('2025'))"` выводит `None`
    - `python -m pytest tests/test_date_normalization.py -v` — все 6 тестов GREEN
  </acceptance_criteria>
  <done>
    - test_russian_full PASS
    - test_short_year PASS
    - test_iso_passthrough PASS
    - test_unparseable PASS
    - test_year_only_returns_none PASS
    - test_none_input PASS
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg
python -m pytest tests/test_date_normalization.py -v
# Ручная проверка нескольких случаев:
python -c "
from modules.ai_extractor import _normalize_date
cases = [
    ('31 декабря 2025 г.', '2025-12-31'),
    ('31.12.25', '2025-12-31'),
    ('2025-12-31', '2025-12-31'),
    ('бессрочный', None),
    ('2025', None),
    (None, None),
]
for raw, expected in cases:
    result = _normalize_date(raw)
    status = 'OK' if result == expected else f'FAIL (got {result!r})'
    print(f'{raw!r:30} → {result!r:15} {status}')
"
```
</verification>

<success_criteria>
- `python -m pytest tests/test_date_normalization.py -v` — 6/6 PASSED
- _normalize_date() присутствует в ai_extractor.py как модульная функция
- В _json_to_metadata() все три даты (date_signed, date_start, date_end) обёрнуты в _normalize_date()
- Импорты dateutil_parser и ParserError добавлены в начало ai_extractor.py
</success_criteria>

<output>
После завершения создать `.planning/phases/01-infrastructure/01-04-SUMMARY.md`
</output>
