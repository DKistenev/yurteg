---
phase: 02-document-lifecycle
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - services/version_service.py
  - modules/ai_extractor.py
  - services/pipeline_service.py
autonomous: true
requirements:
  - LIFE-03

must_haves:
  truths:
    - "При загрузке обновлённого договора система автоматически находит его предыдущую версию (similarity >= 0.85)"
    - "Связанные версии записываются в document_versions с одним contract_group_id"
    - "Новый документ без связи получает новый contract_group_id (равный своему contract_id)"
    - "Эмбеддинги хранятся в SQLite таблице embeddings как BLOB, не пересчитываются при повторном запуске"
    - "Embedding-модель загружается один раз через @st.cache_resource или module-level singleton"
  artifacts:
    - path: "services/version_service.py"
      provides: "Сервис версионирования: compute_embedding, find_version_match, link_versions, get_version_group"
      exports: ["compute_embedding", "find_version_match", "link_versions", "get_version_group", "get_embedding_model"]
    - path: "modules/ai_extractor.py"
      provides: "Извлечение payment_terms, payment_amount, payment_frequency из текста договора"
      contains: "payment_frequency"
    - path: "services/pipeline_service.py"
      provides: "Вызов version_service.find_version_match после сохранения в БД"
      contains: "find_version_match"
  key_links:
    - from: "services/pipeline_service.py"
      to: "services/version_service.find_version_match"
      via: "вызов после database.save_result() для каждого нового документа"
      pattern: "find_version_match"
    - from: "services/version_service.find_version_match"
      to: "embeddings table в SQLite"
      via: "store_embedding / load_embedding через numpy BLOB"
      pattern: "store_embedding|load_embedding"
---

<objective>
Реализовать автоматическое версионирование документов: вычисление эмбеддингов, поиск версий, связывание.

Purpose: LIFE-03 — система сама распознаёт что загруженный файл это новая версия существующего договора, а не отдельный документ. Дубликатов в реестре не будет.
Output: services/version_service.py с полным версионным пайплайном, хук в pipeline_service.py, расширение ai_extractor.py для извлечения платёжных данных (нужны для 02-05).
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-document-lifecycle/02-CONTEXT.md
@.planning/phases/02-document-lifecycle/02-RESEARCH.md
@.planning/phases/02-document-lifecycle/02-01-SUMMARY.md

<interfaces>
<!-- Из modules/database.py (после 02-01) -->
<!-- Таблица embeddings: contract_id PK, vector BLOB, model_version TEXT, created_at -->
<!-- Таблица document_versions: id, contract_group_id, contract_id, version_number, link_method, created_at, linked_at -->

Паттерны работы с эмбеддингами из 02-RESEARCH.md:
```python
import io, numpy as np

def store_embedding(conn, contract_id: int, vector: np.ndarray, model_version: str):
    buf = io.BytesIO()
    np.save(buf, vector)
    conn.execute(
        "INSERT OR REPLACE INTO embeddings (contract_id, vector, model_version) VALUES (?,?,?)",
        (contract_id, buf.getvalue(), model_version)
    )
    conn.commit()

def load_embedding(conn, contract_id: int) -> np.ndarray | None:
    row = conn.execute(
        "SELECT vector FROM embeddings WHERE contract_id=?", (contract_id,)
    ).fetchone()
    return np.load(io.BytesIO(row[0])) if row else None

def cosine_sim(a: np.ndarray, b: np.ndarray) -> float:
    return float(np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b)))

VERSION_LINK_THRESHOLD = 0.85   # подтверждён тестами на русских юридических текстах
TEMPLATE_MATCH_THRESHOLD = 0.60
EMBEDDING_MODEL = "paraphrase-multilingual-MiniLM-L12-v2"
```

Из modules/ai_extractor.py — текущие поля ContractMetadata:
```python
contract_type, counterparty, subject, date_signed, date_start, date_end,
amount, special_conditions, parties, confidence, is_template
# НУЖНО ДОБАВИТЬ: payment_terms (str | None), payment_amount (float | None),
# payment_frequency (str | None: 'monthly'|'quarterly'|'yearly'|'once')
# payment_direction (str | None: 'income'|'expense')
```

Из services/pipeline_service.py (Phase 1):
```python
# process_archive() вызывает controller, который для каждого файла вызывает pipeline
# После database.save_result(result) → здесь добавить вызов version_service
# contract_id доступен через db._conn.lastrowid или запрос по file_hash
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/version_service.py — эмбеддинги и автосвязывание версий</name>
  <files>services/version_service.py</files>
  <read_first>
    - .planning/phases/02-document-lifecycle/02-RESEARCH.md (Pattern 2: Embeddings cache, пороги)
    - modules/database.py (метод _conn, _lock — как обращаться к SQLite)
    - modules/models.py (DocumentVersion dataclass)
  </read_first>
  <action>
Создать services/version_service.py:

```python
"""Сервис версионирования документов.

Использует sentence-transformers MiniLM-L12-v2 для семантического
сравнения текстов договоров. Вектора кэшируются в SQLite (embeddings table).

Не импортирует streamlit — вызывается из pipeline_service и main.py.
"""
import io
import logging
import threading
from typing import Optional

import numpy as np

from modules.database import Database
from modules.models import DocumentVersion

logger = logging.getLogger(__name__)

EMBEDDING_MODEL = "paraphrase-multilingual-MiniLM-L12-v2"
VERSION_LINK_THRESHOLD = 0.85   # sim >= 0.85 → версия того же договора
TEMPLATE_MATCH_THRESHOLD = 0.60  # sim >= 0.60 → подходящий шаблон

_model = None
_model_lock = threading.Lock()


def get_embedding_model():
    """Загружает модель один раз (lazy singleton). Thread-safe."""
    global _model
    if _model is None:
        with _model_lock:
            if _model is None:
                from sentence_transformers import SentenceTransformer
                logger.info("Загрузка embedding-модели %s...", EMBEDDING_MODEL)
                _model = SentenceTransformer(EMBEDDING_MODEL)
                logger.info("Embedding-модель загружена")
    return _model


def compute_embedding(text: str) -> np.ndarray:
    """Вычисляет 384-мерный вектор для текста. Использует первые 512 токенов."""
    model = get_embedding_model()
    # Ограничить длину текста (модель использует первые 512 токенов нативно)
    return model.encode(text[:8000], normalize_embeddings=True)


def _store_embedding(conn, contract_id: int, vector: np.ndarray) -> None:
    buf = io.BytesIO()
    np.save(buf, vector)
    conn.execute(
        "INSERT OR REPLACE INTO embeddings (contract_id, vector, model_version) VALUES (?,?,?)",
        (contract_id, buf.getvalue(), EMBEDDING_MODEL),
    )
    conn.commit()


def _load_embedding(conn, contract_id: int) -> Optional[np.ndarray]:
    row = conn.execute(
        "SELECT vector, model_version FROM embeddings WHERE contract_id=?",
        (contract_id,),
    ).fetchone()
    if row is None:
        return None
    # Если модель изменилась — вернуть None для пересчёта
    if row[1] != EMBEDDING_MODEL:
        return None
    return np.load(io.BytesIO(row[0]))


def _cosine_sim(a: np.ndarray, b: np.ndarray) -> float:
    norm_a, norm_b = np.linalg.norm(a), np.linalg.norm(b)
    if norm_a == 0 or norm_b == 0:
        return 0.0
    return float(np.dot(a, b) / (norm_a * norm_b))


def ensure_embedding(db: Database, contract_id: int, text: str) -> np.ndarray:
    """Возвращает кэшированный вектор или вычисляет и сохраняет новый."""
    with db._lock:
        cached = _load_embedding(db._conn, contract_id)
    if cached is not None:
        return cached
    vector = compute_embedding(text)
    with db._lock:
        _store_embedding(db._conn, contract_id, vector)
    return vector


def find_version_match(
    db: Database,
    contract_id: int,
    text: str,
    contract_type: Optional[str],
    counterparty: Optional[str],
) -> Optional[int]:
    """Ищет существующий документ, который является предыдущей версией этого.

    Алгоритм:
    1. Сохранить/получить эмбеддинг нового документа
    2. Найти кандидатов по contract_type + counterparty (O(1))
    3. Сравнить косинусное сходство с кандидатами
    4. Вернуть contract_group_id первого кандидата с sim >= VERSION_LINK_THRESHOLD

    Returns: contract_group_id существующей группы или None (новый документ)
    """
    new_vector = ensure_embedding(db, contract_id, text)

    # Найти кандидатов — только документы с тем же типом и контрагентом
    with db._lock:
        query = """
            SELECT c.id, dv.contract_group_id
            FROM contracts c
            JOIN document_versions dv ON dv.contract_id = c.id
            WHERE c.id != ?
              AND (:contract_type IS NULL OR c.contract_type = :contract_type)
              AND (:counterparty IS NULL OR c.counterparty = :counterparty)
              AND c.status = 'done'
        """
        candidates = db._conn.execute(query, {
            "contract_id": contract_id,
            "contract_type": contract_type,
            "counterparty": counterparty,
        }).fetchall()

    if not candidates:
        return None

    best_sim = 0.0
    best_group_id = None

    for cand_id, group_id in candidates:
        with db._lock:
            cand_vector = _load_embedding(db._conn, cand_id)
        if cand_vector is None:
            continue
        sim = _cosine_sim(new_vector, cand_vector)
        if sim > best_sim:
            best_sim = sim
            best_group_id = group_id

    if best_sim >= VERSION_LINK_THRESHOLD:
        logger.info(
            "Найдена версия: contract_id=%d → group_id=%d (sim=%.3f)",
            contract_id, best_group_id, best_sim,
        )
        return best_group_id

    return None


def link_versions(
    db: Database,
    contract_id: int,
    group_id: Optional[int],
    link_method: str = "auto_embedding",
) -> int:
    """Связывает документ с группой версий или создаёт новую группу.

    Returns: contract_group_id (существующий или новый)
    """
    with db._lock:
        if group_id is None:
            # Новый договор — group_id = contract_id (convention)
            effective_group_id = contract_id
        else:
            effective_group_id = group_id

        # Определить следующий version_number в группе
        row = db._conn.execute(
            "SELECT MAX(version_number) FROM document_versions WHERE contract_group_id=?",
            (effective_group_id,),
        ).fetchone()
        next_version = (row[0] or 0) + 1

        db._conn.execute(
            """INSERT INTO document_versions
               (contract_group_id, contract_id, version_number, link_method)
               VALUES (?, ?, ?, ?)""",
            (effective_group_id, contract_id, next_version, link_method),
        )
        db._conn.commit()

    logger.info(
        "Документ contract_id=%d добавлен в группу %d как v%d (%s)",
        contract_id, effective_group_id, next_version, link_method,
    )
    return effective_group_id


def get_version_group(db: Database, contract_id: int) -> list[DocumentVersion]:
    """Возвращает все версии договора из той же группы, отсортированные по version_number."""
    with db._lock:
        # Найти group_id для этого contract_id
        row = db._conn.execute(
            "SELECT contract_group_id FROM document_versions WHERE contract_id=?",
            (contract_id,),
        ).fetchone()
        if row is None:
            return []
        group_id = row[0]

        rows = db._conn.execute(
            """SELECT id, contract_group_id, contract_id, version_number,
                      link_method, created_at, linked_at
               FROM document_versions
               WHERE contract_group_id=?
               ORDER BY version_number ASC""",
            (group_id,),
        ).fetchall()

    return [
        DocumentVersion(
            id=r[0], contract_group_id=r[1], contract_id=r[2],
            version_number=r[3], link_method=r[4],
            created_at=r[5], linked_at=r[6],
        )
        for r in rows
    ]
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from services.version_service import (
    compute_embedding, ensure_embedding, find_version_match,
    link_versions, get_version_group, VERSION_LINK_THRESHOLD
)
from modules.database import Database
from pathlib import Path
import tempfile, os

# Проверить порог
assert VERSION_LINK_THRESHOLD == 0.85, f'Порог должен быть 0.85, получен {VERSION_LINK_THRESHOLD}'

# Создать тестовую БД
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = Path(f.name)
db = Database(db_path, db_path.parent)
conn = db._conn

# Вставить два документа
for fname in ['contract_v1.pdf', 'contract_v2.pdf']:
    conn.execute(
        \"\"\"INSERT INTO contracts (filename, original_path, status, contract_type, counterparty)
        VALUES (?, '/tmp/test', 'done', 'NDA', 'Альфа ООО')\"\"\", (fname,)
    )
conn.commit()
ids = [r[0] for r in conn.execute('SELECT id FROM contracts').fetchall()]
c1, c2 = ids[0], ids[1]

# Эмбеддинг первого документа
text1 = 'Договор о неразглашении между Альфа ООО и Бета ООО. Срок действия 2 года.'
vec1 = ensure_embedding(db, c1, text1)
assert vec1.shape == (384,), f'Ожидается 384-мерный вектор, получен {vec1.shape}'

# link_versions — создать группу для первого документа
gid1 = link_versions(db, c1, None, 'auto_embedding')
assert gid1 == c1, f'Первый документ должен стать group_id={c1}, получен {gid1}'

# Найти версию для второго (похожего) документа
text2 = 'Договор о неразглашении между Альфа ООО и Бета ООО. Срок действия 3 года. Редакция 2.'
match = find_version_match(db, c2, text2, 'NDA', 'Альфа ООО')
print(f'Version match: {match} (expected group_id={c1})')
# Может быть None если тексты слишком короткие для высокого сходства — это нормально
# Главное что функция работает без ошибок

# link второй документ
gid2 = link_versions(db, c2, match, 'auto_embedding')
versions = get_version_group(db, c2)
assert len(versions) >= 1, 'get_version_group должен возвращать минимум 1 версию'

print('OK: version_service работает корректно')
db.close()
os.unlink(db_path)
"
</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "def compute_embedding\|def ensure_embedding\|def find_version_match\|def link_versions\|def get_version_group" services/version_service.py возвращает 5 строк
    - grep -n "import streamlit" services/version_service.py возвращает пусто
    - grep -n "VERSION_LINK_THRESHOLD = 0.85" services/version_service.py возвращает строку
    - python-тест выше завершается "OK: version_service работает корректно"
  </acceptance_criteria>
  <done>version_service.py создан. Embedding-модель загружается один раз (singleton). Эмбеддинги кэшируются в SQLite. find_version_match использует порог 0.85. link_versions создаёт или расширяет группу версий.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить ai_extractor.py и ContractMetadata для платёжных полей + хук в pipeline_service</name>
  <files>modules/models.py, modules/ai_extractor.py, services/pipeline_service.py</files>
  <read_first>
    - modules/models.py (ContractMetadata dataclass — добавить новые поля)
    - modules/ai_extractor.py (текущий JSON-промпт для AI, как разбирается ответ)
    - services/pipeline_service.py (где вызывается database.save, куда добавить version_service hook)
  </read_first>
  <action>
**1. modules/models.py** — добавить 4 поля в ContractMetadata (сохранить обратную совместимость через Optional с None-дефолтом):
```python
# Добавить в конец ContractMetadata (после is_template):
payment_terms: Optional[str] = None          # текстовое описание: «ежемесячно до 5-го числа»
payment_amount: Optional[float] = None       # сумма одного платежа (число, без единиц)
payment_frequency: Optional[str] = None     # 'monthly' | 'quarterly' | 'yearly' | 'once' | None
payment_direction: Optional[str] = None     # 'income' | 'expense' | None
```

**2. modules/ai_extractor.py** — расширить JSON-промпт:
Найти в ai_extractor.py строку с JSON-схемой или инструкцией для AI. Добавить в JSON-структуру поля:
```
"payment_terms": "описание порядка оплаты или null",
"payment_amount": число или null (только числовое значение без валюты),
"payment_frequency": "monthly|quarterly|yearly|once или null",
"payment_direction": "income если деньги поступают от контрагента, expense если платим мы, или null"
```
Найти место где парсится JSON-ответ AI в ContractMetadata. Добавить маппинг новых полей:
```python
payment_terms=data.get("payment_terms"),
payment_amount=_safe_float(data.get("payment_amount")),
payment_frequency=data.get("payment_frequency"),
payment_direction=data.get("payment_direction"),
```
Если функция _safe_float не существует — добавить:
```python
def _safe_float(val) -> Optional[float]:
    try:
        return float(val) if val is not None else None
    except (ValueError, TypeError):
        return None
```

**3. services/pipeline_service.py** — добавить хук версионирования после сохранения документа в БД.
Найти место после вызова database.save или после завершения обработки файла. Добавить:
```python
from services.version_service import ensure_embedding, find_version_match, link_versions

# После database.save(result):
try:
    if result.status == "done" and result.metadata and result.text:
        # Получить contract_id только что сохранённого документа
        saved = db._conn.execute(
            "SELECT id FROM contracts WHERE file_hash=?",
            (result.file_info.file_hash,)
        ).fetchone()
        if saved:
            cid = saved[0]
            group_id = find_version_match(
                db, cid, result.text.text,
                result.metadata.contract_type,
                result.metadata.counterparty,
            )
            link_versions(db, cid, group_id, link_method="auto_embedding")
except Exception as e:
    logger.warning("Версионирование не удалось для %s: %s", result.file_info.filename, e)
```
Ошибки версионирования не должны прерывать обработку — только логируются как WARNING.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from modules.models import ContractMetadata
from services.version_service import find_version_match, link_versions

# Проверить новые поля ContractMetadata
m = ContractMetadata()
assert hasattr(m, 'payment_terms'), 'payment_terms отсутствует'
assert hasattr(m, 'payment_amount'), 'payment_amount отсутствует'
assert hasattr(m, 'payment_frequency'), 'payment_frequency отсутствует'
assert hasattr(m, 'payment_direction'), 'payment_direction отсутствует'
assert m.payment_amount is None, 'payment_amount дефолт должен быть None'

# Проверить импорт pipeline_service без ошибок
import services.pipeline_service
print('OK: ContractMetadata расширен, pipeline_service импортируется')
"
</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "payment_terms\|payment_amount\|payment_frequency\|payment_direction" modules/models.py возвращает 4 строки в ContractMetadata
    - grep -n "payment_terms\|payment_amount\|payment_frequency\|payment_direction" modules/ai_extractor.py возвращает строки в промпте и маппинге
    - grep -n "find_version_match\|link_versions" services/pipeline_service.py возвращает минимум 2 строки
    - python-тест выше завершается "OK: ContractMetadata расширен, pipeline_service импортируется"
  </acceptance_criteria>
  <done>ContractMetadata содержит 4 новых поля с None-дефолтами. AI-промпт запрашивает платёжные данные. pipeline_service вызывает версионирование после каждого успешно обработанного документа. Ошибки версионирования не прерывают пайплайн.</done>
</task>

</tasks>

<verification>
1. python -c "from services.version_service import link_versions, get_version_group; print('OK')" — без ошибок
2. python -c "from modules.models import ContractMetadata; m = ContractMetadata(); assert m.payment_frequency is None" — без ошибок
3. grep -n "try:" services/pipeline_service.py — версионирование обёрнуто в try/except
</verification>

<success_criteria>
- version_service.py создан, все 5 функций экспортированы
- ContractMetadata обратно совместима (все новые поля с None-дефолтами)
- Версионирование интегрировано в пайплайн как non-blocking операция
- Эмбеддинги не пересчитываются при повторном запуске (кэш в SQLite)
</success_criteria>

<output>
После завершения создать `.planning/phases/02-document-lifecycle/02-03-SUMMARY.md`
</output>
