---
phase: 02-document-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/database.py
  - modules/models.py
  - config.py
  - services/lifecycle_service.py
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-06

must_haves:
  truths:
    - "Таблица contracts содержит колонку manual_status (TEXT, NULL = нет ручного статуса)"
    - "Таблица contracts содержит колонку warning_days (INTEGER, NULL = берётся из config)"
    - "Таблицы embeddings, document_versions, payments, templates существуют в SQLite"
    - "lifecycle_service.get_status_query() возвращает SQL CASE, где manual_status приоритетнее авто"
    - "config.warning_days_threshold = 30 существует как поле Config"
    - "lifecycle_service.get_attention_required() возвращает список договоров с истекающими сроками"
  artifacts:
    - path: "modules/database.py"
      provides: "Миграции v2–v6: manual_status, warning_days, embeddings, document_versions, payments, templates"
      contains: "_migrate_v2_lifecycle_columns"
    - path: "modules/models.py"
      provides: "Dataclasses: DocumentVersion, Payment, Template, DeadlineAlert"
      exports: ["DocumentVersion", "Payment", "Template", "DeadlineAlert"]
    - path: "config.py"
      provides: "Поле warning_days_threshold: int = 30"
      contains: "warning_days_threshold"
    - path: "services/lifecycle_service.py"
      provides: "Функции статуса и панели внимания"
      exports: ["get_computed_status_sql", "set_manual_status", "clear_manual_status", "get_attention_required"]
  key_links:
    - from: "services/lifecycle_service.py"
      to: "modules/database.py"
      via: "Database instance передаётся параметром"
      pattern: "def.*db.*Database"
    - from: "lifecycle_service.get_computed_status_sql"
      to: "config.warning_days_threshold"
      via: "параметр :warning_days в SQL CASE"
      pattern: "warning_days"
---

<objective>
Создать фундамент жизненного цикла документа: схема БД для всех новых таблиц и сервис статусов.

Purpose: Всё остальное в Phase 2 строится поверх этих таблиц и SQL-паттернов. Без них ни статусы, ни версионирование, ни платежи не работают.
Output: Миграции v2–v6 в database.py, новые dataclasses в models.py, поле warning_days_threshold в config.py, lifecycle_service.py с функциями статуса и панели внимания.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-document-lifecycle/02-CONTEXT.md
@.planning/phases/02-document-lifecycle/02-RESEARCH.md

<interfaces>
<!-- Текущий паттерн миграций из modules/database.py -->
<!-- Последняя применённая миграция: v1 (_migrate_v1_review_columns) -->
<!-- _run_migrations() вызывает миграции строго по порядку -->

Текущая contracts таблица (из _SCHEMA):
```python
_SCHEMA = """
CREATE TABLE IF NOT EXISTS contracts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    original_path TEXT NOT NULL,
    filename TEXT NOT NULL,
    file_hash TEXT UNIQUE,
    status TEXT DEFAULT 'pending',
    error_message TEXT,
    contract_type TEXT,
    counterparty TEXT,
    subject TEXT,
    date_signed TEXT,
    date_start TEXT,
    date_end TEXT,
    amount TEXT,
    special_conditions TEXT,  -- JSON array
    parties TEXT,             -- JSON array
    confidence REAL,
    validation_status TEXT,
    validation_warnings TEXT,  -- JSON array
    validation_score REAL,
    organized_path TEXT,
    processed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    model_used TEXT
    -- review_status и lawyer_comment добавлены через миграцию v1
);
"""
```

Паттерн добавления миграции в _run_migrations():
```python
def _run_migrations(db_path: Path, conn: sqlite3.Connection) -> None:
    # ... backup logic ...
    _ensure_migrations_table(conn)
    if needs_backup:
        _backup_database(db_path)
    _migrate_v1_review_columns(conn)
    # СЮДА добавить: _migrate_v2_lifecycle_columns(conn) и далее
```

Текущие модели (modules/models.py): FileInfo, ExtractedText, AnonymizedText,
ContractMetadata, ValidationResult, ProcessingResult — без изменений.

Текущий Config (config.py): поля listed above — добавить только warning_days_threshold: int = 30
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграции v2–v6 в database.py + новые dataclasses в models.py</name>
  <files>modules/database.py, modules/models.py</files>
  <read_first>
    - modules/database.py (паттерн _migrate_v1_review_columns, _run_migrations, _SCHEMA)
    - modules/models.py (текущие dataclasses для соблюдения стиля)
  </read_first>
  <action>
В modules/database.py добавить пять миграций и зарегистрировать их в _run_migrations():

**_migrate_v2_lifecycle_columns(conn)** — версия 2:
```sql
ALTER TABLE contracts ADD COLUMN manual_status TEXT DEFAULT NULL;
ALTER TABLE contracts ADD COLUMN warning_days INTEGER DEFAULT NULL;
```
Использовать тот же try/except OperationalError паттерн что в v1.

**_migrate_v3_embeddings(conn)** — версия 3:
```sql
CREATE TABLE IF NOT EXISTS embeddings (
    contract_id INTEGER NOT NULL REFERENCES contracts(id) ON DELETE CASCADE,
    vector BLOB NOT NULL,
    model_version TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (contract_id)
);
```

**_migrate_v4_document_versions(conn)** — версия 4:
```sql
CREATE TABLE IF NOT EXISTS document_versions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    contract_group_id INTEGER NOT NULL,
    contract_id INTEGER NOT NULL REFERENCES contracts(id),
    version_number INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    linked_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    link_method TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_versions_group ON document_versions(contract_group_id);
```

**_migrate_v5_payments(conn)** — версия 5:
```sql
CREATE TABLE IF NOT EXISTS payments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    contract_id INTEGER NOT NULL REFERENCES contracts(id) ON DELETE CASCADE,
    payment_date TEXT NOT NULL,
    amount REAL NOT NULL,
    direction TEXT NOT NULL,
    is_periodic INTEGER DEFAULT 0,
    frequency TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_payments_date ON payments(payment_date);
```

**_migrate_v6_templates(conn)** — версия 6:
```sql
CREATE TABLE IF NOT EXISTS templates (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    contract_type TEXT NOT NULL,
    name TEXT NOT NULL,
    original_path TEXT,
    content_text TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_active INTEGER DEFAULT 1
);
```

Для v3–v6 использовать CREATE TABLE IF NOT EXISTS (идемпотентны), обернуть в try/except OperationalError.
Зарегистрировать все пять вызовов в _run_migrations() после _migrate_v1_review_columns(conn).

В modules/models.py добавить четыре новых dataclass (не менять существующие):

```python
@dataclass
class DocumentVersion:
    """Версия документа в группе."""
    id: int
    contract_group_id: int
    contract_id: int
    version_number: int
    link_method: str  # 'auto_embedding' | 'manual'
    created_at: Optional[str] = None
    linked_at: Optional[str] = None

@dataclass
class Payment:
    """Платёж по договору."""
    id: int
    contract_id: int
    payment_date: str      # ISO 8601
    amount: float
    direction: str         # 'income' | 'expense'
    is_periodic: bool = False
    frequency: Optional[str] = None  # 'monthly' | 'quarterly' | 'yearly'
    created_at: Optional[str] = None

@dataclass
class Template:
    """Шаблон-эталон для ревью договора."""
    id: int
    contract_type: str
    name: str
    original_path: Optional[str] = None
    content_text: Optional[str] = None
    is_active: bool = True
    created_at: Optional[str] = None

@dataclass
class DeadlineAlert:
    """Документ в панели «требует внимания»."""
    contract_id: int
    filename: str
    counterparty: Optional[str]
    contract_type: Optional[str]
    date_end: str           # ISO 8601
    days_until_expiry: int  # отрицательное = уже истёк
    computed_status: str    # 'expired' | 'expiring' | 'unknown'
    validation_status: Optional[str] = None
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from modules.database import Database
from pathlib import Path
import tempfile, os
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = Path(f.name)
db = Database(db_path, db_path.parent)
conn = db._conn
# Проверить миграции
for version in [1,2,3,4,5,6]:
    row = conn.execute('SELECT 1 FROM schema_migrations WHERE version=?', (version,)).fetchone()
    assert row, f'Миграция v{version} не применена'
# Проверить таблицы
for table in ['embeddings','document_versions','payments','templates']:
    row = conn.execute(\"SELECT 1 FROM sqlite_master WHERE type='table' AND name=?\", (table,)).fetchone()
    assert row, f'Таблица {table} не создана'
# Проверить колонки
cols = [r[1] for r in conn.execute('PRAGMA table_info(contracts)').fetchall()]
assert 'manual_status' in cols, 'manual_status отсутствует'
assert 'warning_days' in cols, 'warning_days отсутствует'
print('OK: все миграции применены, таблицы созданы')
db.close()
os.unlink(db_path)
"
</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "_migrate_v2_lifecycle_columns\|_migrate_v3_embeddings\|_migrate_v4_document_versions\|_migrate_v5_payments\|_migrate_v6_templates" modules/database.py возвращает 5+ строк определений функций
    - grep -n "DocumentVersion\|Payment\|Template\|DeadlineAlert" modules/models.py возвращает 4 dataclass
    - python-тест выше завершается "OK: все миграции применены, таблицы созданы"
  </acceptance_criteria>
  <done>Все 6 миграций применяются без ошибок. Таблицы embeddings, document_versions, payments, templates существуют. Колонки manual_status и warning_days добавлены в contracts. Четыре новых dataclass экспортируются из models.py.</done>
</task>

<task type="auto">
  <name>Task 2: config.py + services/lifecycle_service.py — статусы и панель внимания</name>
  <files>config.py, services/lifecycle_service.py</files>
  <read_first>
    - config.py (структура Config dataclass, чтобы добавить поле корректно)
    - services/registry_service.py (паттерн работы с Database, импорты, стиль)
    - modules/models.py (DeadlineAlert dataclass, добавленный в Task 1)
  </read_first>
  <action>
В config.py добавить одно поле в Config dataclass после поля `fallback_provider`:
```python
warning_days_threshold: int = 30  # порог предупреждения о сроках (дней)
```

Создать services/lifecycle_service.py — новый модуль без зависимости от Streamlit:

```python
"""Сервис жизненного цикла документа: статусы и панель внимания.

Stateless: все функции принимают db как параметр, не хранят состояние.
Не импортирует streamlit — вызывается из main.py и будущего Telegram-бота.
"""
import logging
import sqlite3
from typing import Optional
from modules.database import Database
from modules.models import DeadlineAlert

logger = logging.getLogger(__name__)

# Допустимые ручные статусы
MANUAL_STATUSES = frozenset({"terminated", "extended", "negotiation", "suspended"})

# Метки для отображения в UI
STATUS_LABELS = {
    "active":      ("✔", "Действует",         "#22c55e"),
    "expiring":    ("⚠", "Скоро истекает",    "#f59e0b"),
    "expired":     ("✗", "Истёк",             "#ef4444"),
    "unknown":     ("?", "Нет даты",           "#9ca3af"),
    "terminated":  ("✖", "Расторгнут",        "#6b7280"),
    "extended":    ("↻", "Продлён",           "#3b82f6"),
    "negotiation": ("~", "На согласовании",  "#8b5cf6"),
    "suspended":   ("⏸", "Приостановлен",    "#f97316"),
}


def get_computed_status_sql(warning_days: int) -> str:
    """Возвращает SQL-фрагмент CASE для вычисления статуса на лету.

    manual_status имеет приоритет над автоматическим статусом.
    warning_days — количество дней для порога 'expiring'.

    Использование:
        sql = f"SELECT *, {get_computed_status_sql(30)} AS computed_status FROM contracts"
        conn.execute(sql, {"warning_days": warning_days})
    """
    return f"""
        CASE
            WHEN manual_status IS NOT NULL THEN manual_status
            WHEN date_end IS NULL          THEN 'unknown'
            WHEN date_end < date('now')    THEN 'expired'
            WHEN date_end < date('now', '+' || :warning_days || ' days') THEN 'expiring'
            ELSE 'active'
        END
    """.strip()


def set_manual_status(db: Database, contract_id: int, status: str) -> None:
    """Устанавливает ручной статус договора. status должен быть из MANUAL_STATUSES."""
    if status not in MANUAL_STATUSES:
        raise ValueError(f"Недопустимый статус: {status!r}. Допустимые: {MANUAL_STATUSES}")
    with db._lock:
        db._conn.execute(
            "UPDATE contracts SET manual_status = ? WHERE id = ?",
            (status, contract_id)
        )
        db._conn.commit()
    logger.info("Ручной статус '%s' установлен для договора id=%d", status, contract_id)


def clear_manual_status(db: Database, contract_id: int) -> None:
    """Сбрасывает ручной статус — договор возвращается к автоматическому."""
    with db._lock:
        db._conn.execute(
            "UPDATE contracts SET manual_status = NULL WHERE id = ?",
            (contract_id,)
        )
        db._conn.commit()
    logger.info("Ручной статус сброшен для договора id=%d", contract_id)


def get_attention_required(db: Database, warning_days: int) -> list[DeadlineAlert]:
    """Возвращает документы для панели «требует внимания»:
    - истёкшие (date_end < today)
    - истекающие в течение warning_days дней
    Исключает документы с manual_status (они под контролем юриста).
    Сортировка: сначала истёкшие, потом по дате окончания ASC.
    """
    sql = f"""
        SELECT
            id, filename, counterparty, contract_type, date_end,
            CAST(julianday(date_end) - julianday('now') AS INTEGER) AS days_until_expiry,
            {get_computed_status_sql(warning_days)} AS computed_status,
            validation_status
        FROM contracts
        WHERE manual_status IS NULL
          AND date_end IS NOT NULL
          AND date_end < date('now', '+' || :warning_days || ' days')
          AND status = 'done'
        ORDER BY date_end ASC
    """
    rows = db._conn.execute(sql, {"warning_days": warning_days}).fetchall()
    alerts = []
    for row in rows:
        alerts.append(DeadlineAlert(
            contract_id=row[0],
            filename=row[1],
            counterparty=row[2],
            contract_type=row[3],
            date_end=row[4],
            days_until_expiry=row[5],
            computed_status=row[6],
            validation_status=row[7],
        ))
    return alerts
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from config import Config
from services.lifecycle_service import (
    get_computed_status_sql, set_manual_status, clear_manual_status,
    get_attention_required, MANUAL_STATUSES, STATUS_LABELS
)
from modules.database import Database
from pathlib import Path
import tempfile, os

# Config имеет новое поле
cfg = Config()
assert hasattr(cfg, 'warning_days_threshold'), 'Поле warning_days_threshold отсутствует в Config'
assert cfg.warning_days_threshold == 30, f'Дефолт должен быть 30, получен {cfg.warning_days_threshold}'

# SQL содержит правильные ключевые слова
sql = get_computed_status_sql(30)
assert 'manual_status' in sql
assert 'expiring' in sql
assert 'expired' in sql

# Тест set/clear/get_attention_required на реальной БД
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = Path(f.name)
db = Database(db_path, db_path.parent)
conn = db._conn
conn.execute(\"\"\"INSERT INTO contracts (filename, original_path, status, date_end, contract_type)
    VALUES ('test.pdf', '/tmp/test.pdf', 'done', date('now', '-5 days'), 'NDA')\"\"\")
conn.commit()
cid = conn.execute('SELECT id FROM contracts WHERE filename=\"test.pdf\"').fetchone()[0]

# set_manual_status
set_manual_status(db, cid, 'terminated')
row = conn.execute('SELECT manual_status FROM contracts WHERE id=?', (cid,)).fetchone()
assert row[0] == 'terminated', f'manual_status не установлен: {row[0]}'

# get_attention_required НЕ возвращает документ с ручным статусом
alerts = get_attention_required(db, 30)
assert not any(a.contract_id == cid for a in alerts), 'Документ с manual_status не должен быть в alerts'

# clear_manual_status и проверить что появляется в alerts
clear_manual_status(db, cid)
alerts = get_attention_required(db, 30)
assert any(a.contract_id == cid for a in alerts), 'Документ должен быть в alerts после сброса manual_status'

print('OK: Config, SQL, set/clear/get_attention_required работают корректно')
db.close()
os.unlink(db_path)
"
</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "warning_days_threshold" config.py возвращает строку с "int = 30"
    - grep -n "def get_computed_status_sql\|def set_manual_status\|def clear_manual_status\|def get_attention_required" services/lifecycle_service.py возвращает 4 строки
    - grep -n "import streamlit" services/lifecycle_service.py возвращает пусто (нет Streamlit-зависимости)
    - python-тест выше завершается "OK: Config, SQL, set/clear/get_attention_required работают корректно"
  </acceptance_criteria>
  <done>Config.warning_days_threshold = 30 существует. lifecycle_service.py создан с четырьмя функциями. get_attention_required корректно исключает документы с manual_status. Нет импорта streamlit.</done>
</task>

</tasks>

<verification>
После завершения обоих задач:
1. python -c "from services.lifecycle_service import get_attention_required; print('import OK')" — без ошибок
2. python -c "from modules.models import DocumentVersion, Payment, Template, DeadlineAlert; print('models OK')" — без ошибок
3. Проверить что _run_migrations вызывает все 6 миграций: grep -c "_migrate_v" modules/database.py — результат >= 6
</verification>

<success_criteria>
- Все 6 миграций применяются при первом запуске Database() на чистой БД
- Все 6 миграций идемпотентны — повторный запуск не вызывает ошибок
- lifecycle_service.py импортируется без Streamlit, без ошибок
- set_manual_status / clear_manual_status атомарны (используют db._lock)
- get_attention_required не возвращает документы с manual_status
</success_criteria>

<output>
После завершения создать `.planning/phases/02-document-lifecycle/02-01-SUMMARY.md`
</output>
