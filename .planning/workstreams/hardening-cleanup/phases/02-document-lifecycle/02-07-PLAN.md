---
phase: 02-document-lifecycle
plan: 07
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
files_modified:
  - services/review_service.py
  - main.py
autonomous: false
requirements:
  - LIFE-08

must_haves:
  truths:
    - "Юрист может загрузить файл и пометить его как шаблон-эталон для типа договора"
    - "Документ из реестра можно отметить как эталон одним кликом"
    - "При открытии вкладки «Ревью» система автоматически подбирает шаблон по типу договора"
    - "Отступления показываются с цветовой подсветкой: зелёный = добавлено, красный = удалено, жёлтый = изменено"
    - "Кнопка «Скачать редлайн» генерирует .docx сравнения с эталоном"
  artifacts:
    - path: "services/review_service.py"
      provides: "Сервис ревью: add_template, match_template, review_against_template"
      exports: ["add_template", "match_template", "review_against_template", "mark_contract_as_template"]
    - path: "main.py"
      provides: "UI добавления шаблона + вкладка «Ревью» с подсветкой отступлений"
      contains: "review_against_template"
  key_links:
    - from: "services/review_service.match_template"
      to: "embeddings table + templates table"
      via: "косинусное сходство эмбеддингов шаблонов с новым документом"
      pattern: "cosine_sim|TEMPLATE_MATCH_THRESHOLD"
    - from: "main.py (вкладка Ревью)"
      to: "services/review_service.review_against_template"
      via: "вызов при открытии вкладки, результат отображается с html цветами"
      pattern: "review_against_template"
---

<objective>
Реализовать ревью договора против шаблона-эталона с библиотекой шаблонов и подсветкой отступлений.

Purpose: LIFE-08 — юрист загружает шаблон NDA, и при открытии любого NDA система сразу показывает что изменилось. Это помогает принять решение, не читая весь документ.
Output: services/review_service.py + UI добавления шаблонов + заполненная вкладка «Ревью» в карточке документа.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-document-lifecycle/02-CONTEXT.md
@.planning/phases/02-document-lifecycle/02-RESEARCH.md
@.planning/phases/02-document-lifecycle/02-03-SUMMARY.md

<interfaces>
<!-- Из version_service.py (02-03) — переиспользовать: -->
```python
from services.version_service import (
    compute_embedding,   # (text: str) -> np.ndarray
    _cosine_sim,         # (a, b) -> float
    TEMPLATE_MATCH_THRESHOLD,  # = 0.60
    get_embedding_model,
)
```

<!-- Схема таблицы templates (из migration v6 в 02-01) -->
```sql
templates (
    id, contract_type TEXT, name TEXT,
    original_path TEXT, content_text TEXT,
    created_at, is_active INTEGER DEFAULT 1
)
```

<!-- Из modules/extractor.py — как извлечь текст из загруженного файла -->
```python
from modules.extractor import Extractor
from modules.models import FileInfo
# extractor.extract(file_info: FileInfo) -> ExtractedText
```

Из CONTEXT.md locked decisions:
- Добавление эталона: два способа — загрузить файл И отметить из реестра
- Сопоставление: авто по типу + ручной выбор из библиотеки
- Отображение: зелёный = добавлено, красный = удалено, жёлтый = изменено
- Выход: скачиваемый .docx с track changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/review_service.py — библиотека шаблонов и ревью</name>
  <files>services/review_service.py</files>
  <read_first>
    - services/version_service.py (compute_embedding, _cosine_sim, generate_redline_docx — переиспользовать)
    - .planning/phases/02-document-lifecycle/02-RESEARCH.md (Pattern 3: Redline, TEMPLATE_MATCH_THRESHOLD)
    - modules/database.py (Database._conn, _lock)
  </read_first>
  <action>
Создать services/review_service.py:

```python
"""Сервис ревью договора против шаблона-эталона.

Библиотека шаблонов хранится в SQLite (templates table, migration v6).
Сопоставление: авто по типу договора через косинусное сходство эмбеддингов.
Отступления: difflib sentence-level diff с цветовой разметкой.

Не импортирует streamlit.
"""
import difflib
import logging
import re
from typing import Optional

import numpy as np

from modules.database import Database
from modules.models import Template
from services.version_service import (
    compute_embedding,
    generate_redline_docx,
    TEMPLATE_MATCH_THRESHOLD,
    _cosine_sim,
)

logger = logging.getLogger(__name__)


def add_template(
    db: Database,
    contract_type: str,
    name: str,
    content_text: str,
    original_path: Optional[str] = None,
) -> int:
    """Добавляет шаблон-эталон в библиотеку. Возвращает id нового шаблона."""
    with db._lock:
        cursor = db._conn.execute(
            """INSERT INTO templates (contract_type, name, original_path, content_text)
               VALUES (?, ?, ?, ?)""",
            (contract_type, name, original_path, content_text),
        )
        db._conn.commit()
        template_id = cursor.lastrowid
    logger.info("Шаблон '%s' (%s) добавлен, id=%d", name, contract_type, template_id)
    return template_id


def mark_contract_as_template(
    db: Database,
    contract_id: int,
    template_name: Optional[str] = None,
) -> Optional[int]:
    """Отмечает документ из реестра как шаблон-эталон.

    Извлекает content_text из subjects как текст-заглушку.
    Returns: template id или None если contract не найден.
    """
    with db._lock:
        row = db._conn.execute(
            "SELECT contract_type, filename, subject, original_path FROM contracts WHERE id=?",
            (contract_id,),
        ).fetchone()
    if row is None:
        logger.warning("contract_id=%d не найден", contract_id)
        return None

    contract_type, filename, subject, original_path = row
    name = template_name or f"Эталон: {filename}"
    content = subject or filename  # используем subject как контент если нет полного текста

    return add_template(db, contract_type or "Прочее", name, content, original_path)


def list_templates(db: Database, contract_type: Optional[str] = None) -> list[Template]:
    """Возвращает активные шаблоны, опционально фильтрует по типу договора."""
    sql = "SELECT id, contract_type, name, original_path, content_text, created_at, is_active FROM templates WHERE is_active=1"
    params: list = []
    if contract_type:
        sql += " AND contract_type=?"
        params.append(contract_type)
    sql += " ORDER BY contract_type, name"

    with db._lock:
        rows = db._conn.execute(sql, params).fetchall()
    return [
        Template(
            id=r[0], contract_type=r[1], name=r[2],
            original_path=r[3], content_text=r[4],
            created_at=r[5], is_active=bool(r[6]),
        )
        for r in rows
    ]


def match_template(
    db: Database,
    document_text: str,
    contract_type: Optional[str],
) -> Optional[Template]:
    """Автоматически подбирает наиболее похожий шаблон.

    Алгоритм:
    1. Если есть шаблон с точным соответствием contract_type → предпочесть их
    2. Сравнить косинусное сходство эмбеддингов
    3. Вернуть шаблон с sim >= TEMPLATE_MATCH_THRESHOLD или None

    Returns: Template или None (предложить ручной выбор в UI)
    """
    templates = list_templates(db, contract_type)  # сначала по типу
    if not templates:
        templates = list_templates(db)  # затем все
    if not templates:
        return None

    doc_vector = compute_embedding(document_text)

    best_sim = 0.0
    best_template = None

    for tmpl in templates:
        if not tmpl.content_text:
            continue
        tmpl_vector = compute_embedding(tmpl.content_text)
        sim = _cosine_sim(doc_vector, tmpl_vector)
        if sim > best_sim:
            best_sim = sim
            best_template = tmpl

    if best_sim >= TEMPLATE_MATCH_THRESHOLD:
        logger.info(
            "Подобран шаблон '%s' (sim=%.3f)", best_template.name, best_sim
        )
        return best_template

    logger.info(
        "Шаблон не подобран автоматически (best_sim=%.3f < threshold=%.2f)",
        best_sim, TEMPLATE_MATCH_THRESHOLD,
    )
    return None


def _split_sentences(text: str) -> list[str]:
    """Разбивает текст на предложения."""
    parts = re.split(r'(?<=[.!?])\s+', text.strip())
    return [p.strip() for p in parts if p.strip()]


# Цвета для разметки отступлений в HTML
_DIFF_COLORS = {
    "added":   "#bbf7d0",  # зелёный фон — добавлено в договоре
    "removed": "#fecaca",  # красный фон — есть в шаблоне, нет в договоре
    "changed": "#fef3c7",  # жёлтый фон — изменено (пара delete+insert)
}


def review_against_template(
    template_text: str,
    document_text: str,
) -> list[dict]:
    """Сравнивает документ с шаблоном. Возвращает список отступлений.

    Каждое отступление:
        {"type": "added"|"removed"|"changed",
         "template_text": str|None,
         "document_text": str|None,
         "color": hex_color}

    "added" — есть в документе, нет в шаблоне
    "removed" — есть в шаблоне, нет в документе
    "changed" — заменено (одновременно delete+insert)
    """
    template_sents = _split_sentences(template_text)
    document_sents = _split_sentences(document_text)

    matcher = difflib.SequenceMatcher(None, template_sents, document_sents)
    result = []

    for tag, i1, i2, j1, j2 in matcher.get_opcodes():
        if tag == "equal":
            continue
        elif tag == "replace":
            result.append({
                "type": "changed",
                "template_text": " ".join(template_sents[i1:i2]),
                "document_text": " ".join(document_sents[j1:j2]),
                "color": _DIFF_COLORS["changed"],
            })
        elif tag == "delete":
            result.append({
                "type": "removed",
                "template_text": " ".join(template_sents[i1:i2]),
                "document_text": None,
                "color": _DIFF_COLORS["removed"],
            })
        elif tag == "insert":
            result.append({
                "type": "added",
                "template_text": None,
                "document_text": " ".join(document_sents[j1:j2]),
                "color": _DIFF_COLORS["added"],
            })

    return result
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from services.review_service import (
    add_template, list_templates, match_template,
    review_against_template, mark_contract_as_template
)
from modules.database import Database
from pathlib import Path
import tempfile, os

with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = Path(f.name)
db = Database(db_path, db_path.parent)

# add_template
tid = add_template(db, 'NDA', 'Стандартное NDA',
    'Стороны обязуются не разглашать конфиденциальную информацию. Срок действия два года.')
assert tid > 0, f'add_template должен вернуть > 0, получен {tid}'

# list_templates
templates = list_templates(db)
assert len(templates) == 1, f'Ожидается 1 шаблон, получено {len(templates)}'
assert templates[0].contract_type == 'NDA'

# review_against_template
deviations = review_against_template(
    'Стороны обязуются не разглашать конфиденциальную информацию. Срок действия два года.',
    'Стороны обязуются не разглашать конфиденциальную информацию. Срок действия три года. Штраф 100 000 руб.'
)
assert len(deviations) > 0, 'Должны быть найдены отступления'
types = [d['type'] for d in deviations]
print(f'Найдены отступления: {types}')

# review с идентичными текстами — 0 отступлений
no_dev = review_against_template('Договор NDA. Срок один год.', 'Договор NDA. Срок один год.')
assert len(no_dev) == 0, f'Идентичные тексты: ожидается 0 отступлений, получено {len(no_dev)}'

print('OK: review_service работает корректно')
db.close()
os.unlink(db_path)
"
</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "def add_template\|def match_template\|def review_against_template\|def mark_contract_as_template" services/review_service.py возвращает 4 строки
    - grep -n "import streamlit" services/review_service.py возвращает пусто
    - grep -n "TEMPLATE_MATCH_THRESHOLD" services/review_service.py возвращает строку использования
    - python-тест выше завершается "OK: review_service работает корректно"
  </acceptance_criteria>
  <done>review_service.py создан с четырьмя функциями. add_template и mark_contract_as_template добавляют шаблоны двумя способами. review_against_template находит отступления с цветовой разметкой.</done>
</task>

<task type="auto">
  <name>Task 2: UI шаблонов и вкладка «Ревью» в main.py</name>
  <files>main.py</files>
  <read_first>
    - main.py (вкладка «Ревью» — заглушка из 02-04; структура навигации — куда добавить библиотеку шаблонов)
    - services/review_service.py (сигнатуры всех функций)
    - modules/extractor.py (как извлечь текст из загруженного файла — st.file_uploader)
  </read_first>
  <action>
**1. Добавить импорты** в начало main.py:
```python
from services.review_service import (
    add_template, list_templates, match_template,
    review_against_template, mark_contract_as_template,
)
```

**2. Добавить раздел «Библиотека шаблонов»** в навигацию (вкладка или sidebar option):

```python
# Раздел «Библиотека шаблонов»:
st.header("Библиотека шаблонов")

# Способ 1: загрузить файл
st.subheader("Загрузить новый шаблон")
col_t1, col_t2 = st.columns(2)
with col_t1:
    tmpl_type = st.text_input("Тип договора", placeholder="NDA, Поставка, Аренда...")
    tmpl_name = st.text_input("Название шаблона", placeholder="Стандартное NDA v1")
with col_t2:
    tmpl_file = st.file_uploader("Файл шаблона (PDF или DOCX)", type=["pdf", "docx"])

if st.button("Добавить шаблон", key="add_template_btn"):
    if not tmpl_type or not tmpl_name:
        st.error("Укажите тип договора и название шаблона")
    elif tmpl_file is None:
        st.error("Загрузите файл шаблона")
    elif db is None:
        st.error("База данных не инициализирована")
    else:
        # Извлечь текст из загруженного файла
        import tempfile
        from pathlib import Path
        from modules.extractor import Extractor
        from modules.models import FileInfo
        from modules.scanner import compute_file_hash

        with tempfile.NamedTemporaryFile(
            suffix=Path(tmpl_file.name).suffix, delete=False
        ) as tmp:
            tmp.write(tmpl_file.read())
            tmp_path = Path(tmp.name)

        try:
            extractor = Extractor()
            fi = FileInfo(
                path=tmp_path,
                filename=tmpl_file.name,
                extension=tmp_path.suffix,
                size_bytes=tmp_path.stat().st_size,
                file_hash="",
            )
            extracted = extractor.extract(fi)
            content = extracted.text or tmpl_file.name
            add_template(db, tmpl_type, tmpl_name, content, tmpl_file.name)
            st.success(f"Шаблон '{tmpl_name}' добавлен")
        except Exception as e:
            st.error(f"Ошибка при обработке файла: {e}")
        finally:
            tmp_path.unlink(missing_ok=True)

# Способ 2: отметить из реестра
st.subheader("Отметить документ из реестра как эталон")
if db is not None:
    all_docs = db._conn.execute(
        "SELECT id, filename, contract_type FROM contracts WHERE status='done' ORDER BY filename"
    ).fetchall()
    if all_docs:
        doc_options = {f"{r[1]} ({r[2] or 'без типа'})": r[0] for r in all_docs}
        selected_for_template = st.selectbox(
            "Выбрать документ", list(doc_options.keys()), key="tmpl_from_registry"
        )
        tmpl_name_from_reg = st.text_input("Название эталона", key="tmpl_name_reg")
        if st.button("Сделать эталоном", key="make_template_btn"):
            cid = doc_options[selected_for_template]
            mark_contract_as_template(db, cid, tmpl_name_from_reg or None)
            st.success("Документ добавлен как шаблон-эталон")

# Показать текущую библиотеку
st.markdown("---")
st.subheader("Текущие шаблоны")
if db is not None:
    templates_list = list_templates(db)
    if not templates_list:
        st.info("Библиотека шаблонов пуста")
    else:
        for t in templates_list:
            st.markdown(f"**{t.contract_type}** — {t.name}")
```

**3. Заполнить вкладку «Ревью»** в карточке документа (была заглушка из 02-04):
Найти `with tab_review:` в main.py, заменить заглушку:
```python
with tab_review:
    if db is None or selected_contract_id is None:
        st.info("Выберите договор в реестре")
    else:
        # Загрузить subject как контент документа (полного текста в БД нет)
        doc_row = db._conn.execute(
            "SELECT contract_type, subject FROM contracts WHERE id=?",
            (selected_contract_id,)
        ).fetchone()
        if not doc_row or not doc_row[1]:
            st.info("Текст договора недоступен для ревью (нет subject в метаданных)")
        else:
            doc_type, doc_subject = doc_row

            # Автосопоставление
            auto_match = match_template(db, doc_subject, doc_type)

            # Выбор шаблона (авто или ручной)
            all_templates = list_templates(db)
            if not all_templates:
                st.warning(
                    "Библиотека шаблонов пуста. "
                    "Добавьте шаблоны в разделе «Библиотека шаблонов»."
                )
            else:
                tmpl_names = {t.name: t for t in all_templates}
                default_idx = 0
                if auto_match:
                    st.success(f"Автоматически подобран шаблон: **{auto_match.name}**")
                    if auto_match.name in tmpl_names:
                        default_idx = list(tmpl_names.keys()).index(auto_match.name)

                selected_tmpl_name = st.selectbox(
                    "Шаблон для сравнения",
                    list(tmpl_names.keys()),
                    index=default_idx,
                    key="review_template_select",
                )
                selected_tmpl = tmpl_names[selected_tmpl_name]

                if st.button("Запустить ревью", key="run_review_btn"):
                    deviations = review_against_template(
                        selected_tmpl.content_text or "",
                        doc_subject,
                    )
                    if not deviations:
                        st.success("Отступлений от шаблона не найдено")
                    else:
                        st.markdown(f"**Найдено отступлений: {len(deviations)}**")
                        for dev in deviations:
                            type_label = {
                                "added":   "Добавлено в договоре",
                                "removed": "Отсутствует в договоре",
                                "changed": "Изменено",
                            }.get(dev["type"], dev["type"])

                            with st.container():
                                st.markdown(
                                    f'<div style="background:{dev["color"]};padding:8px;border-radius:4px;margin:4px 0">'
                                    f'<b>{type_label}</b><br>'
                                    + (f'<span style="text-decoration:line-through;color:#6b7280">{dev["template_text"]}</span><br>' if dev["template_text"] else "")
                                    + (f'{dev["document_text"]}' if dev["document_text"] else "")
                                    + '</div>',
                                    unsafe_allow_html=True,
                                )

                        # Кнопка редлайна
                        st.markdown("---")
                        if st.button("Сгенерировать редлайн .docx", key="review_redline_btn"):
                            from services.version_service import generate_redline_docx
                            docx_bytes = generate_redline_docx(
                                selected_tmpl.content_text or "",
                                doc_subject,
                                title=f"Ревью: {selected_tmpl_name}",
                            )
                            st.download_button(
                                "Скачать редлайн .docx",
                                data=docx_bytes,
                                file_name=f"review_{selected_contract_id}.docx",
                                mime="application/vnd.openxmlformats-officedocument.wordprocessingml.document",
                                key="review_download",
                            )
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "review_against_template\|match_template\|add_template\|mark_contract_as_template\|tab_review\|Библиотека шаблонов" main.py | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "from services.review_service import" main.py возвращает строку импорта
    - grep -n "review_against_template" main.py возвращает минимум 1 строку вызова
    - grep -n "match_template" main.py возвращает минимум 1 строку вызова
    - grep -n "Библиотека шаблонов" main.py возвращает строку с заголовком
    - grep -n "tab_review" main.py — больше не содержит 'Ревью против шаблона — загрузите' (заглушка заменена)
    - grep -n "add_template\|mark_contract_as_template" main.py возвращает строки вызовов
  </acceptance_criteria>
  <done>Раздел «Библиотека шаблонов» добавлен в навигацию с двумя способами добавления. Вкладка «Ревью» в карточке документа показывает автоподбор шаблона, список отступлений с цветовой подсветкой и кнопку редлайна.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    AI-ревью договора против шаблона:
    - Раздел «Библиотека шаблонов» — загрузка файла или отметка из реестра
    - Вкладка «Ревью» в карточке договора — автоподбор шаблона + список отступлений
    - Цветовая подсветка: зелёный = добавлено, красный = удалено, жёлтый = изменено
    - Кнопка скачивания редлайна .docx
  </what-built>
  <how-to-verify>
    1. Запустить: streamlit run main.py
    2. Открыть «Библиотека шаблонов» → загрузить любой PDF/DOCX как шаблон типа «NDA» → нажать «Добавить шаблон»
    3. Перейти в реестр → выбрать договор типа «NDA» → вкладка «Ревью»
    4. Должно появиться «Автоматически подобран шаблон: ...»
    5. Нажать «Запустить ревью» → список отступлений с цветными блоками
    6. Нажать «Сгенерировать редлайн .docx» → «Скачать редлайн» → открыть в Word → убедиться в track changes
    7. Альтернативно: зайти в «Библиотека шаблонов» → выбрать документ из реестра → «Сделать эталоном»
  </how-to-verify>
  <resume-signal>Напиши "approved" или опиши что не работает</resume-signal>
</task>

</tasks>

<verification>
1. python -c "from services.review_service import review_against_template; print('OK')" — без ошибок
2. python-тест Task 1 завершается "OK: review_service работает корректно"
3. grep -n "tab_review\|Ревью" main.py — заглушка заменена реальным кодом
</verification>

<success_criteria>
- review_service.py создан с четырьмя функциями без зависимости от Streamlit
- Два способа добавления шаблона работают (загрузка файла + отметка из реестра)
- Автоподбор шаблона по типу договора через косинусное сходство (порог 0.60)
- Отступления показываются с правильными цветами (зелёный/красный/жёлтый)
- Редлайн .docx скачивается и открывается в Word с track changes
</success_criteria>

<output>
После завершения создать `.planning/phases/02-document-lifecycle/02-07-SUMMARY.md`
</output>
