---
phase: 02-document-lifecycle
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - services/version_service.py
  - main.py
autonomous: false
requirements:
  - LIFE-04

must_haves:
  truths:
    - "Вкладка «Версии» в карточке договора показывает вертикальный таймлайн всех версий"
    - "Клик на пару версий показывает diff по ключевым полям (контрагент, суммы, даты)"
    - "Кнопка «Скачать редлайн» генерирует .docx с track changes (w:ins/w:del) и предлагает скачать"
    - "Редлайн корректно открывается в Word с видимыми правками"
  artifacts:
    - path: "services/version_service.py"
      provides: "Функции diff_versions и generate_redline_docx"
      exports: ["diff_versions", "generate_redline_docx"]
    - path: "main.py"
      provides: "Карточка документа с вкладкой «Версии» — таймлайн + diff + кнопка редлайна"
      contains: "get_version_group"
  key_links:
    - from: "main.py (вкладка Версии)"
      to: "version_service.diff_versions"
      via: "вызов при выборе пары версий в UI"
      pattern: "diff_versions"
    - from: "main.py (кнопка редлайна)"
      to: "version_service.generate_redline_docx"
      via: "st.download_button с BytesIO буфером"
      pattern: "generate_redline_docx"
---

<objective>
Добавить diff между версиями документа и генерацию редлайна в .docx формате.

Purpose: LIFE-04 — юрист видит что именно изменилось между версиями договора, и может скачать редлайн для работы в Word.
Output: diff_versions и generate_redline_docx в version_service.py + вкладка «Версии» в карточке документа в main.py.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-document-lifecycle/02-CONTEXT.md
@.planning/phases/02-document-lifecycle/02-RESEARCH.md
@.planning/phases/02-document-lifecycle/02-03-SUMMARY.md

<interfaces>
<!-- Из version_service.py (02-03) -->
```python
def get_version_group(db: Database, contract_id: int) -> list[DocumentVersion]
# Возвращает все версии отсортированные по version_number ASC

# DocumentVersion:
# id, contract_group_id, contract_id, version_number, link_method, created_at, linked_at
```

Паттерн редлайна из 02-RESEARCH.md:
```python
from docx.oxml.ns import qn
from docx.oxml import OxmlElement

def add_deleted_run(para, text: str, author: str = "ЮрТэг"):
    del_el = OxmlElement('w:del')
    del_el.set(qn('w:id'), str(next_id()))
    del_el.set(qn('w:author'), author)
    del_el.set(qn('w:date'), '2026-01-01T00:00:00Z')
    run = OxmlElement('w:r')
    del_text = OxmlElement('w:delText')
    del_text.set(qn('xml:space'), 'preserve')
    del_text.text = text
    run.append(del_text)
    del_el.append(run)
    para._p.append(del_el)

def add_inserted_run(para, text: str, author: str = "ЮрТэг"):
    ins_el = OxmlElement('w:ins')
    ins_el.set(qn('w:id'), str(next_id()))
    ins_el.set(qn('w:author'), author)
    ins_el.set(qn('w:date'), '2026-01-01T00:00:00Z')
    run = para.add_run(text)
    run._r.getparent().remove(run._r)
    ins_el.append(run._r)
    para._p.append(ins_el)
```

ContractMetadata поля для diff (из models.py):
```
contract_type, counterparty, subject, date_signed, date_start, date_end,
amount, payment_amount, payment_frequency, payment_direction
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: diff_versions и generate_redline_docx в version_service.py</name>
  <files>services/version_service.py</files>
  <read_first>
    - services/version_service.py (добавить функции к существующему файлу)
    - .planning/phases/02-document-lifecycle/02-RESEARCH.md (Pattern 3: Redline .docx через lxml)
    - modules/models.py (ContractMetadata — поля для diff)
  </read_first>
  <action>
Добавить в конец services/version_service.py две функции:

**diff_versions** — сравнивает метаданные двух версий:
```python
import difflib
from dataclasses import asdict, fields
from modules.models import ContractMetadata

# Поля для структурированного diff
_DIFF_FIELDS = [
    ("contract_type",    "Тип договора"),
    ("counterparty",     "Контрагент"),
    ("subject",          "Предмет"),
    ("date_signed",      "Дата подписания"),
    ("date_start",       "Дата начала"),
    ("date_end",         "Дата окончания"),
    ("amount",           "Сумма"),
    ("payment_amount",   "Сумма платежа"),
    ("payment_frequency","Периодичность"),
    ("payment_direction","Направление платежа"),
]


def diff_versions(
    meta_old: ContractMetadata,
    meta_new: ContractMetadata,
) -> list[dict]:
    """Сравнивает метаданные двух версий договора.

    Returns: список изменений, каждое:
        {"field": "Контрагент", "old": "...", "new": "...", "changed": True/False}
    """
    result = []
    old_d = asdict(meta_old)
    new_d = asdict(meta_new)
    for field_key, field_label in _DIFF_FIELDS:
        old_val = str(old_d.get(field_key) or "—")
        new_val = str(new_d.get(field_key) or "—")
        result.append({
            "field": field_label,
            "old": old_val,
            "new": new_val,
            "changed": old_val != new_val,
        })
    return result
```

**generate_redline_docx** — генерирует .docx с track changes:
```python
import io as _io
from itertools import count as _count

_revision_counter = _count(1)

def _next_rev_id() -> str:
    return str(next(_revision_counter))


def generate_redline_docx(text_old: str, text_new: str, title: str = "Редлайн") -> bytes:
    """Генерирует .docx с track changes (w:ins/w:del) для пары текстов.

    Использует difflib на уровне предложений.
    Возвращает байты готового .docx файла.
    """
    from docx import Document
    from docx.oxml.ns import qn
    from docx.oxml import OxmlElement

    def _sentences(text: str) -> list[str]:
        # Разбить по предложениям (простой split по '. ')
        import re
        parts = re.split(r'(?<=[.!?])\s+', text.strip())
        return [p for p in parts if p]

    def _add_deleted_run(para, text: str) -> None:
        del_el = OxmlElement('w:del')
        del_el.set(qn('w:id'), _next_rev_id())
        del_el.set(qn('w:author'), 'ЮрТэг')
        del_el.set(qn('w:date'), '2026-01-01T00:00:00Z')
        run_el = OxmlElement('w:r')
        del_text_el = OxmlElement('w:delText')
        del_text_el.set(qn('xml:space'), 'preserve')
        del_text_el.text = text
        run_el.append(del_text_el)
        del_el.append(run_el)
        para._p.append(del_el)

    def _add_inserted_run(para, text: str) -> None:
        ins_el = OxmlElement('w:ins')
        ins_el.set(qn('w:id'), _next_rev_id())
        ins_el.set(qn('w:author'), 'ЮрТэг')
        ins_el.set(qn('w:date'), '2026-01-01T00:00:00Z')
        run = para.add_run(text)
        run._r.getparent().remove(run._r)
        ins_el.append(run._r)
        para._p.append(ins_el)

    doc = Document()
    doc.add_heading(title, level=1)

    old_sents = _sentences(text_old)
    new_sents = _sentences(text_new)

    matcher = difflib.SequenceMatcher(None, old_sents, new_sents)

    for tag, i1, i2, j1, j2 in matcher.get_opcodes():
        if tag == 'equal':
            for s in old_sents[i1:i2]:
                doc.add_paragraph(s)
        elif tag == 'replace':
            para = doc.add_paragraph()
            for s in old_sents[i1:i2]:
                _add_deleted_run(para, s + ' ')
            for s in new_sents[j1:j2]:
                _add_inserted_run(para, s + ' ')
        elif tag == 'delete':
            para = doc.add_paragraph()
            for s in old_sents[i1:i2]:
                _add_deleted_run(para, s + ' ')
        elif tag == 'insert':
            para = doc.add_paragraph()
            for s in new_sents[j1:j2]:
                _add_inserted_run(para, s + ' ')

    buf = _io.BytesIO()
    doc.save(buf)
    return buf.getvalue()
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from services.version_service import diff_versions, generate_redline_docx
from modules.models import ContractMetadata

# diff_versions
m1 = ContractMetadata(contract_type='NDA', counterparty='Альфа', amount='100000')
m2 = ContractMetadata(contract_type='NDA', counterparty='Бета', amount='150000')
diffs = diff_versions(m1, m2)
changed = [d for d in diffs if d['changed']]
assert len(changed) == 2, f'Ожидается 2 изменения, получено {len(changed)}: {changed}'
field_names = [d['field'] for d in changed]
assert 'Контрагент' in field_names, 'Контрагент должен быть в изменениях'
assert 'Сумма' in field_names, 'Сумма должна быть в изменениях'

# generate_redline_docx
docx_bytes = generate_redline_docx(
    'Первая версия договора. Срок действия один год.',
    'Первая версия договора. Срок действия два года.',
    title='Тест редлайна'
)
assert isinstance(docx_bytes, bytes), 'generate_redline_docx должна вернуть bytes'
assert len(docx_bytes) > 1000, f'Слишком маленький .docx: {len(docx_bytes)} байт'
# Проверить что это валидный .docx (ZIP-архив начинается с PK)
assert docx_bytes[:2] == b'PK', 'Файл не является валидным ZIP/docx'

print('OK: diff_versions и generate_redline_docx работают корректно')
"
</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "def diff_versions\|def generate_redline_docx" services/version_service.py возвращает 2 строки
    - grep -n "w:del\|w:ins\|OxmlElement" services/version_service.py возвращает 4+ строки
    - python-тест выше завершается "OK: diff_versions и generate_redline_docx работают корректно"
  </acceptance_criteria>
  <done>diff_versions сравнивает 10 ключевых полей ContractMetadata. generate_redline_docx генерирует валидный .docx с w:del/w:ins track changes, который открывается в Word.</done>
</task>

<task type="auto">
  <name>Task 2: Карточка документа с вкладкой «Версии» в main.py</name>
  <files>main.py</files>
  <read_first>
    - main.py (найти где отображается детальная информация по выбранному договору, или где будет карточка)
    - services/version_service.py (сигнатуры get_version_group, diff_versions, generate_redline_docx)
  </read_first>
  <action>
В main.py добавить или расширить карточку документа. Если уже есть детальное представление при клике/выборе договора — расширить его. Если нет — добавить как expandable секцию под таблицей реестра.

Структура карточки — 4 вкладки (как в CONTEXT.md locked decision):
```
tab_main, tab_versions, tab_payments, tab_review = st.tabs(
    ["Основное", "Версии", "Платежи", "Ревью"]
)
```

**Вкладка «Версии»** (реализуется в этом плане):
```python
with tab_versions:
    versions = get_version_group(db, selected_contract_id)
    if not versions:
        st.info("Версии не найдены")
    else:
        st.markdown(f"**Всего версий:** {len(versions)}")

        # Вертикальный таймлайн
        for v in versions:
            icon = "📄" if v.version_number < len(versions) else "📋"
            method_label = "авто" if v.link_method == "auto_embedding" else "вручную"
            st.markdown(
                f"{icon} **v{v.version_number}** · {v.created_at or '—'} "
                f"· связан {method_label}"
            )

        # Выбор пары для diff (только если версий >= 2)
        if len(versions) >= 2:
            st.markdown("---")
            st.subheader("Сравнение версий")
            col_v1, col_v2 = st.columns(2)
            version_options = {f"v{v.version_number}": v.contract_id for v in versions}
            with col_v1:
                v_old_label = st.selectbox("Старая версия", list(version_options.keys())[:-1], key="diff_old")
            with col_v2:
                v_new_label = st.selectbox("Новая версия", list(version_options.keys())[1:], key="diff_new")

            if st.button("Сравнить", key="diff_btn"):
                old_cid = version_options[v_old_label]
                new_cid = version_options[v_new_label]
                # Загрузить метаданные обеих версий из БД
                def _load_meta(cid):
                    row = db._conn.execute(
                        "SELECT contract_type, counterparty, subject, date_signed, "
                        "date_start, date_end, amount FROM contracts WHERE id=?", (cid,)
                    ).fetchone()
                    if row:
                        from modules.models import ContractMetadata
                        return ContractMetadata(
                            contract_type=row[0], counterparty=row[1],
                            subject=row[2], date_signed=row[3],
                            date_start=row[4], date_end=row[5], amount=row[6],
                        )
                    return ContractMetadata()

                diffs = diff_versions(_load_meta(old_cid), _load_meta(new_cid))
                changed = [d for d in diffs if d["changed"]]
                if not changed:
                    st.success("Ключевые поля идентичны")
                else:
                    for d in changed:
                        st.markdown(
                            f"**{d['field']}:** "
                            f"~~{d['old']}~~ → **{d['new']}**"
                        )

            # Кнопка редлайна
            st.markdown("---")
            col_rl1, col_rl2 = st.columns(2)
            with col_rl1:
                rl_old = st.selectbox("Старая (редлайн)", list(version_options.keys())[:-1], key="rl_old")
            with col_rl2:
                rl_new = st.selectbox("Новая (редлайн)", list(version_options.keys())[1:], key="rl_new")

            if st.button("Сгенерировать редлайн", key="rl_btn"):
                old_text_row = db._conn.execute(
                    "SELECT filename FROM contracts WHERE id=?",
                    (version_options[rl_old],)
                ).fetchone()
                new_text_row = db._conn.execute(
                    "SELECT filename FROM contracts WHERE id=?",
                    (version_options[rl_new],)
                ).fetchone()
                # Использовать subject как текст-заглушку если полный текст недоступен
                # (полный текст хранится в ExtractedText, не в БД — используем subject)
                old_meta = db._conn.execute(
                    "SELECT subject, contract_type FROM contracts WHERE id=?",
                    (version_options[rl_old],)
                ).fetchone()
                new_meta = db._conn.execute(
                    "SELECT subject, contract_type FROM contracts WHERE id=?",
                    (version_options[rl_new],)
                ).fetchone()
                title = f"Редлайн: {old_meta[1] or 'Договор'} ({rl_old} → {rl_new})"
                docx_bytes = generate_redline_docx(
                    old_meta[0] or "", new_meta[0] or "", title
                )
                st.download_button(
                    "Скачать .docx",
                    data=docx_bytes,
                    file_name=f"redline_{rl_old}_vs_{rl_new}.docx",
                    mime="application/vnd.openxmlformats-officedocument.wordprocessingml.document",
                    key="rl_download",
                )
```

Добавить импорты в начало main.py (если не добавлены в 02-02):
```python
from services.version_service import (
    get_version_group, diff_versions, generate_redline_docx,
)
```

Вкладки «Платежи» и «Ревью» пока остаются с заглушкой:
```python
with tab_payments:
    st.info("Платёжный календарь — доступен после обработки договоров с суммами")

with tab_review:
    st.info("Ревью против шаблона — загрузите шаблон-эталон в библиотеку")
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "tab_versions\|get_version_group\|diff_versions\|generate_redline_docx\|Версии" main.py | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "tab_versions\|tab_payments\|tab_review\|tab_main" main.py возвращает 4 строки (4 вкладки)
    - grep -n "get_version_group" main.py возвращает минимум 1 строку вызова
    - grep -n "diff_versions" main.py возвращает минимум 1 строку вызова
    - grep -n "generate_redline_docx" main.py возвращает минимум 1 строку вызова
    - grep -n "st.download_button" main.py возвращает строку для редлайна
  </acceptance_criteria>
  <done>Карточка документа содержит 4 вкладки. Вкладка «Версии» показывает таймлайн, diff ключевых полей и кнопку скачивания редлайна. Вкладки «Платежи» и «Ревью» содержат заглушки.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Вкладка «Версии» в карточке договора:
    - Вертикальный таймлайн версий (v1 → v2 → ...)
    - Сравнение ключевых полей между парами версий
    - Генерация и скачивание редлайна .docx
  </what-built>
  <how-to-verify>
    1. Запустить: streamlit run main.py
    2. Открыть реестр, выбрать любой договор
    3. В карточке документа должны быть вкладки «Основное», «Версии», «Платежи», «Ревью»
    4. Открыть вкладку «Версии» — если версий нет, появляется «Версии не найдены»
    5. Если есть несколько версий — видны v1, v2, ... в таймлайне
    6. Нажать «Сравнить» — должны появиться изменённые поля с зачёркиванием старого значения
    7. Нажать «Сгенерировать редлайн» → «Скачать .docx» → открыть в Word → убедиться что видны track changes
  </how-to-verify>
  <resume-signal>Напиши "approved" или опиши что не работает</resume-signal>
</task>

</tasks>

<verification>
1. python -c "from services.version_service import diff_versions, generate_redline_docx; print('OK')" — без ошибок
2. Скачанный .docx открывается в Word без ошибок и содержит пометки изменений
</verification>

<success_criteria>
- diff_versions находит все 10 сравниваемых полей
- generate_redline_docx возвращает валидный .docx (ZIP magic bytes PK)
- Карточка документа имеет 4 вкладки (Основное/Версии/Платежи/Ревью)
- Редлайн открывается в Word с track changes
</success_criteria>

<output>
После завершения создать `.planning/phases/02-document-lifecycle/02-04-SUMMARY.md`
</output>
