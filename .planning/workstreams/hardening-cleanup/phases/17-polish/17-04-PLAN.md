---
phase: 17-polish
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified: []
autonomous: false
requirements: [XCUT-04]

must_haves:
  truths:
    - "Навигация header→registry→card→templates→settings выглядит как единый продукт"
    - "Все цвета, шрифты и отступы когерентны на всех экранах"
    - "FullCalendar calendar view не сломан после всех CSS изменений"
    - "Нет janky transitions или stuttering на macOS pywebview"
  artifacts: []
  key_links: []
---

<objective>
Visual seam check — финальная ручная проверка всех экранов после завершения Plans 01-03.

Purpose: Убедиться что все пять экранов (splash, registry, card, templates, settings) визуально когерентны как единый продукт. FullCalendar smoke-test. Performance check transitions < 200ms.
Output: Подтверждение visual seam или список найденных несоответствий для исправления.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-polish/17-01-SUMMARY.md
@.planning/phases/17-polish/17-02-SUMMARY.md
@.planning/phases/17-polish/17-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual seam check — все экраны (XCUT-04)</name>
  <what-built>
Plans 01-03 завершены:
- Templates: color-coded cards с type icon + badges + rich empty state
- Settings: sidebar с indigo active state + section headers + dividers
- Animations: skeleton loader, card-enter stagger, footer версии
- Hover states: аудит завершён
  </what-built>
  <how-to-verify>
Запустить приложение: `python -m streamlit run main.py` или `python main.py` (зависит от точки входа).

**Маршрут проверки:**

1. **Splash** — тёмный hero, крупный заголовок, кнопки
2. **Registry (Реестр)** — stats bar, AG Grid с цветными badges, фильтр-бар
   - Проверить skeleton loader при первом открытии (серые пульсирующие блоки)
   - Переключить сегменты фильтра — анимация строк не перезапускается
3. **Card (Документ)** — открыть любой документ из реестра
   - Breadcrumbs, section dividers, amber AI-ревью блок
4. **Templates (Шаблоны)** — перейти на /templates
   - Карточки: у каждой цветная левая полоса 4px, иконка типа, цветной badge
   - Если нет шаблонов: rich empty state с иконкой и кнопкой
   - Hover: карточка поднимается на 2px
5. **Settings (Настройки)** — перейти на /settings
   - Sidebar: активный пункт «ИИ» — indigo-50 фон, indigo-700 текст
   - Три секции через sidebar — каждая имеет heading + description + 1px divider
6. **Footer** — виден внизу на всех страницах (кроме splash), содержит «ЮрТэг v0.7»
7. **FullCalendar** — включить calendar view в реестре — нет артефактов, сетка корректная
8. **Transitions** — переходы между страницами плавные fade 200ms, нет jank

**Критерии прохождения:**
- Все 8 пунктов OK
- Шрифт IBM Plex Sans на всех экранах
- Цвета slate/indigo/amber когерентны по всему приложению
- Нет белых «дырок» или несоответствий фона
  </how-to-verify>
  <resume-signal>Напишите "approved" если все экраны выглядят когерентно. Или опишите конкретные проблемы (какой экран, что именно не так) — исправим точечно.</resume-signal>
</task>

</tasks>

<verification>
Результат определяется пользователем: "approved" или список конкретных несоответствий.
</verification>

<success_criteria>
- Пользователь написал "approved" — Phase 17 завершена, v0.7 milestone готов к закрытию
- Или: конкретный список проблем зафиксирован → gap closure план при необходимости
</success_criteria>

<output>
После получения "approved" создать `.planning/phases/17-polish/17-04-SUMMARY.md` и обновить STATE.md: milestone v0.7 complete.
</output>
