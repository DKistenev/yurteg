---
phase: 17-polish
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - app/static/design-system.css
  - app/main.py
  - app/pages/registry.py
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03, ANIM-04, XCUT-01, XCUT-02]

must_haves:
  truths:
    - "При переходе между страницами контент плавно появляется (fade 200ms)"
    - "Карточки шаблонов появляются с stagger-эффектом (как .hero-enter на splash)"
    - "При загрузке реестра видны серые пульсирующие блоки вместо пустого белого экрана"
    - "Footer с «ЮрТэг v0.7» виден снизу на всех страницах"
    - "Все интерактивные элементы (кнопки, ссылки, карточки) имеют hover-эффект"
  artifacts:
    - path: "app/static/design-system.css"
      provides: "skeleton-pulse animation, .card-enter stagger, hover-state audits"
      contains: "skeleton-pulse"
    - path: "app/main.py"
      provides: "footer компонент в root()"
      contains: "ЮрТэг v0.7"
    - path: "app/pages/registry.py"
      provides: "skeleton loader перед загрузкой данных реестра"
      contains: "skeleton"
  key_links:
    - from: "app/pages/registry.py"
      to: "app/static/design-system.css"
      via: "CSS class .skeleton-row"
      pattern: "skeleton"
    - from: "app/main.py"
      to: "app/pages/templates.py"
      via: "footer рендерится после ui.sub_pages"
      pattern: "ЮрТэг v0.7"

user_setup: []
---

<objective>
Анимации (stagger cards, skeleton loader), footer версии, аудит hover states.

Purpose: Последний слой полировки — приложение начинает «дышать». Переходы уже реализованы (page-fade-in на .nicegui-content), кнопки уже имеют micro-interactions. Этот план добавляет: skeleton loader для реестра, stagger для template cards, footer, и закрывает пробелы в hover states.
Output: design-system.css расширен, main.py с footer, registry.py с skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

@app/static/design-system.css
@app/static/tokens.css
@app/main.py
@app/pages/registry.py
@app/pages/templates.py

<!-- Нужен SUMMARY из Plan 01 для понимания структуры template cards -->
@.planning/phases/17-polish/17-01-SUMMARY.md

<interfaces>
<!-- Установленные паттерны анимаций -->

Из design-system.css:
```css
/* Page fade-in — УЖЕ РАБОТАЕТ (ANIM-01 частично закрыт) */
@keyframes page-fade-in {
  from { opacity: 0; }
  to   { opacity: 1; }
}
.nicegui-content {
  animation: page-fade-in 200ms ease-out both;
}

/* Stagger pattern — hero-enter (применить для template cards) */
@keyframes hero-slide-up {
  from { opacity: 0; transform: translateY(24px); }
  to   { opacity: 1; transform: translateY(0); }
}
.hero-enter {
  animation: hero-slide-up var(--yt-duration-slow) var(--yt-ease-out) both;
}
.hero-enter:nth-child(1) { animation-delay: 0ms; }
.hero-enter:nth-child(2) { animation-delay: 100ms; }
.hero-enter:nth-child(3) { animation-delay: 200ms; }
.hero-enter:nth-child(4) { animation-delay: 300ms; }

/* Button micro-interactions — УЖЕ РАБОТАЕТ (ANIM-03 закрыт) */
.q-btn:not(.q-btn--flat):hover { transform: scale(1.02); }
```

Из tokens.css:
```css
--yt-duration-fast: 150ms;
--yt-duration-base: 200ms;
--yt-duration-slow: 300ms;
--yt-ease-out: cubic-bezier(0.25, 1, 0.5, 1);
--yt-color-border: var(--yt-p-slate-200);
```

Из main.py:
```python
# root() structure:
render_header(state, on_upload=_handle_upload)
ui.sub_pages({...})
# Footer добавить ПОСЛЕ ui.sub_pages()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Skeleton loader CSS + hover state audit (ANIM-04, XCUT-02)</name>
  <files>app/static/design-system.css</files>
  <read_first>app/static/design-system.css — прочитать весь файл перед изменением</read_first>
  <action>
**Добавить в конец design-system.css** следующие блоки:

**1. Skeleton loader animation (ANIM-04):**
```css
/* ── Skeleton loader (ANIM-04) ─────────────────────────────────────────────── */
/* GitHub-style: серые блоки с pulse. Без backdrop-filter (CPU spikes macOS pywebview) */

@keyframes skeleton-pulse {
  0%, 100% { opacity: 1; }
  50%       { opacity: 0.4; }
}

@layer components {
  .skeleton-row {
    height: 44px;           /* AG Grid row height из --ag-row-height */
    background: var(--yt-p-slate-200);
    border-radius: var(--yt-radius-sm);
    animation: skeleton-pulse 1.5s ease-in-out infinite;
    margin-bottom: 2px;
  }
  .skeleton-row:nth-child(2) { animation-delay: 150ms; opacity: 0.85; }
  .skeleton-row:nth-child(3) { animation-delay: 300ms; opacity: 0.7;  }
  .skeleton-row:nth-child(4) { animation-delay: 450ms; opacity: 0.6;  }
  .skeleton-row:nth-child(5) { animation-delay: 600ms; opacity: 0.5;  }
}
```

**2. Card stagger for template cards (ANIM-02):**
```css
/* ── Template card stagger (ANIM-02) ──────────────────────────────────────── */
/* .card-enter применяется на каждую карточку в grid (аналогично .hero-enter) */

@layer components {
  .card-enter {
    animation: hero-slide-up var(--yt-duration-slow) var(--yt-ease-out) both;
  }
  .card-enter:nth-child(1) { animation-delay: 0ms;   }
  .card-enter:nth-child(2) { animation-delay: 60ms;  }
  .card-enter:nth-child(3) { animation-delay: 120ms; }
  .card-enter:nth-child(4) { animation-delay: 180ms; }
  .card-enter:nth-child(5) { animation-delay: 240ms; }
  .card-enter:nth-child(6) { animation-delay: 300ms; }
  .card-enter:nth-child(n+7) { animation-delay: 360ms; }
}
```

**3. Hover state audit — закрыть пробелы (XCUT-02):**
```css
/* ── Hover states audit (XCUT-02) ─────────────────────────────────────────── */
/* Закрываем случаи где hover не определён */

@layer components {
  /* Nav sidebar items в settings — hover если нет active */
  .settings-nav-item:not(.settings-nav-active):hover {
    background: var(--yt-p-slate-100);
  }

  /* Stats bar items — subtle highlight */
  .stats-item-clickable:hover {
    background: var(--yt-color-accent-subtle);
    border-radius: var(--yt-radius-md);
  }

  /* Breadcrumb link hover (уже есть indigo color в styles.py, добавим underline) */
  .breadcrumb-link:hover {
    text-decoration: underline;
  }
}
```

**Примечание по ANIM-01 (page transitions):** `.nicegui-content { animation: page-fade-in 200ms }` уже реализован в design-system.css. ANIM-01 считается закрытым. Не добавлять slide transitions — macOS pywebview может давать jank с transform на контейнере страницы.

**Примечание по ANIM-03 (micro-interactions):** `.q-btn:not(.q-btn--flat):hover { transform: scale(1.02) }` уже реализован. ANIM-03 закрыт.
  </action>
  <verify>
    <automated>python -c "
css = open('app/static/design-system.css').read()
assert 'skeleton-pulse' in css, 'нет skeleton-pulse keyframe'
assert 'skeleton-row' in css, 'нет .skeleton-row class'
assert 'card-enter' in css, 'нет .card-enter stagger'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
grep -c "skeleton-pulse" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/static/design-system.css | grep -v "^0$"
grep -c "card-enter" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/static/design-system.css | grep -v "^0$"
  </acceptance_criteria>
  <done>
- .skeleton-row с skeleton-pulse animation существует в CSS
- .card-enter stagger (6 шагов по 60ms) существует в CSS
- Hover audit блок добавлен
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Footer в main.py + skeleton в registry.py + card-enter в templates.py (XCUT-01, ANIM-04, ANIM-02)</name>
  <files>app/main.py, app/pages/registry.py, app/pages/templates.py</files>
  <read_first>
app/main.py — весь файл
app/pages/registry.py — строки 113-180 (функция build)
app/pages/templates.py — функция _render_cards и _render_card
  </read_first>
  <action>
**1. Footer в main.py (XCUT-01):**

В функции `root()`, ПОСЛЕ `ui.sub_pages({...})` добавить footer:
```python
# Footer — минимальный, только версия (per CONTEXT.md: «только версия, минимальный»)
with ui.element('footer').classes(
    "w-full py-3 px-8 flex justify-end items-center border-t border-slate-200 bg-white"
):
    ui.label("ЮрТэг v0.7").classes("text-xs text-slate-400")
```

Footer НЕ fixed — в content flow, прокручивается вместе со страницей. Это правильно для десктопного приложения с фиксированной высотой окна (1400×900).

**2. Skeleton loader в registry.py (ANIM-04):**

Найти функцию `build()` в registry.py. Найти место где рендерится AG Grid / _render_empty_state — обычно это блок после stats bar.

Добавить skeleton wrapper который показывается пока данные загружаются:
```python
# Skeleton loader — показывается до первой загрузки данных
# Используем container pattern: skeleton_container скрывается после load_table_data
skeleton_container = ui.column().classes("w-full px-1 pt-2")
with skeleton_container:
    for _ in range(5):
        ui.element('div').classes("skeleton-row")

# AG Grid контейнер — скрыт изначально
grid_container = ui.column().classes("w-full")
grid_container.set_visibility(False)
```

Потом в колбэке загрузки данных (найти место где load_table_data вызывается и grid обновляется):
```python
skeleton_container.set_visibility(False)
grid_container.set_visibility(True)
```

**Примечание:** Если registry.py использует другую структуру (grid сразу рендерится в build), то применить паттерн «skeleton пока grid не initialized»:
```python
# Показать skeleton немедленно, убрать когда grid готов
skeleton = ui.column().classes("w-full px-1 pt-2")
with skeleton:
    for _ in range(5):
        ui.element('div').classes("skeleton-row")

# После создания grid — убрать skeleton с небольшой задержкой
async def _hide_skeleton():
    await asyncio.sleep(0.1)  # дать AG Grid инициализироваться
    skeleton.set_visibility(False)

ui.timer(0.15, _hide_skeleton, once=True)
```

Если этот подход не вписывается в текущую структуру registry.py — упростить: показывать skeleton только при явном состоянии loading в get_state(). Проверить наличие `state.loading` или аналогичного поля.

**3. .card-enter в templates.py (ANIM-02):**

В функции `_render_cards` в блоке `with ui.grid(columns=2)`:
```python
with ui.grid(columns=2).classes("w-full gap-4"):
    for i, tmpl in enumerate(templates):
        # Обернуть карточку в div с .card-enter
        with ui.element('div').classes("card-enter"):
            _render_card(tmpl, container)
```

**ВАЖНО:** `.card-enter` применяется на wrapper div, НЕ на саму ui.card() — иначе конфликт с .q-card transition из design-system.css.

Проверить что animation работает корректно: карточки появляются по очереди при первом рендере страницы.

**Performance budget check:**
- skeleton-pulse: 1.5s infinite — не критично (CSS, без JS)
- card-enter stagger: cap at 360ms для 7+ карточек — в пределах 200ms перцептивного threshold для первой карточки
- page fade-in: уже 200ms — в норме
- НЕ добавлять will-change: transform — macOS pywebview GPU layer overhead
  </action>
  <verify>
    <automated>python -c "
main_src = open('app/main.py').read()
assert 'ЮрТэг v0.7' in main_src, 'footer нет в main.py'

tmpl_src = open('app/pages/templates.py').read()
assert 'card-enter' in tmpl_src, 'card-enter нет в templates.py'

registry_src = open('app/pages/registry.py').read()
assert 'skeleton' in registry_src.lower(), 'skeleton нет в registry.py'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
grep -c "ЮрТэг v0.7" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/main.py | grep -v "^0$"
grep -c "card-enter" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/pages/templates.py | grep -v "^0$"
grep -ci "skeleton" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/pages/registry.py | grep -v "^0$"
  </acceptance_criteria>
  <done>
- Footer «ЮрТэг v0.7» существует в root() после ui.sub_pages
- registry.py показывает skeleton-row блоки при загрузке реестра
- templates.py оборачивает каждую карточку в .card-enter для stagger
  </done>
</task>

</tasks>

<verification>
1. `python -c "from app.main import root; print('main OK')"` — нет синтаксических ошибок
2. `python -c "from app.pages.registry import build; print('registry OK')"`
3. `python -c "from app.pages.templates import build; print('templates OK')"`
4. grep проверки из acceptance_criteria для всех трёх файлов
</verification>

<success_criteria>
- design-system.css содержит .skeleton-row с skeleton-pulse и .card-enter с stagger
- main.py root() содержит footer с «ЮрТэг v0.7»
- registry.py показывает skeleton loader до загрузки данных
- templates.py использует .card-enter wrapper на каждой карточке
- Нет backdrop-filter, нет will-change: transform (performance budget)
- page-fade-in и button micro-interactions были раньше — не сломаны
</success_criteria>

<output>
После завершения создать `.planning/phases/17-polish/17-03-SUMMARY.md`
</output>
