---
phase: 18-layout-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/header.py
  - app/pages/templates.py
  - app/pages/settings.py
autonomous: true
requirements: [BRND-01, LAY-03, LAY-04, PLSH-01, PLSH-02, RBST-02]

must_haves:
  truths:
    - "Лого в header: квадрат с «Юр» (indigo bg) + «Тэг» справа без квадрата"
    - "Диалог «Новое рабочее пространство» стилизован, не generic Quasar card"
    - "Страница шаблонов центрирована (max-w-5xl mx-auto)"
    - "Empty state шаблонов не имеет py-20, контент ближе к заголовку"
    - "Настройки: нет min-h-screen на внешнем row, высота через flex-1"
    - "Template cards имеют aria-label"
  artifacts:
    - path: "app/components/header.py"
      provides: "«Юр» logo mark + styled dialog"
    - path: "app/pages/templates.py"
      provides: "max-w-5xl centering, py-10 empty state, aria-labels"
    - path: "app/pages/settings.py"
      provides: "flex-1 height instead of min-h-screen"
  key_links:
    - from: "header.py render_header"
      to: "logo mark div"
      via: "ui.html() with 'Юр' text"
    - from: "header.py _show_add_dialog"
      to: "ui.dialog card styling"
      via: "CARD_DIALOG class + styled header"
---

<objective>
Переделать логотип, рестайлить диалог «Новое пространство», центрировать шаблоны и настройки.

Purpose: Логотип показывает «Ю» вместо «Юр» (режет слово), диалог выглядит generic, шаблоны и настройки прижаты влево/расползаются на весь экран.

Output: header.py с обновлённым лого и стилизованным диалогом, templates.py с центрированием и правильным empty state spacing, settings.py без min-h-screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-layout-fixes/18-CONTEXT.md
</context>

<interfaces>
<!-- Из app/styles.py — используемые константы -->
```python
CARD_DIALOG = "p-6 min-w-[400px]"
BTN_ACCENT_FILLED = "px-5 py-2 bg-indigo-600 text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 transition-colors duration-150"
TEXT_HEADING_2XL = "text-2xl font-semibold text-slate-900"
TMPL_EMPTY_ICON = "description"
TMPL_EMPTY_TITLE = "text-xl font-semibold text-slate-700 mt-4"
TMPL_EMPTY_BODY = "text-sm text-slate-400 text-center max-w-xs mt-2"
```

<!-- Текущий лого (header.py строки 38-44): -->
```python
with ui.row().classes("items-center gap-2 shrink-0"):
    ui.html(
        '<div class="w-7 h-7 rounded-lg flex items-center justify-content'
        ' text-white text-sm font-bold" style="background: #4f46e5;'
        ' line-height: 1; flex-shrink: 0;">Ю</div>'
    )
    ui.label("рТэг").classes("text-base font-semibold text-white tracking-tight")
```

<!-- Нужный результат: «Юр» в квадрате + «Тэг» как wordmark: -->
```python
with ui.row().classes("items-center gap-2 shrink-0"):
    ui.html(
        '<div style="display:flex;align-items:center;justify-content:center;'
        'width:32px;height:28px;background:#4f46e5;border-radius:8px;'
        'color:white;font-size:0.8rem;font-weight:700;letter-spacing:-0.02em;'
        'flex-shrink:0;line-height:1;">Юр</div>'
    )
    ui.label("Тэг").classes("text-base font-semibold text-white tracking-tight")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Лого «Юр+Тэг» и диалог «Новое пространство» (BRND-01, PLSH-01)</name>
  <files>app/components/header.py</files>
  <read_first>app/components/header.py строки 33-144 (render_header и _show_add_dialog)</read_first>
  <action>
**1. Логотип (BRND-01):**
Найти блок с ui.html() для лого-марки (строки ~38-44). Заменить:
- Старое: квадрат `w-7 h-7` (28x28px) с «Ю», рядом `ui.label("рТэг")`
- Новое: прямоугольник `width:32px; height:28px` с текстом «Юр», рядом `ui.label("Тэг")`

Новый код лого-марки:
```python
ui.html(
    '<div style="display:flex;align-items:center;justify-content:center;'
    'width:32px;height:28px;background:#4f46e5;border-radius:8px;'
    'color:white;font-size:0.8rem;font-weight:700;letter-spacing:-0.02em;'
    'flex-shrink:0;line-height:1;">Юр</div>'
)
ui.label("Тэг").classes("text-base font-semibold text-white tracking-tight")
```

**2. Диалог «Новое рабочее пространство» (PLSH-01):**
Найти `_show_add_dialog()` (строки ~124-144). Текущий вид — generic `ui.card()` без стиля.

Заменить содержимое диалога на стилизованную версию:
```python
with ui.dialog() as dlg, ui.card().classes("p-0 min-w-[420px] overflow-hidden shadow-xl"):
    # Заголовок с цветной полосой
    with ui.element("div").style(
        "background:#4f46e5;padding:20px 24px 16px;"
    ):
        with ui.element("p").style("color:white;font-size:1rem;font-weight:600;margin:0;"):
            ui.html("Новое рабочее пространство")
        with ui.element("p").style("color:#c7d2fe;font-size:0.8rem;margin:4px 0 0;"):
            ui.html("Создайте отдельный реестр для клиента или проекта")

    # Тело диалога
    with ui.column().classes("p-6 gap-4"):
        name_input = ui.input(
            placeholder="Например: ООО Ромашка"
        ).props("outlined dense").classes("w-full").style("font-size:0.875rem;")
        name_input.props('label="Название пространства" aria-label="Название рабочего пространства"')

        with ui.row().classes("gap-2 justify-end w-full"):
            ui.button("Отмена", on_click=dlg.close).props("flat no-caps").classes(
                "text-slate-500 text-sm"
            )

            async def _add() -> None:
                n = name_input.value.strip()
                if n:
                    await run.io_bound(cm.add_client, n)
                    _switch_client(state, n, btn, None)
                    dlg.close()

            ui.button("Создать", on_click=_add).props("no-caps").classes(
                "px-4 py-1.5 bg-indigo-600 text-white text-sm font-semibold rounded-lg"
                " hover:bg-indigo-700 transition-colors duration-150"
            )
```

Параметры `state`, `cm`, `btn` остаются из closure — они уже есть в `_show_add_dialog(state, cm, btn, menu)`.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "import ast; ast.parse(open('app/components/header.py').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
- grep "Юр" app/components/header.py — присутствует в ui.html() строке лого
- grep '"Тэг"' app/components/header.py — присутствует в ui.label()
- grep "рТэг" app/components/header.py — НЕ присутствует (удалено)
- grep "Новое рабочее пространство" app/components/header.py — присутствует в стилизованном блоке
- grep "background:#4f46e5" app/components/header.py — 2+ вхождения (лого + диалог header)
- python syntax check проходит без ошибок
  </acceptance_criteria>
  <done>Лого показывает «Юр» в indigo квадрате + «Тэг» как wordmark. Диалог имеет indigo header с описанием, стилизованные кнопки.</done>
</task>

<task type="auto">
  <name>Task 2: Центрировать шаблоны и настройки, исправить empty state spacing (LAY-03, LAY-04, PLSH-02, RBST-02)</name>
  <files>app/pages/templates.py, app/pages/settings.py</files>
  <read_first>
  - app/pages/templates.py строки 279-308 (функция build) и строки 49-69 (_render_cards empty state)
  - app/pages/settings.py строки 28-51 (функция build, outer row + sidebar)
  </read_first>
  <action>
**templates.py — три правки:**

1. **Центрирование (LAY-03):** В `build()` строка 281:
   - Было: `ui.column().classes("w-full p-8")`
   - Стало: `ui.column().classes("w-full max-w-5xl mx-auto p-8")`

2. **Empty state spacing (PLSH-02):** В `_render_cards()` строка 59:
   - Было: `ui.column().classes("w-full items-center justify-center py-20")`
   - Стало: `ui.column().classes("w-full items-center justify-center py-10")`

3. **ARIA на template cards (RBST-02):** В `_render_card()` строка ~90, на `ui.card()` добавить:
   ```python
   with ui.card().classes(
       "overflow-hidden cursor-default border border-slate-200 shadow-none rounded-xl"
   ).style("padding:0").props(f'role="article" aria-label="Шаблон: {tmpl.name}"'):
   ```
   Также в empty state CTA кнопка (~строка 66):
   ```python
   ui.button(
       "Добавить первый шаблон", on_click=on_add
   ).classes(BTN_ACCENT_FILLED).props('no-caps aria-label="Добавить первый шаблон договора"')
   ```

**settings.py — одна правка (LAY-04):**
Строка 35: внешний `ui.row()` имеет `min-h-screen` — это заставляет настройки растягиваться на весь viewport.
- Было: `ui.row().classes("w-full min-h-screen gap-0")`
- Стало: `ui.row().classes("w-full flex-1 gap-0")`

Левый sidebar (строка 38) тоже имеет `min-h-screen`:
- Было: `ui.column().classes("w-52 min-h-screen border-r ...")`
- Стало: `ui.column().classes("w-52 self-stretch border-r ...")`

`self-stretch` заставляет sidebar растягиваться до высоты родителя (flex-1 row), а не до viewport.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import ast
for f in ['app/pages/templates.py', 'app/pages/settings.py']:
    ast.parse(open(f).read())
    print(f, 'syntax ok')
"</automated>
  </verify>
  <acceptance_criteria>
- grep "max-w-5xl" app/pages/templates.py — присутствует
- grep "py-10" app/pages/templates.py — присутствует в empty state column
- grep "py-20" app/pages/templates.py — НЕ присутствует
- grep "aria-label" app/pages/templates.py — 2+ вхождений (card + CTA)
- grep "min-h-screen" app/pages/settings.py — 0 вхождений
- grep "flex-1" app/pages/settings.py — присутствует на outer row
- grep "self-stretch" app/pages/settings.py — присутствует на sidebar column
- python syntax check обоих файлов проходит без ошибок
  </acceptance_criteria>
  <done>Templates центрированы с max-w-5xl, empty state поднят ближе к заголовку. Settings не растягивается на весь viewport. Template cards имеют aria-label атрибуты.</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:
1. Оба syntax check проходят
2. grep-проверки из acceptance_criteria — все выполнены
3. Логотип: «Юр» в квадрате, «Тэг» — wordmark (не «Ю» + «рТэг»)
</verification>

<success_criteria>
Логотип показывает «Юр+Тэг». Диалог добавления пространства стилизован с indigo header. Шаблоны и настройки центрированы и не растягиваются на весь экран. Empty state шаблонов компактнее.
</success_criteria>

<output>
После завершения создать `.planning/phases/18-layout-fixes/18-02-SUMMARY.md`
</output>
