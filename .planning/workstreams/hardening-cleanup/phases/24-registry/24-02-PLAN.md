---
phase: 24-registry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/registry.py
  - app/static/design-system.css
autonomous: true
requirements: [REG-05]

must_haves:
  truths:
    - "Кнопка «Календарь» открывает таймлайн событий, а не пустую сетку FullCalendar"
    - "Лента событий слева группирована по времени: Сегодня, На этой неделе, В следующем месяце"
    - "Карточки событий имеют цветную полоску слева: красная для окончания, синяя для платежа, жёлтая для скоро истекает"
    - "Справа виден мини-календарь с цветными точками на датах"
    - "Под мини-календарём сводка за месяц: количество истекающих, платежей, сумма"
    - "Клик по карточке события открывает карточку документа"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "Timeline calendar view с лентой событий и мини-календарём"
      contains: "timeline"
    - path: "app/static/design-system.css"
      provides: "CSS стили для timeline карточек и мини-календаря"
      contains: "timeline-card"
  key_links:
    - from: "app/pages/registry.py"
      to: "services/payment_service.py"
      via: "get_calendar_events для данных событий"
      pattern: "get_calendar_events"
    - from: "app/pages/registry.py"
      to: "/document/{id}"
      via: "navigate при клике по карточке"
      pattern: "navigate.*document"
---

<objective>
Заменить FullCalendar grid на таймлайн событий + мини-календарь по утверждённому мокапу (variant B).

Purpose: Вместо пустой сетки FullCalendar показать полезный таймлайн ближайших событий с суммами и мини-календарём.
Output: Полностью новый вид «Календарь» в registry.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-registry/24-CONTEXT.md

@app/pages/registry.py
@services/payment_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Заменить FullCalendar на таймлайн + мини-календарь</name>
  <read_first>
    - app/pages/registry.py (строки 568-712 — calendar section: _ensure_fullcalendar, _show_calendar, _switch_view)
    - services/payment_service.py (get_calendar_events — структура данных событий)
    - .superpowers/brainstorm/99248-1774442361/calendar-redesign.html (variant B: Timeline — ВСЕ CSS оттуда)
    - app/static/design-system.css (существующие стили)
  </read_first>
  <files>app/pages/registry.py, app/static/design-system.css</files>
  <action>
**REG-05: Таймлайн событий + мини-календарь** (per D-05 decision, variant B из calendar-redesign.html)

**ШАГ 1: Удалить FullCalendar загрузку**

Удалить `_ensure_fullcalendar()` полностью — больше не нужна JS-библиотека FullCalendar.
Переписать `_show_calendar()` для рендеринга NiceGUI-компонентов вместо JS.

**ШАГ 2: Добавить CSS стили в design-system.css**

Добавить в `app/static/design-system.css` стили из мокапа variant B:

```css
/* ── Timeline Calendar (REG-05) ─────────────────────────────────────── */

.timeline-card {
  background: white;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
  padding: 10px 14px;
  margin-bottom: 8px;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: background 0.15s;
}
.timeline-card:hover {
  background: #f8fafc;
}
.timeline-card-end { border-left: 3px solid #dc2626; }
.timeline-card-pay { border-left: 3px solid #2563eb; }
.timeline-card-expiring { border-left: 3px solid #d97706; }

.timeline-type {
  font-size: 10px;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}
.timeline-type-end { color: #dc2626; }
.timeline-type-pay { color: #2563eb; }
.timeline-type-expiring { color: #d97706; }

.timeline-name {
  font-size: 13px;
  font-weight: 500;
  color: #1e293b;
  margin-top: 1px;
}
.timeline-meta {
  font-size: 11px;
  color: #94a3b8;
  margin-top: 1px;
}
.timeline-amount {
  font-size: 15px;
  font-weight: 700;
  color: #1e293b;
}
.timeline-date {
  font-size: 12px;
  color: #94a3b8;
}
.timeline-group-title {
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 6px;
  margin-top: 16px;
}
.timeline-group-today { color: #4f46e5; }
.timeline-group-week { color: #64748b; }
.timeline-group-month { color: #64748b; }
.timeline-future { opacity: 0.7; }

/* Mini calendar */
.mini-cal {
  background: white;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
  padding: 12px;
}
.mini-cal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8px;
}
.mini-cal-month {
  font-size: 13px;
  font-weight: 600;
  color: #1e293b;
}
.mini-cal-nav {
  display: flex;
  gap: 2px;
}
.mini-cal-btn {
  width: 22px;
  height: 22px;
  border-radius: 6px;
  border: 1px solid #e2e8f0;
  background: white;
  color: #64748b;
  cursor: pointer;
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
}
.mini-cal-grid {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 1px;
  text-align: center;
  font-size: 10px;
}
.mini-cal-day-header {
  color: #94a3b8;
  padding: 2px;
}
.mini-cal-day {
  padding: 4px;
  position: relative;
  color: #1e293b;
}
.mini-cal-day-other { color: #cbd5e1; }
.mini-cal-day-today {
  background: #eef2ff;
  border-radius: 4px;
  font-weight: 700;
  color: #4f46e5;
}
.mini-cal-dot {
  position: absolute;
  bottom: 0;
  width: 4px;
  height: 4px;
  border-radius: 50%;
}
.mini-cal-dot-end { background: #dc2626; }
.mini-cal-dot-pay { background: #2563eb; }
.mini-cal-dot-expiring { background: #d97706; }

/* Summary panel */
.cal-summary {
  background: white;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
  padding: 14px;
  margin-top: 12px;
}
.cal-sum-title {
  font-size: 12px;
  font-weight: 600;
  color: #1e293b;
  margin-bottom: 8px;
}
.cal-sum-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 4px 0;
  font-size: 12px;
}
.cal-sum-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}
.cal-sum-label {
  color: #64748b;
  flex: 1;
}
.cal-sum-count {
  font-weight: 600;
  color: #1e293b;
}

/* Hide AG Grid page size selector (REG-03) */
.ag-paging-page-size { display: none !important; }
```

**ШАГ 3: Переписать _show_calendar()**

Заменить тело `_show_calendar()`. Вместо JS FullCalendar рендерить NiceGUI компоненты:

```python
async def _show_calendar() -> None:
    """Render timeline view with event cards and mini-calendar (REG-05)."""
    import calendar as cal_module
    from datetime import date, timedelta

    db = _client_manager.get_db(state.current_client)
    today = date.today()

    # 1. Собрать все события
    # Платежи
    try:
        payment_events = await run.io_bound(get_calendar_events, db)
    except Exception:
        logger.exception("Ошибка загрузки событий (платежи)")
        payment_events = []

    # Окончания договоров
    try:
        end_rows = await run.io_bound(
            lambda: db.conn.execute(
                "SELECT id, contract_type, counterparty, subject, date_end, amount "
                "FROM contracts WHERE date_end IS NOT NULL AND status = 'done'"
            ).fetchall()
        )
    except Exception:
        logger.exception("Ошибка загрузки событий (договоры)")
        end_rows = []

    # Скоро истекающие (warning_days)
    try:
        expiring_rows = await run.io_bound(
            lambda: db.conn.execute(
                "SELECT id, contract_type, counterparty, subject, date_end, amount "
                "FROM contracts WHERE date_end IS NOT NULL AND status = 'done' "
                "AND date_end > date('now') AND date_end <= date('now', '+' || :wd || ' days')",
                {"wd": state.warning_days_threshold}
            ).fetchall()
        )
    except Exception:
        logger.exception("Ошибка загрузки скоро истекающих")
        expiring_rows = []

    # 2. Формировать unified events list
    events = []
    expiring_ids = {dict(r)["id"] for r in expiring_rows} if expiring_rows else set()

    for r in end_rows:
        c = dict(r)
        evt_type = "expiring" if c["id"] in expiring_ids else "end"
        events.append({
            "type": evt_type,
            "date": c["date_end"],
            "contract_id": c["id"],
            "contract_type": c.get("contract_type", ""),
            "counterparty": c.get("counterparty", ""),
            "subject": c.get("subject", ""),
            "amount": c.get("amount"),
        })
    for ev in payment_events:
        events.append({
            "type": "pay",
            "date": ev.get("start", ""),
            "contract_id": ev.get("extendedProps", {}).get("contract_id"),
            "contract_type": "",
            "counterparty": ev.get("title", "").split(" · ")[0] if " · " in ev.get("title", "") else ev.get("title", ""),
            "subject": ev.get("title", ""),
            "amount": ev.get("extendedProps", {}).get("amount"),
        })

    # Sort by date
    events.sort(key=lambda e: e.get("date", "9999"))

    # 3. Group events: today, this week, this month, later
    def _parse_date(d):
        try:
            return date.fromisoformat(str(d)[:10])
        except Exception:
            return None

    groups = {"today": [], "week": [], "month": [], "later": []}
    week_end = today + timedelta(days=7)
    month_end = today.replace(day=28) + timedelta(days=4)
    month_end = month_end.replace(day=1)  # first day of next month

    for ev in events:
        d = _parse_date(ev["date"])
        if d is None:
            continue
        if d == today:
            groups["today"].append(ev)
        elif d <= week_end:
            groups["week"].append(ev)
        elif d < month_end:
            groups["month"].append(ev)
        else:
            groups["later"].append(ev)

    # 4. Count stats for summary
    end_count = sum(1 for e in events if e["type"] == "end" and _parse_date(e["date"]) and _parse_date(e["date"]).month == today.month)
    pay_count = sum(1 for e in events if e["type"] == "pay" and _parse_date(e["date"]) and _parse_date(e["date"]).month == today.month)
    exp_count = sum(1 for e in events if e["type"] == "expiring")
    pay_total = sum(float(str(e.get("amount", 0) or 0).replace(" ", "").replace(",", ".")) for e in events if e["type"] == "pay" and _parse_date(e["date"]) and _parse_date(e["date"]).month == today.month)

    # 5. Render UI
    calendar_container.clear()
    with calendar_container:
        with ui.row().classes("w-full gap-4 px-5 py-4"):
            # LEFT: Timeline
            with ui.column().classes("flex-1"):
                ui.label("Ближайшие события").style("font-size:15px; font-weight:600; color:#1e293b; margin-bottom:14px;")

                def _render_event_card(ev, future=False):
                    type_map = {
                        "end": ("Окончание договора", "timeline-card-end", "timeline-type-end"),
                        "pay": ("Платёж", "timeline-card-pay", "timeline-type-pay"),
                        "expiring": (f"Скоро истекает ({state.warning_days_threshold} дн.)", "timeline-card-expiring", "timeline-type-expiring"),
                    }
                    type_label, card_cls, type_cls = type_map.get(ev["type"], ("Событие", "", ""))
                    extra_cls = " timeline-future" if future else ""

                    cid = ev.get("contract_id")
                    card = ui.element("div").classes(f"timeline-card {card_cls}{extra_cls}")
                    with card:
                        with ui.column().classes("gap-0"):
                            ui.label(type_label).classes(f"timeline-type {type_cls}")
                            ui.label(ev.get("counterparty", "")).classes("timeline-name")
                            ui.label(ev.get("subject", "")).classes("timeline-meta")
                        with ui.column().classes("items-end gap-0 shrink-0"):
                            d = _parse_date(ev["date"])
                            if d and d != today:
                                ui.label(d.strftime("%d %b")).classes("timeline-date")
                            if ev.get("amount"):
                                try:
                                    amt = f"{int(float(str(ev['amount']).replace(' ','').replace(',','.'))):,}".replace(",", " ") + " ₽"
                                except (ValueError, TypeError):
                                    amt = str(ev["amount"])
                                ui.label(amt).classes("timeline-amount")
                    if cid:
                        card.on("click", lambda c=cid: ui.navigate.to(f"/document/{c}"))

                # Today
                if groups["today"]:
                    ui.label(f"Сегодня · {today.strftime('%d %B').lstrip('0')}").classes("timeline-group-title timeline-group-today").style("margin-top:4px;")
                    for ev in groups["today"]:
                        _render_event_card(ev)

                # This week
                if groups["week"]:
                    ui.label("На этой неделе").classes("timeline-group-title timeline-group-week")
                    for ev in groups["week"]:
                        _render_event_card(ev)

                # This month
                if groups["month"]:
                    month_names = ["", "января", "февраля", "марта", "апреля", "мая", "июня", "июля", "августа", "сентября", "октября", "ноября", "декабря"]
                    ui.label(f"В {month_names[today.month]}").classes("timeline-group-title timeline-group-month")
                    for ev in groups["month"]:
                        _render_event_card(ev)

                # Later (future month)
                if groups["later"]:
                    next_month = (today.month % 12) + 1
                    ui.label(f"Позже").classes("timeline-group-title timeline-group-month")
                    for ev in groups["later"][:5]:  # Max 5 future events
                        _render_event_card(ev, future=True)

                if not any(groups.values()):
                    ui.label("Нет предстоящих событий").classes("text-sm text-slate-400 py-8 text-center")

            # RIGHT: Mini-calendar + summary (220px)
            with ui.column().classes("shrink-0").style("width: 220px;"):
                # Mini calendar — render as HTML for pixel-perfect match
                _render_mini_calendar(today, events)

                # Summary
                with ui.element("div").classes("cal-summary"):
                    ui.label("Итого в этом месяце").classes("cal-sum-title")
                    _sum_row("#dc2626", "Истекает", str(end_count))
                    _sum_row("#2563eb", "Платежей", str(pay_count))
                    _sum_row("#d97706", "Скоро истекает", str(exp_count))
                    with ui.element("div").style("border-top:1px solid #f1f5f9; margin-top:8px; padding-top:8px;"):
                        with ui.row().classes("cal-sum-row"):
                            ui.label("Сумма платежей").classes("cal-sum-label")
                            try:
                                pay_fmt = f"{int(pay_total):,}".replace(",", " ") + " ₽"
                            except Exception:
                                pay_fmt = "—"
                            ui.label(pay_fmt).classes("cal-sum-count").style("font-size:14px;")
```

**ШАГ 4: Добавить helper-функции в registry.py** (внутри `registry_page()` scope или как module-level):

```python
def _sum_row(dot_color: str, label: str, count: str) -> None:
    with ui.row().classes("cal-sum-row"):
        ui.element("div").classes("cal-sum-dot").style(f"background:{dot_color};")
        ui.label(label).classes("cal-sum-label")
        ui.label(count).classes("cal-sum-count")

def _render_mini_calendar(today, events) -> None:
    """Render mini calendar with colored dots for events."""
    import calendar as cal_module
    from datetime import date

    # Build date->event_types map
    date_events = {}
    for ev in events:
        try:
            d = date.fromisoformat(str(ev["date"])[:10])
            date_events.setdefault(d, []).append(ev["type"])
        except Exception:
            continue

    year, month = today.year, today.month
    month_names_ru = ["", "Январь", "Февраль", "Март", "Апрель", "Май", "Июнь", "Июль", "Август", "Сентябрь", "Октябрь", "Ноябрь", "Декабрь"]

    with ui.element("div").classes("mini-cal"):
        with ui.element("div").classes("mini-cal-header"):
            ui.label(f"{month_names_ru[month]} {year}").classes("mini-cal-month")
            # Nav buttons (static for now — no month switching in MVP)
            with ui.element("div").classes("mini-cal-nav"):
                ui.element("button").classes("mini-cal-btn").text("◂")
                ui.element("button").classes("mini-cal-btn").text("▸")

        with ui.element("div").classes("mini-cal-grid"):
            # Day headers
            for day_name in ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]:
                ui.element("div").classes("mini-cal-day-header").text(day_name)

            # Calendar days
            cal = cal_module.Calendar(firstweekday=0)  # Monday first
            for d in cal.itermonthdates(year, month):
                is_other = d.month != month
                is_today = d == today
                cls = "mini-cal-day"
                if is_other:
                    cls += " mini-cal-day-other"
                if is_today:
                    cls += " mini-cal-day-today"

                day_el = ui.element("div").classes(cls)
                with day_el:
                    ui.element("span").text(str(d.day))
                    # Color dots for events on this date
                    types_on_day = date_events.get(d, [])
                    dot_types = list(set(types_on_day))
                    for i, t in enumerate(dot_types[:2]):  # Max 2 dots
                        dot_cls = {"end": "mini-cal-dot-end", "pay": "mini-cal-dot-pay", "expiring": "mini-cal-dot-expiring"}.get(t, "")
                        offset = 40 + i * 20  # percentage left offset
                        ui.element("div").classes(f"mini-cal-dot {dot_cls}").style(f"left:{offset}%; transform:translateX(-50%);")
```

**ШАГ 5: Удалить FullCalendar зависимости**

1. Удалить `_ensure_fullcalendar()` и `_fc_loaded` dict.
2. Удалить JS `window.initCalendar` вызов.
3. Оставить import `get_calendar_events` — данные о платежах всё ещё нужны.
4. НЕ удалять vendor/ файлы FullCalendar — они могут понадобиться позже.

**ШАГ 6: Обновить _switch_view()**

Оставить логику переключения, но `_show_calendar()` теперь рендерит NiceGUI-компоненты, не JS. Убрать `await _ensure_fullcalendar()` из `_show_calendar()`.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "src=open('app/pages/registry.py').read(); assert 'timeline-card' in src or 'timeline_card' in src or 'Ближайшие события' in src, 'timeline not found'; assert 'mini-cal' in src or 'mini_cal' in src or 'render_mini_calendar' in src or '_render_mini_calendar' in src, 'mini calendar not found'; print('OK: timeline + mini-cal present')" && python -c "src=open('app/static/design-system.css').read(); assert 'timeline-card' in src, 'timeline CSS missing'; assert 'mini-cal' in src, 'mini-cal CSS missing'; assert 'cal-summary' in src, 'summary CSS missing'; print('OK: CSS present')" && grep -c "FullCalendar" app/pages/registry.py | head -1</automated>
  </verify>
  <acceptance_criteria>
    - grep "timeline-card" app/static/design-system.css shows timeline card styles present
    - grep "mini-cal" app/static/design-system.css shows mini calendar styles present
    - grep "Ближайшие события" app/pages/registry.py shows timeline heading present
    - grep "cal-summary" app/static/design-system.css shows summary styles present
    - grep -c "window.initCalendar" app/pages/registry.py returns 0 (FullCalendar JS removed)
    - grep "timeline-card-end\|timeline-card-pay\|timeline-card-expiring" app/static/design-system.css shows all 3 event type colors
    - grep "#dc2626\|#2563eb\|#d97706" app/static/design-system.css shows exact color values from mockup
  </acceptance_criteria>
  <done>Кнопка «Календарь» открывает таймлайн с лентой событий слева (группировка Сегодня/Неделя/Месяц, цветные полоски, суммы) и мини-календарём справа (точки на датах + сводка). FullCalendar JS убран. Клик по карточке ведёт в документ.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.pages.registry import registry_page"` — импорт работает
2. `grep -c "FullCalendar" app/pages/registry.py` — 0 или только в комментариях
3. `grep "timeline-card" app/static/design-system.css` — стили присутствуют
4. `grep "Ближайшие события" app/pages/registry.py` — заголовок таймлайна
5. `grep "mini-cal" app/static/design-system.css` — стили мини-календаря
</verification>

<success_criteria>
- Кнопка «Календарь» открывает таймлайн событий (не FullCalendar grid)
- Лента событий группирована по времени
- Карточки с цветными полосками (красная, синяя, жёлтая)
- Мини-календарь с точками справа
- Сводка за месяц под мини-календарём
- Клик по карточке → карточка документа
</success_criteria>

<output>
After completion, create `.planning/phases/24-registry/24-02-SUMMARY.md`
</output>
