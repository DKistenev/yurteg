---
phase: 16-registry-card
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - app/pages/registry.py
  - app/components/ui_helpers.py
autonomous: true
requirements: [REGI-04]

must_haves:
  truths:
    - "При пустой БД без фильтров страница реестра показывает rich empty state с CTA и тремя карточками-возможностями"
    - "CTA кнопка «Выбрать папку» визуально выделена (filled indigo)"
    - "Три карточки с иконками описывают что произойдёт: извлечём метаданные / разложим по папкам / проверим сроки"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "обновлённый _render_empty_state с rich layout"
      contains: "_render_empty_state"
  key_links:
    - from: "app/pages/registry.py (_render_empty_state)"
      to: "app/components/ui_helpers.py (empty_state)"
      via: "прямой вызов empty_state() с расширенными параметрами OR inline рендер"
      pattern: "_render_empty_state"
---

<objective>
Rich empty state реестра: мощный CTA «Выбрать папку» + три карточки возможностей с иконками и описанием.

Purpose: Первый запуск — не пустая страница, а онбординговый момент с уверенным приглашением.
Output: Обновлённый _render_empty_state в registry.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-registry-card/16-CONTEXT.md
@.planning/phases/16-registry-card/16-01-SUMMARY.md

<interfaces>
<!-- Из app/pages/registry.py (после 16-01) -->

```python
# _render_empty_state(container, state) — полностью заменить тело функции
# Текущий вызов в _init():
#   _render_empty_state(grid_container, state)
# Сигнатура НЕ меняется

# BTN_ACCENT_FILLED из app/styles.py:
# "px-5 py-2 bg-indigo-600 text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 transition-colors duration-150"

# Три пункта из CONTEXT.md (решение пользователя):
# "Виечём метаданные", "Разложим по папкам", "Проверим сроки"
```

Из app/components/ui_helpers.py — текущая сигнатура:
```python
def empty_state(
    icon_svg: str,
    title: str,
    description: str,
    button_label: str = "",
    on_click = None,
    hints: list[str] = None,
) -> None:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rich empty state — CTA + три карточки возможностей</name>
  <files>app/pages/registry.py</files>

  <read_first>
    - app/pages/registry.py — функция _render_empty_state (строки 41-68) и _init() (строки 380-428)
    - app/styles.py — BTN_ACCENT_FILLED, TEXT_HEADING_2XL, TEXT_BODY
    - CONTEXT.md: «Empty state = CTA 'Выбрать папку' + 3 пункта что произойдёт»
  </read_first>

  <action>
**В app/pages/registry.py** полностью заменить функцию `_render_empty_state` (строки 41-68):

```python
def _render_empty_state(container, state) -> None:
    """Rich empty state — CTA + три карточки возможностей (REGI-04).

    Per Phase 16 decision: «Выбрать папку» + 3 пункта:
      извлечём метаданные / разложим по папкам / проверим сроки
    Функциональный callback _on_pick_folder сохранён без изменений.
    """
    async def _on_pick_folder():
        from app.components.process import pick_folder
        source_dir = await pick_folder()
        if source_dir and hasattr(state, "_on_upload") and state._on_upload:
            await state._on_upload(source_dir)

    CAPABILITIES = [
        {
            "icon": '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#4f46e5" stroke-width="1.5"><path d="M9 12h6M9 16h6M9 8h6M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>',
            "title": "Извлечём метаданные",
            "body": "Тип, контрагент, суммы, сроки — автоматически из PDF и DOCX",
        },
        {
            "icon": '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#4f46e5" stroke-width="1.5"><path d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"/></svg>',
            "title": "Разложим по папкам",
            "body": "Структура по типам документов и контрагентам создаётся автоматически",
        },
        {
            "icon": '<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#4f46e5" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>',
            "title": "Проверим сроки",
            "body": "Уведомления об истечении договоров — никаких пропущенных дедлайнов",
        },
    ]

    with container:
        # Центральная колонка с max-w
        with ui.column().classes("w-full items-center py-16 gap-10"):

            # ── Hero-текст ────────────────────────────────────────────────────
            with ui.column().classes("items-center gap-3 text-center max-w-lg"):
                # Иконка-якорь
                ui.html(
                    '<div style="width:64px;height:64px;background:#eef2ff;border-radius:16px;'
                    'display:flex;align-items:center;justify-content:center;margin:0 auto 8px;">'
                    '<svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#4f46e5" stroke-width="1.5">'
                    '<path d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"/>'
                    '</svg></div>'
                )
                with ui.element("h2").style("font-size:1.5rem;font-weight:700;color:#0f172a;margin:0"):
                    ui.html("Загрузите первые документы")
                with ui.element("p").style("font-size:0.9rem;color:#64748b;margin:0;line-height:1.6"):
                    ui.html("Выберите папку с PDF или DOCX — ЮрТэг обработает архив за несколько минут")

            # ── CTA кнопка (filled, per BTN_ACCENT_FILLED) ───────────────────
            ui.button(
                "Выбрать папку",
                on_click=_on_pick_folder,
            ).classes(BTN_ACCENT_FILLED + " text-base px-8 py-3")

            # ── Три карточки возможностей ─────────────────────────────────────
            with ui.row().classes("gap-4 w-full max-w-2xl justify-center flex-wrap"):
                for cap in CAPABILITIES:
                    with ui.column().classes(
                        "bg-white border border-slate-200 rounded-xl p-5 gap-3 items-start"
                        " flex-1 min-w-[180px] max-w-[220px]"
                    ):
                        ui.html(cap["icon"])
                        with ui.column().classes("gap-1"):
                            with ui.element("p").style("font-size:0.875rem;font-weight:600;color:#0f172a;margin:0"):
                                ui.html(cap["title"])
                            with ui.element("p").style("font-size:0.8rem;color:#64748b;margin:0;line-height:1.5"):
                                ui.html(cap["body"])
```

Добавить `BTN_ACCENT_FILLED` к импорту из app.styles в строке 26:
```python
from app.styles import SEG_ACTIVE, SEG_INACTIVE, TOGGLE_ACTIVE, TOGGLE_INACTIVE, STATS_BAR, STATS_ITEM, BTN_ACCENT_FILLED
```

(Если уже есть — не дублировать, только добавить `BTN_ACCENT_FILLED` в существующий импорт.)
  </action>

  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python3 -c "import app.pages.registry; print('registry OK')" && grep "CAPABILITIES\|Извлечём метаданные\|Разложим по папкам\|Проверим сроки" app/pages/registry.py | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CAPABILITIES" app/pages/registry.py` — находит список карточек
    - `grep "Извлечём метаданные" app/pages/registry.py` — найдено
    - `grep "Разложим по папкам" app/pages/registry.py` — найдено
    - `grep "Проверим сроки" app/pages/registry.py` — найдено
    - `grep "BTN_ACCENT_FILLED" app/pages/registry.py` — используется в empty state
    - `python3 -c "import app.pages.registry"` — OK без ошибок
  </acceptance_criteria>

  <done>Rich empty state с CTA и тремя карточками возможностей; callback _on_pick_folder сохранён; BTN_ACCENT_FILLED кнопка выделена</done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# 1. Python синтаксис
python3 -c "import app.pages.registry; print('OK')"

# 2. Все три пункта присутствуют
grep -c "Извлечём метаданные\|Разложим по папкам\|Проверим сроки" app/pages/registry.py

# 3. CTA кнопка filled
grep "BTN_ACCENT_FILLED" app/pages/registry.py
```
</verification>

<success_criteria>
- [ ] Empty state показывает иконку-якорь + заголовок + описание (REGI-04)
- [ ] CTA «Выбрать папку» — filled indigo кнопка
- [ ] Три карточки с иконками: метаданные / папки / сроки
- [ ] Функциональный callback pick_folder сохранён без регрессий
- [ ] Python импорт без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/16-registry-card/16-02-SUMMARY.md`
</output>
