---
phase: 10-pipeline-wiring
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/pages/registry.py
  - app/main.py
autonomous: false
requirements:
  - PROC-03
  - PROC-04

must_haves:
  truths:
    - "Прогресс-бар обновляется в реальном времени при обработке файлов"
    - "Текст «3/12 файлов» + имя текущего файла видны во время обработки"
    - "Таблица реестра обновляется автоматически после завершения обработки"
    - "Toast «Обработано N документов (M ошибок)» после завершения"
    - "Ошибочные файлы отмечены красным ✗ в логе, клик раскрывает причину"
    - "Прогресс-секция скрывается после завершения (10с задержка если есть ошибки)"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "Progress section above table + on_upload wiring"
      contains: "linear_progress"
    - path: "app/main.py"
      provides: "render_header call with on_upload callback"
      contains: "on_upload"
  key_links:
    - from: "app/pages/registry.py"
      to: "app/components/process.py"
      via: "start_pipeline call with ui_refs"
      pattern: "start_pipeline"
    - from: "app/pages/registry.py"
      to: "app/components/registry_table.py"
      via: "load_table_data after pipeline completes"
      pattern: "load_table_data"
    - from: "app/main.py"
      to: "app/components/header.py"
      via: "render_header(state, on_upload=callback)"
      pattern: "on_upload"
---

<objective>
Wire the progress UI into the registry page and connect header upload button to the full pipeline flow.

Purpose: The lawyer sees real-time progress when processing documents, and the registry auto-refreshes with new rows after completion.
Output: Updated `app/pages/registry.py` with progress section, updated `app/main.py` with on_upload wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-pipeline-wiring/10-CONTEXT.md
@.planning/phases/10-pipeline-wiring/10-RESEARCH.md
@.planning/phases/10-pipeline-wiring/10-01-SUMMARY.md

@app/pages/registry.py
@app/main.py
@app/components/process.py
@app/components/header.py
@app/components/registry_table.py
@app/state.py
</context>

<interfaces>
<!-- From Plan 01 artifacts -->

From app/components/process.py (created in Plan 01):
```python
async def pick_folder() -> Path | None: ...
async def start_pipeline(source_dir: Path, state: AppState, ui_refs: dict) -> dict: ...
# ui_refs keys: section, bar, count, file_label, error_col, upload_btn
```

From app/components/header.py (modified in Plan 01):
```python
def render_header(state: AppState, on_upload=None) -> None: ...
_header_refs = {"upload_btn": None}  # button reference for ui_refs
```

From app/components/registry_table.py:
```python
async def load_table_data(grid, state: "AppState", segment: str = "all") -> None: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add progress section to registry.py and wire on_upload callback</name>
  <files>app/pages/registry.py, app/main.py</files>
  <read_first>
    - app/pages/registry.py
    - app/main.py
    - app/components/process.py (from Plan 01)
    - app/components/header.py (from Plan 01 — check _header_refs export)
    - .planning/phases/10-pipeline-wiring/10-RESEARCH.md (Pattern 3, Pattern 4)
  </read_first>
  <action>
**registry.py changes — per D-06, D-07, D-08, D-12, D-13:**

1. Add import: `from app.components.process import start_pipeline`
2. Add import: `from app.components.header import _header_refs`

3. Inside `build()`, AFTER the search+segments row and BEFORE `grid_container`, add the progress section (D-06: inline above table):

```python
# Progress section — hidden by default (D-12)
progress_section = ui.column().classes("w-full px-6 py-3 gap-2")
progress_section.set_visibility(False)

with progress_section:
    with ui.row().classes("items-center gap-3 w-full"):
        progress_bar = ui.linear_progress(value=0).classes("flex-1")
        count_label = ui.label("0/0 файлов").classes("text-sm text-gray-500 shrink-0")
    file_label = ui.label("").classes("text-xs text-gray-400")
    error_col = ui.column().classes("gap-1")
```

4. Build `ui_refs` dict after progress section:
```python
ui_refs = {
    "section": progress_section,
    "bar": progress_bar,
    "count": count_label,
    "file_label": file_label,
    "error_col": error_col,
    "upload_btn": _header_refs.get("upload_btn"),
}
```

5. Create `_on_upload` async callback inside `build()`:
```python
async def _on_upload(source_dir: Path):
    # Re-grab upload_btn ref (may not be available at module init)
    ui_refs["upload_btn"] = _header_refs.get("upload_btn")
    stats = await start_pipeline(source_dir, state, ui_refs)
    # After pipeline: refresh table (D-11)
    if grid_ref["grid"]:
        await load_table_data(grid_ref["grid"], state, active_segment["value"])
```

6. Store `_on_upload` in state or a module-level ref so header can call it. Simplest approach: store on `state` object as a temporary attribute:
```python
state._on_upload = _on_upload  # dynamic attr on dataclass instance
```

**app/main.py changes:**

1. Find where `render_header(state)` is called.
2. Change to: `render_header(state, on_upload=lambda path: state._on_upload(path) if hasattr(state, '_on_upload') else None)`

Alternative (cleaner): In registry.py build(), register the callback on state. In main.py, pass `on_upload` that delegates:
```python
async def _handle_upload(path):
    s = get_state()
    if hasattr(s, '_on_upload') and s._on_upload:
        await s._on_upload(path)

render_header(state, on_upload=_handle_upload)
```

Use whichever pattern is simpler given the actual main.py structure. The key constraint: header button click -> pick_folder -> start_pipeline with registry's ui_refs -> load_table_data after completion.
  </action>
  <verify>
    <automated>cd "/Users/danilakistenev/Downloads/Личное/ЮР тэг/yurteg" && grep -n "linear_progress" app/pages/registry.py && grep -n "start_pipeline" app/pages/registry.py && grep -n "on_upload" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "linear_progress" app/pages/registry.py returns match (progress bar)
    - grep "start_pipeline" app/pages/registry.py returns match (pipeline call)
    - grep "progress_section" app/pages/registry.py returns match (container)
    - grep "error_col" app/pages/registry.py returns match (error log)
    - grep "on_upload" app/main.py returns match (wiring in main)
    - grep "set_visibility" app/pages/registry.py returns match (show/hide progress)
  </acceptance_criteria>
  <done>Progress section renders above table (hidden by default), shows bar+count+filename during processing, error log with expandable items, table refreshes after completion, all wired through header upload button</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — full pipeline flow</name>
  <files>app/pages/registry.py, app/main.py, app/components/process.py, app/components/header.py</files>
  <action>
Human visually verifies the full pipeline wiring end-to-end. All code changes are complete from Task 1 and Plan 01. This checkpoint confirms the flow works in the running application.
  </action>
  <what-built>Complete pipeline wiring: native folder picker, async processing, real-time progress, auto-refresh, error display</what-built>
  <how-to-verify>
    1. Start the app: `cd "/Users/danilakistenev/Downloads/Личное/ЮР тэг/yurteg" && python app/main.py`
    2. Verify button visible in header on all pages (Documents, Templates, Settings) — per D-01, D-02
    3. Click the button — verify native macOS folder dialog opens (not browser upload) — per D-03
    4. Select a folder with 3-5 PDF/DOCX files
    5. Verify: progress bar appears above the registry table — per D-06, D-07
    6. Verify: UI is NOT frozen during processing — per D-16
    7. After completion: verify toast notification appears with count — per D-10
    8. Verify: new documents appear in the registry table WITHOUT manual refresh — per D-11
    9. Verify: progress section hides after completion — per D-12
    10. Click upload again — verify button works for second batch
    11. (Optional) Include an invalid file — verify red error entry in progress log — per D-13
  </how-to-verify>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>Full pipeline flow works: upload button -> native picker -> async processing -> real-time progress -> toast -> table auto-refresh -> error log</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full flow: click upload -> pick folder -> progress bar -> toast -> table refreshed
- `grep "linear_progress" app/pages/registry.py` shows progress bar component
- `grep "on_upload" app/main.py` shows wiring
- `grep "start_pipeline" app/pages/registry.py` shows pipeline call
- No `ui.upload` used anywhere (Pitfall 5)
- All UI updates from callbacks use `call_soon_threadsafe` (verified in process.py from Plan 01)
</verification>

<success_criteria>
- Progress section visible during processing, hidden after
- Progress bar + file count + filename update in real-time
- Registry table auto-refreshes with new rows after processing
- Toast notification shows result summary
- Error log shows failed files with expandable details
- Entire flow works end-to-end from header button click
</success_criteria>

<output>
After completion, create `.planning/phases/10-pipeline-wiring/10-02-SUMMARY.md`
</output>
