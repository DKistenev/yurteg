---
phase: 15-splash
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/onboarding/splash.py
autonomous: false
requirements: [SPLS-01, SPLS-02, SPLS-03]

must_haves:
  truths:
    - "Splash занимает весь экран с тёмным slate-900 фоном — белого bg-white нет"
    - "Крупный заголовок IBM Plex Sans 700 text-4xl+ (TEXT_HERO) читается на тёмном фоне"
    - "Hero-zone реализован как ui.element('div').classes('hero-zone') — structural wrapper, не padding inflation"
    - "Прогресс-бар модели (940 МБ GGUF) работает и обновляется через call_soon_threadsafe"
    - "Wizard step 1 → step 2 переход работает без регрессий"
    - "Шаг 2 (Telegram): сохранение токена и «Пропустить» функционируют"
    - "(Stretch) Элементы hero появляются с stagger через .hero-enter CSS класс"
  artifacts:
    - path: "app/components/onboarding/splash.py"
      provides: "Full-screen hero splash с 2-step wizard"
      contains: "hero-zone"
      min_lines: 100
  key_links:
    - from: "splash.py render_splash()"
      to: "app/main.py root()"
      via: "early return gate"
      pattern: "render_splash"
    - from: "ui.element div hero-zone"
      to: "design-system.css .hero-zone"
      via: "CSS class hook"
      pattern: "hero-zone"
    - from: "BTN_ACCENT_FILLED"
      to: "app/styles.py"
      via: "import"
      pattern: "BTN_ACCENT_FILLED"
---

<objective>
Полная переработка splash screen в hero full-screen экран с тёмным slate-900 фоном и крупной типографикой. Валидирует hero-zone structural wrapper паттерн перед применением в сложном registry (Phase 16).

Purpose: Первое впечатление = продукт, не wireframe. Splash должен ощущаться как "добро пожаловать в продукт", не "заполни форму".
Output: app/components/onboarding/splash.py — полностью переписанный, с тёмным hero, wizard 2 шага, прогресс-баром модели и stretch SPLS-03 stagger-анимацией.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Ключевые контракты — читай перед реализацией. Никакого exploration не нужно. -->

Из app/styles.py:
```python
# v0.7 Hero constants (используй их, не старые)
TEXT_HERO = "font-bold text-white tracking-tight leading-tight"
TEXT_HERO_SUB = "text-lg font-light text-slate-300"
TEXT_EYEBROW = "text-xs font-semibold text-slate-400 uppercase tracking-widest"
BTN_ACCENT_FILLED = "px-5 py-2 bg-indigo-600 text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 transition-colors duration-150"

# Старые — НЕ импортировать для v0.7 splash
# BTN_PRIMARY = "px-6 py-2 bg-indigo-600 text-white..."  ← заменяется BTN_ACCENT_FILLED
# TEXT_HEADING_XL = "text-xl font-semibold text-slate-900"  ← заменяется TEXT_HERO
# BTN_FLAT = "text-sm text-slate-400..."  ← заменяется ghost стилем
```

Из app/static/tokens.css:
```css
--yt-color-hero-bg: var(--yt-p-slate-900);  /* #0f172a — тёмный фон hero */
--yt-text-hero: clamp(2.5rem, 5vw, 3.5rem);  /* fluid hero size */
```

Из app/static/design-system.css:
```css
/* .hero-enter — уже определён, использовать для SPLS-03 stretch */
.hero-enter {
  animation: hero-slide-up 300ms cubic-bezier(0.25, 1, 0.5, 1) both;
}
.hero-enter:nth-child(1) { animation-delay: 0ms; }
.hero-enter:nth-child(2) { animation-delay: 100ms; }
.hero-enter:nth-child(3) { animation-delay: 200ms; }
.hero-enter:nth-child(4) { animation-delay: 300ms; }
/* hero-slide-up: from opacity:0 translateY(24px) → to opacity:1 translateY(0) */
```

Текущий splash.py (контракты, которые нельзя трогать):
```python
# Сигнатура — не менять
def render_splash() -> None: ...

# Callback chain — сохранить полностью
def _finish() -> None:
    save_setting("first_run_completed", True)
    ui.navigate.to("/")

def _save_and_finish(token_input: ui.input) -> None:
    if token_input.value and token_input.value.strip():
        save_setting("bot_token", token_input.value.strip())
    _finish()

def _show_step_2() -> None: ...  # меняет содержимое wizard_area

# Async model download — сохранить логику полностью
async def _run_model_download() -> None:
    # run.io_bound + loop.call_soon_threadsafe pattern
    # Pitfall 1 guard: progress_bar.set_value(1.0) после ensure_model

ui.timer(0, _run_model_download, once=True)
```

Импорты из config и services — сохранить:
```python
from config import Config, save_setting
from modules.postprocessor import get_grammar_path
from services.llama_server import LlamaServerManager
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero wrapper + Step 1 visual rework</name>
  <files>app/components/onboarding/splash.py</files>

  <read_first>
    - app/components/onboarding/splash.py (текущая реализация — понять что менять)
    - app/styles.py (TEXT_HERO, TEXT_HERO_SUB, TEXT_EYEBROW, BTN_ACCENT_FILLED)
    - app/static/tokens.css (--yt-color-hero-bg, --yt-text-hero)
    - app/static/design-system.css (.hero-enter, hero-slide-up keyframes)
  </read_first>

  <action>
Полностью переписать app/components/onboarding/splash.py. Функциональная логика (_finish, _save_and_finish, _show_step_2, _run_model_download, ui.timer) — сохранить дословно. Менять только визуальный слой.

СТРУКТУРА НОВОГО SPLASH:

render_splash() создаёт:
  1. full-screen wrapper через ui.element('div') с классом 'hero-zone' и стилем:
     min-height: 100vh; background: var(--yt-color-hero-bg); display: flex; align-items: center; justify-content: center;

  2. Внутри: content column через ui.column().classes('max-w-xl w-full mx-auto px-8 gap-8')

  3. Элементы column (в порядке, важен для nth-child stagger):
     a) eyebrow: ui.element('p').classes('hero-enter ' + TEXT_EYEBROW) с текстом 'ЮрТэг'
     b) headline: ui.element('h1') со стилем font-size: var(--yt-text-hero) и классами 'hero-enter ' + TEXT_HERO
        текст: 'Ваш архив договоров — под контролем'
     c) subtext: ui.element('p').classes('hero-enter ' + TEXT_HERO_SUB)
        текст: 'Загрузите папку → готовый реестр за 20 минут'
     d) bullets block: ui.element('div').classes('hero-enter flex flex-col gap-3')
        3 строки через ui.row().classes('gap-3 items-start'):
          - иконка '✓' в span.classes('text-indigo-400 text-sm shrink-0 mt-0.5')
          - текст в span.classes('text-slate-300 text-sm font-normal')
        Тексты:
          'Автоматически извлекаем даты, стороны и тип документа'
          'Раскладываем файлы по папкам по типу'
          'Напоминаем об истекающих договорах'
     e) progress section: ui.column().classes('gap-2 w-full hero-enter')
          progress_label = ui.label("Загрузка ИИ-помощника (0/940 МБ)").classes('text-slate-400 text-sm font-normal')
          progress_bar = ui.linear_progress(value=0).props("color=indigo track-color=grey-8").classes("w-full h-1.5 rounded-full")
     f) wizard_area = ui.column().classes('w-full gap-4')
        (содержимое wizard_area — отдельная логика ниже)

ШАГ 1 WIZARD (наполнение wizard_area):
  with wizard_area:
    with ui.row().classes('w-full justify-between items-center'):
      «Пропустить» → ui.button("Пропустить").props("flat no-caps").classes("text-slate-400 text-sm hover:text-slate-200").on_click(lambda: _finish())
      «Далее» → ui.button("Далее: Уведомления →").props("no-caps").classes(BTN_ACCENT_FILLED).on_click(_show_step_2)

ШАГ 2 WIZARD (_show_step_2) — адаптировать под тёмный фон:
  wizard_area.clear()
  with wizard_area:
    ui.element('h2').classes('text-xl font-semibold text-white') с текстом 'Уведомления'
    ui.element('p').classes('text-slate-300 text-sm font-normal') с текстом:
      'Получайте уведомления об истекающих документах прямо в мессенджер.'
    token_input = ui.input(placeholder="110201543:AAHdqTcvCH1vGWJxfSeofSs0K").props("outlined dense dark").classes("w-full")
    with ui.row().classes('w-full justify-between items-center'):
      «Пропустить» → ui.button("Пропустить").props("flat no-caps").classes("text-slate-400 text-sm hover:text-slate-200").on_click(lambda: _finish())
      save_btn = ui.button("Сохранить и начать").props("no-caps").classes(BTN_ACCENT_FILLED)
      (логика _on_save_click — сохранить дословно из текущего файла)

ИМПОРТЫ — обновить строку импорта из app.styles:
  from app.styles import TEXT_HERO, TEXT_HERO_SUB, TEXT_EYEBROW, BTN_ACCENT_FILLED
  Удалить из импорта: TEXT_HEADING_XL, BTN_PRIMARY, BTN_FLAT

НЕ ТРОГАТЬ:
  - _MODEL_SIZE_MB = 940
  - Всю логику _run_model_download (loop, call_soon_threadsafe, Pitfall 1 guard, try/except, ensure_model, ensure_server_binary, start)
  - ui.timer(0, _run_model_download, once=True) — строго в конце render_splash
  - from config import Config, save_setting
  - from modules.postprocessor import get_grammar_path
  - from services.llama_server import LlamaServerManager
  - from nicegui import run, ui
  - import asyncio, import logging, logger = logging.getLogger(__name__)
  </action>

  <verify>
    <automated>cd "/Users/danilakistenev/Downloads/Личное/ЮР тэг/yurteg" && python3 -c "from app.components.onboarding.splash import render_splash; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - grep "bg-white" app/components/onboarding/splash.py returns 0 lines (белого фона нет)
    - grep "hero-zone" app/components/onboarding/splash.py returns 1+ line (structural wrapper)
    - grep "yt-color-hero-bg" app/components/onboarding/splash.py returns 1+ line (тёмный фон через токен)
    - grep "TEXT_HERO" app/components/onboarding/splash.py returns 1+ line (новая константа)
    - grep "BTN_ACCENT_FILLED" app/components/onboarding/splash.py returns 1+ line (новая кнопка)
    - grep "TEXT_HEADING_XL\|BTN_PRIMARY\b\|BTN_FLAT\b" app/components/onboarding/splash.py returns 0 lines (старые удалены)
    - grep "call_soon_threadsafe" app/components/onboarding/splash.py returns 2+ lines (async progress жив)
    - grep "hero-enter" app/components/onboarding/splash.py returns 3+ lines (stagger элементы)
    - python3 import succeeds without errors
  </acceptance_criteria>

  <done>splash.py переписан: тёмный hero-zone wrapper, TEXT_HERO заголовок, BTN_ACCENT_FILLED кнопки, stagger .hero-enter на ≥3 элементах, вся функциональная логика модели и wizard сохранена.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Полный рерайт splash screen: тёмный slate-900 full-screen hero, IBM Plex Sans 700 заголовок, BTN_ACCENT_FILLED кнопки, stagger-анимация .hero-enter, прогресс-бар в тёмном стиле, wizard шаги 1→2.</what-built>
  <how-to-verify>
    1. Временно выставь first_run_completed=false в ~/.yurteg/settings.json (или удали файл) чтобы splash показался при запуске
    2. Запусти приложение (python3 main.py или через entrypoint)
    3. Проверь:
       - Весь экран тёмный slate-900, нет белого фона
       - Заголовок крупный, белый, bold — ощущается как продукт
       - Кнопка «Далее: Уведомления →» заполненная indigo, не flat
       - «Пропустить» — ghost/text стиль, не полноценная кнопка
       - Прогресс-бар стартует и заполняется (или уже на 100% если модель скачана)
       - Клик «Далее» → переход на шаг 2 (Telegram форма на том же тёмном фоне)
       - Шаг 2: ввод токена, «Пропустить» и «Сохранить и начать» работают
       - После «Пропустить» / «Сохранить» → переход в реестр (/)
  </how-to-verify>
  <resume-signal>Напиши "approved" или опиши проблемы для фикса</resume-signal>
</task>

</tasks>

<verification>
Итоговые проверки после выполнения всех задач:

```bash
# 1. Импорт без ошибок
python3 -c "from app.components.onboarding.splash import render_splash; print('OK')"

# 2. hero-zone wrapper присутствует
grep "hero-zone" app/components/onboarding/splash.py

# 3. Старые v0.6 константы удалены
grep "TEXT_HEADING_XL\|BTN_PRIMARY\b\|BTN_FLAT\b" app/components/onboarding/splash.py | wc -l
# ожидаем 0

# 4. Белый фон убран
grep "bg-white" app/components/onboarding/splash.py | wc -l
# ожидаем 0

# 5. Async model download не сломан
grep "call_soon_threadsafe\|io_bound\|ensure_model" app/components/onboarding/splash.py | wc -l
# ожидаем ≥4

# 6. Stagger анимация
grep "hero-enter" app/components/onboarding/splash.py | wc -l
# ожидаем ≥3
```
</verification>

<success_criteria>
- SPLS-01: Splash full-screen, тёмный фон, крупный TEXT_HERO заголовок IBM Plex Sans 700
- SPLS-02: hero-zone div с background: var(--yt-color-hero-bg) — тёмная accent surface
- SPLS-03 (stretch): ≥3 элемента с .hero-enter классом → stagger через design-system.css
- Регрессий нет: model download, wizard routing, Telegram save — всё работает
- hero-zone паттерн провалидирован — готов к применению в Phase 16 registry
</success_criteria>

<output>
После выполнения создать файл .planning/phases/15-splash/15-01-SUMMARY.md

Структура файла (phase, plan, subsystem, tags, dependency_graph, key_files, decisions, metrics в frontmatter):
  phase: 15-splash
  plan: 01
  subsystem: onboarding/splash
  tags: [splash, hero-zone, dark-surface, wizard, stagger]
  dependency_graph.requires: [14-01, 14-02]
  dependency_graph.provides: [hero-zone-pattern-validated, dark-splash]
  dependency_graph.affects: Phase 16 registry hero-zone pattern
  key_files.modified: app/components/onboarding/splash.py
  decisions: задокументировать решения принятые в процессе
  metrics: duration, completed timestamp, tasks_completed: 1, files_changed: 1

Тело файла должно включать:
  - One-liner что сделано
  - What Was Built
  - Deviations from Plan (или None)
  - Known Stubs (или None)
  - Self-Check: PASSED/FAILED с результатами grep-проверок
</output>
