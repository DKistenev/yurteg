---
milestone: v1.0
audited: 2026-03-29T00:15:00Z
status: tech_debt
scores:
  requirements: 59/61
  phases: 6/6
  integration: 17/18
  tests: 264/265
gaps:
  requirements:
    - id: "DINT-01"
      status: "partial"
      phase: "Phase 40"
      evidence: "contract_number column exists but _json_to_metadata never populates it from LLM JSON; LLM prompt doesn't request the field"
    - id: "ERRH-10"
      status: "deferred"
      phase: "Phase 42"
      evidence: "Row unpacking bounds check — low risk, schema is fixed"
    - id: "ERRH-17"
      status: "deferred"
      phase: "Phase 42"
      evidence: "db._lock documentation — cosmetic"
  integration:
    - from: "ai_extractor._json_to_metadata"
      to: "version_service.find_version_match"
      issue: "contract_number never populated from LLM → Path 1 version matching never fires"
tech_debt:
  - phase: "Phase 39"
    items:
      - "LLMProvider.close() defined but never called — HTTP connections not explicitly closed"
      - "ZAI/OpenRouter fallback_provider construction raises ValueError if API key missing (crashes app startup)"
  - phase: "Phase 40"
    items:
      - "contract_number added to DB/models but LLM prompt doesn't extract it"
  - phase: "Phase 42"
    items:
      - "ERRH-10: Row unpacking without bounds check (low risk)"
      - "ERRH-17: db._lock remains private member (documentation only)"
---

# Milestone v1.0 Hackathon-Ready — Backend Audit Report

## Summary

| Metric | Score | Notes |
|--------|-------|-------|
| **Requirements** | 59/61 | 2 deferred (low-impact), 1 partial |
| **Phases** | 6/6 | All complete |
| **Integration** | 17/18 | 1 gap: contract_number not populated |
| **Tests** | 264 passed | 230 existing + 34 new; 1 fixed (provider test) |

## Backend Phases (38-43) — All Complete

| Phase | Commits | Requirements |
|-------|---------|-------------|
| 38: Cross-Scope + Config | ee52a05, 079f43b | XSCOPE-04/05/06, CONF-01..06 (9/9) |
| 39: Provider Cleanup | fc4bd4f | PROV-01..09 (9/9) |
| 40: Data Integrity | 544a0db | DINT-01..07 (6/7 — DINT-01 partial) |
| 41: Thread Safety | d7b0c38 | TSAFE-01..08 (8/8) |
| 42: Error Handling | e418f81 | ERRH-01..18 (16/18 — 2 deferred) |
| 43: Test Coverage | 845bd6d | TEST: 34 new tests, all passing |

## Integration Check Results

**18 exports verified**, 17 correctly wired, 1 gap:

### contract_number data flow gap
- `ContractMetadata.contract_number` field added (models.py)
- `migration v10` creates DB column (database.py)
- `save_result` writes it (database.py)
- `controller.py` deanonymizes it
- **BUT**: `ai_extractor._json_to_metadata` never maps it from LLM JSON
- **AND**: LLM prompt template doesn't request it
- **Impact**: Version matching Path 1 (by contract number) never fires
- **Risk for demo**: Low — Path 2 (by embedding similarity) works fine
- **Fix**: Add `contract_number` to LLM prompt + `_json_to_metadata`

### No deadlock risk
RLock + direct `db.conn` access pattern verified safe across all services.

## Tech Debt

| Item | Risk | Fix Effort |
|------|------|-----------|
| `close()` never called on providers | Low (desktop app) | 5 min |
| Fallback provider crashes on missing API key | Medium (demo risk) | 10 min |
| contract_number not in LLM prompt | Low (embed matching works) | 15 min |
| ERRH-10: row unpacking | Low (schema fixed) | 5 min |
| ERRH-17: db._lock docs | Cosmetic | 2 min |

## Test Results

```
264 passed, 0 failed (after provider test fix)
34 new tests in test_backend_audit.py
Thread safety: 5-thread concurrent writes verified
Payment edge cases: negative, zero, start>end, invalid frequency
Config hardening: graceful validation, active_model, thread-safe settings
```
