---
phase: 18-layout-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
  - app/styles.py
  - app/static/design-system.css
  - app/pages/registry.py
autonomous: true
requirements: [LAY-05, BRND-02, BRND-03, PLSH-03, PLSH-04]

must_haves:
  truths:
    - "Footer «ЮрТэг v0.7.1» центрирован по горизонтали"
    - "Inactive segment buttons видны как кнопки: белый фон, бордер (не flat text)"
    - "Calendar toggle имеет текстовые labels «Список» / «Календарь»"
    - "hero-enter:nth-child(5) имеет animation-delay: 400ms"
    - "Dead CSS rules (settings-nav-item, stats-item-clickable) удалены из design-system.css"
  artifacts:
    - path: "app/main.py"
      provides: "footer justify-center, version v0.7.1"
    - path: "app/styles.py"
      provides: "SEG_INACTIVE с border/bg"
    - path: "app/static/design-system.css"
      provides: "hero-enter:nth-child(5) + cleaned dead CSS"
    - path: "app/pages/registry.py"
      provides: "calendar toggle с text labels"
  key_links:
    - from: "app/styles.py SEG_INACTIVE"
      to: "registry.py segment buttons"
      via: ".classes(SEG_INACTIVE)"
    - from: "design-system.css .hero-enter"
      to: "splash/onboarding components"
      via: ".hero-enter class"
---

<objective>
Footer, segment buttons, calendar toggle, CSS cleanup — четыре независимых точечных правки.

Purpose: Footer прижат вправо (justify-end), inactive segments выглядят как plain text, calendar toggle без подписей непонятен, design-system.css содержит dead rules которые загрязняют стили.

Output: main.py с centered footer и обновлённой версией, styles.py с border-styled SEG_INACTIVE, design-system.css с nth-child(5) и без dead CSS, registry.py с text labels на toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-layout-fixes/18-CONTEXT.md
</context>

<interfaces>
<!-- Текущие константы в app/styles.py: -->
```python
SEG_ACTIVE = "px-4 py-1.5 text-sm font-semibold rounded-md bg-indigo-600 text-white transition-colors duration-150"
SEG_INACTIVE = "px-4 py-1.5 text-sm font-semibold rounded-md text-slate-600 hover:bg-slate-100 transition-colors duration-150"
```

<!-- Текущий footer (main.py строки 170-173): -->
```python
with ui.element('footer').classes(
    "w-full py-3 px-8 flex justify-end items-center border-t border-slate-200 bg-white"
):
    ui.label("ЮрТэг v0.7").classes("text-xs text-slate-400")
```

<!-- Текущий calendar toggle (registry.py строки 147-151): -->
```python
with ui.row().classes("items-center gap-1.5"):
    list_btn = ui.button("≡").props("flat no-caps").classes(TOGGLE_ACTIVE)
    list_btn.props('title="Список" aria-label="Вид списком"')
    cal_btn = ui.button("⊞").props("flat no-caps").classes(TOGGLE_INACTIVE)
    cal_btn.props('title="Календарь" aria-label="Вид календарём"')
```

<!-- Dead CSS блок в design-system.css (строки ~268-283): -->
```css
/* Nav sidebar items в settings — hover если нет active */
.settings-nav-item:not(.settings-nav-active):hover {
    background: var(--yt-p-slate-100);
}
/* Stats bar items — subtle highlight */
.stats-item-clickable:hover {
    background: var(--yt-color-accent-subtle);
    border-radius: var(--yt-radius-md);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Footer центр + версия, SEG_INACTIVE с border, calendar labels (LAY-05, BRND-02, BRND-03)</name>
  <files>app/main.py, app/styles.py, app/pages/registry.py</files>
  <read_first>
  - app/main.py строки 169-174 (footer)
  - app/styles.py строки 32-34 (SEG_INACTIVE)
  - app/pages/registry.py строки 145-152 (calendar toggle)
  </read_first>
  <action>
**1. Footer (LAY-05) — app/main.py:**
Строка ~171: изменить `justify-end` → `justify-center`.
Строка ~173: изменить `"ЮрТэг v0.7"` → `"ЮрТэг v0.7.1"`.

До:
```python
with ui.element('footer').classes(
    "w-full py-3 px-8 flex justify-end items-center border-t border-slate-200 bg-white"
):
    ui.label("ЮрТэг v0.7").classes("text-xs text-slate-400")
```

После:
```python
with ui.element('footer').classes(
    "w-full py-3 px-8 flex justify-center items-center border-t border-slate-200 bg-white"
):
    ui.label("ЮрТэг v0.7.1").classes("text-xs text-slate-400")
```

**2. SEG_INACTIVE с border (BRND-02) — app/styles.py:**
Строка 34: добавить `bg-white border border-slate-200` к SEG_INACTIVE.

До:
```python
SEG_INACTIVE = "px-4 py-1.5 text-sm font-semibold rounded-md text-slate-600 hover:bg-slate-100 transition-colors duration-150"
```

После:
```python
SEG_INACTIVE = "px-4 py-1.5 text-sm font-semibold rounded-md text-slate-600 bg-white border border-slate-200 hover:bg-slate-50 transition-colors duration-150"
```

Примечание: `hover:bg-slate-100` → `hover:bg-slate-50` чтобы hover был мягче на белом фоне.

**3. Calendar toggle с text labels (BRND-03) — app/pages/registry.py:**
Строки ~147-152: заменить иконки-символы на текстовые кнопки с иконкой + label.

До:
```python
with ui.row().classes("items-center gap-1.5"):
    list_btn = ui.button("≡").props("flat no-caps").classes(TOGGLE_ACTIVE)
    list_btn.props('title="Список" aria-label="Вид списком"')
    cal_btn = ui.button("⊞").props("flat no-caps").classes(TOGGLE_INACTIVE)
    cal_btn.props('title="Календарь" aria-label="Вид календарём"')
```

После:
```python
with ui.row().classes("items-center gap-1 bg-slate-100 p-1 rounded-lg"):
    list_btn = ui.button("≡ Список").props("flat no-caps").classes(TOGGLE_ACTIVE + " text-xs px-3 py-1")
    list_btn.props('aria-label="Вид списком"')
    cal_btn = ui.button("⊞ Календарь").props("flat no-caps").classes(TOGGLE_INACTIVE + " text-xs px-3 py-1")
    cal_btn.props('aria-label="Вид календарём"')
```

Обернуть в `bg-slate-100 p-1 rounded-lg` — pill-контейнер, аналогично segment bar. Toggle кнопки получают текст + иконку, сразу понятно что делают.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import ast
for f in ['app/main.py', 'app/styles.py', 'app/pages/registry.py']:
    ast.parse(open(f).read())
    print(f, 'syntax ok')
"</automated>
  </verify>
  <acceptance_criteria>
- grep "justify-center" app/main.py — присутствует в footer строке
- grep "v0.7.1" app/main.py — присутствует
- grep "justify-end" app/main.py — НЕ присутствует в footer (может быть в комментарии — ок)
- grep "border border-slate-200" app/styles.py — присутствует в SEG_INACTIVE строке
- grep "bg-white" app/styles.py — присутствует в SEG_INACTIVE строке
- grep "Список" app/pages/registry.py — присутствует в button text (≡ Список)
- grep "Календарь" app/pages/registry.py — присутствует в button text (⊞ Календарь)
- все python syntax checks проходят
  </acceptance_criteria>
  <done>Footer по центру с версией v0.7.1. Inactive segments имеют белый фон с бордером. Calendar toggle читаем с текстовыми подписями.</done>
</task>

<task type="auto">
  <name>Task 2: hero-enter:nth-child(5) + удалить dead CSS (PLSH-03, PLSH-04)</name>
  <files>app/static/design-system.css</files>
  <read_first>app/static/design-system.css строки 181-284 (hero-enter блок и нижняя часть файла с dead CSS)</read_first>
  <action>
**1. Добавить hero-enter:nth-child(5) (PLSH-03):**
Найти блок `.hero-enter` (строки ~186-194):
```css
@layer components {
  .hero-enter {
    animation: hero-slide-up var(--yt-duration-slow) var(--yt-ease-out) both;
  }
  .hero-enter:nth-child(1) { animation-delay: 0ms; }
  .hero-enter:nth-child(2) { animation-delay: 100ms; }
  .hero-enter:nth-child(3) { animation-delay: 200ms; }
  .hero-enter:nth-child(4) { animation-delay: 300ms; }
}
```

Добавить строку для nth-child(5) перед закрывающей `}`:
```css
  .hero-enter:nth-child(5) { animation-delay: 400ms; }
```

Результат:
```css
@layer components {
  .hero-enter {
    animation: hero-slide-up var(--yt-duration-slow) var(--yt-ease-out) both;
  }
  .hero-enter:nth-child(1) { animation-delay: 0ms; }
  .hero-enter:nth-child(2) { animation-delay: 100ms; }
  .hero-enter:nth-child(3) { animation-delay: 200ms; }
  .hero-enter:nth-child(4) { animation-delay: 300ms; }
  .hero-enter:nth-child(5) { animation-delay: 400ms; }
}
```

**2. Удалить dead CSS (PLSH-04):**
Найти и удалить весь блок `/* ── Hover states audit (XCUT-02) ──` (строки ~265-284), который содержит:
- `.settings-nav-item:not(.settings-nav-active):hover` — settings использует inline Tailwind классы через _switch(), класс settings-nav-item нигде не присвоен
- `.stats-item-clickable:hover` — класс stats-item-clickable нигде не используется в Python коде

Удалить блок целиком (от комментария `/* ── Hover states audit` до закрывающей `}` блока @layer components). Сохранить `.breadcrumb-link:hover` — он используется (BREADCRUMB_LINK в styles.py).

Если `.breadcrumb-link:hover` находится в том же `@layer components` блоке что и dead rules, вынести его в отдельный `@layer components { }` блок:
```css
@layer components {
  .breadcrumb-link:hover {
    text-decoration: underline;
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
css = open('app/static/design-system.css').read()
assert 'nth-child(5)' in css, 'Missing nth-child(5)'
assert 'settings-nav-item' not in css, 'Dead CSS settings-nav-item still present'
assert 'stats-item-clickable' not in css, 'Dead CSS stats-item-clickable still present'
assert 'breadcrumb-link' in css, 'breadcrumb-link should be kept'
print('CSS checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
- grep "nth-child(5)" app/static/design-system.css — присутствует с animation-delay: 400ms
- grep "settings-nav-item" app/static/design-system.css — 0 вхождений
- grep "stats-item-clickable" app/static/design-system.css — 0 вхождений
- grep "breadcrumb-link" app/static/design-system.css — присутствует (не удалён)
- python automated check проходит
  </acceptance_criteria>
  <done>hero-enter поддерживает 5 дочерних элементов. Dead CSS правила для несуществующих классов удалены. Файл чище.</done>
</task>

</tasks>

<verification>
После выполнения обоих задач:
1. Все python syntax checks проходят
2. CSS automated check проходит (все 4 assert-а)
3. grep-проверки из acceptance_criteria — все выполнены
</verification>

<success_criteria>
Footer центрирован, показывает v0.7.1. Inactive segment buttons визуально отличимы (белый фон + бордер). Calendar toggle читаем с текстом «Список» / «Календарь». design-system.css имеет nth-child(5) и не содержит dead rules.
</success_criteria>

<output>
После завершения создать `.planning/phases/18-layout-fixes/18-03-SUMMARY.md`
</output>
