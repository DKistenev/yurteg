---
phase: 21-ui-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
  - app/pages/document.py
autonomous: true
requirements: [UIFIX-02, UIFIX-03]

must_haves:
  truths:
    - "Кнопка «Скачать PDF» скачивает оригинальный файл документа"
    - "Кнопка «Переобработать» запускает pipeline для одного документа и показывает результат"
  artifacts:
    - path: "app/main.py"
      provides: "GET /download/{doc_id} route"
      contains: "/download/{doc_id}"
    - path: "app/pages/document.py"
      provides: "Reprocess button handler calling pipeline"
      contains: "reprocess"
  key_links:
    - from: "app/pages/document.py:228-233"
      to: "app/main.py:/download/{doc_id}"
      via: "ui.download URL"
      pattern: "/download/"
    - from: "app/pages/document.py:231-233"
      to: "controller.py:process_archive"
      via: "pipeline invocation"
      pattern: "process_archive|start_pipeline"
---

<objective>
Implement the download PDF route and reprocess button functionality — the two action buttons on the document card page that currently do nothing.

Purpose: Users need to download original documents and re-run AI extraction on individual files.
Output: Working /download/{doc_id} FastAPI route and reprocess handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/AUDIT-2026-03-25.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/main.py (existing redline route as pattern, lines 184-204):
```python
from fastapi.responses import Response as FastAPIResponse

@app.get('/download/redline/{contract_id}/{other_id}')
async def download_redline(contract_id: int, other_id: int):
    from services.client_manager import ClientManager as _CM
    cm = _CM()
    db = cm.get_db("Основной реестр")
    c1 = await run.io_bound(db.get_contract_by_id, contract_id)
    # ... returns FastAPIResponse with file bytes
```

From modules/database.py:
```python
def get_contract_by_id(self, contract_id: int) -> dict | None:
    # Returns dict with 'original_path', 'filename', etc.
```

From controller.py:
```python
class Controller:
    def process_archive(self, source_dir: Path, grouping="both",
                        force_reprocess=False, on_progress=None,
                        on_file_done=None, output_dir_override=None) -> dict:
```

From app/components/process.py:
```python
async def start_pipeline(source_dir: Path, state: AppState, ui_refs: dict) -> dict:
    # Full pipeline with progress UI
```

From app/pages/document.py (current broken buttons, lines 228-237):
```python
ui.button("Скачать PDF", on_click=lambda: ui.download(f"/download/{doc_id}"))
ui.button("Переобработать", on_click=lambda: ui.navigate.to(f"/document/{doc_id}"))  # does nothing
```

From services/client_manager.py:
```python
class ClientManager:
    DEFAULT_CLIENT = "Основной реестр"
    def get_db(self, name: str) -> Database: ...
    def get_db_path(self, name: str) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /download/{doc_id} route and reprocess handler (UIFIX-02, UIFIX-03)</name>
  <files>app/main.py, app/pages/document.py</files>
  <read_first>
    - app/main.py (full file — need to add route near existing redline route)
    - app/pages/document.py (lines 220-240 — action bar buttons; lines 1-40 — imports)
    - controller.py (lines 35-75 — process_archive signature)
    - app/components/process.py (lines 50-80 — start_pipeline signature)
  </read_first>
  <action>
1. **UIFIX-02 — Add /download/{doc_id} route in app/main.py** (place after the existing redline route, ~line 204):
   ```python
   @app.get('/download/{doc_id}')
   async def download_document(doc_id: int):
       """Скачивает оригинальный файл документа."""
       from pathlib import Path as _Path
       from services.client_manager import ClientManager as _CM

       cm = _CM()
       db = cm.get_db("Основной реестр")
       doc = await run.io_bound(db.get_contract_by_id, doc_id)
       if doc is None:
           return FastAPIResponse(content="Документ не найден", status_code=404)

       original_path = _Path(doc.get("original_path", ""))
       if not original_path.exists():
           return FastAPIResponse(
               content=f"Файл не найден: {original_path.name}", status_code=404
           )

       content_bytes = original_path.read_bytes()
       suffix = original_path.suffix.lower()
       media_type = {
           ".pdf": "application/pdf",
           ".docx": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
           ".doc": "application/msword",
       }.get(suffix, "application/octet-stream")

       return FastAPIResponse(
           content=content_bytes,
           media_type=media_type,
           headers={"Content-Disposition": f'attachment; filename="{original_path.name}"'},
       )
   ```

   IMPORTANT: This route MUST be placed BEFORE the `ui.run()` call but AFTER the existing redline route. The path `/download/{doc_id}` must not conflict with `/download/redline/{contract_id}/{other_id}` — FastAPI matches more specific routes first, so redline (3 segments) won't conflict with download (2 segments).

2. **UIFIX-03 — Reprocess button in app/pages/document.py** (replace the no-op navigate at lines 231-233):
   Replace the current "Переобработать" button with a working handler:
   ```python
   async def _reprocess():
       """Re-run pipeline for this single document."""
       from pathlib import Path as _Path
       original_path = _Path(contract.get("original_path", ""))
       if not original_path.exists():
           ui.notify("Файл не найден — переобработка невозможна", type="negative")
           return
       # Create a temp dir with symlink to single file, run pipeline
       import tempfile, os
       with tempfile.TemporaryDirectory() as tmpdir:
           tmp_path = _Path(tmpdir)
           link = tmp_path / original_path.name
           try:
               os.symlink(original_path, link)
           except OSError:
               # Windows fallback or permission issue — copy instead
               import shutil
               shutil.copy2(original_path, link)
           from config import Config
           from controller import Controller
           from services.client_manager import ClientManager
           cm = ClientManager()
           db_path = cm.get_db_path(state.current_client)
           ctrl = Controller(Config())
           ui.notify("Переобработка запущена...", type="info")
           stats = await run.io_bound(
               ctrl.process_archive,
               tmp_path,
               "both",       # grouping
               True,         # force_reprocess
               None,         # on_progress
               None,         # on_file_done
               db_path.parent,  # output_dir_override — same dir as existing DB
           )
           done = stats.get("done", 0)
           errors = stats.get("errors", 0)
           if errors:
               ui.notify(f"Переобработка завершена с ошибками: {errors}", type="warning")
           else:
               ui.notify(f"Документ переобработан", type="positive")
           # Reload the page to show updated data
           ui.navigate.to(f"/document/{doc_id}")
   ```

   Wire the button:
   ```python
   ui.button("Переобработать", on_click=_reprocess).props("flat dense no-caps").classes(ACTION_BTN)
   ```

   The `_reprocess` async function must be defined inside `build()` where `contract`, `doc_id`, `state`, `run` are in scope.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "download/{doc_id}" app/main.py && grep -n "reprocess\|process_archive" app/pages/document.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "download/{doc_id}" app/main.py returns a route definition line
    - grep "FastAPIResponse" app/main.py shows it's used in the download route
    - grep "_reprocess" app/pages/document.py returns the handler definition
    - grep "process_archive" app/pages/document.py returns the pipeline call
    - grep "ui.navigate.to.*document.*doc_id" app/pages/document.py confirms page reload after reprocess
    - The old no-op `ui.navigate.to(f"/document/{doc_id}")` as the direct on_click of "Переобработать" is gone
  </acceptance_criteria>
  <done>Download button returns original file via FastAPI route; Reprocess button runs pipeline for single document and reloads page.</done>
</task>

</tasks>

<verification>
1. `grep -n "/download/{doc_id}" app/main.py` — route exists
2. `grep -n "_reprocess" app/pages/document.py` — handler exists
3. `python -c "import app.main"` — no import errors (syntax check)
</verification>

<success_criteria>
- /download/{doc_id} route returns file bytes with correct Content-Type (UIFIX-02)
- Reprocess button triggers pipeline for single document (UIFIX-03)
</success_criteria>

<output>
After completion, create `.planning/phases/21-ui-fixes/21-02-SUMMARY.md`
</output>
