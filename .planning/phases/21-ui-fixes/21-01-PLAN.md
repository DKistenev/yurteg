---
phase: 21-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/registry.py
  - app/components/bulk_actions.py
  - app/pages/settings.py
  - app/main.py
  - app/pages/document.py
  - app/pages/templates.py
autonomous: true
requirements: [UIFIX-01, UIFIX-04, UIFIX-05, UIFIX-06]

must_haves:
  truths:
    - "Клик по строке реестра открывает боковую панель без ошибок"
    - "Порог напоминаний, сохранённый в настройках, корректно читается при загрузке"
    - "Массовая смена статуса показывает человекочитаемые русские названия"
    - "Ошибки в except-блоках логируются через logger.exception()"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "Fixed get_contract_by_id call"
      contains: "get_contract_by_id"
    - path: "app/components/bulk_actions.py"
      provides: "Fixed get_contract_by_id + STATUS_LABELS label extraction"
      contains: "get_contract_by_id"
    - path: "app/main.py"
      provides: "Fixed warning_days_threshold key"
      contains: "warning_days_threshold"
  key_links:
    - from: "app/pages/registry.py:917"
      to: "modules/database.py:get_contract_by_id"
      via: "method call"
      pattern: "get_contract_by_id"
    - from: "app/main.py:157"
      to: "app/pages/settings.py:366"
      via: "shared settings key"
      pattern: "warning_days_threshold"
---

<objective>
Fix crash bugs and data display issues in the UI: broken method name (get_contract), mismatched settings key (warning_days), tuple display in bulk status dialog, and silent exception swallowing.

Purpose: Eliminate crashes and data inconsistencies so all existing UI elements work correctly.
Output: 4 bug fixes across 6 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/AUDIT-2026-03-25.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From modules/database.py:
```python
def get_contract_by_id(self, contract_id: int) -> dict | None:  # line 380
```
NOTE: There is NO method called `get_contract()`. The correct name is `get_contract_by_id()`.

From services/lifecycle_service.py:
```python
STATUS_LABELS = {
    "active":      ("✔", "Действует",         "#22c55e"),
    "expiring":    ("⚠", "Скоро истекает",    "#f59e0b"),
    "expired":     ("✗", "Истёк",             "#ef4444"),
    "unknown":     ("?", "Нет даты",           "#9ca3af"),
    "terminated":  ("✖", "Расторгнут",        "#6b7280"),
    "extended":    ("↻", "Продлён",           "#3b82f6"),
    "negotiation": ("~", "На согласовании",  "#8b5cf6"),
    "suspended":   ("⏸", "Приостановлен",    "#f97316"),
}
```
Values are tuples (icon, label, color). `ui.select` options dict needs only the label string.

From config.py:
```python
def save_setting(key: str, value) -> None: ...
def load_settings() -> dict: ...
```
Settings saved with key "warning_days_threshold" in settings.py:366, but read as "warning_days" in main.py:157.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix method name and settings key mismatches (UIFIX-01, UIFIX-04)</name>
  <files>app/pages/registry.py, app/components/bulk_actions.py, app/main.py</files>
  <read_first>
    - app/pages/registry.py (lines 910-925 — split panel click handler)
    - app/components/bulk_actions.py (lines 55-65 — export loop)
    - app/main.py (lines 155-160 — warning_days read)
  </read_first>
  <action>
1. **registry.py line 917**: Change `db.get_contract` to `db.get_contract_by_id`:
   ```python
   doc = await run.io_bound(db.get_contract_by_id, doc_id)
   ```

2. **bulk_actions.py line 62**: Change `db.get_contract` to `db.get_contract_by_id`:
   ```python
   doc = db.get_contract_by_id(doc_id)
   ```

3. **main.py line 157**: Change `"warning_days"` to `"warning_days_threshold"` to match the key saved by settings.py:
   ```python
   state.warning_days_threshold = app_settings.get("warning_days_threshold", 30)
   ```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "get_contract(" app/pages/registry.py app/components/bulk_actions.py | grep -v "get_contract_by_id" | grep -v "get_contract_id_by_hash" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "get_contract_by_id" app/pages/registry.py returns match at line ~917
    - grep "get_contract_by_id" app/components/bulk_actions.py returns match at line ~62
    - grep "warning_days_threshold" app/main.py returns match at line ~157
    - No remaining references to bare `db.get_contract(` (without _by_id) in app/pages/ or app/components/
  </acceptance_criteria>
  <done>Split panel opens without AttributeError; bulk export fetches docs correctly; warning_days_threshold value persists across restarts.</done>
</task>

<task type="auto">
  <name>Task 2: Fix bulk status labels and add logging to silent excepts (UIFIX-05, UIFIX-06)</name>
  <files>app/components/bulk_actions.py, app/pages/registry.py, app/pages/settings.py, app/pages/templates.py, app/pages/document.py</files>
  <read_first>
    - app/components/bulk_actions.py (lines 111-129 — status dialog)
    - app/pages/document.py (lines 270-275, 305-310, 360-370, 420-430, 470-475, 510-530 — except blocks)
    - app/pages/settings.py (lines 240-250, 345-350 — except blocks)
    - app/pages/registry.py (lines 620-650, 790-815 — except blocks)
    - app/pages/templates.py (lines 235-265, 320-345 — except blocks)
  </read_first>
  <action>
1. **UIFIX-05 — bulk_actions.py line 117**: STATUS_LABELS values are tuples (icon, label, color). The `ui.select` options dict currently passes the full tuple as the display value. Extract only the label (index 1):
   ```python
   options={k: v[1] for k, v in STATUS_LABELS.items() if k in MANUAL_STATUSES},
   ```

2. **UIFIX-06 — Add logging to all silent except blocks in app/pages/**:
   - Add `import logging` and `logger = logging.getLogger(__name__)` to files that lack it: `registry.py`, `settings.py`, `templates.py` (document.py already has it).
   - In every bare `except Exception:` block (without logging), add `logger.exception("описание контекста")` as the first statement. Use context-appropriate Russian messages, e.g.:
     - document.py except blocks: "Ошибка при загрузке/обработке документа"
     - settings.py except blocks: "Ошибка при сохранении/загрузке настроек"
     - registry.py except blocks: "Ошибка при обработке реестра"
     - templates.py except blocks: "Ошибка при работе с шаблонами"
   - Do NOT change logic or control flow — only add logging.
   - Blocks that already have `as e:` and use `e` (like `templates.py:237`, `registry.py:796`) — verify they log; if not, add `logger.exception(...)`.

3. **Dead button cleanup** — document.py lines 235-237: Remove the dead `action_review_btn = ui.button("Проверить по шаблону")` that duplicates the working `review_btn` below it.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from app.components.bulk_actions import show_bulk_status_dialog; print('import ok')" && grep -c "logger" app/pages/registry.py app/pages/settings.py app/pages/templates.py app/pages/document.py</automated>
  </verify>
  <acceptance_criteria>
    - grep 'v\[1\]' app/components/bulk_actions.py returns match at STATUS_LABELS line
    - grep 'logger' app/pages/registry.py returns at least 3 matches
    - grep 'logger' app/pages/settings.py returns at least 3 matches
    - grep 'logger' app/pages/templates.py returns at least 3 matches
    - grep 'action_review_btn' app/pages/document.py returns 0 matches
  </acceptance_criteria>
  <done>Bulk status dialog shows "Действует", "Истёк" etc. instead of tuples; all except blocks in app/pages/ log errors; dead button removed.</done>
</task>

</tasks>

<verification>
1. `grep -rn "db.get_contract(" app/pages/ app/components/ | grep -v "get_contract_by_id" | grep -v "get_contract_id_by_hash"` — returns empty (no broken method calls)
2. `grep -n "warning_days_threshold" app/main.py` — shows corrected key
3. `grep -n "v\[1\]" app/components/bulk_actions.py` — shows label extraction
4. `grep -rn "except Exception" app/pages/ | wc -l` vs `grep -rn "logger" app/pages/ | wc -l` — logger count >= except count
</verification>

<success_criteria>
- Split panel click does not crash (UIFIX-01)
- Warning days setting persists correctly (UIFIX-04)
- Bulk status shows human-readable Russian labels (UIFIX-05)
- All except blocks log errors (UIFIX-06)
</success_criteria>

<output>
After completion, create `.planning/phases/21-ui-fixes/21-01-SUMMARY.md`
</output>
