# Phase 2: Жизненный цикл документа - Context

**Gathered:** 2026-03-20
**Status:** Ready for planning

<domain>
## Phase Boundary

Юрист видит что происходит с каждым документом — без ручной проверки сроков и статусов. Автоматические статусы, ручная коррекция, версионирование с автосвязыванием, сравнение версий (diff + redline), платёжный календарь, панель «требует внимания», ревью договора против шаблона-эталона. Всё максимально автоматизировано — ноль ручных действий по умолчанию.

</domain>

<decisions>
## Implementation Decisions

### Платёжный календарь
- Формат: календарь-сетка (стиль Google Calendar) — месяц/неделя, платежи в ячейках дней
- Данные: сумма + направление (красный = расход, зелёный = доход), контрагент, кликабельная ссылка на договор
- Периодические платежи: AI извлекает «ежемесячно» / «ежеквартально» → система автоматически разворачивает в календарь
- Итого за период: пока нет (v2)
- Экспорт: пока нет (v2)

### Ревью против шаблона
- Добавление эталона: два способа — загрузить отдельный файл + пометить «это эталон для NDA», или отметить документ из реестра как эталон
- Сопоставление: автоматически по типу договора (NDA → эталон NDA) + ручной выбор из библиотеки если нужно
- Отображение отступлений: текст документа показывается внутри приложения с подсветкой изменений (зелёный = добавлено, красный = удалено, жёлтый = изменено)
- Выход: скачиваемый .docx с track changes (redline) — юрист открывает в Word и видит правки как привык
- Движок сравнения: векторные эмбеддинги (sentence-transformers) для семантического сравнения пунктов

### Карточка документа
- Структура: вкладки — Основное / Версии / Платежи / Ревью
- Вкладка «Основное»: метаданные (тип, контрагент, суммы, даты), статус, ручная коррекция статуса
- Вкладка «Версии»: вертикальный таймлайн (v1 11.03 → v2 15.03 → v3 20.03), клик на пару = diff
- Вкладка «Платежи»: платежи по этому конкретному договору
- Вкладка «Ревью»: сравнение с эталоном + redline

### Статус документа в реестре
- Иконки рядом с названием: ✔ (действует), ⚠ (истекает), ✗ (истёк), и другие для ручных статусов
- Автоматический статус вычисляется на лету (SQL CASE по date_end + текущей дате)
- Ручной статус (расторгнут / продлён / на согласовании / приостановлен) приоритетнее автоматического

### Панель «требует внимания»
- Расположение: верх главной страницы — первое что видит юрист при открытии
- Содержимое: документы с истекающими сроками + проблемы валидации

### Версионирование
- Автоматическое связывание через эмбеддинги + метаданные файла (дата создания, автор)
- Все версии в одной карточке — единый объект «договор» с историей (как в Firma)
- Diff между версиями: сравнение ключевых полей + семантическое сравнение пунктов через эмбеддинги
- Redline генерируется в .docx с track changes — двумя кликами (как в Firma)

### Эмбеддинги и кэширование
- Эмбеддинги считаются один раз и хранятся в SQLite (таблица embeddings)
- Новый документ: считается только его вектор, сравнивается с кэшем
- Модель: лёгкая multilingual (sentence-transformers), ~130-470 МБ RAM
- Пересчёт только при смене модели эмбеддингов (поле model_version)
- В вехе 3 — тестируем эмбеддинги из QWEN, если лучше — переключаемся

### Claude's Discretion
- Конкретная embedding-модель (MiniLM vs E5 — подобрать по качеству на русских юридических текстах)
- Детали реализации таймлайна версий
- Алгоритм авторазворота периодических платежей
- Пороговые значения для классификации отступлений (OK / внимание / критично)
- Конкретная реализация текстового diff в интерфейсе

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Текущая архитектура (после фазы 1)
- `.planning/phases/01-infrastructure/01-VERIFICATION.md` — что было построено в фазе 1
- `.planning/codebase/ARCHITECTURE.md` — паттерн пайплайна, слои
- `.planning/codebase/CONCERNS.md` — оставшийся техдолг

### Ресёрч
- `.planning/research/FEATURES.md` — фичи: table stakes vs differentiators
- `.planning/research/ARCHITECTURE.md` — целевая архитектура
- `.planning/research/PITFALLS.md` — подводные камни (APScheduler + Streamlit, дата-бомба)

### Требования
- `.planning/REQUIREMENTS.md` — LIFE-01 через LIFE-08

### Фаза 1 артефакты
- `providers/` — провайдер-абстракция (фаза 1)
- `services/pipeline_service.py` — сервис-слой (фаза 1)
- `services/registry_service.py` — сервис-слой (фаза 1)
- `modules/database.py` — система миграций (фаза 1)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `services/pipeline_service.py` — фасад для обработки документов, вызывается без Streamlit
- `services/registry_service.py` — запросы к БД, фильтрация, сортировка
- `providers/` — абстракция AI-провайдеров для extract_metadata
- `modules/database.py` — система миграций, ready для новых таблиц (embeddings, templates, payments)
- `modules/validator.py` — L1-L4 валидация, можно расширить для проверки эмбеддингов
- `modules/models.py` — dataclasses для ContractMetadata (содержит date_signed, date_start, date_end, amount, counterparty)

### Established Patterns
- Модули общаются через dataclasses
- SQLite с Lock для thread-safety
- Streamlit session_state для UI-состояния
- ThreadPoolExecutor для параллельной обработки

### Integration Points
- `main.py` — текущий UI (1400 строк), сюда добавляются вкладки карточки, календарь, панель внимания
- `modules/ai_extractor.py` — сюда добавить извлечение платёжных данных и условий периодичности
- `modules/database.py` — новые таблицы: embeddings, document_versions, templates, payments

</code_context>

<specifics>
## Specific Ideas

- Firma как референс для версионирования — все версии в одной карточке, редлайн в 2 клика
- «Какой-то value должен быть помимо реестра — что-то что помогает принять решения» (Daniel)
- Принцип: ноль ручных действий по умолчанию, ручная коррекция только как исключение
- Кэшировать всё что дорого вычислять (эмбеддинги, AI-результаты, сравнения)
- Принцип: каждая фича должна помогать юристу ПРИНЯТЬ РЕШЕНИЕ, а не просто организовать файлы

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 02-document-lifecycle*
*Context gathered: 2026-03-20*
