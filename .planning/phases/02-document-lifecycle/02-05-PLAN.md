---
phase: 02-document-lifecycle
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - services/payment_service.py
  - services/pipeline_service.py
autonomous: true
requirements:
  - LIFE-07

must_haves:
  truths:
    - "payment_service.py создаёт платёжные записи в таблице payments для каждого договора"
    - "Периодические платежи (monthly/quarterly/yearly) разворачиваются в конкретные даты через dateutil.relativedelta"
    - "Разовый платёж (once или NULL frequency) записывается одной записью на date_start или date_signed"
    - "get_calendar_events() возвращает список в формате streamlit-calendar FullCalendar JSON"
    - "Красный цвет = expense (#ef4444), зелёный = income (#22c55e)"
  artifacts:
    - path: "services/payment_service.py"
      provides: "unroll_payments, save_payments, get_calendar_events"
      exports: ["unroll_payments", "save_payments", "get_calendar_events"]
    - path: "services/pipeline_service.py"
      provides: "Вызов save_payments после успешной обработки документа"
      contains: "save_payments"
  key_links:
    - from: "services/pipeline_service.py"
      to: "services/payment_service.save_payments"
      via: "вызов после database.save, если payment_amount is not None"
      pattern: "save_payments"
    - from: "services/payment_service.get_calendar_events"
      to: "payments table в SQLite"
      via: "SELECT на payments JOIN contracts для получения контрагента"
      pattern: "SELECT.*payments"
---

<objective>
Реализовать платёжный сервис: разворот периодических платежей и формирование событий для календаря.

Purpose: LIFE-07 — данные для платёжного календаря. Payment_service накапливает события в БД, UI-план (02-06) только читает их.
Output: services/payment_service.py с тремя функциями + хук в pipeline_service.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-document-lifecycle/02-CONTEXT.md
@.planning/phases/02-document-lifecycle/02-RESEARCH.md
@.planning/phases/02-document-lifecycle/02-01-SUMMARY.md

<interfaces>
<!-- Из modules/models.py (после 02-03) — ContractMetadata новые поля: -->
```python
payment_terms: Optional[str]     # текстовое описание
payment_amount: Optional[float]  # сумма одного платежа
payment_frequency: Optional[str] # 'monthly' | 'quarterly' | 'yearly' | 'once' | None
payment_direction: Optional[str] # 'income' | 'expense' | None
```

<!-- Схема таблицы payments (из migration v5 в 02-01) -->
```sql
payments (
    id, contract_id, payment_date TEXT (ISO 8601),
    amount REAL, direction TEXT ('income'|'expense'),
    is_periodic INTEGER (0|1),
    frequency TEXT (monthly|quarterly|yearly|NULL),
    created_at
)
```

Паттерн unroll из 02-RESEARCH.md:
```python
from dateutil.relativedelta import relativedelta

FREQUENCY_DELTA = {
    'monthly':   relativedelta(months=1),
    'quarterly': relativedelta(months=3),
    'yearly':    relativedelta(years=1),
}

def unroll_payments(start, end, amount, frequency):
    delta = FREQUENCY_DELTA.get(frequency)
    if not delta:
        return [{'date': start, 'amount': amount}]
    result, current = [], start
    while current <= end:
        result.append({'date': current, 'amount': amount})
        current += delta
    return result
```

Формат событий для streamlit-calendar (FullCalendar):
```python
{
    "title": "Альфа ООО • 50 000 ₽",
    "start": "2026-03-01",       # ISO 8601
    "end": "2026-03-01",
    "backgroundColor": "#ef4444",  # expense
    "extendedProps": {"contract_id": 42, "direction": "expense"},
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/payment_service.py — unroll, save, get_calendar_events</name>
  <files>services/payment_service.py</files>
  <read_first>
    - .planning/phases/02-document-lifecycle/02-RESEARCH.md (Pattern 4: Payment unrolling, Pattern 5: streamlit-calendar)
    - modules/database.py (Database._conn, _lock — паттерн доступа)
    - modules/models.py (ContractMetadata — payment_amount, payment_frequency, payment_direction)
  </read_first>
  <action>
Создать services/payment_service.py:

```python
"""Сервис платёжного календаря.

Разворачивает периодические платежи из ContractMetadata в конкретные даты
и сохраняет в SQLite таблицу payments. Формирует события для streamlit-calendar.

Не импортирует streamlit — вызывается из pipeline_service и main.py.
"""
import logging
from datetime import date, datetime
from typing import Optional

from dateutil.relativedelta import relativedelta

from modules.database import Database
from modules.models import ContractMetadata

logger = logging.getLogger(__name__)

# Маппинг частоты → relativedelta
FREQUENCY_DELTA = {
    "monthly":   relativedelta(months=1),
    "quarterly": relativedelta(months=3),
    "yearly":    relativedelta(years=1),
}

# Цвета направления платежа (FullCalendar backgroundColor)
DIRECTION_COLOR = {
    "income":  "#22c55e",   # зелёный
    "expense": "#ef4444",   # красный
}


def _parse_date(s: Optional[str]) -> Optional[date]:
    """Парсит ISO 8601 строку в date. Возвращает None при ошибке."""
    if not s:
        return None
    try:
        return datetime.strptime(s[:10], "%Y-%m-%d").date()
    except ValueError:
        return None


def unroll_payments(
    start: date,
    end: date,
    amount: float,
    frequency: Optional[str],
    direction: str = "expense",
) -> list[dict]:
    """Разворачивает периодический платёж в список событий.

    Для разового платежа (frequency=None или 'once') возвращает одну запись.
    Для периодических — все даты от start до end включительно.

    Returns: список dict с ключами: date (date), amount (float),
             direction (str), is_periodic (bool), frequency (str|None)
    """
    delta = FREQUENCY_DELTA.get(frequency or "")
    is_periodic = delta is not None

    if not is_periodic:
        return [{
            "date": start,
            "amount": amount,
            "direction": direction,
            "is_periodic": False,
            "frequency": None,
        }]

    result = []
    current = start
    # Защита от бесконечного цикла: максимум 1200 итераций (~100 лет monthly)
    max_iter = 1200
    while current <= end and max_iter > 0:
        result.append({
            "date": current,
            "amount": amount,
            "direction": direction,
            "is_periodic": True,
            "frequency": frequency,
        })
        current += delta
        max_iter -= 1

    return result


def save_payments(
    db: Database,
    contract_id: int,
    meta: ContractMetadata,
) -> int:
    """Сохраняет платежи для договора в таблицу payments.

    Удаляет существующие записи перед записью (идемпотентно).
    Returns: количество сохранённых записей.
    """
    if meta.payment_amount is None:
        return 0

    # Определить временной диапазон
    start = _parse_date(meta.date_start) or _parse_date(meta.date_signed)
    end = _parse_date(meta.date_end)

    if start is None:
        logger.warning(
            "contract_id=%d: нет даты начала, платежи не сохранены", contract_id
        )
        return 0

    if end is None:
        # Если нет даты окончания — записать только начальный платёж
        end = start

    direction = meta.payment_direction or "expense"
    events = unroll_payments(start, end, meta.payment_amount, meta.payment_frequency, direction)

    with db._lock:
        # Удалить старые записи (идемпотентность при повторной обработке)
        db._conn.execute("DELETE FROM payments WHERE contract_id=?", (contract_id,))
        db._conn.executemany(
            """INSERT INTO payments (contract_id, payment_date, amount, direction,
                                     is_periodic, frequency)
               VALUES (:contract_id, :payment_date, :amount, :direction,
                       :is_periodic, :frequency)""",
            [
                {
                    "contract_id": contract_id,
                    "payment_date": e["date"].isoformat(),
                    "amount": e["amount"],
                    "direction": e["direction"],
                    "is_periodic": 1 if e["is_periodic"] else 0,
                    "frequency": e["frequency"],
                }
                for e in events
            ],
        )
        db._conn.commit()

    logger.info(
        "contract_id=%d: сохранено %d платежей (%s)",
        contract_id, len(events), meta.payment_frequency or "once",
    )
    return len(events)


def get_calendar_events(
    db: Database,
    start_date: Optional[date] = None,
    end_date: Optional[date] = None,
) -> list[dict]:
    """Возвращает события для streamlit-calendar в формате FullCalendar.

    Если start_date/end_date заданы — фильтрует по диапазону.
    Каждое событие: title, start, end, backgroundColor, extendedProps.
    """
    sql = """
        SELECT p.id, p.contract_id, p.payment_date, p.amount,
               p.direction, p.frequency,
               c.counterparty, c.contract_type, c.filename
        FROM payments p
        JOIN contracts c ON c.id = p.contract_id
        WHERE 1=1
    """
    params: list = []
    if start_date:
        sql += " AND p.payment_date >= ?"
        params.append(start_date.isoformat())
    if end_date:
        sql += " AND p.payment_date <= ?"
        params.append(end_date.isoformat())
    sql += " ORDER BY p.payment_date ASC"

    with db._lock:
        rows = db._conn.execute(sql, params).fetchall()

    events = []
    for row in rows:
        pid, cid, pdate, amount, direction, freq, counterparty, ctype, fname = row
        color = DIRECTION_COLOR.get(direction, "#9ca3af")
        amount_str = f"{amount:,.0f} ₽".replace(",", " ")
        title = f"{counterparty or fname} • {amount_str}"
        events.append({
            "title": title,
            "start": pdate,
            "end": pdate,
            "backgroundColor": color,
            "extendedProps": {
                "contract_id": cid,
                "direction": direction,
                "amount": amount,
                "counterparty": counterparty,
                "contract_type": ctype,
            },
        })
    return events
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from datetime import date
from services.payment_service import unroll_payments, save_payments, get_calendar_events
from modules.database import Database
from modules.models import ContractMetadata
from pathlib import Path
import tempfile, os

# unroll monthly
events = unroll_payments(date(2026, 1, 1), date(2026, 3, 31), 50000.0, 'monthly', 'expense')
assert len(events) == 3, f'Ожидается 3 ежемесячных платежа, получено {len(events)}'
assert events[0]['date'] == date(2026, 1, 1)
assert events[1]['date'] == date(2026, 2, 1)
assert events[2]['date'] == date(2026, 3, 1)
assert all(e['is_periodic'] for e in events)

# unroll once (no frequency)
events_once = unroll_payments(date(2026, 3, 1), date(2026, 12, 31), 100000.0, None)
assert len(events_once) == 1, f'Разовый платёж должен быть 1 запись, получено {len(events_once)}'

# save_payments + get_calendar_events
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = Path(f.name)
db = Database(db_path, db_path.parent)
conn = db._conn
conn.execute(\"\"\"INSERT INTO contracts (filename, original_path, status, counterparty)
    VALUES ('test.pdf', '/tmp/test', 'done', 'Тест ООО')\"\"\")
conn.commit()
cid = conn.execute('SELECT id FROM contracts').fetchone()[0]

meta = ContractMetadata(
    date_start='2026-01-01', date_end='2026-03-31',
    payment_amount=50000.0, payment_frequency='monthly', payment_direction='expense'
)
count = save_payments(db, cid, meta)
assert count == 3, f'Ожидается 3 записи, сохранено {count}'

cal_events = get_calendar_events(db)
assert len(cal_events) == 3, f'Ожидается 3 события в календаре, получено {len(cal_events)}'
assert cal_events[0]['backgroundColor'] == '#ef4444', 'expense должен быть красным'

print('OK: payment_service работает корректно')
db.close()
os.unlink(db_path)
"
</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "def unroll_payments\|def save_payments\|def get_calendar_events" services/payment_service.py возвращает 3 строки
    - grep -n "import streamlit" services/payment_service.py возвращает пусто
    - grep -n "relativedelta" services/payment_service.py возвращает строку импорта
    - python-тест выше завершается "OK: payment_service работает корректно"
  </acceptance_criteria>
  <done>payment_service.py создан с тремя функциями. unroll_payments корректно работает для monthly/quarterly/yearly/once. save_payments идемпотентна. get_calendar_events возвращает события с правильными цветами.</done>
</task>

<task type="auto">
  <name>Task 2: Хук save_payments в pipeline_service.py</name>
  <files>services/pipeline_service.py</files>
  <read_first>
    - services/pipeline_service.py (найти место после database.save, где добавляется version_service — рядом добавить payment_service)
    - services/payment_service.py (сигнатура save_payments)
  </read_first>
  <action>
В services/pipeline_service.py найти блок где добавлен хук version_service (из 02-03). Добавить рядом хук payment_service:

```python
from services.payment_service import save_payments

# В том же try-блоке после version_service, или в отдельном try-блоке:
try:
    if result.status == "done" and result.metadata and result.metadata.payment_amount is not None:
        saved = db._conn.execute(
            "SELECT id FROM contracts WHERE file_hash=?",
            (result.file_info.file_hash,)
        ).fetchone()
        if saved:
            save_payments(db, saved[0], result.metadata)
except Exception as e:
    logger.warning("Сохранение платежей не удалось для %s: %s", result.file_info.filename, e)
```

Ошибки не прерывают пайплайн — только WARNING.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "save_payments" services/pipeline_service.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "save_payments" services/pipeline_service.py возвращает минимум 2 строки (импорт + вызов)
    - grep -n "payment_amount is not None" services/pipeline_service.py возвращает строку (защитная проверка)
    - python -c "import services.pipeline_service" завершается без ошибок
  </acceptance_criteria>
  <done>pipeline_service вызывает save_payments для каждого документа с payment_amount. Ошибки обёрнуты в try/except, не прерывают обработку архива.</done>
</task>

</tasks>

<verification>
1. python -c "from services.payment_service import unroll_payments, get_calendar_events; print('OK')" — без ошибок
2. Тест из Task 1 завершается успешно
3. grep -c "save_payments\|find_version_match" services/pipeline_service.py — минимум 4 строки (2 импорта + 2 вызова)
</verification>

<success_criteria>
- unroll_payments корректно работает для monthly (31-дневные месяцы через relativedelta, не +30 days)
- save_payments идемпотентна: повторный вызов с теми же данными не дублирует записи
- get_calendar_events возвращает правильные цвета: expense=#ef4444, income=#22c55e
- Ошибки в payment_service не прерывают основной пайплайн
</success_criteria>

<output>
После завершения создать `.planning/phases/02-document-lifecycle/02-05-SUMMARY.md`
</output>
