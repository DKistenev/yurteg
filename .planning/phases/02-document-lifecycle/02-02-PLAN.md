---
phase: 02-document-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - main.py
autonomous: false
requirements:
  - LIFE-02
  - LIFE-05
  - LIFE-06

must_haves:
  truths:
    - "Реестр показывает иконку статуса (✔/⚠/✗/...) рядом с каждым договором"
    - "Юрист может выбрать ручной статус из выпадающего списка прямо в реестре"
    - "При выборе «Сбросить» договор возвращается к автоматическому статусу"
    - "Панель «требует внимания» видна в верхней части главной страницы при наличии проблем"
    - "Порог предупреждения настраивается (30/60/90 дней) в сайдбаре"
    - "Панель не отображается если нет истекающих/истёкших документов"
  artifacts:
    - path: "main.py"
      provides: "UI панели внимания + контролы ручного статуса в реестре"
      contains: "get_attention_required"
    - path: "main.py"
      provides: "Селектор порога warning_days в сайдбаре"
      contains: "warning_days_threshold"
  key_links:
    - from: "main.py (реестр)"
      to: "lifecycle_service.set_manual_status / clear_manual_status"
      via: "st.selectbox onchange → lifecycle_service call → st.rerun()"
      pattern: "set_manual_status|clear_manual_status"
    - from: "main.py (панель внимания)"
      to: "lifecycle_service.get_attention_required"
      via: "вызов при загрузке страницы, до отрисовки реестра"
      pattern: "get_attention_required"
---

<objective>
Добавить в Streamlit UI ручной override статуса договора и панель «требует внимания».

Purpose: LIFE-02 и LIFE-05 — видимый результат для юриста. После этого плана юрист впервые видит цветные статусы и панель с проблемными документами.
Output: Изменения в main.py: панель внимания вверху страницы, статус-иконки и selectbox в реестре, настройка порога в сайдбаре.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-document-lifecycle/02-CONTEXT.md
@.planning/phases/02-document-lifecycle/02-01-SUMMARY.md

<interfaces>
<!-- Из lifecycle_service.py (создан в 02-01) -->
```python
from services.lifecycle_service import (
    get_computed_status_sql,   # -> str (SQL CASE fragment)
    set_manual_status,         # (db, contract_id: int, status: str) -> None
    clear_manual_status,       # (db, contract_id: int) -> None
    get_attention_required,    # (db, warning_days: int) -> list[DeadlineAlert]
    MANUAL_STATUSES,           # frozenset: {"terminated","extended","negotiation","suspended"}
    STATUS_LABELS,             # dict: status -> (icon, label, hex_color)
)
```

DeadlineAlert (из modules/models.py):
```python
@dataclass
class DeadlineAlert:
    contract_id: int
    filename: str
    counterparty: Optional[str]
    contract_type: Optional[str]
    date_end: str           # ISO 8601
    days_until_expiry: int  # отрицательное = уже истёк
    computed_status: str    # 'expired' | 'expiring'
    validation_status: Optional[str] = None
```

STATUS_LABELS:
```python
STATUS_LABELS = {
    "active":      ("✔", "Действует",        "#22c55e"),
    "expiring":    ("⚠", "Скоро истекает",   "#f59e0b"),
    "expired":     ("✗", "Истёк",            "#ef4444"),
    "unknown":     ("?", "Нет даты",          "#9ca3af"),
    "terminated":  ("✖", "Расторгнут",       "#6b7280"),
    "extended":    ("↻", "Продлён",          "#3b82f6"),
    "negotiation": ("~", "На согласовании",  "#8b5cf6"),
    "suspended":   ("⏸", "Приостановлен",   "#f97316"),
}
```

SQL для computed_status:
```python
# В запросе к БД добавить в SELECT:
computed_status_expr = get_computed_status_sql(warning_days)
sql = f"""
    SELECT *, {computed_status_expr} AS computed_status
    FROM contracts
    WHERE status = 'done'
    ORDER BY processed_at DESC
"""
rows = db._conn.execute(sql, {"warning_days": warning_days}).fetchall()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Панель «требует внимания» и настройка порога в сайдбаре</name>
  <files>main.py</files>
  <read_first>
    - main.py (найти место сайдбара, место до отрисовки реестра, текущие импорты)
    - services/lifecycle_service.py (сигнатуры функций)
  </read_first>
  <action>
В main.py внести следующие изменения:

**1. Добавить импорт в начало файла (после существующих imports):**
```python
from services.lifecycle_service import (
    get_computed_status_sql, set_manual_status, clear_manual_status,
    get_attention_required, MANUAL_STATUSES, STATUS_LABELS,
)
```

**2. В сайдбар добавить selectbox для настройки порога** (найти секцию с настройками, добавить после других параметров):
```python
warning_days = st.sidebar.selectbox(
    "Предупреждать о сроках за",
    options=[30, 60, 90],
    index=0,
    format_func=lambda d: f"{d} дней",
    key="warning_days_threshold",
)
```
Сохранить значение в st.session_state["warning_days_threshold"] = warning_days для использования по всему UI.

**3. Панель «требует внимания»** — разместить ПЕРЕД отрисовкой таблицы реестра (найти место, где рендерится основная вкладка с реестром):
```python
# Панель «требует внимания» — показывать только если есть проблемы
if db is not None:
    _warning_days = st.session_state.get("warning_days_threshold", 30)
    alerts = get_attention_required(db, _warning_days)
    if alerts:
        with st.expander(f"⚠ Требует внимания — {len(alerts)} договор(ов)", expanded=True):
            for alert in alerts:
                icon, label, color = STATUS_LABELS.get(
                    alert.computed_status, ("?", alert.computed_status, "#9ca3af")
                )
                days_text = (
                    f"истёк {abs(alert.days_until_expiry)} дн. назад"
                    if alert.days_until_expiry < 0
                    else f"истекает через {alert.days_until_expiry} дн."
                )
                st.markdown(
                    f'<span style="color:{color}">{icon}</span> '
                    f'**{alert.filename}** — {alert.counterparty or "нет контрагента"} '
                    f'· {days_text}',
                    unsafe_allow_html=True,
                )
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "get_attention_required\|warning_days_threshold\|warning_days_threshold" main.py | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "from services.lifecycle_service import" main.py возвращает строку с импортом
    - grep -n "get_attention_required" main.py возвращает минимум 1 строку вызова
    - grep -n "warning_days_threshold" main.py возвращает минимум 2 строки (selectbox + get)
    - grep -n "Требует внимания" main.py возвращает строку с текстом
  </acceptance_criteria>
  <done>Сайдбар содержит selectbox 30/60/90 дней. Панель «требует внимания» отображается выше реестра при наличии истекающих/истёкших договоров. При отсутствии проблем панель не рендерится.</done>
</task>

<task type="auto">
  <name>Task 2: Статус-иконки и ручной override в таблице реестра</name>
  <files>main.py</files>
  <read_first>
    - main.py (секция с отображением реестра/таблицы договоров, SQL-запрос к БД, структура DataFrame)
  </read_first>
  <action>
Найти в main.py SQL-запрос, который читает договоры из contracts для отображения в таблице реестра.

**1. Изменить SQL-запрос**: добавить computed_status в SELECT:
```python
_warning_days = st.session_state.get("warning_days_threshold", 30)
status_sql = get_computed_status_sql(_warning_days)
# Вставить в существующий SELECT: добавить ", {status_sql} AS computed_status"
# Добавить параметр {"warning_days": _warning_days} в execute()
```

**2. Добавить колонку «Статус» в отображение** — перед основными колонками таблицы добавить:
```python
# Если реестр отображается как st.dataframe или st.table:
# Добавить колонку "Статус" в DataFrame перед рендером
df["Статус"] = df["computed_status"].apply(
    lambda s: STATUS_LABELS.get(s, ("?", s, "#9ca3af"))[0]
    + " "
    + STATUS_LABELS.get(s, ("?", s, "#9ca3af"))[1]
)
```

**3. Добавить контрол ручного статуса** — для каждого договора в детальном режиме (или как отдельный selectbox в строке таблицы через st.data_editor) реализовать:
Если таблица рендерится через st.dataframe — добавить отдельный selectbox под/рядом с таблицей:
```python
# После таблицы: секция ручной коррекции статуса
st.markdown("---")
st.subheader("Ручная коррекция статуса")
col1, col2, col3 = st.columns([2, 2, 1])
with col1:
    # Выбор договора из реестра
    contract_options = {row["filename"]: row["id"] for _, row in df.iterrows()}
    selected_name = st.selectbox("Договор", options=list(contract_options.keys()), key="status_override_contract")
with col2:
    status_options = {
        "Авто (сбросить)": None,
        "Расторгнут": "terminated",
        "Продлён": "extended",
        "На согласовании": "negotiation",
        "Приостановлен": "suspended",
    }
    selected_status_label = st.selectbox("Статус", options=list(status_options.keys()), key="status_override_value")
with col3:
    st.write("")  # выравнивание
    if st.button("Применить", key="status_override_btn"):
        contract_id = contract_options[selected_name]
        new_status = status_options[selected_status_label]
        if new_status is None:
            clear_manual_status(db, contract_id)
        else:
            set_manual_status(db, contract_id, new_status)
        st.rerun()
```

Если в main.py уже есть детальная карточка договора — разместить контрол там вместо отдельной секции под таблицей.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "set_manual_status\|clear_manual_status\|computed_status\|Статус" main.py | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "set_manual_status\|clear_manual_status" main.py возвращает минимум 2 строки вызовов
    - grep -n "computed_status" main.py возвращает минимум 2 строки (в SQL и в df)
    - grep -n "get_computed_status_sql" main.py возвращает минимум 1 строку вызова
    - grep -n "st.rerun" main.py возвращает строку (перезагрузка после изменения статуса)
  </acceptance_criteria>
  <done>Таблица реестра содержит колонку «Статус» с иконками. Контрол ручного override доступен юристу. После применения статуса страница перезагружается через st.rerun(). Ручной статус не перезатирается следующим открытием — хранится в manual_status колонке.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Панель «требует внимания» в верхней части реестра и ручной override статуса.
    - Сайдбар: selectbox «Предупреждать о сроках за» (30/60/90 дней)
    - Панель «требует внимания» с иконками ⚠/✗ и количеством дней до/после истечения
    - Колонка «Статус» в таблице реестра (✔/⚠/✗/↻/...)
    - Секция ручного выбора статуса с кнопкой «Применить»
  </what-built>
  <how-to-verify>
    1. Запустить: streamlit run main.py
    2. Открыть реестр — если есть документы с date_end в прошлом или ближайшие 30 дней, панель «требует внимания» должна быть видна вверху
    3. Убедиться что колонка «Статус» присутствует в таблице с иконками
    4. Выбрать любой договор в секции «Ручная коррекция» → статус «Расторгнут» → нажать «Применить»
    5. Страница перезагружается, статус меняется на «✖ Расторгнут»
    6. Выбрать тот же договор → «Авто (сбросить)» → «Применить» → статус возвращается к автоматическому
    7. Изменить порог в сайдбаре с 30 на 90 — панель должна показать больше/меньше документов
  </how-to-verify>
  <resume-signal>Напиши "approved" или опиши что не работает</resume-signal>
</task>

</tasks>

<verification>
После автоматических задач до checkpoint:
1. grep -n "get_attention_required\|set_manual_status\|get_computed_status_sql" main.py — 3+ строки
2. python -c "import main" — без синтаксических ошибок (если нет Streamlit context, IgnoreError)
</verification>

<success_criteria>
- Панель «требует внимания» отображается только при наличии истекающих/истёкших договоров
- Изменение ручного статуса немедленно отражается в реестре (st.rerun())
- Порог 30/60/90 дней влияет и на панель, и на computed_status в реестре
- Ручной статус не перезатирается автоматикой при следующем открытии приложения
</success_criteria>

<output>
После завершения создать `.planning/phases/02-document-lifecycle/02-02-SUMMARY.md`
</output>
