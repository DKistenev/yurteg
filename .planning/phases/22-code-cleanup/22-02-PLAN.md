---
phase: 22-code-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - tests/conftest.py
  - tests/test_lifecycle.py
  - tests/test_payments.py
  - tests/test_versioning.py
  - tests/test_notifications.py
  - tests/test_design_polish.py
  - tests/test_service_layer.py
  - tests/stress_test.py
  - tests/test_ai_extractor_wiring.py
autonomous: true
requirements:
  - CLEAN-03

must_haves:
  truths:
    - "pytest runs with 0 failures and 0 xfail"
    - "No test depends on SOCKS5 proxy environment variables"
    - "Lifecycle, versioning, payment tests pass without xfail markers"
    - "Streamlit-specific test (test_startup_toast_only_once) is deleted"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Proxy env var cleanup fixture"
      contains: "monkeypatch"
    - path: "tests/test_lifecycle.py"
      provides: "Lifecycle tests without xfail"
    - path: "tests/test_payments.py"
      provides: "Payment tests without xfail"
    - path: "tests/test_versioning.py"
      provides: "Versioning tests without xfail"
  key_links:
    - from: "tests/conftest.py"
      to: "all test files"
      via: "autouse fixture clearing proxy env"
      pattern: "ALL_PROXY|HTTPS_PROXY|HTTP_PROXY|NO_PROXY"
---

<objective>
Fix all failing and xfail tests so `pytest` passes green with zero failures and zero xfail markers.

Purpose: CLEAN-03 — all 270+ tests green, no workarounds, no suppressions.
Output: Clean test suite ready for Phase 23 coverage expansion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-code-cleanup/22-CONTEXT.md
@.planning/phases/22-code-cleanup/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix proxy failures and remove xfail markers</name>
  <files>tests/conftest.py, tests/test_lifecycle.py, tests/test_payments.py, tests/test_versioning.py, tests/test_notifications.py, tests/test_design_polish.py, tests/stress_test.py, tests/test_service_layer.py, tests/test_ai_extractor_wiring.py</files>
  <read_first>
    - tests/conftest.py (full file)
    - tests/test_lifecycle.py (first 140 lines — see xfail markers and test structure)
    - tests/test_payments.py (first 70 lines)
    - tests/test_versioning.py (first 80 lines)
    - tests/test_notifications.py (full file — small, has Streamlit xfail)
    - tests/test_design_polish.py (full file — CSS assertion tests)
    - tests/stress_test.py lines 1-30 and any xfail markers (grep)
    - tests/test_service_layer.py (check if registry_service test was removed in Plan 01, and verify no broken imports)
    - tests/test_ai_extractor_wiring.py (references _try_model — verify still valid after Plan 01)
  </read_first>
  <action>
    CLEAN-03: Fix all test failures. Three categories:

    **Category 1: SOCKS5 proxy failures (10 tests across 3 files)**
    Root cause: System env var `ALL_PROXY=socks5h://localhost:49644` is picked up by httpx when creating OpenAI clients. httpx doesn't support `socks5h://` scheme.

    Fix in tests/conftest.py — add an autouse session-scoped fixture:
    ```python
    @pytest.fixture(autouse=True, scope="session")
    def _clear_proxy_env():
        """Remove proxy env vars that break httpx in tests."""
        import os
        proxy_vars = ["ALL_PROXY", "HTTPS_PROXY", "HTTP_PROXY", "NO_PROXY",
                       "all_proxy", "https_proxy", "http_proxy", "no_proxy"]
        saved = {k: os.environ.pop(k) for k in proxy_vars if k in os.environ}
        yield
        os.environ.update(saved)
    ```

    This fixes: test_factory_zai, test_factory_openrouter, test_zai_thinking_disabled, test_ollama_instantiates (test_providers.py), 4 TestCheckConnection tests (test_settings_templates.py), and 6 stress_test.py tests that create provider instances.

    **Category 2: xfail markers (8+1 tests)**
    These services now work. Remove xfail markers:
    - tests/test_lifecycle.py: remove `@pytest.mark.xfail(...)` on lines 30, 69, 105, 131 (4 tests)
    - tests/test_payments.py: remove `@pytest.mark.xfail(...)` on lines 24, 56 (2 tests)
    - tests/test_versioning.py: remove `@pytest.mark.xfail(...)` on lines 24, 62 (2 tests)
    - tests/test_notifications.py: delete `test_startup_toast_only_once()` function entirely (lines 46-53) — it was a Streamlit-specific placeholder that always asserts False. Keep the other two tests which test lifecycle_service logic.

    After removing xfail, run each file individually to verify they pass:
    ```
    python -m pytest tests/test_lifecycle.py -x -v
    python -m pytest tests/test_payments.py -x -v
    python -m pytest tests/test_versioning.py -x -v
    python -m pytest tests/test_notifications.py -x -v
    ```
    If any test genuinely fails after removing xfail, FIX it (likely needs DB schema or service import update), do NOT re-add xfail.

    **Category 3: CSS assertion tests (2 tests)**
    tests/test_design_polish.py: `test_status_css_slate` and `test_animation_keyframes` assert Tailwind classes exist in app/main.py. Since v0.6+ the app uses tokens.css and `--yt-*` custom properties, not Tailwind classes.

    Options (choose based on what tests actually check):
    - If tests check for CSS patterns that moved to tokens.css or styles.py — update assertions to check the correct file
    - If tests check for Tailwind utility classes that no longer exist — delete the tests (they test an obsolete implementation detail)
    Read the full test file to decide.

    **Category 4: Post-Plan-01 cleanup**
    - tests/test_service_layer.py: verify the registry_service test was removed in Plan 01. If not, remove it now.
    - tests/test_ai_extractor_wiring.py: verify _try_model references still work (Plan 01 kept _try_model alive). No changes expected.
    - tests/stress_test.py: check xfail references in comments (lines 301, 1577, 2312, 2323) — update or remove stale comments about xfail.
    - Any test that imports from deleted main.py or desktop_app.py — delete or rewrite.

    **Final validation:**
    ```
    python -m pytest -x -q
    ```
    Must show 0 failed, 0 xfailed. Some tests may be skipped (acceptable if skip reason is valid like missing optional dependency).
  </action>
  <verify>
    <automated>python -m pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - No FAIL in pytest output: `python -m pytest -q 2>&1 | grep -c "failed"` returns "0" or line contains "0 failed"
    - No xfail markers: `grep -rn "xfail" tests/ --include="*.py" | grep -v "comment\|#\|xfail-" | grep "@pytest.mark.xfail"` returns nothing
    - Proxy fixture exists: `grep "ALL_PROXY" tests/conftest.py` matches
    - Streamlit test deleted: `grep "test_startup_toast_only_once" tests/test_notifications.py` returns nothing
  </acceptance_criteria>
  <done>pytest passes with 0 failures, 0 xfail. All 260+ tests green.</done>
</task>

</tasks>

<verification>
- `python -m pytest -q` — 0 failed, 0 xfailed
- `grep -rn "@pytest.mark.xfail" tests/ --include="*.py"` — no results
- `grep "ALL_PROXY" tests/conftest.py` — proxy cleanup fixture exists
</verification>

<success_criteria>
Full pytest suite passes green. No failures, no xfail suppressions. Test count >= 260.
</success_criteria>

<output>
After completion, create `.planning/phases/22-code-cleanup/22-02-SUMMARY.md`
</output>
