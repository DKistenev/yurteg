---
phase: 22-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - desktop_app.py
  - modules/ai_extractor.py
  - services/registry_service.py
  - config.py
  - modules/reporter.py
  - app/styles.py
  - app/components/ui_helpers.py
  - app/components/skeleton.py
  - app/state.py
  - app/pages/document.py
  - app/pages/templates.py
  - app/pages/settings.py
  - tests/test_service_layer.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "main.py (Streamlit) and desktop_app.py do not exist"
    - "No dead functions remain in ai_extractor.py, ui_helpers.py, skeleton.py"
    - "No unused imports remain in pages and reporter"
    - "AppState has no unused fields"
    - "config.py has no use_prod_model/model_prod/model_dev"
    - "registry_service.py does not exist"
  artifacts:
    - path: "modules/ai_extractor.py"
      provides: "AI extraction without legacy _create_client, _try_model removed only if truly dead; _merge_system_into_user replaced with import"
      contains: "from providers.openrouter import _merge_system_into_user"
    - path: "config.py"
      provides: "Config without dead model toggle"
  key_links:
    - from: "modules/ai_extractor.py"
      to: "providers/openrouter.py"
      via: "import _merge_system_into_user"
      pattern: "from providers.openrouter import _merge_system_into_user"
---

<objective>
Delete dead Streamlit UI (main.py, desktop_app.py), remove unused functions/imports/fields across backend and frontend modules.

Purpose: CLEAN-01 and CLEAN-02 — single entry point (NiceGUI), no dead code in backend/frontend.
Output: Cleaner codebase with only live code remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-code-cleanup/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Streamlit UI and desktop_app.py (CLEAN-01)</name>
  <files>main.py, desktop_app.py</files>
  <read_first>
    - main.py (first 10 lines only — confirm it's Streamlit)
    - desktop_app.py (first 50 lines — confirm it launches Streamlit main.py)
    - Run: grep -rn "from main import\|import main\|desktop_app" --include="*.py" . | grep -v __pycache__ | grep -v test_ — confirm no production imports
  </read_first>
  <action>
    CLEAN-01: Delete main.py (2247-line Streamlit UI) and desktop_app.py (Streamlit launcher via pywebview).

    Both are replaced by NiceGUI app/main.py. Neither is imported by any production code.

    Steps:
    1. `git rm main.py` — Streamlit UI, 2247 lines
    2. `git rm desktop_app.py` — pywebview launcher that references main.py line 42

    Do NOT delete app/main.py — that is the active NiceGUI entry point.
  </action>
  <verify>
    <automated>test ! -f main.py && test ! -f desktop_app.py && test -f app/main.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - main.py does not exist: `test ! -f main.py`
    - desktop_app.py does not exist: `test ! -f desktop_app.py`
    - app/main.py still exists (NiceGUI): `test -f app/main.py`
  </acceptance_criteria>
  <done>Streamlit main.py and desktop_app.py deleted. NiceGUI app/main.py untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead functions, imports, fields (CLEAN-02)</name>
  <files>modules/ai_extractor.py, services/registry_service.py, config.py, modules/reporter.py, app/styles.py, app/components/ui_helpers.py, app/components/skeleton.py, app/state.py, app/pages/document.py, app/pages/templates.py, app/pages/settings.py, tests/test_service_layer.py</files>
  <read_first>
    - modules/ai_extractor.py lines 185-220 (_merge_system_into_user definition)
    - modules/ai_extractor.py lines 350-370 (calls to _merge_system_into_user)
    - providers/openrouter.py lines 1-20 (confirm _merge_system_into_user exists there)
    - config.py lines 1-30 and 145-155 (model_dev/model_prod/use_prod_model/active_model)
    - app/state.py full file
    - app/components/ui_helpers.py full file
    - app/components/skeleton.py full file
    - tests/test_service_layer.py lines 40-55 (registry_service test)
  </read_first>
  <action>
    CLEAN-02: Remove all dead code identified in audit.

    **ai_extractor.py:**
    - Delete `_merge_system_into_user()` definition (lines 189-219)
    - Add `from providers.openrouter import _merge_system_into_user` at module top (with other imports)
    - Do NOT delete `_create_client()` or `_try_model()` — they ARE still called on lines 316, 338, 366, 447, 533, 594 (CONTEXT.md was wrong about these being dead)

    **services/registry_service.py:**
    - Delete the entire file
    - In tests/test_service_layer.py: delete the test function that imports registry_service (around line 45-50). If it's the only test in the class/file, delete the whole test section.

    **config.py:**
    - Delete fields: `model_dev`, `model_prod`, `use_prod_model`
    - Update `active_model` property to return `"glm-4.7"` directly (both were the same value anyway)
    - Grep first to confirm no other file reads these fields

    **modules/reporter.py:**
    - Remove unused imports on lines 302-303: `DataPoint`, `PatternFillProperties`, `ColorChoice` — these are inside a function, only remove if truly unused within that function scope. Read the function first.

    **app/styles.py:**
    - Delete unused constants: BTN_PRIMARY, BTN_FLAT, TEXT_HEADING_XL, TEXT_BODY, SECTION_LABEL, DIVIDER, TEMPLATE_CARD
    - Keep CARD_SECTION (imported in document.py, though unused there — will be cleaned via import removal)

    **app/components/ui_helpers.py:**
    - Delete `form_dialog()` and `section_card()` functions (unused)
    - Keep `empty_state()` — used in registry.py

    **app/components/skeleton.py:**
    - Delete `skeleton_rows()` and `skeleton_card()` functions
    - If no other functions remain, delete the file

    **app/state.py:**
    - Delete unused fields: `filter_type`, `filter_status`, `selected_doc_id`
    - Keep ALL other fields (output_dir, report_path, show_results, force_reprocess, processing_time, upload_dir are used in main.py which is being deleted, but also check if any NiceGUI code uses them)
    - After main.py deletion: re-grep to confirm which fields are now truly dead and remove those too

    **Unused imports in pages:**
    - app/pages/document.py: remove imports CARD_SECTION, TEXT_SUBHEAD, HEX, META_KEY, META_VAL, ACTION_BTN_PRIMARY, generate_redline_docx — verify each is unused with grep
    - app/pages/templates.py: remove imports TEXT_MUTED, TMPL_EMPTY_ICON, APPLE_CARD_ICON — verify each
    - app/pages/settings.py: remove imports TEXT_LABEL_SECTION, APPLE_CARD_COMPACT_ICON, ENTITY_TYPES — verify each
  </action>
  <verify>
    <automated>python -c "import modules.ai_extractor; import config; import app.state; import app.components.ui_helpers; print('imports OK')" && grep -c "_merge_system_into_user" modules/ai_extractor.py | grep -q "^2$" && test ! -f services/registry_service.py && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - _merge_system_into_user NOT defined in ai_extractor.py: `grep "def _merge_system_into_user" modules/ai_extractor.py` returns nothing
    - _merge_system_into_user imported from openrouter: `grep "from providers.openrouter import _merge_system_into_user" modules/ai_extractor.py` matches
    - registry_service.py deleted: `test ! -f services/registry_service.py`
    - config has no model_dev: `grep "model_dev" config.py` returns nothing
    - form_dialog not in ui_helpers: `grep "def form_dialog" app/components/ui_helpers.py` returns nothing
    - All modules import cleanly: `python -c "import modules.ai_extractor; import config; import app.state"` exits 0
  </acceptance_criteria>
  <done>All dead functions, imports, constants, and fields removed. Modules import without errors.</done>
</task>

</tasks>

<verification>
- `test ! -f main.py && test ! -f desktop_app.py` — Streamlit UI gone
- `test ! -f services/registry_service.py` — dead service gone
- `python -c "import modules.ai_extractor; import config; import app.state; import app.components.ui_helpers"` — no import errors
- `grep -rn "def _merge_system_into_user" modules/ai_extractor.py` — not defined there
- `grep "model_dev\|model_prod\|use_prod_model" config.py` — no dead config fields
</verification>

<success_criteria>
main.py and desktop_app.py deleted. All dead functions/imports/fields removed from 10+ files. All remaining modules import cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/22-code-cleanup/22-01-SUMMARY.md`
</output>
