---
phase: 11-settings-templates
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/pages/settings.py
autonomous: true
requirements: [SETT-01, SETT-02, SETT-03, SETT-04]

must_haves:
  truths:
    - "Settings page shows left nav with 3 sections: AI, Обработка, Telegram"
    - "Switching nav highlights active section and renders its content in right panel"
    - "Provider radio saves on change, API key field shows only for cloud providers"
    - "Anonymization checkboxes toggle entity types and save to settings.json"
    - "Warning days number input saves on blur"
    - "Telegram section shows bot token field, server URL, and connection status dot"
    - "Check connection button tests HTTP health and updates status indicator"
  artifacts:
    - path: "app/pages/settings.py"
      provides: "Full settings page with 3 sections"
      min_lines: 120
      contains: "def build"
  key_links:
    - from: "app/pages/settings.py"
      to: "config.py"
      via: "load_settings / save_setting"
      pattern: "load_settings|save_setting"
    - from: "app/pages/settings.py"
      to: "modules/anonymizer.py"
      via: "ENTITY_TYPES import"
      pattern: "ENTITY_TYPES"
    - from: "app/pages/settings.py"
      to: "services/telegram_sync.py"
      via: "TelegramSync.check_connection"
      pattern: "check_connection"
---

<objective>
Build the Settings page with macOS Preferences layout (left nav + right panel) containing 3 sections: AI provider, Обработка, Telegram.

Purpose: Юрист управляет провайдером, анонимизацией и Telegram-ботом без ручного редактирования файлов.
Output: app/pages/settings.py fully implemented.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-settings-templates/11-RESEARCH.md
@.planning/phases/11-settings-templates/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — settings persistence -->
From config.py (added in Plan 01):
```python
_SETTINGS_FILE = Path.home() / ".yurteg" / "settings.json"

def load_settings() -> dict: ...
def save_setting(key: str, value) -> None: ...
```

From modules/anonymizer.py:
```python
ENTITY_TYPES = {
    "ФИО": "Фамилия, имя, отчество",
    "ТЕЛЕФОН": "Номера телефонов",
    "EMAIL": "Электронная почта",
    "ПАСПОРТ": "Серия и номер паспорта",
    "СНИЛС": "Номер СНИЛС",
    "ИНН": "ИНН физлиц и юрлиц",
    "ОГРН": "ОГРН организаций",
    "КПП": "КПП организаций",
    "СЧЁТ": "Банковские счета",
}
```

From services/telegram_sync.py (added in Plan 01):
```python
class TelegramSync:
    def __init__(self, server_url: str, chat_id: int) -> None
    def is_configured(self) -> bool
    def check_connection(self) -> bool  # GET {base}/health, timeout=5s
```

From app/state.py:
```python
def get_state() -> AppState  # per-connection state from app.storage.client
```

From app/main.py routing:
```python
"/settings": settings.build  # already wired in ui.sub_pages
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page — left nav + AI section</name>
  <files>app/pages/settings.py</files>
  <read_first>
    - app/pages/settings.py (current stub — will be fully replaced)
    - app/pages/registry.py lines 1-30 (import patterns for NiceGUI pages)
    - app/components/process.py lines 1-20 (run.io_bound pattern)
    - config.py (load_settings, save_setting — added in Plan 01)
    - modules/anonymizer.py lines 26-36 (ENTITY_TYPES dict)
    - services/telegram_sync.py (TelegramSync class with check_connection)
  </read_first>
  <action>
    Replace app/pages/settings.py stub with full implementation. Single `build()` function, no arguments.

    **Layout per D-01, D-02:** `ui.row` with two columns:
    - Left: `ui.column` w-48, border-r, bg-gray-50, p-4 — 3 `ui.button` items ("AI", "Обработка", "Telegram")
    - Right: `ui.column` flex-1, p-8 — dynamic content via `content.clear()` + `with content:`

    **Active nav highlight per Pitfall 7:** Store dict of nav buttons. On `_switch(section)`:
    - Reset all buttons to `text-gray-600 bg-transparent`
    - Set active button to `text-gray-900 bg-white shadow-sm rounded-lg`

    **AI section per D-04, D-05, D-06:**
    - `ui.label("AI-провайдер").classes("text-lg font-medium text-gray-900")`
    - `ui.radio` with options `{"ollama": "Локальный (Qwen)", "zai": "ZAI GLM-4.7", "openrouter": "OpenRouter"}`, value from `load_settings().get("active_provider", "ollama")`
    - `on_change` callback: call `save_setting("active_provider", e.value)` and toggle API key row visibility
    - API key row (per D-05): `ui.input(label="API-ключ", password=True, password_toggle_button=True)` shown only when provider != "ollama"
    - API key `.on("blur", lambda e: save_setting(f"api_key_{provider}", e.sender.value))`
    - Load initial key value from `load_settings().get(f"api_key_{current_provider}", "")`

    **Обработка section per D-07, D-08:**
    - `ui.label("Анонимизация").classes("text-lg font-medium text-gray-900")`
    - `ui.label("Какие типы ПД маскировать при отправке в облачный AI:").classes("text-sm text-gray-500 mb-2")`
    - For each key, label in ENTITY_TYPES: `ui.checkbox(label, value=key in current_types, on_change=...)` where current_types = set from load_settings or all keys by default
    - Checkbox on_change: toggle key in set, `save_setting("anonymize_types", list(types))`
    - Separator
    - `ui.label("Предупреждения").classes("text-base font-medium text-gray-900 mt-4")`
    - `ui.number(label="За сколько дней предупреждать об истечении", value=warning_days, min=1, max=365)` with `.on("blur", lambda e: save_setting("warning_days", int(e.sender.value)))`

    **Telegram section per D-09, D-10, D-11:**
    - `ui.label("Telegram-бот").classes("text-lg font-medium text-gray-900")`
    - `ui.input(label="URL сервера бота", value=...).on("blur", save)`
    - `ui.input(label="Токен бота", password=True, value=...).on("blur", save)`
    - Status row: `ui.label("● Не подключён").classes("text-sm text-red-500")` as status_label
    - `ui.button("Проверить подключение", on_click=_check_telegram).props("flat no-caps size=sm")`
    - `_check_telegram` is async: reads server_url from settings, creates TelegramSync, calls `await run.io_bound(tg.check_connection)`, updates status_label text and classes (green-600 / red-500)

    **Key anti-patterns to avoid:**
    - Do NOT use `ui.tabs` — use `ui.button` + `content.clear()` per Pattern 1
    - Do NOT use `Config()` for persistence — use `load_settings()` / `save_setting()` per Pitfall 3
    - Do NOT use `on_change` for text inputs — use `.on("blur", cb)` per Pitfall 5
    - Dot indicator for Telegram status (per specifics): green/red dot character "●" NOT a badge
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from app.pages.settings import build; print('settings.build imported OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def build" app/pages/settings.py
    - grep -q "load_settings\|save_setting" app/pages/settings.py
    - grep -q "ENTITY_TYPES" app/pages/settings.py
    - grep -q "check_connection" app/pages/settings.py
    - grep -q "content.clear()" app/pages/settings.py
    - grep -q "ui.radio" app/pages/settings.py
    - grep -q "password" app/pages/settings.py
    - grep -qv "ui.tabs" app/pages/settings.py (must NOT contain ui.tabs)
    - wc -l app/pages/settings.py shows >= 120 lines
    - python -c "from app.pages.settings import build" exits 0
  </acceptance_criteria>
  <done>
    - Settings page renders with left nav (AI / Обработка / Telegram)
    - Clicking nav item switches right panel content, active button highlighted
    - AI section: radio buttons for provider, conditional API key field, save on change/blur
    - Обработка section: 9 anonymization checkboxes, warning_days number input
    - Telegram section: server URL, token, status dot, check connection button
    - All settings persist to ~/.yurteg/settings.json
  </done>
</task>

</tasks>

<verification>
python -c "from app.pages.settings import build; print('OK')"
grep -c "load_settings\|save_setting\|ENTITY_TYPES\|check_connection" app/pages/settings.py
</verification>

<success_criteria>
- Settings page imports without error
- Contains all 3 sections with correct widgets
- Uses load_settings/save_setting for persistence (NOT Config())
- Uses .on("blur") for text inputs (NOT on_change)
- Uses ui.button nav (NOT ui.tabs)
</success_criteria>

<output>
After completion, create `.planning/phases/11-settings-templates/11-02-SUMMARY.md`
</output>
