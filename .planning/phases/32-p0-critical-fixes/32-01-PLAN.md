---
phase: 32-p0-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
  - app/components/registry_table.py
  - app/pages/registry.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03

must_haves:
  truths:
    - "IBM Plex Sans шрифт отображается в приложении (не системный fallback)"
    - "AG Grid не выдаёт deprecation warnings в консоли при рендере таблицы"
    - "Виджет дедлайнов вызывается ровно один раз при инициализации реестра"
  artifacts:
    - path: "app/main.py"
      provides: "app.add_static_files('/static', ...) вызывается до ui.run()"
      contains: "add_static_files"
    - path: "app/components/registry_table.py"
      provides: "rowSelection как объект с mode/checkboxes/headerCheckbox"
      contains: "\"mode\": \"multiRow\""
    - path: "app/pages/registry.py"
      provides: "_refresh_deadline_widget вызывается по одному разу в _init и _delete_bulk"
  key_links:
    - from: "app/main.py"
      to: "/static/fonts/IBMPlexSans-Regular.woff2"
      via: "app.add_static_files('/static', _STATIC)"
      pattern: "add_static_files.*static"
    - from: "app/components/registry_table.py"
      to: "AG Grid rowSelection config"
      via: "rowSelection dict в ui.aggrid options"
      pattern: "\"mode\".*multiRow"
---

<objective>
Устранить 3 критических бага из аудита, которые делают приложение сломанным на первый взгляд: шрифты не подаются браузеру, AG Grid выдаёт deprecation warnings, виджет дедлайнов вызывается дважды.

Purpose: Приложение должно выглядеть правильно при первом запуске — готово к хакатону.
Output: Три файла исправлены, три бага устранены.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend/STATE.md
@.planning/workstreams/frontend/ROADMAP.md
@.planning/phases/32-p0-critical-fixes/32-CONTEXT.md

<interfaces>
<!-- Ключевые фрагменты кода для executor — читать до внесения правок. -->

From app/main.py (строки 85-130):
```python
_STATIC = _Path(__file__).parent / "static"

# Font (must load before any rendered element) — served locally, no CDN
ui.add_head_html("""
<style>
@font-face {
  font-family: 'IBM Plex Sans';
  ...
  src: url('/static/fonts/IBMPlexSans-Regular.woff2') format('woff2');
}
...
</style>
""", shared=True)

# tokens.css, design-system.css загружаются после шрифтов
```
ПРОБЛЕМА: `app.add_static_files("/static", str(_STATIC))` НЕ вызывается нигде в файле.
Шрифты @font-face ссылаются на /static/fonts/... но NiceGUI не знает где их искать.

From app/components/registry_table.py (строки 392-441):
```python
grid = ui.aggrid(
    {
        "columnDefs": COLUMN_DEFS,
        "rowData": [],
        ...
        "rowSelection": "multiple",          # <- УСТАРЕВШИЙ синтаксис AG Grid v32+
        "suppressRowClickSelection": True,    # <- УСТАРЕВШИЙ, нужно перенести в rowSelection
        ...
    },
    ...
)
```
COLUMN_DEFS[0] (строки 67-81):
```python
{
    "headerName": "",
    "field": "selected",
    "checkboxSelection": True,           # <- УСТАРЕВШИЙ, нужно перенести в rowSelection
    "headerCheckboxSelection": True,      # <- УСТАРЕВШИЙ, нужно перенести в rowSelection
    ...
},
```

From app/pages/registry.py (строки 1129-1138, 1203-1205):
```python
# _delete_bulk:
await _refresh_deadline_widget()  # строка 1137
await _refresh_deadline_widget()  # строка 1138  <- ДУБЛЬ, удалить

# _init:
await _refresh_deadline_widget()  # строка 1204
await _refresh_deadline_widget()  # строка 1205  <- ДУБЛЬ, удалить
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Зарегистрировать static files в NiceGUI (AUDIT-01, D-01)</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py строки 82-130 — текущая загрузка шрифтов и CSS, где стоит _STATIC
  </read_first>
  <action>
    В app/main.py после строки `_STATIC = _Path(__file__).parent / "static"` (строка ~87)
    добавить вызов:

    ```python
    app.add_static_files("/static", str(_STATIC))
    ```

    Вставить ЭТО СТРОКОЙ НИЖЕ объявления `_STATIC`, ДО первого `ui.add_head_html` вызова.
    Импорт `app` уже есть в строке 13: `from nicegui import app, run, ui`.
    Ничего больше не менять — шрифт-файлы уже лежат в app/static/fonts/, @font-face
    ссылается на /static/fonts/... — после этой строки NiceGUI начнёт их отдавать.

    Per D-01: добавить сразу после `_STATIC = ...`, не в конце файла.
  </action>
  <verify>
    <automated>python -c "
import ast, pathlib
src = pathlib.Path('app/main.py').read_text()
tree = ast.parse(src)
calls = [n for n in ast.walk(tree) if isinstance(n, ast.Call)]
found = any(
    (isinstance(c.func, ast.Attribute) and c.func.attr == 'add_static_files')
    for c in calls
)
assert found, 'add_static_files NOT FOUND in main.py'
print('OK: add_static_files found')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "add_static_files" app/main.py` возвращает строку с `/static`
    - Строка находится ПОСЛЕ `_STATIC = _Path(...)` и ДО первого `ui.add_head_html`
    - `python -c "import app.main"` не падает с ImportError
  </acceptance_criteria>
  <done>app/main.py содержит `app.add_static_files("/static", str(_STATIC))` после объявления _STATIC</done>
</task>

<task type="auto">
  <name>Task 2: Мигрировать AG Grid rowSelection на v32.2 API (AUDIT-02, D-03, D-04, D-05)</name>
  <files>app/components/registry_table.py</files>
  <read_first>
    - app/components/registry_table.py строки 67-81 — COLUMN_DEFS[0] с checkbox полями
    - app/components/registry_table.py строки 392-441 — render_registry_table с gridOptions
  </read_first>
  <action>
    Два изменения в registry_table.py:

    **1. COLUMN_DEFS[0] (строки ~67-81):** Убрать поля `checkboxSelection` и `headerCheckboxSelection`.
    Оставшийся объект:
    ```python
    {
        "headerName": "",
        "field": "selected",
        "width": 40,
        "maxWidth": 40,
        "pinned": "left",
        "sortable": False,
        "filter": False,
        "resizable": False,
        "suppressSizeToFit": True,
    },
    ```

    **2. В render_registry_table() (строки ~399-438):** Заменить:
    ```python
    "rowSelection": "multiple",
    "suppressRowClickSelection": True,
    ```
    На:
    ```python
    "rowSelection": {
        "mode": "multiRow",
        "checkboxes": True,
        "headerCheckbox": True,
        "suppressRowClickSelection": True,
    },
    ```
    Строку `"suppressRowClickSelection": True` на верхнем уровне gridOptions — УДАЛИТЬ
    (она теперь внутри rowSelection объекта).

    Per D-03, D-04, D-05: это полная миграция на AG Grid 32.2 rowSelection API.
  </action>
  <verify>
    <automated>python -c "
import pathlib
src = pathlib.Path('app/components/registry_table.py').read_text()
assert '\"mode\": \"multiRow\"' in src or \"'mode': 'multiRow'\" in src, 'rowSelection mode:multiRow not found'
assert 'checkboxSelection' not in src, 'checkboxSelection still in COLUMN_DEFS (should be removed)'
print('OK: AG Grid rowSelection migrated')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "checkboxSelection" app/components/registry_table.py` — пустой результат (поле удалено из COLUMN_DEFS)
    - `grep -n "\"mode\"" app/components/registry_table.py` — находит `"mode": "multiRow"` в rowSelection
    - `grep -n "suppressRowClickSelection" app/components/registry_table.py` — находит только ВНУТРИ rowSelection объекта
    - `python -c "from app.components.registry_table import COLUMN_DEFS, render_registry_table"` не падает
  </acceptance_criteria>
  <done>COLUMN_DEFS не содержит checkboxSelection/headerCheckboxSelection; rowSelection в gridOptions — объект с mode/checkboxes/headerCheckbox/suppressRowClickSelection</done>
</task>

<task type="auto">
  <name>Task 3: Удалить дублирующие вызовы _refresh_deadline_widget (AUDIT-03, D-06, D-07)</name>
  <files>app/pages/registry.py</files>
  <read_first>
    - app/pages/registry.py строки 1129-1145 — функция _delete_bulk, двойной вызов на строках 1137-1138
    - app/pages/registry.py строки 1203-1214 — функция _init, двойной вызов на строках 1204-1205
  </read_first>
  <action>
    Удалить два дублирующих вызова в registry.py:

    **1. В функции _delete_bulk (~строка 1138):**
    Найти блок:
    ```python
    await _refresh_deadline_widget()
    await _refresh_deadline_widget()
    ```
    Оставить только ПЕРВЫЙ вызов (строка 1137), удалить ВТОРОЙ (строка 1138).

    **2. В функции _init (~строка 1205):**
    Найти блок:
    ```python
    await _refresh_deadline_widget()
    await _refresh_deadline_widget()
    ```
    Оставить только ПЕРВЫЙ вызов (строка 1204), удалить ВТОРОЙ (строка 1205).

    Per D-06 и D-07. Никакой другой логики не менять.
  </action>
  <verify>
    <automated>python -c "
import pathlib
src = pathlib.Path('app/pages/registry.py').read_text()
lines = src.splitlines()
consecutive_doubles = 0
for i in range(len(lines) - 1):
    if ('_refresh_deadline_widget()' in lines[i] and
        '_refresh_deadline_widget()' in lines[i+1]):
        consecutive_doubles += 1
assert consecutive_doubles == 0, f'Found {consecutive_doubles} consecutive duplicate _refresh_deadline_widget calls'
print('OK: no duplicate consecutive calls')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Нет двух подряд идущих строк с `_refresh_deadline_widget()` в registry.py
    - `grep -c "_refresh_deadline_widget()" app/pages/registry.py` возвращает 3 или меньше (было 4 дубля)
    - `python -c "import ast; ast.parse(open('app/pages/registry.py').read())"` не падает
  </acceptance_criteria>
  <done>В _delete_bulk и _init остался ровно один вызов _refresh_deadline_widget() каждый; дубли удалены</done>
</task>

</tasks>

<verification>
После выполнения всех задач:

1. `python -c "import app.main"` — нет ImportError
2. `grep "add_static_files" app/main.py` — есть строка с `/static`
3. `grep "mode.*multiRow\|multiRow.*mode" app/components/registry_table.py` — находит
4. `python -c "
import pathlib
src = pathlib.Path('app/pages/registry.py').read_text()
lines = src.splitlines()
for i in range(len(lines)-1):
    if '_refresh_deadline_widget()' in lines[i] and '_refresh_deadline_widget()' in lines[i+1]:
        raise AssertionError(f'Duplicate at lines {i+1}-{i+2}')
print('No duplicates')
"`
5. `pytest -x -q` — тесты не падают (регрессий нет)
</verification>

<success_criteria>
- IBM Plex Sans доступен по URL `/static/fonts/IBMPlexSans-Regular.woff2` при запуске приложения
- AG Grid таблица рендерится без deprecation warnings (`checkboxSelection deprecated` и `rowSelection: "multiple" deprecated` исчезают)
- Виджет дедлайнов вызывается ровно один раз при _init и один раз при _delete_bulk
</success_criteria>

<output>
После завершения создать `.planning/phases/32-p0-critical-fixes/32-01-SUMMARY.md`
</output>
