---
phase: 07-app-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/__init__.py
  - app/state.py
  - app/pages/__init__.py
  - app/pages/registry.py
  - app/pages/document.py
  - app/pages/templates.py
  - app/pages/settings.py
  - app/components/__init__.py
  - app/components/header.py
  - tests/test_app_scaffold.py
autonomous: true
requirements:
  - FUND-01
  - FUND-02

must_haves:
  truths:
    - "AppState dataclass содержит все 20 полей с правильными типами и дефолтами"
    - "get_state() возвращает AppState из app.storage.client"
    - "Каждая страница имеет build() функцию"
    - "Header рендерит три навигационных ссылки: Документы, Шаблоны, ⚙"
  artifacts:
    - path: "app/state.py"
      provides: "AppState dataclass + get_state() accessor"
      exports: ["AppState", "get_state"]
    - path: "app/components/header.py"
      provides: "Persistent header с Linear/Notion стилем"
      exports: ["render_header"]
    - path: "app/pages/registry.py"
      provides: "Документы placeholder page"
      exports: ["build"]
    - path: "app/pages/templates.py"
      provides: "Шаблоны placeholder page"
      exports: ["build"]
    - path: "app/pages/settings.py"
      provides: "Настройки placeholder page"
      exports: ["build"]
    - path: "tests/test_app_scaffold.py"
      provides: "Unit tests for AppState and imports"
      min_lines: 30
  key_links:
    - from: "app/components/header.py"
      to: "app/state.py"
      via: "import AppState"
      pattern: "from app.state import"
    - from: "app/pages/registry.py"
      to: "app/state.py"
      via: "import get_state"
      pattern: "from app.state import"
---

<objective>
Create the app/ directory scaffold with AppState dataclass, page placeholders, persistent header, and unit tests.

Purpose: Establish the typed state model and page module pattern that all subsequent phases (8-13) will build upon. Per D-01, D-02, D-04, D-05.
Output: app/state.py, app/pages/*.py, app/components/header.py, tests/test_app_scaffold.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-app-scaffold/07-CONTEXT.md
@.planning/phases/07-app-scaffold/07-RESEARCH.md

<interfaces>
<!-- Existing config.py interface used by state.py for defaults -->
From config.py:
```python
@dataclass
class Config:
    active_provider: str = "ollama"        # "zai" | "openrouter" | "ollama"
    fallback_provider: str = "zai"
    llama_server_port: int = 8080
    warning_days_threshold: int = 30
    max_workers: int = 5
```

From services/client_manager.py:
```python
class ClientManager:
    @staticmethod
    def list_clients() -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppState dataclass and page module stubs</name>
  <files>
    app/__init__.py,
    app/state.py,
    app/pages/__init__.py,
    app/pages/registry.py,
    app/pages/document.py,
    app/pages/templates.py,
    app/pages/settings.py,
    app/components/__init__.py
  </files>
  <read_first>
    config.py (existing Config dataclass — verify field names for AppState defaults),
    main.py lines 750-970 (existing st.session_state keys — source of truth for AppState fields),
    .planning/phases/07-app-scaffold/07-RESEARCH.md (Pattern 1 AppState field mapping, Pattern 2 page build() signature)
  </read_first>
  <action>
    1. Create `app/__init__.py` — empty file.

    2. Create `app/state.py` per D-04 and D-05:
       - `@dataclass` class `AppState` with exactly these 20 fields:
         - Processing: `source_dir: str = ""`, `output_dir: Optional[Path] = None`, `report_path: Optional[Path] = None`, `show_results: bool = False`, `force_reprocess: bool = False`, `processing: bool = False`, `processing_time: Optional[float] = None`, `upload_dir: Optional[Path] = None`
         - Settings cache: `warning_days_threshold: int = 30`, `telegram_chat_id: int = 0`, `telegram_server_url: str = ""`, `tg_queue_fetched: bool = False`, `startup_toast_shown: bool = False`, `deadlines_pushed: bool = False`, `auto_bind_summary: Optional[dict] = None`
         - Navigation: `current_client: str = "Основной реестр"`, `selected_doc_id: Optional[int] = None`
         - Filters: `filter_type: str = ""`, `filter_status: str = ""`, `filter_search: str = ""`
       - Function `get_state() -> AppState`: imports `app` from nicegui, checks `'state' not in app.storage.client`, creates `AppState()` if missing, returns `app.storage.client['state']`

    3. Create `app/pages/__init__.py` — empty file.

    4. Create `app/pages/registry.py` per D-02:
       - Function `build()` (no parameters — calls `get_state()` internally per Research Open Question 3).
       - Body: `with ui.column().classes('w-full p-8'):` containing `ui.label('Документы').classes('text-2xl font-semibold text-gray-900')` and `ui.label('Реестр документов — Phase 8').classes('text-gray-400 text-sm')`

    5. Create `app/pages/document.py`:
       - Function `build(doc_id: str)` — accepts doc_id from URL path parameter.
       - Body: placeholder label "Карточка документа — Phase 9" with doc_id displayed.

    6. Create `app/pages/templates.py`:
       - Function `build()` — placeholder label "Шаблоны — Phase 11".

    7. Create `app/pages/settings.py`:
       - Function `build()` — placeholder label "Настройки — Phase 11".

    8. Create `app/components/__init__.py` — empty file.

    All page modules import `from nicegui import ui`. All use `from app.state import get_state` inside build() to access state.
  </action>
  <verify>
    <automated>cd "/Users/danilakistenev/Downloads/Личное/ЮР тэг/yurteg" && python -c "from app.state import AppState, get_state; s = AppState(); assert s.current_client == 'Основной реестр'; assert s.warning_days_threshold == 30; assert s.filter_search == ''; print('AppState OK:', len(AppState.__dataclass_fields__), 'fields')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def build" app/pages/registry.py` returns 1
    - `grep -c "def build" app/pages/templates.py` returns 1
    - `grep -c "def build" app/pages/settings.py` returns 1
    - `grep -c "def build" app/pages/document.py` returns 1
    - `grep "class AppState" app/state.py` matches
    - `grep "def get_state" app/state.py` matches
    - `grep "current_client.*Основной реестр" app/state.py` matches
    - `python -c "from app.state import AppState; assert len(AppState.__dataclass_fields__) == 20"` exits 0
    - `grep "app.storage.client" app/state.py` matches (per D-05)
  </acceptance_criteria>
  <done>AppState has 20 typed fields with correct defaults. Four page modules have build() functions. get_state() uses app.storage.client per D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Create persistent header component and test scaffold</name>
  <files>
    app/components/header.py,
    tests/test_app_scaffold.py
  </files>
  <read_first>
    app/state.py (just created — verify AppState import path),
    .planning/phases/07-app-scaffold/07-RESEARCH.md (Pattern 5 header code, D-12/13/14 requirements),
    services/client_manager.py lines 1-30 (ClientManager.list_clients interface for profile selector)
  </read_first>
  <action>
    1. Create `app/components/header.py` per D-12, D-13, D-14:
       - Function `render_header(state: AppState)`:
         - `with ui.header().classes('bg-white border-b border-gray-200 px-6 py-0 flex items-center gap-8 h-12'):`
         - Left: `ui.label('ЮрТэг').classes('text-base font-semibold text-gray-900 shrink-0')`
         - Center: `with ui.row().classes('gap-6 flex-1 justify-center'):` containing three `_nav_link()` calls: `_nav_link('Документы', '/')`, `_nav_link('Шаблоны', '/templates')`, `_nav_link('⚙', '/settings')`
         - Right: `with ui.row().classes('shrink-0 items-center gap-1 cursor-pointer'):` containing profile indicator using state.current_client
       - Helper `_nav_link(label: str, path: str)`:
         - `ui.link(label, path).classes('text-sm text-gray-600 hover:text-gray-900 no-underline border-b-2 border-transparent hover:border-gray-900 pb-0.5')`
       - Import `from app.state import AppState` at top.
       - Import `from nicegui import ui` at top.

    2. Create `tests/test_app_scaffold.py`:
       - Test `test_appstate_has_all_fields`: instantiate `AppState()`, assert exactly 20 fields in `AppState.__dataclass_fields__`, assert each default value matches spec (source_dir=="", current_client=="Основной реестр", warning_days_threshold==30, processing==False, selected_doc_id is None, etc.)
       - Test `test_appstate_field_types`: verify field annotations match expected types using `AppState.__dataclass_fields__[name].type`
       - Test `test_page_modules_importable`: `from app.pages import registry, document, templates, settings` — assert each has callable `build` attribute
       - Test `test_header_importable`: `from app.components.header import render_header` — assert callable
       - Test `test_state_module_importable`: `from app.state import AppState, get_state` — assert both exist
       - All tests use standard pytest (no NiceGUI server needed — import-level tests only). Skip get_state() runtime test (requires NiceGUI context).
  </action>
  <verify>
    <automated>cd "/Users/danilakistenev/Downloads/Личное/ЮР тэг/yurteg" && python -m pytest tests/test_app_scaffold.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "render_header" app/components/header.py` matches
    - `grep "_nav_link" app/components/header.py` matches
    - `grep "ЮрТэг" app/components/header.py` matches (logo text per D-13)
    - `grep "Документы" app/components/header.py` matches (tab label per D-13)
    - `grep "Шаблоны" app/components/header.py` matches (tab label per D-13)
    - `grep "border-b border-gray-200" app/components/header.py` matches (Linear/Notion style)
    - `python -m pytest tests/test_app_scaffold.py -x -q` all pass
    - `grep -c "def test_" tests/test_app_scaffold.py` returns at least 5
  </acceptance_criteria>
  <done>Header renders three nav links (Документы, Шаблоны, ⚙) in Linear/Notion style. Test scaffold covers AppState fields, imports, and page module availability. All tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_app_scaffold.py -x -q` — all tests pass
- `python -c "from app.state import AppState; print(len(AppState.__dataclass_fields__))"` — prints 20
- `python -c "from app.components.header import render_header; print('header OK')"` — no import errors
- `python -c "from app.pages import registry, templates, settings, document; print('pages OK')"` — no import errors
</verification>

<success_criteria>
- app/ directory exists with state.py, pages/, components/
- AppState has exactly 20 fields per research mapping
- Four page modules each have build() function
- Header component renders ЮрТэг logo + 3 nav links + profile area
- All tests in test_app_scaffold.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-app-scaffold/07-01-SUMMARY.md`
</output>
