---
phase: 05-pipeline-local-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/ai_extractor.py
  - controller.py
  - main.py
autonomous: true
requirements: [PROV-03, PROC-01, PROC-02]

must_haves:
  truths:
    - "Документ обработан локальной моделью — метаданные чистые, без мусора и строк None"
    - "Анонимизация пропускается при active_provider=ollama — ПД не маскируются без нужды"
    - "Пользователь переключает провайдер через UI — следующие документы идут через выбранный провайдер"
  artifacts:
    - path: "modules/ai_extractor.py"
      provides: "sanitize_metadata вызов после ответа локальной модели"
      contains: "sanitize_metadata"
    - path: "controller.py"
      provides: "условный пропуск anonymize для ollama"
      contains: "active_provider"
    - path: "main.py"
      provides: "selectbox переключателя провайдера в sidebar"
      contains: "active_provider"
  key_links:
    - from: "modules/ai_extractor.py"
      to: "modules/postprocessor.py"
      via: "import sanitize_metadata"
      pattern: "from modules\\.postprocessor import sanitize_metadata"
    - from: "controller.py"
      to: "config.active_provider"
      via: "условие пропуска anonymize"
      pattern: "active_provider.*ollama"
    - from: "main.py"
      to: "config.active_provider"
      via: "selectbox в sidebar"
      pattern: "st\\.selectbox.*Провайдер"
---

<objective>
Подключить post-processing ответов локальной модели, пропустить анонимизацию для локального провайдера, и добавить UI-переключатель провайдера в sidebar.

Purpose: Документы обрабатываются end-to-end через локальную LLM без мусора в метаданных и без лишней анонимизации. Пользователь может переключиться на облако через UI.
Output: Три модифицированных файла — ai_extractor.py, controller.py, main.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline-local-llm/05-CONTEXT.md
@.planning/phases/04-server-provider/04-01-SUMMARY.md
@.planning/phases/04-server-provider/04-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From modules/postprocessor.py:
```python
def sanitize_metadata(metadata: ContractMetadata) -> ContractMetadata:
    """Применяет профили FIELD_PROFILES к каждому полю ContractMetadata.
    Модифицирует объект in-place И возвращает его."""
```

From modules/anonymizer.py:
```python
@dataclass
class AnonymizedText:
    text: str                        # Анонимизированный текст
    replacements: dict[str, str]     # маска -> исходное значение
    stats: dict[str, int]            # {"ФИО": 3, "ТЕЛЕФОН": 2, ...}

def anonymize(text: str, types: Optional[set[str]] = None) -> AnonymizedText:
```

From providers/base.py:
```python
class LLMProvider(ABC):
    name: str  # "zai" | "openrouter" | "ollama"
    def complete(self, messages: list[dict], **kwargs) -> str: ...
    def verify_key(self) -> bool: ...
```

From config.py:
```python
@dataclass
class Config:
    active_provider: str = "ollama"   # "zai" | "openrouter" | "ollama"
    fallback_provider: str = "zai"
```

From controller.py (current anonymize call, line ~160):
```python
anonymized = anonymize(text.text, self.config.anonymize_types)
result.anonymized = anonymized
prepared.append((result, anonymized))
```

From controller.py (current de-anonymize block, line ~197):
```python
if anonymized.replacements:
    if metadata.counterparty:
        metadata.counterparty = _deanonymize(metadata.counterparty, anonymized.replacements)
    if metadata.parties:
        metadata.parties = [_deanonymize(p, anonymized.replacements) for p in metadata.parties]
```

From main.py sidebar (line ~631-636, existing "Настройки" section):
```python
st.markdown('<div style="font-size:0.7rem;...">Настройки</div>', unsafe_allow_html=True)
tab_main, tab_anon = st.tabs(["📂 Структура", "🔒 Анонимизация"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Подключить sanitize_metadata и пропуск анонимизации в пайплайне</name>
  <files>modules/ai_extractor.py, controller.py</files>
  <read_first>
    - modules/ai_extractor.py (full — understand extract_metadata flow)
    - controller.py (full — understand _run_pipeline stages A/B/C)
    - modules/postprocessor.py (first 50 lines — sanitize_metadata signature)
    - modules/anonymizer.py (first 35 lines — AnonymizedText dataclass)
    - config.py (active_provider field)
  </read_first>
  <action>
**ai_extractor.py — sanitize_metadata после ответа локальной модели (per D-01, D-02):**

1. Добавить импорт вверху файла:
   ```python
   from modules.postprocessor import sanitize_metadata
   ```

2. В функции `extract_metadata()`, после строки `return result` (строка ~316) и после проверки пустых полей, добавить вызов sanitize_metadata перед каждым return ContractMetadata. Точнее: нужно обернуть ОБА return-а (строки ~315 и ~316) — при успешном ответе основной модели. Самый чистый подход: добавить sanitize ПЕРЕД финальными return-ами.

   Конкретно: после `result = _try_model(...)` (строка ~304), если result — ContractMetadata, перед return вызвать:
   ```python
   if isinstance(result, ContractMetadata) and config.active_provider == "ollama":
       sanitize_metadata(result)
   ```

   Аналогично для fallback внутри основной модели (строка ~314, `fb = _try_model(...)`):
   ```python
   if isinstance(fb, ContractMetadata) and (fb.contract_type or fb.counterparty):
       if config.active_provider == "ollama":
           sanitize_metadata(fb)
       return fb
   ```

   И для fallback-модели OpenRouter (строка ~329):
   НЕ применять sanitize для OpenRouter — облачные провайдеры возвращают чистые ответы (per D-02).

**controller.py — пропуск анонимизации для ollama (per D-03, D-04, D-05, D-06):**

3. В `_run_pipeline()`, Этап A (строка ~160), заменить безусловный вызов `anonymize()` на условный:
   ```python
   if self.config.active_provider == "ollama":
       # Локальная модель — ПД не покидают машину, анонимизация не нужна
       anonymized = AnonymizedText(
           text=text.text,
           replacements={},
           stats={},
       )
   else:
       anonymized = anonymize(text.text, self.config.anonymize_types)
   ```

   Для этого добавить импорт AnonymizedText вверху controller.py:
   ```python
   from modules.models import ProcessingResult, AnonymizedText
   ```
   (ProcessingResult уже импортируется — добавить AnonymizedText в тот же импорт)

4. Де-анонимизация (строки ~197-206) уже защищена условием `if anonymized.replacements:` — при пустом replacements ничего не выполняется. Никаких изменений не нужно (per D-05).
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from modules.ai_extractor import sanitize_metadata
print('OK: sanitize_metadata imported in ai_extractor')
" && python -c "
from modules.models import AnonymizedText
a = AnonymizedText(text='test', replacements={}, stats={})
assert a.replacements == {}
print('OK: AnonymizedText with empty replacements works')
" && grep -n 'sanitize_metadata' modules/ai_extractor.py && grep -n 'active_provider.*ollama' controller.py && grep -n 'AnonymizedText' controller.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sanitize_metadata' modules/ai_extractor.py` returns >= 2 (import + usage)
    - `grep 'from modules.postprocessor import sanitize_metadata' modules/ai_extractor.py` matches
    - `grep 'active_provider.*ollama' controller.py` matches (anonymization skip condition)
    - `grep 'AnonymizedText' controller.py` matches (import + usage for empty wrapper)
    - `grep 'replacements={}' controller.py` matches (empty replacements for local provider)
    - De-anonymization block unchanged — still guarded by `if anonymized.replacements:`
  </acceptance_criteria>
  <done>sanitize_metadata вызывается в ai_extractor.py только для ollama-провайдера. Анонимизация пропускается в controller.py при active_provider=="ollama" — AnonymizedText создаётся с пустыми replacements. Де-анонимизация корректно пропускается (уже была защищена условием).</done>
</task>

<task type="auto">
  <name>Task 2: UI-переключатель провайдера в sidebar</name>
  <files>main.py</files>
  <read_first>
    - main.py (lines 600-750 — sidebar section)
    - config.py (active_provider field, full file)
    - providers/__init__.py (get_provider function)
  </read_first>
  <action>
**main.py — selectbox провайдера в sidebar (per D-07, D-08, D-09):**

1. Добавить новый expander "Провайдер" в sidebar, ПЕРЕД блоком "Настройки" (перед строкой ~631 с `st.markdown('<div style="font-size:0.7rem...`). Размещение — после "---" разделителя (строка 629), перед блоком "Настройки":

   ```python
   with st.sidebar.expander("Провайдер", expanded=False):
       _provider_options = {
           "Локальная модель (QWEN 1.5B)": "ollama",
           "ZAI (GLM-4.7, облако)": "zai",
           "OpenRouter (облако)": "openrouter",
       }
       _current_idx = list(_provider_options.values()).index(config.active_provider) if config.active_provider in _provider_options.values() else 0
       _selected_provider = st.selectbox(
           "AI-провайдер",
           list(_provider_options.keys()),
           index=_current_idx,
           key="provider_select",
           label_visibility="collapsed",
       )
       _new_provider = _provider_options[_selected_provider]
       if _new_provider != config.active_provider:
           config.active_provider = _new_provider
           # Проверить наличие API-ключа для облачных провайдеров
           if _new_provider in ("zai", "openrouter"):
               import os as _os
               _has_key = bool(
                   _os.environ.get("ZHIPU_API_KEY", "")
                   or _os.environ.get("ZAI_API_KEY", "")
                   or _os.environ.get("OPENROUTER_API_KEY", "")
               )
               if not _has_key:
                   st.warning("API-ключ не найден. Установите ZHIPU_API_KEY или OPENROUTER_API_KEY.")
           st.rerun()
   ```

2. Переключение работает без перезапуска приложения (per D-09) — Streamlit rerun перечитывает config.active_provider, Controller пересоздаётся с новым провайдером.

3. Config persistence (per D-08): config.active_provider уже хранится в dataclass Config, который используется глобально в main.py. Для persistence между перезапусками — сохранять в st.session_state и проверять при старте:
   ```python
   # В начале sidebar блока (перед expander), восстановить из session_state:
   if "provider_select_value" in st.session_state:
       config.active_provider = st.session_state["provider_select_value"]
   ```

   И при изменении:
   ```python
   st.session_state["provider_select_value"] = _new_provider
   ```

   Примечание: st.session_state переживает reruns, но НЕ перезапуск приложения. Для полной persistence нужен JSON-файл, но это discretion area — session_state достаточно для MVP (выбор провайдера не критичен между перезапусками, дефолт ollama корректный).
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n 'Провайдер' main.py && grep -n 'provider_select' main.py && grep -n 'active_provider' main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'expander.*Провайдер' main.py` matches (expander exists)
    - `grep 'selectbox' main.py | grep -i 'провайдер\|provider_select'` matches
    - `grep 'ollama' main.py` matches in provider options
    - `grep 'active_provider' main.py` matches (config update on change)
    - `grep 'st.rerun' main.py` matches in provider switch block
    - Provider options include all 3: ollama, zai, openrouter
  </acceptance_criteria>
  <done>Expander "Провайдер" появился в sidebar с selectbox из трёх вариантов. Переключение без перезапуска — st.rerun() при изменении. Предупреждение об отсутствии API-ключа при выборе облачного провайдера.</done>
</task>

</tasks>

<verification>
1. `grep -c 'sanitize_metadata' modules/ai_extractor.py` >= 2
2. `grep 'active_provider.*ollama' controller.py` matches
3. `grep 'AnonymizedText.*replacements={}' controller.py` matches
4. `grep 'expander.*Провайдер' main.py` matches
5. `python -c "from modules.ai_extractor import extract_metadata; print('OK')"` succeeds
6. `python -c "from controller import Controller; print('OK')"` succeeds
</verification>

<success_criteria>
- sanitize_metadata вызывается в ai_extractor.py после ответа локальной модели (PROC-01)
- Анонимизация пропускается в controller.py для ollama-провайдера (PROC-02)
- UI-переключатель провайдера работает в sidebar (PROV-03)
- Все три requirement ID (PROV-03, PROC-01, PROC-02) закрыты
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-local-llm/05-01-SUMMARY.md`
</output>
