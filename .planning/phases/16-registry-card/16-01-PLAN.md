---
phase: 16-registry-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/registry.py
  - app/components/registry_table.py
  - app/static/design-system.css
  - app/styles.py
autonomous: true
requirements: [REGI-01, REGI-02, REGI-03, REGI-05, REGI-06]

must_haves:
  truths:
    - "Над таблицей видна stats bar со светлым фоном и тремя числами: Всего / Истекают / Требуют внимания"
    - "Заголовок «Реестр» отображается с визуальным весом (text-2xl font-semibold)"
    - "Фильтр-бар (сегменты) имеет filled active state и визуально когерентен со stats bar"
    - "Статусные бейджи в таблице — цветные filled pills (green/amber/red), не голый текст"
    - "AG Grid таблица тематически оформлена через --ag-* CSS variables в .ag-theme-quartz"
    - "Анимация строк не перезапускается при переключении сегмента (Pitfall 7 guard)"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "stats bar, heading, filter bar — новый layout"
      contains: "_render_stats_bar"
    - path: "app/components/registry_table.py"
      provides: "STATUS_CELL_RENDERER с filled pills"
      contains: "status-pill"
    - path: "app/static/design-system.css"
      provides: "AG Grid theming через --ag-* variables, status-pill CSS"
      contains: ".ag-theme-quartz"
    - path: "app/styles.py"
      provides: "STATS_BAR константы"
      contains: "STATS_BAR"
  key_links:
    - from: "app/pages/registry.py (_render_stats_bar)"
      to: "app/components/registry_table.py (load_table_data)"
      via: "вызов _fetch_counts() при инициализации и после фильтрации"
      pattern: "_render_stats_bar"
    - from: "app/static/design-system.css (.ag-theme-quartz)"
      to: "app/components/registry_table.py (render_registry_table)"
      via: "CSS variables подхватываются AG Grid автоматически"
      pattern: "\\.ag-theme-quartz"
---

<objective>
Визуальный рерайт реестра: stats bar, типографический заголовок, filter bar с filled active state, цветные статусные бейджи, AG Grid theming через CSS variables.

Purpose: Главный рабочий экран обретает визуальный характер — данные читаются с первого взгляда.
Output: Обновлённые registry.py, registry_table.py, design-system.css, styles.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-registry-card/16-CONTEXT.md

@.planning/phases/14-header/14-01-SUMMARY.md
@.planning/phases/15-splash/15-01-SUMMARY.md

<interfaces>
<!-- Ключевые контракты для этого плана. Не требуют разведки по кодовой базе. -->

Из app/styles.py (уже существуют — ИСПОЛЬЗОВАТЬ):
```python
STAT_NUMBER = "text-2xl font-bold tabular-nums"
STAT_LABEL  = "text-xs uppercase tracking-wide"
SECTION_LABEL = "text-xs font-semibold uppercase tracking-wider"
SEG_ACTIVE  = "px-4 py-1.5 text-sm font-semibold rounded-md bg-indigo-600 text-white transition-colors duration-150"
SEG_INACTIVE = "px-4 py-1.5 text-sm font-semibold rounded-md text-slate-600 hover:bg-slate-100 transition-colors duration-150"
BTN_ACCENT_FILLED = "px-5 py-2 bg-indigo-600 text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 transition-colors duration-150"
```

Из app/components/registry_table.py:
```python
# Текущий STATUS_CELL_RENDERER возвращает <span class="status-active"> — голый текст
# Нужно заменить на filled pill с цветом

# _fetch_rows() возвращает список dict с полями:
# id, contract_type, counterparty, amount, computed_status,
# date_end, validation_score, validation_warnings, processed_at

# load_table_data(grid, state, segment) — вызывается при смене сегмента
# Сейчас НЕ возвращает counts — нужно добавить отдельный _fetch_counts()
```

Из app/pages/registry.py:
```python
# build() — точка входа, рендерит всю страницу
# active_segment = {"value": "all"}  — текущий сегмент
# grid_ref = {"grid": None}           — ссылка на AG Grid
# Функция _switch_segment(key) вызывает load_table_data()
```

Из app/static/tokens.css (CSS переменные для --ag-* mapping):
```css
--yt-color-surface:        #ffffff;
--yt-surface-bg:           #f1f5f9;   /* slate-100 */
--yt-color-border:         #e2e8f0;   /* slate-200 */
--yt-color-text-primary:   #0f172a;   /* slate-900 */
--yt-color-text-secondary: #475569;   /* slate-600 */
--yt-color-accent:         #4f46e5;   /* indigo-600 */
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AG Grid theming + status pills в design-system.css и registry_table.py</name>
  <files>app/static/design-system.css, app/components/registry_table.py</files>

  <read_first>
    - app/static/design-system.css — добавить после существующих блоков (не заменять)
    - app/components/registry_table.py — заменить STATUS_CELL_RENDERER (строки 34-47)
    - DevTools tip из CONTEXT.md: class на AG Grid контейнере = `ag-theme-quartz` (не alpine)
  </read_first>

  <action>
**В app/static/design-system.css** добавить в конец файла (после .hero-enter блока):

```css
/* ── AG Grid theming (.ag-theme-quartz) — вне @layer, AG Grid не уважает @layer ── */
/* Per Phase 16 decisions: ТОЛЬКО через --ag-* variables, scope = .ag-theme-quartz */

.ag-theme-quartz {
  --ag-background-color:        var(--yt-color-surface);
  --ag-odd-row-background-color: var(--yt-p-slate-50);
  --ag-header-background-color: var(--yt-p-slate-100);
  --ag-header-foreground-color: var(--yt-p-slate-600);
  --ag-row-hover-color:         var(--yt-p-indigo-50);
  --ag-selected-row-background-color: var(--yt-p-indigo-50);
  --ag-border-color:            var(--yt-color-border);
  --ag-cell-horizontal-border:  solid var(--yt-color-border);
  --ag-font-family:             var(--yt-font-family);
  --ag-font-size:               var(--yt-text-body);
  --ag-row-height:              44px;
  --ag-header-height:           40px;
}

/* ── Status badge pills (filled semantic colors) — REGI-02 ── */
/* ВАЖНО: имена классов status-* — API contract, НЕ переименовывать (Per Phase 16 decision) */
/* Pitfall 7 guard: animation-fill-mode: backwards предотвращает replay при фильтрации */

.status-active      { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#dcfce7; color:#15803d; }
.status-expiring    { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#fef9c3; color:#a16207; }
.status-expired     { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#fee2e2; color:#b91c1c; }
.status-unknown     { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#f1f5f9; color:#64748b; }
.status-terminated  { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#f1f5f9; color:#475569; }
.status-extended    { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#dbeafe; color:#1d4ed8; }
.status-negotiation { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#ede9fe; color:#6d28d9; }
.status-suspended   { display:inline-flex; align-items:center; gap:4px; padding:2px 8px; border-radius:9999px; font-size:0.75rem; font-weight:600; background:#f3f4f6; color:#4b5563; }
```

**В app/components/registry_table.py** заменить STATUS_CELL_RENDERER (строки 34-47) на:

```python
STATUS_CELL_RENDERER = """(params) => {
    const labels = {
        active:      ['\u2714', '\u0414\u0435\u0439\u0441\u0442\u0432\u0443\u0435\u0442', 'status-active'],
        expiring:    ['\u26a0', '\u0421\u043a\u043e\u0440\u043e \u0438\u0441\u0442\u0435\u043a\u0430\u0435\u0442', 'status-expiring'],
        expired:     ['\u2717', '\u0418\u0441\u0442\u0451\u043a', 'status-expired'],
        unknown:     ['', '\u041d\u0435\u0442 \u0434\u0430\u0442\u044b', 'status-unknown'],
        terminated:  ['', '\u0420\u0430\u0441\u0442\u043e\u0440\u0433\u043d\u0443\u0442', 'status-terminated'],
        extended:    ['\u21bb', '\u041f\u0440\u043e\u0434\u043b\u0451\u043d', 'status-extended'],
        negotiation: ['', '\u041d\u0430 \u0441\u043e\u0433\u043b\u0430\u0441\u043e\u0432\u0430\u043d\u0438\u0438', 'status-negotiation'],
        suspended:   ['\u23f8', '\u041f\u0440\u0438\u043e\u0441\u0442\u0430\u043d\u043e\u0432\u043b\u0435\u043d', 'status-suspended'],
    };
    const [icon, label, cls] = labels[params.value] || ['', params.value, 'status-unknown'];
    const iconHtml = icon ? \`<span>\${icon}</span>\` : '';
    return \`<span class="\${cls}">\${iconHtml}\${label}</span>\`;
}"""
```

Также добавить функцию `_fetch_counts()` в registry_table.py после `_fuzzy_filter()`:

```python
def _fetch_counts(client_name: str, warning_days: int) -> dict:
    """Возвращает агрегатные counts для stats bar (REGI-01).

    Синхронная функция — вызывается через run.io_bound().
    Returns: {"total": int, "expiring": int, "attention": int}
    """
    db = _client_manager.get_db(client_name)
    try:
        total_row = db.conn.execute(
            "SELECT COUNT(*) FROM contracts WHERE status = 'done'"
        ).fetchone()
        total = total_row[0] if total_row else 0

        expiring_sql = f"""
            SELECT COUNT(*) FROM contracts
            WHERE status = 'done'
            AND date_end IS NOT NULL
            AND date_end > date('now')
            AND date_end <= date('now', '+' || :warning_days || ' days')
        """
        exp_row = db.conn.execute(expiring_sql, {"warning_days": warning_days}).fetchone()
        expiring = exp_row[0] if exp_row else 0

        attention_row = db.conn.execute(
            "SELECT COUNT(*) FROM contracts WHERE status = 'done' AND (validation_score < 0.7 OR validation_warnings IS NOT NULL AND validation_warnings != '[]')"
        ).fetchone()
        attention = attention_row[0] if attention_row else 0

        return {"total": total, "expiring": expiring, "attention": attention}
    except Exception:
        return {"total": 0, "expiring": 0, "attention": 0}
```

Добавить экспорт `_fetch_counts` в строку импорта в registry.py позже (Task 2 сделает это).
  </action>

  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python3 -c "from app.components.registry_table import _fetch_counts; print('_fetch_counts OK')" && grep -c "ag-theme-quartz" app/static/design-system.css && grep "status-pill\|display:inline-flex" app/static/design-system.css | head -3</automated>
  </verify>

  <acceptance_criteria>
    - `grep "\.ag-theme-quartz" app/static/design-system.css` — находит блок
    - `grep "status-active" app/static/design-system.css` — находит filled pill CSS
    - `grep "display:inline-flex" app/static/design-system.css` — находит минимум 8 строк
    - `grep "_fetch_counts" app/components/registry_table.py` — находит функцию
    - `python3 -c "from app.components.registry_table import _fetch_counts"` — OK без ошибок
  </acceptance_criteria>

  <done>AG Grid тематически оформлен через --ag-* CSS variables; статусные бейджи — filled pills; _fetch_counts() экспортируется</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stats bar, heading «Реестр», filter bar в registry.py</name>
  <files>app/pages/registry.py, app/styles.py</files>

  <read_first>
    - app/pages/registry.py — полный файл, особенно функция build() начиная со строки 71
    - app/styles.py — существующие константы (STAT_NUMBER, STAT_LABEL, SEG_ACTIVE, SEG_INACTIVE)
    - app/components/registry_table.py — убедиться что _fetch_counts экспортируется (Task 1 добавил)
    - CONTEXT.md решение: stats bar на СВЕТЛОМ фоне (slate-100), не тёмная полоса
    - Pitfall 7: анимация строк НЕ должна перезапускаться при смене сегмента
  </read_first>

  <action>
**В app/styles.py** добавить в конец (после BTN_ACCENT_FILLED):

```python
# Stats bar item (Phase 16, REGI-01)
STATS_BAR = "flex items-center gap-6 px-6 py-3 bg-white border-b border-slate-200"
STATS_ITEM = "flex flex-col items-center gap-0"
```

**В app/pages/registry.py** обновить импорты:

1. Добавить `_fetch_counts` к импорту из registry_table (строка 27-33):
```python
from app.components.registry_table import (
    load_table_data,
    load_version_children,
    render_registry_table,
    _client_manager,
    _collapse_version_children,
    _fetch_counts,
)
```

2. Добавить импорт новых констант из styles:
```python
from app.styles import SEG_ACTIVE, SEG_INACTIVE, TOGGLE_ACTIVE, TOGGLE_INACTIVE, STATS_BAR, STATS_ITEM
```

**В функции build()** заменить блок `with ui.column().classes("w-full"):` (строки 79-123) на расширенную версию с stats bar и heading:

```python
with ui.column().classes("w-full"):
    # ── Stats bar (REGI-01) — светлый фон, не тёмная полоса ──────────────────
    stats_row = ui.row().classes(STATS_BAR)
    total_num = ui.label("—")
    expiring_num = ui.label("—")
    attention_num = ui.label("—")

    with stats_row:
        with ui.column().classes(STATS_ITEM):
            total_num.classes(STAT_NUMBER + " text-slate-900")
            ui.label("документов").classes(STAT_LABEL + " text-slate-500")
        ui.label("·").classes("text-slate-300 text-xl font-light")
        with ui.column().classes(STATS_ITEM):
            expiring_num.classes(STAT_NUMBER + " text-amber-600")
            ui.label("истекают").classes(STAT_LABEL + " text-slate-500")
        ui.label("·").classes("text-slate-300 text-xl font-light")
        with ui.column().classes(STATS_ITEM):
            attention_num.classes(STAT_NUMBER + " text-red-600")
            ui.label("требуют внимания").classes(STAT_LABEL + " text-slate-500")

    # ── Page heading + controls row ──────────────────────────────────────────
    with ui.row().classes("w-full px-6 pt-5 pb-2 items-center gap-4"):
        # REGI-05: Заголовок с визуальным весом
        ui.label("Реестр").classes("text-2xl font-semibold text-slate-900 mr-auto")

        # Calendar toggle — right-aligned (DSGN-04, D-15)
        with ui.row().classes("items-center gap-1.5"):
            list_btn = ui.button("≡").props("flat no-caps").classes(TOGGLE_ACTIVE)
            list_btn.props('title="Список" aria-label="Вид списком"')
            cal_btn = ui.button("⊞").props("flat no-caps").classes(TOGGLE_INACTIVE)
            cal_btn.props('title="Календарь" aria-label="Вид календарём"')

    # ── Search + Filter bar row (REGI-06) ────────────────────────────────────
    with ui.row().classes("w-full px-6 pb-4 items-center gap-4 search-row"):
        search_input = (
            ui.input(placeholder="Поиск по реестру...")
            .props("outlined dense")
            .classes("flex-1 max-w-md")
        )

        # REGI-06: Filter bar с filled active state
        with ui.row().classes("gap-1.5 bg-slate-100 p-1 rounded-lg"):
            for key, label in [
                ("all", "Все"),
                ("expiring", "Истекают ⚠"),
                ("attention", "Требуют внимания"),
            ]:
                btn = ui.button(
                    label,
                    on_click=lambda k=key: _switch_segment(k),
                ).props("flat unelevated no-caps")
                btn.classes(SEG_ACTIVE if key == "all" else SEG_INACTIVE)
                seg_buttons[key] = btn

    # Progress section — hidden by default (D-12)
    progress_section = ui.column().classes("w-full px-6 py-4 gap-2")
    progress_section.set_visibility(False)

    with progress_section:
        with ui.row().classes("items-center gap-3 w-full"):
            progress_bar = ui.linear_progress(value=0).classes("flex-1")
            count_label = ui.label("0/0 файлов").classes("text-sm text-slate-500 shrink-0")
        file_label = ui.label("").classes("text-xs text-slate-400")
        error_col = ui.column().classes("gap-1")

    grid_container = ui.column().classes("w-full")

    # Calendar container — hidden by default
    calendar_container = ui.column().classes("w-full px-6 py-4")
    calendar_container.set_visibility(False)
```

**Добавить `_refresh_stats()` helper** внутри build() после создания seg_buttons:

```python
async def _refresh_stats() -> None:
    """Обновляет counts в stats bar (REGI-01). Вызывается при init и после фильтрации."""
    counts = await run.io_bound(_fetch_counts, state.current_client, state.warning_days_threshold)
    total_num.set_text(str(counts["total"]))
    expiring_num.set_text(str(counts["expiring"]))
    attention_num.set_text(str(counts["attention"]))
```

**Pitfall 7 guard — в `_switch_segment()`** обновить, чтобы добавить JS отключение анимации перед обновлением данных:

```python
async def _switch_segment(key: str) -> None:
    active_segment["value"] = key
    _apply_segment_classes(key)
    if grid_ref["grid"]:
        # Pitfall 7: отключаем row animation перед фильтрацией, чтобы не replay
        await ui.run_javascript(
            "document.querySelectorAll('.ag-row').forEach(r => r.style.animation = 'none')"
        )
        await load_table_data(grid_ref["grid"], state, key)
```

**В `_init()`** добавить вызов `_refresh_stats()` в начало:

```python
async def _init() -> None:
    await _refresh_stats()
    with grid_container:
        # ... остальной код без изменений
```

Убедиться что `run` импортирован в registry.py (уже есть в строке 21: `from nicegui import run, ui`).
  </action>

  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python3 -c "import app.pages.registry; print('registry import OK')" && grep "_render_stats_bar\|_refresh_stats\|STATS_BAR\|text-2xl font-semibold" app/pages/registry.py | head -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep "_refresh_stats" app/pages/registry.py` — находит функцию и вызов в _init()
    - `grep "text-2xl font-semibold" app/pages/registry.py` — находит heading «Реестр»
    - `grep "STATS_BAR\|STATS_ITEM" app/styles.py` — находит константы
    - `grep "_fetch_counts" app/pages/registry.py` — находит импорт и использование
    - `grep "animation.*none\|Pitfall 7" app/pages/registry.py` — находит guard
    - `python3 -c "import app.pages.registry"` — OK без ошибок
  </acceptance_criteria>

  <done>Stats bar показывает три числа на светлом фоне; heading «Реестр» с text-2xl font-semibold; filter bar с filled active state; Pitfall 7 guard работает</done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# 1. Синтаксис Python
python3 -c "import app.pages.registry; import app.components.registry_table; import app.styles; print('All imports OK')"

# 2. AG Grid theming в CSS
grep -c "\.ag-theme-quartz" app/static/design-system.css

# 3. Filled pills в CSS
grep -c "display:inline-flex" app/static/design-system.css

# 4. Stats bar constants
grep "STATS_BAR\|STATS_ITEM" app/styles.py

# 5. Pitfall 7 guard
grep "animation.*none\|Pitfall 7" app/pages/registry.py

# 6. _fetch_counts доступен
python3 -c "from app.components.registry_table import _fetch_counts; print('counts OK')"
```
</verification>

<success_criteria>
- [ ] AG Grid table тематически оформлен (--ag-* variables в .ag-theme-quartz)
- [ ] Статусные бейджи — filled pills зелёный/янтарный/красный (REGI-02, REGI-03)
- [ ] Stats bar показывает три числа над таблицей на белом/slate-100 фоне (REGI-01)
- [ ] Заголовок «Реестр» с text-2xl font-semibold (REGI-05)
- [ ] Filter bar сегменты с filled active state (REGI-06)
- [ ] Анимация строк не replay при смене сегмента (Pitfall 7 guard)
- [ ] Все Python импорты без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/16-registry-card/16-01-SUMMARY.md`
</output>
