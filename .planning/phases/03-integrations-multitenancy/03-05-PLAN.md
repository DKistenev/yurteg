---
phase: 03-integrations-multitenancy
plan: 05
type: execute
wave: 2
depends_on:
  - "03-00"
files_modified:
  - services/client_manager.py
  - main.py
  - requirements.txt
autonomous: true
requirements:
  - PROF-01

must_haves:
  truths:
    - "User can create named clients and each gets an isolated database file"
    - "User can switch between clients via selectbox in sidebar"
    - "Switching client changes the active database — registry shows different data"
    - "Default single-registry mode works without creating any clients"
    - "Auto-binding suggests client based on counterparty name (fuzzy match)"
  artifacts:
    - path: "services/client_manager.py"
      provides: "ClientManager class: add, list, get_db, find_client_by_counterparty"
      contains: "class ClientManager"
    - path: "main.py"
      provides: "Client selectbox in sidebar, new client creation"
      contains: "client_manager"
    - path: "requirements.txt"
      provides: "rapidfuzz dependency"
      contains: "rapidfuzz"
  key_links:
    - from: "main.py"
      to: "services/client_manager.py"
      via: "ClientManager.get_db() determines which Database is active"
      pattern: "client_manager\\.get_db"
    - from: "services/client_manager.py"
      to: "modules/database.py"
      via: "Database(path) constructor"
      pattern: "Database\\("
---

<objective>
Multi-client mode: isolated registries per client with selectbox switching.

Purpose: Юрист ведёт несколько клиентов в одном приложении, документы не смешиваются.
Output: ClientManager service with fuzzy matching + sidebar UI for client management.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integrations-multitenancy/03-CONTEXT.md
@.planning/phases/03-integrations-multitenancy/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClientManager service with fuzzy matching</name>
  <files>services/client_manager.py, requirements.txt</files>
  <read_first>
    - modules/database.py (Database.__init__ signature — takes db_path: Path)
    - config.py (current Config fields to understand data directory patterns)
    - requirements.txt (current dependencies — add rapidfuzz)
    - .planning/phases/03-integrations-multitenancy/03-RESEARCH.md (Pattern 5: ClientManager, rapidfuzz example)
  </read_first>
  <action>
**services/client_manager.py**:

```python
import json
import logging
import re
from pathlib import Path
from typing import Optional

from modules.database import Database

logger = logging.getLogger(__name__)

class ClientManager:
    """Управление мультиклиентскими реестрами. Каждый клиент = отдельный .db файл."""

    DEFAULT_CLIENT = "Основной реестр"

    def __init__(self, data_dir: Optional[Path] = None):
        self._data_dir = data_dir or (Path.home() / ".yurteg")
        self._data_dir.mkdir(parents=True, exist_ok=True)
        self._meta_file = self._data_dir / "clients.json"
        self._clients: dict[str, Path] = self._load()

    def _load(self) -> dict[str, Path]:
        if self._meta_file.exists():
            try:
                data = json.loads(self._meta_file.read_text(encoding="utf-8"))
                return {name: Path(p) for name, p in data.items()}
            except (json.JSONDecodeError, ValueError):
                logger.warning("Corrupted clients.json, starting fresh")
        return {self.DEFAULT_CLIENT: self._data_dir / "yurteg.db"}

    def _save(self) -> None:
        data = {name: str(p) for name, p in self._clients.items()}
        self._meta_file.write_text(
            json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8"
        )

    def list_clients(self) -> list[str]:
        return list(self._clients.keys())

    def get_db_path(self, name: str) -> Path:
        if name not in self._clients:
            raise KeyError(f"Client not found: {name}")
        return self._clients[name]

    def get_db(self, name: str) -> Database:
        return Database(self.get_db_path(name))

    def add_client(self, name: str) -> Path:
        if name in self._clients:
            return self._clients[name]
        slug = re.sub(r'[^\w\s-]', '', name.lower()).strip()
        slug = re.sub(r'[\s]+', '_', slug)
        if not slug:
            slug = "client"
        path = self._data_dir / f"client_{slug}.db"
        # Avoid path collisions
        counter = 1
        while path.exists() and str(path) not in [str(p) for p in self._clients.values()]:
            path = self._data_dir / f"client_{slug}_{counter}.db"
            counter += 1
        self._clients[name] = path
        self._save()
        return path

    def remove_client(self, name: str) -> bool:
        if name == self.DEFAULT_CLIENT:
            return False  # Cannot remove default
        if name in self._clients:
            del self._clients[name]
            self._save()
            return True
        return False

    def find_client_by_counterparty(
        self, counterparty: str, threshold: int = 85
    ) -> Optional[str]:
        """Fuzzy match counterparty name against client names.

        Uses rapidfuzz token_sort_ratio — handles word reordering:
        'ООО Рога и Копыта' matches 'Рога и Копыта ООО'.
        Returns client name or None if no match above threshold.
        """
        try:
            from rapidfuzz import process, fuzz
        except ImportError:
            logger.warning("rapidfuzz not installed, fuzzy matching disabled")
            return None

        client_names = [n for n in self._clients.keys() if n != self.DEFAULT_CLIENT]
        if not client_names:
            return None

        result = process.extractOne(
            counterparty,
            client_names,
            scorer=fuzz.token_sort_ratio,
            score_cutoff=threshold,
        )
        return result[0] if result else None
```

**requirements.txt** — Add `rapidfuzz>=3.14` to the end of the file (after existing dependencies).

Do NOT import Streamlit in client_manager.py.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import tempfile, pathlib
from services.client_manager import ClientManager
cm = ClientManager(data_dir=pathlib.Path(tempfile.mkdtemp()))
assert cm.list_clients() == ['Основной реестр']
cm.add_client('ООО Рога')
assert 'ООО Рога' in cm.list_clients()
db = cm.get_db('ООО Рога')
print(f'DB path: {db.db_path}')
assert 'client_' in str(db.db_path)
print('ClientManager OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - services/client_manager.py contains `class ClientManager` with methods `list_clients`, `get_db`, `add_client`, `remove_client`, `find_client_by_counterparty`, `get_db_path`
    - services/client_manager.py contains `DEFAULT_CLIENT = "Основной реестр"`
    - services/client_manager.py contains `from rapidfuzz import process, fuzz` (inside try/except)
    - services/client_manager.py does NOT contain `import streamlit`
    - requirements.txt contains `rapidfuzz>=3.14`
    - Import and basic operations succeed
  </acceptance_criteria>
  <done>ClientManager service with fuzzy matching, file-per-client isolation, JSON metadata</done>
</task>

<task type="auto">
  <name>Task 2: Add client selectbox and management UI to main.py sidebar</name>
  <files>main.py</files>
  <read_first>
    - main.py (current sidebar structure, where Database is initialized, session_state usage)
    - services/client_manager.py (just created — ClientManager API)
    - .planning/phases/03-integrations-multitenancy/03-RESEARCH.md (Pitfall 5: selectbox client switching cache invalidation)
  </read_first>
  <action>
Add client management to main.py sidebar. This must be placed BEFORE the Database initialization, since the selected client determines which .db file to open.

**1. Initialize ClientManager early** (before db creation):
```python
from services.client_manager import ClientManager
client_manager = ClientManager()
```

**2. Client selectbox in sidebar** (at the top of sidebar, before other settings):
```python
# --- Мультиклиентский режим ---
st.sidebar.markdown("### Клиент")
_clients = client_manager.list_clients()
_selected_client = st.sidebar.selectbox(
    "Активный реестр",
    _clients,
    index=0,
    key="active_client",
    label_visibility="collapsed",
)

# New client creation
with st.sidebar.expander("Управление клиентами", expanded=False):
    _new_name = st.text_input("Название нового клиента", key="new_client_name")
    if st.button("Создать", key="create_client_btn") and _new_name:
        client_manager.add_client(_new_name)
        st.rerun()
```

**3. Update Database initialization** to use selected client:
Find the line where `Database(...)` is called and replace the hardcoded path with:
```python
db = client_manager.get_db(_selected_client)
```

If the current code uses `Database(output_dir / "yurteg.db")` or similar, replace that with the ClientManager call. Keep the old path as the default in ClientManager (it already defaults to ~/.yurteg/yurteg.db for "Основной реестр").

**4. Important: Cache invalidation on client switch.**
If any `@st.cache_data` or `@st.cache_resource` is used with database queries, ensure `_selected_client` is passed as a parameter to invalidate cache on switch. Check existing code for `@st.cache` decorators and add `_selected_client` arg if needed.

Do NOT add the auto-binding feature to main.py in this task (that integrates with controller and is more complex). The fuzzy matching is available in ClientManager for future use in the processing pipeline.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "client_manager\|active_client\|Активный реестр\|Управление клиентами\|create_client_btn" main.py</automated>
  </verify>
  <acceptance_criteria>
    - main.py contains `from services.client_manager import ClientManager`
    - main.py contains `client_manager = ClientManager()`
    - main.py contains `st.sidebar.selectbox(` with `"active_client"` key
    - main.py contains `client_manager.get_db(`
    - main.py contains `st.button("Создать"` for new client creation
    - main.py contains `client_manager.add_client(`
  </acceptance_criteria>
  <done>Client selectbox in sidebar, client creation, database switches per selected client</done>
</task>

</tasks>

<verification>
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_client_manager.py -v
</verification>

<success_criteria>
- ClientManager manages per-client .db files
- Sidebar shows client selectbox
- Database switches when client is selected
- rapidfuzz fuzzy matching available
</success_criteria>

<output>
After completion, create `.planning/phases/03-integrations-multitenancy/03-05-SUMMARY.md`
</output>
