# Phase 3: Интеграции и мультидоступ - Context

**Gathered:** 2026-03-20
**Status:** Ready for planning

<domain>
## Phase Boundary

Юрист получает уведомления о сроках даже когда приложение закрыто (через Telegram), может отправить документ боту для обработки, и может вести несколько клиентов в одном инструменте с изолированными реестрами. Google Drive и совместный доступ нескольких юристов отложены.

</domain>

<decisions>
## Implementation Decisions

### Telegram-бот — архитектура
- Единый бот @YurTagBot на сервере (webhook), не отдельный бот на каждого юриста
- Бот как «почтовый ящик»: принимает файл → сохраняет в очередь на сервере → приложение юриста забирает файлы при запуске и обрабатывает локально
- Данные остаются локальными — сервер хранит только очередь файлов и минимальные метаданные для уведомлений
- Доступ: привязка по chat_id, один владелец на аккаунт (v1)

### Telegram-бот — онбординг
- Привязка через /start: юрист пишет /start боту → бот выдаёт одноразовый код → юрист вводит код в приложении → привязка готова
- Не нужно искать chat_id вручную, не нужно создавать бота в BotFather

### Telegram-бот — ответы
- После обработки файла бот отправляет краткую карточку: «✅ Договор аренды с ООО Рога, 150 000 ₽, до 31.12.2026»
- Ключевые метаданные одним сообщением — тип, контрагент, сумма, дата окончания

### Уведомления о сроках — Telegram
- Сервер бота проверяет сроки по cron и отправляет уведомления
- Расписание настраивается юристом: ежедневный дайджест / при наступлении порога / оба / выключить
- Данные на сервере для проверки сроков: минимум — date_end, название документа, контрагент, статус. Никаких текстов, никаких сумм
- Приложение синхронизирует метаданные на сервер при запуске

### Уведомления внутри приложения
- st.toast при запуске: «⚠️ 3 документа истекают на этой неделе»
- Дополнение к панели «требует внимания» из фазы 2 (уже реализована)

### Мультиклиентский режим
- Опциональный — по умолчанию один реестр на все документы, разделение на клиентов по желанию юриста
- Изоляция: отдельная БД на клиента (client_roga.db, client_kopyta.db) — надёжно, легко бэкапить
- Переключение: выпадающий список (selectbox) в сайдбаре с названиями клиентов
- Создание: кнопка «Новый клиент» в сайдбаре → ввод названия
- Автопривязка документов по контрагенту: AI извлекает контрагента → если клиент с таким названием есть — привязать автоматически. Если нет — предложить создать
- После обработки — сводка для ручной коррекции: «5 док → ООО Буп, 3 док → ИП Петров, 2 док → не определено»

### Claude's Discretion
- Конкретный стек серверной части бота (python-telegram-bot vs aiogram, FastAPI vs Flask)
- Формат очереди файлов на сервере (SQLite vs Redis vs файловая система)
- Протокол sync метаданных (REST API vs простые HTTP-запросы)
- Реализация одноразовых кодов привязки
- Fuzzy matching названий клиентов при автопривязке (ООО «Рога» vs ООО "Рога" vs Рога)
- Схема хранения настроек уведомлений

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Текущая архитектура (после фаз 1-2)
- `.planning/codebase/ARCHITECTURE.md` — паттерн пайплайна, слои, data flow
- `.planning/codebase/INTEGRATIONS.md` — текущие внешние интеграции (AI providers, SQLite)
- `.planning/codebase/CONCERNS.md` — техдолг и известные проблемы

### Контекст предыдущих фаз
- `.planning/phases/01-infrastructure/01-CONTEXT.md` — сервис-слой, провайдер-абстракция
- `.planning/phases/02-document-lifecycle/02-CONTEXT.md` — статусы, версионирование, платёжный календарь

### Требования
- `.planning/REQUIREMENTS.md` — INTG-01 через INTG-04, PROF-01, PROF-02

### Ключевой код
- `services/pipeline_service.py` — единая точка входа для обработки (уже готов для вызова из бота, не импортирует Streamlit)
- `services/lifecycle_service.py` — проверка сроков (не импортирует Streamlit)
- `modules/database.py` — система миграций, готова для новых таблиц
- `config.py` — Config dataclass, расширить для Telegram-настроек

### Известные блокеры
- STATE.md: «APScheduler + Streamlit threading caveats» — решается серверным ботом (cron на сервере, не в Streamlit)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `services/pipeline_service.py` — вызов обработки без UI, готов для бота (process_archive)
- `services/lifecycle_service.py` — get_attention_required() для данных уведомлений
- `modules/database.py` — миграции, расширяемая схема
- `modules/models.py` — dataclasses ContractMetadata, ProcessingResult
- `config.py` — Config dataclass с настройками AI, расширить для TG

### Established Patterns
- Сервис-слой не импортирует Streamlit — можно вызывать из бота
- Модули общаются через dataclasses
- SQLite с Lock для thread-safety
- Callback-pattern (on_progress, on_file_done) для уведомлений о прогрессе

### Integration Points
- `main.py` — добавить: selectbox клиентов, кнопку привязки TG, настройки уведомлений, st.toast
- `controller.py` — хук версионирования и платежей уже здесь, добавить хук мультиклиента
- `modules/database.py` — новые миграции для таблиц clients, telegram_bindings, notification_settings

</code_context>

<specifics>
## Specific Ideas

- Принцип «почтового ящика»: бот только принимает и хранит файлы, обработка всегда локальная — local-first сохраняется
- Автопривязка клиентов продолжает принцип «ноль ручных действий, ручная коррекция как исключение»
- Минимум данных на сервере — только то, что нужно для уведомлений (даты, названия, статусы). Тексты документов никогда не покидают машину юриста
- Сводка после обработки: «5 док → ООО Буп, 3 док → ИП Петров, 2 док → не определено» — юрист видит и может поправить

</specifics>

<deferred>
## Deferred Ideas

- **Google Drive автообработка (INTG-03)** — отложена, Telegram-бот покрывает сценарий «отправить файл без открытия приложения». Вернуться когда появится серверная инфраструктура
- **PROF-02: Совместный доступ нескольких юристов** — требует серверной БД (PostgreSQL/Supabase). Отложено в v2
- **Яндекс.Диск (INTG-07)** — аналогично Google Drive, v2

</deferred>

---

*Phase: 03-integrations-multitenancy*
*Context gathered: 2026-03-20*
