---
phase: 03-integrations-multitenancy
plan: 06
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-03"
  - "03-04"
  - "03-05"
files_modified:
  - tests/test_telegram_bot.py
  - tests/test_client_manager.py
  - tests/test_notifications.py
autonomous: false
requirements:
  - INTG-01
  - INTG-02
  - INTG-04
  - PROF-01

must_haves:
  truths:
    - "All xfail tests are replaced with real assertions"
    - "Full test suite passes"
    - "Integration between components verified end-to-end"
  artifacts:
    - path: "tests/test_telegram_bot.py"
      provides: "Real tests for bot server database operations"
      contains: "def test_file_queue_enqueue"
    - path: "tests/test_client_manager.py"
      provides: "Real tests for client management"
      contains: "def test_add_client"
    - path: "tests/test_notifications.py"
      provides: "Real tests for notification logic"
      contains: "def test_startup_toast"
  key_links: []
---

<objective>
Finalize tests: replace all remaining xfail stubs with real assertions. Run full suite. Checkpoint for human verification.

Purpose: All phase 3 code is tested and verified before closing the phase.
Output: Green test suite + human sign-off on Telegram binding and client switching.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integrations-multitenancy/03-01-SUMMARY.md
@.planning/phases/03-integrations-multitenancy/03-02-SUMMARY.md
@.planning/phases/03-integrations-multitenancy/03-03-SUMMARY.md
@.planning/phases/03-integrations-multitenancy/03-04-SUMMARY.md
@.planning/phases/03-integrations-multitenancy/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all xfail stubs with real test assertions</name>
  <files>tests/test_telegram_bot.py, tests/test_client_manager.py, tests/test_notifications.py</files>
  <read_first>
    - tests/test_telegram_bot.py (current xfail stubs)
    - tests/test_client_manager.py (current xfail stubs)
    - tests/test_notifications.py (current state — some may already be real from 03-01)
    - bot_server/database.py (ServerDatabase API for bot tests)
    - services/client_manager.py (ClientManager API for client tests)
    - services/lifecycle_service.py (get_attention_required for notification tests)
    - modules/database.py (Database class for creating test DBs)
  </read_first>
  <action>
Update all test files, removing @pytest.mark.xfail and adding real assertions:

**tests/test_telegram_bot.py** — test bot_server/database.py operations:
- `test_file_queue_enqueue`: Create ServerDatabase(tmp_path / "test.db"), call enqueue_file(123, "/tmp/test.pdf", "test.pdf", "application/pdf"), verify fetch_queue(123) returns 1 item with correct filename.
- `test_file_queue_fetch`: Enqueue 2 files, fetch_queue returns both, mark_fetched on first, second fetch_queue returns only the unmarked one.
- `test_binding_code_generation`: save_pending_binding(123, "654321", 15), consume returns {chat_id: 123}.
- `test_binding_code_expiry`: save_pending_binding with ttl_minutes=0 (or manually set expires_at to past), consume returns None.
- `test_binding_code_consume`: After consume, second consume of same code returns None.
- `test_deadline_alerts`: save_deadlines with date_end = today+5 days, get_alerts_for_user with warning_days=30 returns it. date_end = today+60 should NOT be returned.
- `test_push_deadlines_minimal_data`: save_deadlines with {contract_ref, counterparty, date_end, status}, verify only those fields are stored (no extra fields like amount or subject).

Use `tmp_path` fixture for DB paths. Import `from bot_server.database import ServerDatabase`.

**tests/test_client_manager.py** — test services/client_manager.py:
- `test_add_client`: ClientManager(tmp_path), add_client("ООО Рога"), verify in list_clients() and .db file path contains "client_".
- `test_get_db`: add_client, get_db returns Database instance, verify db.db_path matches expected.
- `test_list_clients`: Add 3 clients, list_clients returns all 4 (including default).
- `test_default_client`: Fresh ClientManager, list_clients returns ["Основной реестр"].
- `test_fuzzy_match_exact`: add_client("ООО Рога и Копыта"), find_client_by_counterparty("ООО Рога и Копыта") returns that name.
- `test_fuzzy_match_reorder`: add_client("ООО Рога и Копыта"), find_client_by_counterparty("Рога и Копыта ООО") returns match.
- `test_fuzzy_match_no_match`: add_client("ООО Рога"), find_client_by_counterparty("ИП Совершенно Другой") returns None.
- `test_switch_client`: Add 2 clients, get_db for each returns different db_path values.

**tests/test_notifications.py** — ensure real assertions (may already be done in 03-01):
- Verify all tests have real assertions and no remaining xfail markers.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_telegram_bot.py tests/test_client_manager.py tests/test_notifications.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "xfail" tests/test_telegram_bot.py tests/test_client_manager.py tests/test_notifications.py` returns 0 for all files (or only for truly untestable UI tests)
    - `pytest tests/test_telegram_bot.py tests/test_client_manager.py tests/test_notifications.py -v` exits 0
    - tests/test_telegram_bot.py contains `from bot_server.database import ServerDatabase`
    - tests/test_client_manager.py contains `from services.client_manager import ClientManager`
    - At least 15 tests pass across all 3 files
  </acceptance_criteria>
  <done>All phase 3 tests have real assertions and pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of phase 3 features</name>
  <action>Human verifies all phase 3 features work end-to-end in the running application.</action>
  <what-built>
Phase 3 complete implementation:
1. In-app toast notification at startup about expiring documents
2. Telegram bot server (bot_server/) with FastAPI + webhook + file queue
3. Local app Telegram binding UI and auto-fetch of queued files
4. APScheduler daily deadline digest notifications via Telegram
5. Multi-client mode with isolated databases and sidebar switching
  </what-built>
  <how-to-verify>
1. Start the app: `cd yurteg && streamlit run main.py`
2. Verify toast appears at startup if there are expiring documents
3. Check sidebar: "Клиент" selectbox visible with "Основной реестр"
4. Click "Управление клиентами" expander, create a test client, verify selectbox updates
5. Switch between clients, verify registry data changes
6. Check "Telegram" expander in sidebar — binding code input visible
7. Run: `python -m pytest tests/ -v` — all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/ -v
</verification>

<success_criteria>
- Full test suite green
- All phase 3 features working end-to-end
- Human sign-off received
</success_criteria>

<output>
After completion, create `.planning/phases/03-integrations-multitenancy/03-06-SUMMARY.md`
</output>
