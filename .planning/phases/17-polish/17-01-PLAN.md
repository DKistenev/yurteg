---
phase: 17-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/templates.py
  - app/styles.py
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03]

must_haves:
  truths:
    - "Каждая карточка шаблона визуально различима по типу — через цветную левую полосу 4px"
    - "На карточке есть иконка типа документа в accent-цвете и цветной badge типа"
    - "При hover карточка поднимается (shadow-md lift) — transition уже в design-system.css"
    - "Empty state шаблонов: иконка + заголовок + описание что такое шаблоны + CTA кнопка"
  artifacts:
    - path: "app/pages/templates.py"
      provides: "Рерайт _render_card и _render_cards с color-coded левой полосой"
      contains: "border-l-4"
    - path: "app/styles.py"
      provides: "TMPL_TYPE_COLORS — маппинг типов документов на цвета"
      contains: "TMPL_TYPE_COLORS"
  key_links:
    - from: "app/pages/templates.py"
      to: "app/styles.py"
      via: "import TMPL_TYPE_COLORS"
      pattern: "TMPL_TYPE_COLORS"
---

<objective>
Переработка страницы шаблонов: color-coded карточки с type icon, colored badges, rich empty state.

Purpose: Templates page — единственный оставшийся экран без визуального характера. После этого плана он будет когерентен с registry и card (Phase 16).
Output: templates.py с новым _render_card, styles.py с TMPL_TYPE_COLORS, rich empty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@app/pages/templates.py
@app/styles.py
@app/static/design-system.css
@app/static/tokens.css

<interfaces>
<!-- Паттерны из Phase 16, применяемые здесь -->

Из app/styles.py:
```python
# Section structural (Phase 16 pattern — применить для empty state)
SECTION_LABEL = "text-xs font-semibold uppercase tracking-wider"
DIVIDER = "border-t border-slate-200 my-4"
SECTION_DIVIDER_HEADER = "text-xs font-semibold text-slate-400 uppercase tracking-wider pb-2 border-b border-slate-200 w-full mb-4"

# Accent CTA (established pattern — использовать в empty state CTA)
BTN_ACCENT_FILLED = "px-5 py-2 bg-indigo-600 text-white text-sm font-semibold rounded-lg hover:bg-indigo-700 transition-colors duration-150"

# Template card base (уже есть, расширить)
TEMPLATE_CARD = "bg-white border border-slate-200 rounded-xl p-5 cursor-pointer"
```

Из design-system.css:
```css
/* Card hover lift — уже работает, не трогать */
.q-card {
  transition: box-shadow 200ms ease, transform 200ms cubic-bezier(0.25, 1, 0.5, 1);
}
.q-card.cursor-default:hover {
  transform: translateY(-2px);
}
```

Из templates.py:
```python
# Сигнатуры функций — не менять (логика CRUD остаётся)
def _render_cards(container: ui.column) -> None  # перерендеривает весь контейнер
def _render_card(tmpl, cards_container: ui.column) -> None  # одна карточка
def build() -> None  # точка входа страницы
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Добавить TMPL_TYPE_COLORS в styles.py</name>
  <files>app/styles.py</files>
  <read_first>app/styles.py — прочитать весь файл перед изменением</read_first>
  <action>
Добавить в конец app/styles.py два новых блока:

```python
# Template type color palette (Phase 17, TMPL-01, TMPL-02)
# 4px left border color + badge bg/text per document type
# Ключи — русские строки из Config().document_types_hints
TMPL_TYPE_COLORS: dict[str, dict[str, str]] = {
    "Договор поставки":       {"border": "#4f46e5", "badge_bg": "#eef2ff", "badge_text": "#4338ca", "icon": "📦"},
    "Договор аренды":         {"border": "#059669", "badge_bg": "#d1fae5", "badge_text": "#065f46", "icon": "🏠"},
    "Трудовой договор":       {"border": "#0284c7", "badge_bg": "#e0f2fe", "badge_text": "#0369a1", "icon": "👤"},
    "Договор подряда":        {"border": "#d97706", "badge_bg": "#fef3c7", "badge_text": "#92400e", "icon": "🔧"},
    "Договор оказания услуг": {"border": "#7c3aed", "badge_bg": "#ede9fe", "badge_text": "#5b21b6", "icon": "✨"},
    "Лицензионное соглашение":{"border": "#db2777", "badge_bg": "#fce7f3", "badge_text": "#9d174d", "icon": "📄"},
    "Договор займа":          {"border": "#dc2626", "badge_bg": "#fee2e2", "badge_text": "#991b1b", "icon": "💰"},
    "Прочее":                 {"border": "#94a3b8", "badge_bg": "#f1f5f9", "badge_text": "#475569", "icon": "📋"},
}

# Fallback для неизвестных типов
TMPL_TYPE_DEFAULT = TMPL_TYPE_COLORS["Прочее"]
```

Добавить также константы для template empty state:
```python
# Template empty state (TMPL-03)
TMPL_EMPTY_ICON = "description"  # Material icon name для ui.icon()
TMPL_EMPTY_TITLE = "text-xl font-semibold text-slate-700 mt-4"
TMPL_EMPTY_BODY = "text-sm text-slate-400 text-center max-w-xs mt-2"
```
  </action>
  <verify>
    <automated>python -c "from app.styles import TMPL_TYPE_COLORS, TMPL_TYPE_DEFAULT; assert 'Прочее' in TMPL_TYPE_COLORS; assert 'border' in TMPL_TYPE_DEFAULT; print('OK')"</automated>
  </verify>
  <acceptance_criteria>grep -c "TMPL_TYPE_COLORS" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/styles.py | grep -v "^0$"</acceptance_criteria>
  <done>TMPL_TYPE_COLORS dict существует в styles.py и импортируется без ошибок</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Переработать _render_card и _render_cards в templates.py (TMPL-01, TMPL-02, TMPL-03)</name>
  <files>app/pages/templates.py</files>
  <read_first>app/pages/templates.py — прочитать весь файл перед изменением</read_first>
  <action>
**Изменить импорты** — добавить TMPL_TYPE_COLORS, TMPL_TYPE_DEFAULT, BTN_ACCENT_FILLED:
```python
from app.styles import TEXT_HEADING_2XL, TEXT_MUTED, TMPL_TYPE_COLORS, TMPL_TYPE_DEFAULT, BTN_ACCENT_FILLED
```

**Переписать `_render_card`** — добавить color-coded левую полосу (4px), type icon, colored badge (TMPL-01, TMPL-02).

Структура карточки:
```
ui.card (relative overflow-hidden rounded-xl bg-white border border-slate-200 p-0 cursor-default)
  └── ui.row (w-full h-full gap-0)
       ├── левая полоса: ui.element('div') 4px ширина, background = colors['border'], rounded слева
       └── правая часть: ui.column (p-5 gap-1 flex-1)
            ├── ui.row (items-center gap-2 mb-1)
            │    ├── ui.html(f'<span style="font-size:1.1rem">{colors["icon"]}</span>')
            │    └── ui.label(tmpl.name).classes("font-semibold text-slate-900 text-sm")
            ├── type badge: ui.html с inline style (badge_bg, badge_text)
            ├── preview: ui.label(preview).classes("text-xs text-slate-400 mt-2 line-clamp-3")
            ├── ui.label(tmpl.created_at).classes("text-xs text-slate-300 mt-1")
            └── action buttons row (без изменений — flat no-caps)
```

Конкретная реализация левой полосы (НЕ через Tailwind border-l — это 1px, нам нужно 4px filled):
```python
# Левая полоса — 4px filled div, не border
with ui.element('div').style(f'width:4px;background:{colors["border"]};border-radius:12px 0 0 12px;flex-shrink:0'):
    pass
```

Badge реализация:
```python
badge_html = (
    f'<span style="display:inline-flex;align-items:center;padding:2px 8px;'
    f'border-radius:9999px;font-size:0.7rem;font-weight:600;'
    f'background:{colors["badge_bg"]};color:{colors["badge_text"]}">'
    f'{colors["icon"]} {tmpl.contract_type}</span>'
)
ui.html(badge_html)
```

Получить цвета: `colors = TMPL_TYPE_COLORS.get(tmpl.contract_type, TMPL_TYPE_DEFAULT)`

**Карточка hover lift**: добавить `.classes("cursor-default")` на ui.card() — design-system.css уже обрабатывает `.q-card.cursor-default:hover { transform: translateY(-2px) }`.

**Переписать empty state в `_render_cards`** (TMPL-03). Вместо текущих двух строк:
```python
with ui.column().classes("w-full items-center justify-center py-20"):
    ui.icon("description").classes("text-slate-300 text-6xl")
    ui.label("Шаблоны не добавлены").classes("text-xl font-semibold text-slate-700 mt-4")
    ui.label(
        "Добавьте образец договора — система будет использовать его как эталон при проверке новых документов"
    ).classes("text-sm text-slate-400 text-center max-w-xs mt-2 mb-6")
    ui.html(
        f'<button class="{BTN_ACCENT_FILLED}" '
        f'onclick="document.querySelector(\\'[data-add-tmpl]\\').click()">Добавить первый шаблон</button>'
    )
```

Примечание: CTA кнопка в empty state должна вызывать тот же `_add_template_flow`. Реализовать через обычный ui.button с BTN_ACCENT_FILLED в props или через forward-ref паттерн. Безопаснее — передать callback:
```python
def _render_cards(container: ui.column, on_add: callable = None) -> None:
    ...
    if not templates:
        with ui.column().classes("w-full items-center justify-center py-20"):
            ui.icon("description").props('size=64px color=slate-300')
            ui.label("Шаблоны не добавлены").classes("text-xl font-semibold text-slate-700 mt-4")
            ui.label("...").classes("text-sm text-slate-400 text-center max-w-xs mt-2 mb-6")
            if on_add:
                ui.button("Добавить первый шаблон", on_click=on_add).classes(BTN_ACCENT_FILLED).props("no-caps")
        return
```

В `build()` передавать lambda:
```python
_render_cards(cards_container, on_add=lambda: _add_template_flow(cards_container))
```

И в callback `_render_card` тоже обновить вызов _render_cards чтобы передавать on_add.

**ВАЖНО:** Не менять логику CRUD (_add_template_flow, _open_edit_dialog, _open_delete_dialog) — только визуал карточки и empty state.

**Заголовок страницы** в `build()`: кнопка «+ Добавить шаблон» уже flat no-caps color=primary — оставить, но обернуть в BTN_ACCENT_FILLED если выглядит слабо. На усмотрение исполнителя — главное кнопка должна быть visible и clickable.
  </action>
  <verify>
    <automated>python -c "
import sys; sys.path.insert(0, '.')
# Проверяем что модуль импортируется без ошибок
import importlib.util
spec = importlib.util.spec_from_file_location('templates', 'app/pages/templates.py')
m = importlib.util.module_from_spec(spec)
# Проверяем наличие нужных импортов и ключевых слов
src = open('app/pages/templates.py').read()
assert 'TMPL_TYPE_COLORS' in src, 'TMPL_TYPE_COLORS не импортирован'
assert 'border-l-4' in src or 'width:4px' in src, 'нет 4px полосы'
assert 'badge_bg' in src or 'badge_html' in src, 'нет badge'
assert 'Добавить первый шаблон' in src, 'нет empty state CTA'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
grep -c "width:4px\|border-l-4" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/pages/templates.py | grep -v "^0$"
grep -c "badge_bg\|badge_html" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/pages/templates.py | grep -v "^0$"
grep -c "Добавить первый шаблон" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/pages/templates.py | grep -v "^0$"
  </acceptance_criteria>
  <done>
- Карточки шаблонов имеют 4px цветную левую полосу по типу документа
- На карточке есть emoji иконка типа + colored badge с названием типа
- Empty state содержит иконку, заголовок, описание и CTA кнопку «Добавить первый шаблон»
- Hover lift работает (через cursor-default + design-system.css)
- CRUD логика не сломана
  </done>
</task>

</tasks>

<verification>
1. `python -c "from app.pages.templates import build; print('templates OK')"` — импорт без ошибок
2. `python -c "from app.styles import TMPL_TYPE_COLORS; assert len(TMPL_TYPE_COLORS) >= 7"` — палитра типов
3. grep проверки из acceptance_criteria для обоих файлов
</verification>

<success_criteria>
- TMPL_TYPE_COLORS содержит минимум 7 типов документов
- _render_card использует 4px цветную полосу слева
- Каждая карточка имеет emoji иконку и colored badge типа
- Empty state: иконка + заголовок + описание + кнопка «Добавить первый шаблон»
- Hover lift через .cursor-default (уже в design-system.css, не трогать)
</success_criteria>

<output>
После завершения создать `.planning/phases/17-polish/17-01-SUMMARY.md`
</output>
