---
phase: 17-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/settings.py
autonomous: true
requirements: [SETT-01, SETT-02]

must_haves:
  truths:
    - "Каждая секция настроек начинается с uppercase заголовка и 1px разделителя"
    - "Под заголовком секции есть краткое описание (text-sm text-slate-400)"
    - "Активный пункт в sidebar выделен indigo-50 фоном и indigo-700 текстом, не просто белый shadow"
    - "Sidebar визуально читается как навигация, а не как список кнопок"
  artifacts:
    - path: "app/pages/settings.py"
      provides: "Рерайт sidebar active state + section headers с описаниями"
      contains: "bg-indigo-50"
  key_links:
    - from: "app/pages/settings.py"
      to: "app/styles.py"
      via: "import SECTION_DIVIDER_HEADER"
      pattern: "SECTION_DIVIDER_HEADER"
---

<objective>
Переработка страницы настроек: секции с uppercase заголовками + описаниями, sidebar с чётким active state.

Purpose: Settings — последний экран без v0.7 визуального языка. Sidebar и section headers приведены к единому стандарту Phase 16 (SECTION_DIVIDER_HEADER pattern).
Output: settings.py с обновлённым sidebar и всеми тремя секциями с описаниями.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

@app/pages/settings.py
@app/styles.py

<interfaces>
<!-- Паттерны Phase 16 для применения в settings.py -->

Из app/styles.py:
```python
# Section divider — установленный паттерн Phase 16, применить для секций настроек
SECTION_DIVIDER_HEADER = "text-xs font-semibold text-slate-400 uppercase tracking-wider pb-2 border-b border-slate-200 w-full mb-4"

TEXT_HEADING = "text-lg font-semibold text-slate-900"
TEXT_SECONDARY = "text-sm text-slate-500"
TEXT_LABEL_SECTION = "text-xs font-semibold text-slate-400 uppercase tracking-wide"
```

Из settings.py:
```python
# Sidebar nav — текущий active state (ЗАМЕНИТЬ):
btn.classes(
    remove="text-slate-600 bg-transparent",
    add="text-slate-900 bg-white shadow-sm rounded-lg",  # ← заменить на indigo
)

# Секции — текущие заголовки (ОБНОВИТЬ):
ui.label("ИИ-помощник").classes(TEXT_HEADING)
ui.label("Защита данных").classes(TEXT_HEADING)
ui.label("Уведомления в Telegram").classes(TEXT_HEADING)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sidebar active state + section headers + descriptions (SETT-01, SETT-02)</name>
  <files>app/pages/settings.py</files>
  <read_first>app/pages/settings.py — прочитать весь файл перед изменением</read_first>
  <action>
**Изменить импорты** — добавить SECTION_DIVIDER_HEADER:
```python
from app.styles import TEXT_HEADING, TEXT_LABEL_SECTION, TEXT_SECONDARY, SECTION_DIVIDER_HEADER
```

**Обновить sidebar active state** в функции `_switch` (SETT-02):

Заменить текущий active класс с `bg-white shadow-sm` на indigo-50:
```python
# БЫЛО:
add="text-slate-900 bg-white shadow-sm rounded-lg"
# СТАЛО:
add="text-indigo-700 bg-indigo-50 rounded-lg font-medium"
```

Inactive state (оставить похожим, но чуть более явным):
```python
# БЫЛО:
add="text-slate-600 bg-transparent"
# СТАЛО:
add="text-slate-600 hover:bg-slate-100 bg-transparent"
```

**Sidebar заголовок** «Настройки» — сделать SECTION_DIVIDER_HEADER или оставить TEXT_LABEL_SECTION. Использовать `SECTION_DIVIDER_HEADER` — это визуально выделяет группу навигации.

**Sidebar общий контейнер** — добавить паддинг снизу для дыхания:
```python
with ui.column().classes(
    "w-52 min-h-screen border-r border-slate-200 bg-white p-4 gap-1"
):
```
Изменить bg-slate-50 → bg-white (sidebar на белом фоне, контент на --yt-surface-bg slate-100 — так sidebar «всплывает»).

**Добавить section descriptions** в каждую render-функцию (SETT-01):

В `_render_ai_section()` — после TEXT_HEADING добавить SECTION_DIVIDER_HEADER:
```python
# Заменить структуру:
ui.label("ИИ-помощник").classes(TEXT_HEADING)
ui.label("Выберите модель...").classes(TEXT_SECONDARY)
# На:
ui.label("ИИ-помощник").classes(TEXT_HEADING + " mb-1")
ui.label("Выберите провайдера для извлечения метаданных из документов. Локальная модель работает без интернета и бесплатна.").classes(TEXT_SECONDARY + " mb-4")
ui.element('div').classes("border-t border-slate-200 w-full mb-4")  # 1px divider
```

В `_render_processing_section()`:
```python
ui.label("Защита данных").classes(TEXT_HEADING + " mb-1")
ui.label("Выберите, какие персональные данные скрывать перед отправкой на обработку ИИ.").classes(TEXT_SECONDARY + " mb-4")
ui.element('div').classes("border-t border-slate-200 w-full mb-4")
```
(убрать старый TEXT_SECONDARY label «Какие персональные данные...»)

В `_render_telegram_section()`:
```python
ui.label("Уведомления в Telegram").classes(TEXT_HEADING + " mb-1")
ui.label("Подключите бота, чтобы получать напоминания об истекающих договорах прямо в мессенджер.").classes(TEXT_SECONDARY + " mb-4")
ui.element('div').classes("border-t border-slate-200 w-full mb-4")
```
(убрать дубль description что уже есть ниже)

**Подсекция «Предупреждения» в _render_processing_section**:
```python
# Заменить:
ui.label("Предупреждения").classes("text-base font-semibold text-slate-900 mt-4")
# На SECTION_DIVIDER_HEADER:
ui.label("Предупреждения").classes(SECTION_DIVIDER_HEADER + " mt-4")
```

**ВАЖНО:** Не менять функциональность (radio buttons, checkboxes, inputs, save_setting вызовы). Только заголовки, описания, sidebar активный стейт.
  </action>
  <verify>
    <automated>python -c "
src = open('app/pages/settings.py').read()
assert 'bg-indigo-50' in src, 'нет indigo active state в sidebar'
assert 'SECTION_DIVIDER_HEADER' in src, 'нет импорта SECTION_DIVIDER_HEADER'
assert 'border-t border-slate-200' in src, 'нет 1px dividers в секциях'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
grep -c "bg-indigo-50" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/pages/settings.py | grep -v "^0$"
grep -c "border-t border-slate-200" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/app/pages/settings.py | grep -v "^0$"
  </acceptance_criteria>
  <done>
- Активный пункт sidebar: bg-indigo-50 + text-indigo-700 + font-medium
- Каждая из трёх секций имеет: heading + description text + 1px divider после
- Подсекция «Предупреждения» использует SECTION_DIVIDER_HEADER
- Функциональность (radio, checkboxes, inputs) не сломана
  </done>
</task>

</tasks>

<verification>
1. `python -c "from app.pages.settings import build; print('settings OK')"` — импорт без ошибок
2. grep проверки из acceptance_criteria
3. Визуально: sidebar «ИИ» при открытии должен быть indigo-50, не просто white shadow
</verification>

<success_criteria>
- sidebar active = bg-indigo-50 text-indigo-700 font-medium (не bg-white shadow-sm)
- Все три секции: heading + description + 1px border-t divider
- «Предупреждения» подсекция через SECTION_DIVIDER_HEADER
- settings.py импортирует без синтаксических ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/17-polish/17-02-SUMMARY.md`
</output>
