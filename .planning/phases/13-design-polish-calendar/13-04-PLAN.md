---
phase: 13-design-polish-calendar
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03]
files_modified: []
autonomous: false
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05]

must_haves:
  truths:
    - "Весь интерфейс использует slate palette без gray"
    - "CTA-кнопки indigo-600"
    - "IBM Plex Sans шрифт на всех страницах"
    - "Календарь переключается и показывает события"
    - "Staggered rows анимация на первой загрузке"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint: run the application, verify all design polish changes render correctly — palette, font, animations, calendar.

Purpose: Confirm the visual result matches design intent before closing the phase.
Output: Verified application or list of issues to fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-design-polish-calendar/13-UI-SPEC.md
@.planning/phases/13-design-polish-calendar/13-01-SUMMARY.md
@.planning/phases/13-design-polish-calendar/13-02-SUMMARY.md
@.planning/phases/13-design-polish-calendar/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated full-suite verification</name>
  <files>tests/test_design_polish.py</files>
  <action>
Run the full test suite to confirm no regressions:
1. `python -m pytest tests/test_design_polish.py -v` — all design polish tests
2. `python -m pytest tests/ -q -m "not slow"` — full suite, no regressions
3. `grep -rn "bg-gray-900" app/` — must return 0 matches
4. `grep -rn "gray" app/ | grep -v "^.*:#" | grep -v "green\|yellow\|red\|blue\|purple\|orange\|grey"` — must return 0 matches (no gray Tailwind classes remain)
5. `python -c "from app.state import AppState; s = AppState(); assert s.calendar_visible == False"` — AppState field exists

If any test fails or grep shows remaining gray usages, fix them before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/ -q -m "not slow" 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite passes, zero gray Tailwind classes remain in app/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of design polish</name>
  <files>app/main.py</files>
  <action>
Launch the application for user visual verification. Run: `python app/main.py`
User will inspect all screens for palette, font, animation, and calendar correctness.
  </action>
  <verify>
    <automated>echo "Human verification — user confirms visual result"</automated>
  </verify>
  <done>User types "approved" confirming all DSGN requirements are visually correct</done>
  <what-built>
Complete UI redesign: slate palette, indigo accent, IBM Plex Sans font, staggered row animations, page transitions, and calendar view toggle in registry.
  </what-built>
  <how-to-verify>
1. Run `cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python app/main.py`
2. **Font check:** All text should render in IBM Plex Sans (compare with previous system font)
3. **Palette check:** No gray tones — everything should have a cool blue-ish slate tint. CTA buttons are indigo (purple-blue), not dark gray.
4. **Registry page:**
   - Segment buttons: active button is indigo, not dark gray
   - Row animation: on first load, rows should appear with a subtle cascade (barely noticeable = correct)
   - Toggle buttons (list and calendar) visible to the right of segments
5. **Calendar view:**
   - Click the calendar toggle
   - Month calendar appears with events (if data exists)
   - Contract end dates shown in indigo color
   - Payment events shown in gray (slate-400)
   - Click an event — tooltip appears with counterparty name and "Открыть"
   - Click list toggle to return to table view
6. **Document card:** Back button, headings, metadata labels all use slate tones
7. **Settings page:** Left nav has slate-50 background, active section highlighted with white bg
8. **Templates page:** Cards have hover shadow transition
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human visual confirmation of all DSGN requirements.
</verification>

<success_criteria>
1. User approves visual result
2. No remaining gray classes in UI
3. Calendar toggle works with real data
4. Animations are subtle and professional
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-polish-calendar/13-04-SUMMARY.md`
</output>
