---
phase: 13-design-polish-calendar
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - app/pages/registry.py
autonomous: true
requirements: [DSGN-04]

must_haves:
  truths:
    - "Переключатель Список/Календарь виден в реестре справа от сегментов"
    - "Клик на иконку календаря показывает месячный FullCalendar с событиями"
    - "Клик на иконку списка возвращает AG Grid таблицу"
    - "События: даты окончания контрактов (indigo) и платежи (slate-400)"
    - "Клик по событию показывает tooltip с кнопкой «Открыть →»"
    - "«Открыть →» переходит на карточку документа"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "Calendar toggle buttons, _show_calendar async function, _switch_view logic"
  key_links:
    - from: "app/pages/registry.py"
      to: "services/payment_service.py"
      via: "get_calendar_events()"
      pattern: "get_calendar_events"
    - from: "app/pages/registry.py"
      to: "app/main.py"
      via: "window.initCalendar JS function"
      pattern: "ui\\.run_javascript.*initCalendar"
    - from: "app/pages/registry.py"
      to: "app/state.py"
      via: "state.calendar_visible"
      pattern: "calendar_visible"
---

<objective>
Calendar view toggle in registry page: add List/Calendar toggle buttons next to segment controls, implement async calendar data loading from payment_service + contract end dates, render FullCalendar via ui.run_javascript, handle view switching through AppState.calendar_visible.

Purpose: Lawyers can see contract deadlines and payment schedules visually on a month calendar without manually scanning the registry table.
Output: Working calendar toggle in registry with FullCalendar rendering real data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-design-polish-calendar/13-UI-SPEC.md
@.planning/phases/13-design-polish-calendar/13-RESEARCH.md
@.planning/phases/13-design-polish-calendar/13-CONTEXT.md
@.planning/phases/13-design-polish-calendar/13-01-SUMMARY.md
@app/pages/registry.py
@app/state.py
@services/payment_service.py

<interfaces>
<!-- From app/state.py (after Plan 01): -->
```python
@dataclass
class AppState:
    # ... existing fields ...
    calendar_visible: bool = False
```

<!-- From app/main.py (after Plan 01): -->
# window.initCalendar(events) — global JS function injected via _CALENDAR_JS
# Expects: Array of {id, title, start, color, extendedProps: {type, contract_id, counterparty, ...}}

<!-- From services/payment_service.py: -->
```python
def get_calendar_events(db) -> list[dict]:
    """Returns list of dicts with keys: title, start, end, color, contract_id, ...
    Current color scheme: income=#22c55e, expense=#ef4444 — OVERRIDE to #94a3b8 in registry.py
    """
```

<!-- From app/components/registry_table.py: -->
```python
_client_manager = ClientManager()  # module-level singleton
async def render_registry_table(state) -> ui.aggrid: ...
async def load_table_data(grid, state, segment) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar toggle + FullCalendar component in registry.py</name>
  <files>app/pages/registry.py</files>
  <read_first>
    - app/pages/registry.py (full file — need exact build() structure to insert toggle and calendar)
    - services/payment_service.py (get_calendar_events signature and return format)
    - app/state.py (AppState with calendar_visible)
    - .planning/phases/13-design-polish-calendar/13-UI-SPEC.md (Component 3: Calendar Toggle, Component 4: FullCalendar)
    - .planning/phases/13-design-polish-calendar/13-RESEARCH.md (Pattern 2: FullCalendar init, Pitfall 2: getElementById null, Pitfall 5: color scheme)
  </read_first>
  <action>
**Add imports at top of registry.py:**
```python
import json
from nicegui import run
from services.payment_service import get_calendar_events
```
(run is probably already imported via other paths — check. json is new.)

**Add toggle button CSS constants** after _SEG_ACTIVE/_SEG_INACTIVE:
```python
_TOGGLE_ACTIVE = "p-2 rounded-md bg-slate-100 text-slate-700"
_TOGGLE_INACTIVE = "p-2 rounded-md text-slate-400 hover:text-slate-600 hover:bg-slate-50 transition-colors duration-150"
```

**In build() function, modify the search+segments row:**

After the segment buttons `with ui.row().classes("gap-1 bg-slate-100 ...")` block (which already exists), add a toggle button pair right-aligned:

```python
# Calendar toggle (per D-15, DSGN-04)
with ui.row().classes("ml-auto items-center gap-1"):
    list_btn = ui.button("≡").props("flat no-caps").classes(_TOGGLE_ACTIVE)
    list_btn.props('title="Список"')
    cal_btn = ui.button("⊞").props("flat no-caps").classes(_TOGGLE_INACTIVE)
    cal_btn.props('title="Календарь"')
```

These buttons must be inside the existing `search-row` ui.row, after the segment container.

**Add calendar container** after grid_container:
```python
calendar_container = ui.column().classes("w-full px-6")
calendar_container.set_visibility(False)
```

**Add _switch_view function** inside build():
```python
async def _switch_view(view: str) -> None:
    state.calendar_visible = (view == "calendar")
    if state.calendar_visible:
        # Toggle button styling
        list_btn.classes(remove=_TOGGLE_ACTIVE + " " + _TOGGLE_INACTIVE)
        list_btn.classes(_TOGGLE_INACTIVE)
        cal_btn.classes(remove=_TOGGLE_ACTIVE + " " + _TOGGLE_INACTIVE)
        cal_btn.classes(_TOGGLE_ACTIVE)
        # Hide grid, show calendar
        grid_container.set_visibility(False)
        calendar_container.set_visibility(True)
        await _show_calendar()
    else:
        # Toggle button styling
        list_btn.classes(remove=_TOGGLE_ACTIVE + " " + _TOGGLE_INACTIVE)
        list_btn.classes(_TOGGLE_ACTIVE)
        cal_btn.classes(remove=_TOGGLE_ACTIVE + " " + _TOGGLE_INACTIVE)
        cal_btn.classes(_TOGGLE_INACTIVE)
        # Show grid, hide calendar
        grid_container.set_visibility(True)
        calendar_container.set_visibility(False)
```

Wire buttons: `list_btn.on_click(lambda: _switch_view("list"))` and `cal_btn.on_click(lambda: _switch_view("calendar"))`.

**Add _show_calendar async function** inside build():
```python
async def _show_calendar() -> None:
    """Fetch events, render FullCalendar container, init via JS (per D-10, D-11, D-12, D-16)."""
    db = _client_manager.get_db(state.current_client)

    # Payment events — override color to slate-400 (per Pitfall 5, D-12)
    try:
        payment_events = await run.io_bound(get_calendar_events, db)
    except Exception:
        payment_events = []
    for ev in payment_events:
        ev["color"] = "#94a3b8"  # slate-400 — all payments same color

    # Contract end-date events — indigo (per D-12)
    try:
        contracts = await run.io_bound(db.get_all_results)
    except Exception:
        contracts = []
    end_events = []
    for c in contracts:
        date_end = c.get("date_end")
        if date_end:
            end_events.append({
                "id": f"contract-{c['id']}",
                "title": f"{c.get('counterparty', '')} · {c.get('contract_type', '')}",
                "start": date_end,
                "color": "#4f46e5",  # indigo-600
                "extendedProps": {
                    "type": "end_date",
                    "contract_id": c["id"],
                    "counterparty": c.get("counterparty", ""),
                    "doc_type": c.get("contract_type", ""),
                },
            })

    all_events = payment_events + end_events
    json_str = json.dumps(all_events, ensure_ascii=False, default=str)

    calendar_container.clear()
    with calendar_container:
        ui.html('<div id="yurteg-calendar"></div>').classes("w-full")

    # Per Pitfall 2: delay JS init to ensure DOM element exists
    ui.timer(0.1, lambda: ui.run_javascript(f"window.initCalendar({json_str})"), once=True)
```

**Error handling:** If data loading fails, show a warning notification:
```python
if not payment_events and not contracts:
    ui.notify("Не удалось загрузить события календаря. Попробуйте переключить вид.", type="warning")
```

**Empty state interaction:** When empty state is rendered (_render_empty_state), hide toggle buttons since calendar makes no sense with no data:
After the empty state check in _init(), hide toggle buttons:
```python
if not rows and not state.filter_search and active_segment["value"] == "all":
    grid.set_visibility(False)
    list_btn.set_visibility(False)
    cal_btn.set_visibility(False)
    _render_empty_state(grid_container, state)
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import ast, sys
with open('app/pages/registry.py') as f:
    source = f.read()
tree = ast.parse(source)
checks = {
    'json_import': 'import json' in source or 'from json' in source,
    'get_calendar_events_import': 'get_calendar_events' in source,
    'toggle_active': '_TOGGLE_ACTIVE' in source,
    'toggle_inactive': '_TOGGLE_INACTIVE' in source,
    'calendar_visible': 'calendar_visible' in source,
    'initCalendar': 'initCalendar' in source,
    'yurteg_calendar': 'yurteg-calendar' in source,
    'indigo_600_events': '#4f46e5' in source,
    'slate_400_payments': '#94a3b8' in source,
}
for k, v in checks.items():
    status = 'PASS' if v else 'FAIL'
    print(f'  {status}: {k}')
all_pass = all(checks.values())
print(f'\nAll checks passed: {all_pass}')
sys.exit(0 if all_pass else 1)
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "calendar_visible" app/pages/registry.py returns at least 2
    - grep -c "initCalendar" app/pages/registry.py returns at least 1
    - grep -c "yurteg-calendar" app/pages/registry.py returns 1
    - grep -c "get_calendar_events" app/pages/registry.py returns at least 2 (import + usage)
    - grep -c "#4f46e5" app/pages/registry.py returns at least 1 (indigo for end dates)
    - grep -c "#94a3b8" app/pages/registry.py returns at least 1 (slate-400 for payments)
    - No syntax errors: python -c "import ast; ast.parse(open('app/pages/registry.py').read())"
  </acceptance_criteria>
  <done>Calendar toggle and FullCalendar component working in registry. List/Calendar buttons toggle between AG Grid and FullCalendar. Events show contract end dates (indigo) and payments (slate-400). Tooltip on event click shows counterparty + "Открыть →" link.</done>
</task>

</tasks>

<verification>
- `python -c "import ast; ast.parse(open('app/pages/registry.py').read()); print('Syntax OK')"` — prints "Syntax OK"
- `grep -c "initCalendar" app/pages/registry.py` — returns >= 1
- `grep -c "calendar_visible" app/pages/registry.py` — returns >= 2
- `python -m pytest tests/test_design_polish.py -x -q` — all tests still pass
</verification>

<success_criteria>
1. Toggle buttons (≡ list, ⊞ calendar) visible in registry next to segment controls
2. Clicking calendar toggle calls _show_calendar which fetches real data from payment_service and contracts
3. FullCalendar renders via window.initCalendar JS with correct event colors
4. Clicking list toggle returns to AG Grid view
5. Calendar tooltip shows event info with "Открыть →" navigation
6. Empty state hides toggle buttons
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-polish-calendar/13-03-SUMMARY.md`
</output>
