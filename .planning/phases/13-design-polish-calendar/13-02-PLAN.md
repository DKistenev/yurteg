---
phase: 13-design-polish-calendar
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/registry.py
  - app/pages/document.py
  - app/pages/settings.py
  - app/pages/templates.py
  - app/components/header.py
  - app/components/onboarding/splash.py
  - app/components/onboarding/tour.py
autonomous: true
requirements: [DSGN-01, DSGN-02]

must_haves:
  truths:
    - "Все gray-* Tailwind классы в app/pages/ и app/components/ заменены на slate-* эквиваленты"
    - "Все bg-gray-900 CTA-кнопки заменены на bg-indigo-600"
    - "text-gray-900 для заголовков заменён на text-slate-900 (NOT indigo)"
    - "Семантические цвета (green, yellow, red, blue, purple, orange) НЕ затронуты"
    - "Hover transitions добавлены к карточкам шаблонов и навигации настроек"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "_SEG_ACTIVE with bg-indigo-600, all gray replaced with slate"
    - path: "app/components/header.py"
      provides: "Header with slate palette and indigo active tab border"
    - path: "app/components/onboarding/splash.py"
      provides: "CTA buttons with bg-indigo-600, bg-slate-50 for capability section"
  key_links:
    - from: "app/pages/registry.py"
      to: "_SEG_ACTIVE constant"
      via: "Tailwind class string"
      pattern: "bg-indigo-600"
    - from: "app/components/header.py"
      to: "_nav_link function"
      via: "Tailwind class string"
      pattern: "text-slate-600.*hover:text-slate-900"
---

<objective>
Per-file palette migration: replace all gray-* Tailwind classes with slate-* equivalents and bg-gray-900 with bg-indigo-600 across all app/pages/ and app/components/ files. This is the bulk visual migration — every file handled individually per the UI-SPEC migration map.

Purpose: Cohesive slate palette across the entire UI. No file uses gray-* after this plan.
Output: 7 migrated files with consistent slate/indigo palette.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-design-polish-calendar/13-UI-SPEC.md
@.planning/phases/13-design-polish-calendar/13-CONTEXT.md
@app/pages/registry.py
@app/pages/document.py
@app/pages/settings.py
@app/pages/templates.py
@app/components/header.py
@app/components/onboarding/splash.py
@app/components/onboarding/tour.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate registry.py, document.py, header.py</name>
  <files>app/pages/registry.py, app/pages/document.py, app/components/header.py</files>
  <read_first>
    - app/pages/registry.py (full file — _SEG_ACTIVE, _SEG_INACTIVE, _render_empty_state, all .classes() calls)
    - app/pages/document.py (full file — metadata grid, nav buttons, section headings)
    - app/components/header.py (full file — header bg, logo, nav links, upload button, client dropdown)
    - .planning/phases/13-design-polish-calendar/13-UI-SPEC.md (Color section — neutral ramp migration map)
  </read_first>
  <action>
**CRITICAL RULE: NO batch sed/regex. Read each file, find each gray-* usage, apply the correct replacement per migration map. bg-gray-900 → bg-indigo-600 (CTA only). text-gray-900 → text-slate-900 (headings).**

**app/pages/registry.py:**
- `_SEG_ACTIVE`: `bg-gray-900` → `bg-indigo-600` (per D-24, this is a CTA/active element)
- `_SEG_INACTIVE`: `text-gray-600` → `text-slate-600`, `hover:bg-gray-100` → `hover:bg-slate-100`
- `_render_empty_state`:
  - Folder SVG stroke `#d1d5db` → `#cbd5e1` (slate-300)
  - `text-gray-900` → `text-slate-900` (heading)
  - `text-gray-500` → `text-slate-500` (body text)
  - `bg-gray-900` → `bg-indigo-600` (CTA button)
  - `text-gray-400` → `text-slate-400` (hint bullets)
- `build()`:
  - `bg-gray-100` (segment container) → `bg-slate-100`
  - `text-gray-500` (count label) → `text-slate-500`
  - `text-gray-400` (file label) → `text-slate-400`
  - `text-gray-500` in _confirm_delete → `text-slate-500`
- Add `transition-colors duration-150` to segment buttons for hover effect (per D-19)

**app/pages/document.py:**
- `_render_metadata`:
  - `text-gray-400` (field labels) → `text-slate-400`
  - `text-gray-900` (field values) → `text-slate-900`
  - `text-gray-700` (conditions) → `text-slate-700`
- `_render_deviations`:
  - `#9ca3af` → `#94a3b8` (slate-400)
  - `#6b7280` → `#64748b` (slate-500)
  - `#111827` → `#0f172a` (slate-900)
- `build()`:
  - `text-gray-500` (not found) → `text-slate-500`
  - `text-gray-600` (back button, cancel) → `text-slate-600`
  - `text-gray-900` (headings) → `text-slate-900`
  - `text-gray-700` (metadata section heading) → `text-slate-700`
  - `text-gray-400` (version labels) → `text-slate-400`
  - `text-gray-500` (status reset button) → `text-slate-500`

**app/components/header.py:**
- `render_header`:
  - `border-gray-200` → `border-slate-200`
  - `text-gray-900` (logo) → `text-slate-900`
  - `text-gray-700` (upload button) → `text-slate-700`
  - `text-gray-600` (profile button) → `text-slate-600`
- `_switch`:
  - `text-gray-600` → `text-slate-600`
  - `text-gray-900` → `text-slate-900`
- `_nav_link`:
  - `text-gray-600` → `text-slate-600`
  - `hover:text-gray-900` → `hover:text-slate-900`
  - `hover:border-gray-900` → `hover:border-slate-900`
  - Add `transition-colors duration-150` to nav links (per D-19)
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -rn "gray" app/pages/registry.py app/pages/document.py app/components/header.py | grep -v "^.*:#.*gray" | grep -v "green\|yellow\|red\|blue\|purple\|orange" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "bg-gray-900" app/pages/registry.py returns 0
    - grep -c "bg-indigo-600" app/pages/registry.py returns at least 1
    - grep -c "gray" app/pages/document.py returns 0 (no gray classes remain, except in comments)
    - grep -c "gray" app/components/header.py returns 0 (no gray classes remain)
    - grep -c "text-slate-900" app/components/header.py returns at least 1 (logo)
    - grep -c "border-slate-200" app/components/header.py returns 1
    - Semantic colors (green, yellow, red, blue, purple, orange) untouched in document.py
  </acceptance_criteria>
  <done>Registry, document, and header files fully migrated from gray to slate/indigo. All CTA bg-gray-900 replaced with bg-indigo-600. All text headings text-gray-900 replaced with text-slate-900.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate settings.py, templates.py, splash.py, tour.py</name>
  <files>app/pages/settings.py, app/pages/templates.py, app/components/onboarding/splash.py, app/components/onboarding/tour.py</files>
  <read_first>
    - app/pages/settings.py (full file — left nav, section headings, form labels)
    - app/pages/templates.py (full file — card grid, dialog headings, action buttons)
    - app/components/onboarding/splash.py (full file — wizard CTAs, capability bullets)
    - app/components/onboarding/tour.py (full file — tooltip styling, overlay, inline JS hex)
    - .planning/phases/13-design-polish-calendar/13-UI-SPEC.md (Color section)
  </read_first>
  <action>
**Same critical rule: per-file, manual replacement. bg-gray-900 → bg-indigo-600 for CTAs. text-gray-900 → text-slate-900 for headings.**

**app/pages/settings.py:**
- `border-gray-200` → `border-slate-200`
- `bg-gray-50` → `bg-slate-50` (left nav background)
- `text-gray-400` → `text-slate-400` (section label)
- `text-gray-600` → `text-slate-600` (nav buttons, body text)
- `text-gray-900` → `text-slate-900` (active nav button, section headings like "AI-провайдер", "Анонимизация")
- `text-gray-500` → `text-slate-500` (descriptions)
- `text-gray-700` → `text-slate-700` (checkbox labels)
- Add `transition-colors duration-150 hover:bg-slate-50` to nav buttons (per D-19)

**app/pages/templates.py:**
- `text-gray-900` → `text-slate-900` (card names, dialog headings, page heading)
- `text-gray-500` → `text-slate-500` (card type labels)
- `text-gray-400` → `text-slate-400` (preview text, date, status labels, page subtitle)
- `text-gray-300` → `text-slate-300` (empty state hint, card date)
- `text-gray-600` → `text-slate-600` (delete dialog subtitle)
- Add `transition-colors duration-150 hover:bg-slate-100` to template cards (per D-19 — on the ui.card element in _render_card)

**app/components/onboarding/splash.py:**
- `text-gray-900` → `text-slate-900` (logo, headings)
- `bg-gray-50` → `bg-slate-50` (capability bullets section)
- `text-gray-400` → `text-slate-400` (bullet dots, skip buttons)
- `text-gray-600` → `text-slate-600` (bullet text, skip hover)
- `text-gray-500` → `text-slate-500` (progress label)
- `bg-gray-900` → `bg-indigo-600` (2 CTA buttons: "Далее: Telegram" and "Сохранить и начать")
- `hover:text-gray-600` → `hover:text-slate-600` (skip button hover)
- Progress bar `color=grey-9` → `color=indigo` (Quasar prop, matches accent)
- Progress bar `track-color=grey-3` → `track-color=grey-3` (keep — Quasar uses own grey scale, not Tailwind)

**app/components/onboarding/tour.py:**
- Inline CSS `border: 1px solid #e5e7eb` → `border: 1px solid #e2e8f0` (slate-200 instead of gray-200)
- Inline JS hex values in showStep function:
  - `color: #9ca3af` (step counter) → `color: #94a3b8` (slate-400)
  - `color: #111827` (title) → `color: #0f172a` (slate-900)
  - `color: #6b7280` (body) → `color: #64748b` (slate-500)
  - `color: #9ca3af` (skip button) → `color: #94a3b8` (slate-400)
  - `background: #111827` (next button) → `background: #4f46e5` (indigo-600 — this is a CTA)
- `applySpotlight` outline: `#111827` → `#4f46e5` (indigo-600 — spotlight ring = accent)
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && total=$(grep -rn "gray" app/pages/settings.py app/pages/templates.py app/components/onboarding/splash.py app/components/onboarding/tour.py | grep -v "^.*:#" | grep -v "green\|yellow\|red\|blue\|purple\|orange\|grey-3" | wc -l); echo "Remaining gray usages: $total"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "bg-gray-900" app/components/onboarding/splash.py returns 0
    - grep -c "bg-indigo-600" app/components/onboarding/splash.py returns 2 (both CTAs)
    - grep -c "bg-gray-50" app/pages/settings.py returns 0
    - grep -c "bg-slate-50" app/pages/settings.py returns at least 1
    - grep -c "#111827" app/components/onboarding/tour.py returns 0
    - grep -c "#4f46e5" app/components/onboarding/tour.py returns at least 2
    - grep -c "text-gray-900" app/pages/templates.py returns 0
    - grep -c "text-slate-900" app/pages/templates.py returns at least 3
    - grep -c "transition-colors" app/pages/templates.py returns at least 1 (hover on cards)
  </acceptance_criteria>
  <done>Settings, templates, splash, and tour files fully migrated. All bg-gray-900 CTAs → bg-indigo-600. All text headings → slate-900. Tour inline JS hex codes migrated to slate/indigo ramp. Hover transitions added.</done>
</task>

</tasks>

<verification>
- `grep -rn "bg-gray-900" app/pages/ app/components/` — returns 0 matches
- `grep -rn "gray" app/pages/ app/components/ | grep -v "^.*:#" | grep -v "green\|yellow\|red\|blue\|purple\|orange\|grey-3"` — returns 0 matches (no gray-* Tailwind classes remain outside comments and semantic colors)
- `python -m pytest tests/test_design_polish.py::test_seg_active_indigo -x -q` — passes
</verification>

<success_criteria>
1. Zero `bg-gray-900` occurrences in app/pages/ and app/components/ (was 4, now 0)
2. Zero `gray-*` Tailwind classes in app/ (all migrated to slate-* equivalents)
3. All `text-gray-900` headings → `text-slate-900` (NOT indigo)
4. Semantic status colors (green, yellow, red) untouched
5. Hover transitions added to template cards, settings nav, header nav links
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-polish-calendar/13-02-SUMMARY.md`
</output>
