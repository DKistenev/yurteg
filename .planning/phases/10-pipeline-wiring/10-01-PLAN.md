---
phase: 10-pipeline-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/process.py
  - app/components/header.py
autonomous: true
requirements:
  - PROC-01
  - PROC-02

must_haves:
  truths:
    - "Кнопка «+ Загрузить» видна в header на любой странице"
    - "Клик по кнопке открывает нативный macOS folder picker"
    - "Выбор папки запускает pipeline через run.io_bound (UI не зависает)"
    - "Отмена folder picker ничего не делает (no crash)"
    - "Повторный клик во время обработки невозможен (кнопка disabled)"
  artifacts:
    - path: "app/components/process.py"
      provides: "Folder picker + async pipeline runner"
      exports: ["pick_folder", "start_pipeline"]
    - path: "app/components/header.py"
      provides: "Upload button in persistent header"
      contains: "Загрузить"
  key_links:
    - from: "app/components/header.py"
      to: "app/components/process.py"
      via: "import pick_folder, start_pipeline"
      pattern: "from app.components.process import"
    - from: "app/components/process.py"
      to: "services/pipeline_service.py"
      via: "run.io_bound(pipeline_service.process_archive, ...)"
      pattern: "run\\.io_bound.*process_archive"
---

<objective>
Create the pipeline trigger mechanism: native folder picker + async pipeline runner, wired to header upload button.

Purpose: Enable the lawyer to select a folder of documents and kick off processing without blocking the UI.
Output: `app/components/process.py` (new), updated `app/components/header.py` with upload button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pipeline-wiring/10-CONTEXT.md
@.planning/phases/10-pipeline-wiring/10-RESEARCH.md

@app/components/header.py
@app/state.py
@services/pipeline_service.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/state.py:
```python
@dataclass
class AppState:
    processing: bool = False
    source_dir: str = ""
    current_client: str = "Основной реестр"
    # ... other fields

def get_state() -> AppState:
    ...
```

From services/pipeline_service.py:
```python
def process_archive(
    source_dir: Path,
    config: Config,
    grouping: str = "both",
    force_reprocess: bool = False,
    on_progress: Optional[Callable[[int, int, str], None]] = None,
    on_file_done: Optional[Callable] = None,
    output_dir_override: Optional[Path] = None,
) -> dict:
    # Returns: dict(total, done, errors, skipped, output_dir, report_path)
```

From app/components/header.py:
```python
def render_header(state: AppState) -> None:
    # Current signature — will add on_upload callback parameter
```

From app/components/registry_table.py:
```python
async def load_table_data(grid, state: "AppState", segment: str = "all") -> None:
    ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create process.py — folder picker and async pipeline runner</name>
  <files>app/components/process.py</files>
  <read_first>
    - app/state.py
    - services/pipeline_service.py
    - config.py
    - .planning/phases/10-pipeline-wiring/10-RESEARCH.md (Pattern 1, Pattern 2, Pitfalls 1-5)
  </read_first>
  <action>
Create `app/components/process.py` with two public functions:

1. `async def pick_folder() -> Path | None` — per D-03, D-04:
   - `import webview` at top level
   - `await app.native.main_window.create_file_dialog(dialog_type=webview.FOLDER_DIALOG)`
   - Return `Path(result[0])` if result, else `None` (Pitfall 1: always check `if not result`)

2. `async def start_pipeline(source_dir: Path, state: AppState, ui_refs: dict) -> dict` — per D-16, D-09:
   - `ui_refs` dict contains keys: `section` (ui.column), `bar` (ui.linear_progress), `count` (ui.label), `file_label` (ui.label), `error_col` (ui.column), `upload_btn` (ui.button)
   - Capture `loop = asyncio.get_running_loop()` BEFORE calling run.io_bound (Pitfall 5)
   - Set `state.processing = True`, disable upload button, show progress section
   - Define `on_progress(current, total, message)` closure:
     - Debounce: track `last_update = [0.0]`, skip if `time.monotonic() - last_update[0] < 0.5` (Claude's discretion from CONTEXT.md)
     - Use `loop.call_soon_threadsafe(ui_refs['bar'].set_value, val)` for bar
     - Use `loop.call_soon_threadsafe(ui_refs['count'].set_text, f"{current}/{total} файлов")` for count
     - Use `loop.call_soon_threadsafe(ui_refs['file_label'].set_text, message)` for filename
   - Define `on_file_done(result)` closure:
     - If `result.status == "error"`: append `(result.file_info.filename, result.error_message)` to `error_entries` list
   - Call `stats = await run.io_bound(pipeline_service.process_archive, source_dir, config, on_progress=on_progress, on_file_done=on_file_done)`
   - After completion: set `state.processing = False`, enable upload button
   - Build toast message: `f"Обработано {done} документов"` + `f" ({errors} ошибок)"` if errors (D-10)
   - Call `ui.notify(msg, type="positive" if not errors else "warning")` (D-10)
   - Render error log in `ui_refs['error_col']` via `_render_error_log()` helper (D-13, D-14)
   - If no errors: hide progress section immediately. If errors: `ui.timer(10, hide, once=True)` (D-12, specifics)
   - Return stats dict

3. Private helper `_render_error_log(error_col, error_entries: list[tuple[str, str]])` — per D-13, D-14:
   - `error_col.clear()`
   - For each (filename, message): `ui.expansion(f"✗ {filename}").classes("text-red-600 text-sm")` with `ui.label(message)` inside

Import `Config` from `config`, `pipeline_service` from `services`, `asyncio`, `time`, `run` and `ui` from `nicegui`.
  </action>
  <verify>
    <automated>cd "/Users/danilakistenev/Downloads/Личное/ЮР тэг/yurteg" && python -c "from app.components.process import pick_folder, start_pipeline; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "pick_folder" app/components/process.py returns matches
    - grep "start_pipeline" app/components/process.py returns matches
    - grep "call_soon_threadsafe" app/components/process.py returns matches
    - grep "run.io_bound" app/components/process.py returns matches
    - grep "FOLDER_DIALOG" app/components/process.py returns matches
    - grep "time.monotonic" app/components/process.py returns matches (debounce)
    - grep "_render_error_log" app/components/process.py returns matches
  </acceptance_criteria>
  <done>process.py exports pick_folder and start_pipeline, uses run.io_bound for async, call_soon_threadsafe for UI updates, debounces progress, renders error log with expandable items</done>
</task>

<task type="auto">
  <name>Task 2: Add upload button to header — per D-01, D-02</name>
  <files>app/components/header.py</files>
  <read_first>
    - app/components/header.py
    - app/components/process.py (just created in Task 1)
    - app/state.py
  </read_first>
  <action>
Modify `app/components/header.py`:

1. Change signature: `render_header(state: AppState, on_upload=None) -> None` — add optional `on_upload` callback parameter. This is cleaner than storing callback in state (Open Question 1 from RESEARCH).

2. Add `from app.components.process import pick_folder` at top.

3. In render_header, between the center nav tabs row and the right client dropdown row, add the upload button (D-01: рядом с табами, перед иконкой профиля):

```python
# Upload button — per D-01, D-02
async def _on_upload_click():
    if state.processing:
        return
    source_dir = await pick_folder()
    if source_dir and on_upload:
        await on_upload(source_dir)

upload_btn = ui.button(
    "+ Загрузить",
    on_click=_on_upload_click,
).props("flat no-caps").classes("text-sm text-gray-700 shrink-0")
```

4. Store `upload_btn` reference so it can be passed as part of ui_refs to `start_pipeline`. Add it to `state` temporarily or return it. Simplest: make the button accessible by storing ref in a module-level dict:
   - Before render_header: `_header_refs = {"upload_btn": None}`
   - After creating button: `_header_refs["upload_btn"] = upload_btn`
   - Export `_header_refs` so registry.py can read the button ref

5. When `state.processing` is True, the button should appear disabled. Use `upload_btn.bind_enabled_from(state, 'processing', backward=lambda p: not p)` OR simpler: just check `state.processing` in `_on_upload_click` (already done above) and visually indicate with `.props('disable')` bound to state.

Simpler approach per Claude's discretion: just guard with `if state.processing: return` in click handler. The `start_pipeline` function handles disable/enable of the button via `ui_refs['upload_btn']`.
  </action>
  <verify>
    <automated>cd "/Users/danilakistenev/Downloads/Личное/ЮР тэг/yurteg" && grep -n "Загрузить" app/components/header.py && grep -n "pick_folder" app/components/header.py && grep -n "on_upload" app/components/header.py</automated>
  </verify>
  <acceptance_criteria>
    - grep "Загрузить" app/components/header.py returns match (button text)
    - grep "pick_folder" app/components/header.py returns match (import)
    - grep "on_upload" app/components/header.py returns match (callback parameter)
    - grep "_header_refs" app/components/header.py returns match (button ref export)
    - Header signature includes on_upload parameter
  </acceptance_criteria>
  <done>Upload button visible in header, calls pick_folder on click, delegates to on_upload callback with Path, guards against double-click during processing</done>
</task>

</tasks>

<verification>
- `python -c "from app.components.process import pick_folder, start_pipeline"` succeeds
- `grep "Загрузить" app/components/header.py` shows button
- `grep "on_upload" app/components/header.py` shows callback parameter
- `grep "FOLDER_DIALOG" app/components/process.py` shows native picker
- `grep "run.io_bound" app/components/process.py` shows async pipeline call
</verification>

<success_criteria>
- process.py created with pick_folder + start_pipeline functions
- header.py has upload button with on_upload callback
- Native folder picker used (not ui.upload)
- Pipeline runs via run.io_bound (non-blocking)
- Progress callbacks use call_soon_threadsafe (thread-safe)
- Debounce on progress updates (500ms)
</success_criteria>

<output>
After completion, create `.planning/phases/10-pipeline-wiring/10-01-SUMMARY.md`
</output>
