---
phase: "35"
plan: "01"
type: "verification-and-polish"
autonomous: true
requirements: [TMPL-01, SETT-01, ONBR-01]
---

# Phase 35, Plan 01: Templates, Settings & Onboarding Polish

## Objective
Verify and polish auxiliary screens (Templates, Settings, Onboarding) for visual consistency and end-to-end functionality.

## Tasks

### Task 1: Templates Visual Consistency (TMPL-01)
type="auto"
- Fix emoji icons rendered inside `<span class="material-icons">` — use emoji directly
- Add `min-w-0` to flex-1 content columns to prevent overflow on narrow viewports
- Align demo card padding (p-5) with actual card padding
- Section header icons use proper emoji sizing

### Task 2: Settings Scroll-to-Section (SETT-01)
type="auto"
- Verify scroll-to-section already implemented in Phase 33
- Verify section IDs exist on all 3 sections (ai, processing, notifications)
- Verify summary card click triggers scrollIntoView
- **Expected: already done, skip if verified**

### Task 3: Onboarding Wizard + Tour E2E (ONBR-01)
type="auto"
- Verify splash wizard renders and completes (sets first_run_completed=True)
- Verify all 5 tour targets exist in DOM (upload, nav, filters, calendar, workspace)
- Verify tour completion callback saves tour_completed=True
- Verify tour restart button in header works
- **Expected: already done, fix if broken**

## Success Criteria
1. Template cards use emoji icons directly (no material-icons wrapper)
2. Template cards don't overflow on narrow viewports (min-w-0)
3. Settings scroll-to-section works via summary card click
4. Onboarding splash and tour complete without errors
