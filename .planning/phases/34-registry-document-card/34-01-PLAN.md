---
phase: "34"
plan: "01"
type: frontend
autonomous: true
wave: 1
depends_on: ["33-01"]
requirements: [REG-01, REG-02, REG-03, DOC-01, DOC-02]
---

# Phase 34 Plan 01: Registry & Document Card Polish

## Objective

Довести реестр и карточку документа до демо-качества: поиск с иконкой, календарь с навигацией, фильтрация прошлых событий, превью PDF, feedback при сохранении.

## Tasks

### Task 1: REG-01 — Search icon + clear button
- type: auto
- Add `clearable` and `prepend-inner-icon=search` props to search input in registry.py
- Done: search input shows magnifying glass icon and X clear button

### Task 2: REG-02 — Filter past events in calendar
- type: auto
- Add "past" group for events where date < today
- Past events rendered as collapsible "Просрочены -- N" header, collapsed by default
- Future events in "week" group exclude past dates
- Done: "На этой неделе" shows only future events; past events in collapsible section

### Task 3: REG-03 — Mini-calendar month navigation
- type: auto
- Add state dict for year/month, < > buttons, "Сегодня" reset link
- Re-render calendar on navigation clicks using container.clear() pattern
- Done: mini-calendar navigates between months, "Сегодня" resets to current month

### Task 4: DOC-01 — PDF preview (verify existing)
- type: auto
- Verify two-column layout with iframe already exists in document.py
- Done: confirmed existing implementation matches D-01 through D-04

### Task 5: DOC-02 — Toast on note save
- type: auto
- Add `ui.notify("Сохранено", type="positive")` after successful db.update_review
- Done: toast appears after saving note

## Verification

1. Search input has magnifying glass icon and clear button
2. Mini-calendar has < > navigation buttons and "Сегодня" link
3. Past calendar events in collapsible "Просрочены" section
4. Document card has two-column PDF preview layout
5. Note save shows "Сохранено" toast

## Success Criteria

- All 5 requirements (REG-01, REG-02, REG-03, DOC-01, DOC-02) addressed
- No regressions in existing calendar/registry functionality
