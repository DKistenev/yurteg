---
phase: 19-onboarding-demo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/process.py
  - app/pages/templates.py
autonomous: true
requirements:
  - RBST-01

must_haves:
  truths:
    - "В web mode (без pywebview) pick_folder() не крашится — возвращает None с уведомлением"
    - "В web mode _pick_file() в templates.py не крашится — graceful fallback"
    - "В native mode всё работает как раньше — нет регрессии"
  artifacts:
    - path: "app/components/process.py"
      provides: "pick_folder() с try/except для ImportError/AttributeError webview"
      contains: "webview"
    - path: "app/pages/templates.py"
      provides: "_pick_file() с try/except webview import"
      contains: "webview"
  key_links:
    - from: "app/components/process.py"
      to: "webview"
      via: "try/except ImportError — возвращает None без краша"
      pattern: "ImportError"
    - from: "app/pages/templates.py"
      to: "webview"
      via: "try/except ImportError в _pick_file()"
      pattern: "ImportError"
---

<objective>
Добавить graceful fallback в pick_folder() и _pick_file() когда приложение запущено в web mode без pywebview.

Purpose: В web mode (`python -m streamlit run` или `nicegui run` без `--native`) вызов `app.native.main_window.create_file_dialog()` бросает AttributeError или webview не импортируется. RBST-01 требует чтобы приложение не крашилось.
Output: Защищённые pick_folder() и _pick_file() с try/except
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Extracted from app/components/process.py -->
```python
# Текущая реализация pick_folder():
import webview
from nicegui import app, run, ui

async def pick_folder() -> Optional[Path]:
    result = await app.native.main_window.create_file_dialog(
        dialog_type=webview.FOLDER_DIALOG,
    )
    if not result:
        return None
    return Path(result[0])
```

<!-- Extracted from app/pages/templates.py -->
```python
# Текущая реализация _pick_file():
import webview
from nicegui import app, run, ui

async def _pick_file() -> Optional[Path]:
    result = await app.native.main_window.create_file_dialog(
        dialog_type=webview.OPEN_DIALOG,
        file_types=("PDF файлы (*.pdf)", "Word документы (*.docx)"),
    )
    if not result:
        return None
    return Path(result[0])
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Web mode fallback в pick_folder() и _pick_file()</name>
  <files>app/components/process.py, app/pages/templates.py</files>
  <action>
**app/components/process.py** — обновить pick_folder():

Заменить прямой импорт webview на try/except в самой функции (не на уровне модуля — webview может не быть установлен):

```python
async def pick_folder() -> Optional[Path]:
    """Открывает нативный OS folder picker.

    Per D-03/D-04: app.native.main_window.create_file_dialog с FOLDER_DIALOG.
    Pitfall 1: всегда проверять `if result` перед использованием.
    RBST-01: в web mode (без pywebview) — graceful fallback с уведомлением.

    Returns:
        Path к выбранной папке или None если юрист отменил диалог или web mode.
    """
    try:
        import webview  # noqa: PLC0415 — local import guard for web mode
        result = await app.native.main_window.create_file_dialog(
            dialog_type=webview.FOLDER_DIALOG,
        )
        if not result:
            return None
        return Path(result[0])
    except (ImportError, AttributeError):
        # Web mode: pywebview недоступен или app.native не инициализирован
        ui.notify(
            "Выбор папки недоступен в веб-режиме. Используйте кнопку «Загрузить тестовые данные».",
            type="warning",
            timeout=5000,
        )
        return None
```

Убрать `import webview` из верхнего уровня модуля (строка 16 `import webview`) — заменить на локальный импорт внутри функции.

---

**app/pages/templates.py** — обновить _pick_file():

Аналогичный паттерн:

```python
async def _pick_file() -> Optional[Path]:
    """Открывает нативный OS file picker для PDF/DOCX.

    Pitfall 5: всегда проверять `if result` перед использованием.
    RBST-01: graceful fallback в web mode.
    """
    try:
        import webview  # noqa: PLC0415
        result = await app.native.main_window.create_file_dialog(
            dialog_type=webview.OPEN_DIALOG,
            file_types=("PDF файлы (*.pdf)", "Word документы (*.docx)"),
        )
        if not result:
            return None
        return Path(result[0])
    except (ImportError, AttributeError):
        ui.notify(
            "Выбор файла недоступен в веб-режиме.",
            type="warning",
            timeout=4000,
        )
        return None
```

Убрать `import webview` из верхнего уровня templates.py (строка 13 `import webview`).

ВАЖНО: не трогать ничего кроме import webview и самих функций — всё остальное остаётся без изменений.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import ast, pathlib

# Проверить что верхнеуровневого 'import webview' нет
proc_src = pathlib.Path('app/components/process.py').read_text()
tmpl_src = pathlib.Path('app/pages/templates.py').read_text()

# try/except с ImportError должен быть
assert 'ImportError' in proc_src, 'Missing ImportError guard in process.py'
assert 'ImportError' in tmpl_src, 'Missing ImportError guard in templates.py'

# Локальный import webview внутри try
assert 'import webview' in proc_src, 'Missing local webview import in process.py'
assert 'import webview' in tmpl_src, 'Missing local webview import in templates.py'

# Модуль импортируется без ошибок
from app.components import process
print('OK: web mode fallback in place')
"</automated>
  </verify>
  <done>
  - pick_folder() и _pick_file() защищены try/except (ImportError, AttributeError)
  - В web mode показывают ui.notify с объяснением и возвращают None
  - В native mode поведение идентично прежнему
  - Верхнеуровневый `import webview` удалён из обоих файлов
  </done>
</task>

</tasks>

<verification>
python -c "from app.components.process import pick_folder" — без ошибок
grep -n "ImportError" app/components/process.py
grep -n "ImportError" app/pages/templates.py
python -m py_compile app/components/process.py app/pages/templates.py
</verification>

<success_criteria>
- Оба файла компилируются без ошибок
- try/except (ImportError, AttributeError) в pick_folder() и _pick_file()
- ui.notify с объяснением в fallback ветке
- Верхнеуровневый `import webview` удалён — модули импортируются в web-окружении без webview
</success_criteria>

<output>
После завершения создать `.planning/phases/19-onboarding-demo/19-03-SUMMARY.md`
</output>
