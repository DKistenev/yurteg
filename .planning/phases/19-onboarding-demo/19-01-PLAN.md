---
phase: 19-onboarding-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/onboarding/tour.py
  - app/components/header.py
autonomous: true
requirements:
  - ONBR-01
  - ONBR-02

must_haves:
  truths:
    - "При первом открытии реестра с документами автоматически запускается тур"
    - "Тур подсвечивает 5 элементов интерфейса по очереди с карточками-подсказками"
    - "В header есть маленькая кнопка «? Гид» — нажатие сбрасывает флаг и перезапускает тур"
    - "Тур выглядит как SaaS-качество: тёмный оверлей, indigo outline, белая карточка с тенью"
  artifacts:
    - path: "app/components/onboarding/tour.py"
      provides: "5-step guided tour — TOUR_STEPS обновлён, render_tour без изменений сигнатуры"
      contains: "TOUR_STEPS"
    - path: "app/components/header.py"
      provides: "Кнопка «? Гид» между upload CTA и client dropdown"
      contains: "tour-guide-btn"
  key_links:
    - from: "app/components/header.py"
      to: "app/components/onboarding/tour.py"
      via: "reset tour_completed + ui.navigate.to('/') для перезапуска тура"
      pattern: "save_setting.*tour_completed.*False"
    - from: "app/pages/registry.py"
      to: "app/components/onboarding/tour.py"
      via: "render_tour вызывается в _init() при rows и not tour_completed"
      pattern: "render_tour"
---

<objective>
Обновить guided tour до 5 шагов (вместо 3) с улучшенным визуалом, добавить кнопку «? Гид» в header для повторного запуска.

Purpose: Юрист должен понимать приложение с первого открытия — ONBR-01 требует тур при первом входе, ONBR-02 требует повторный запуск по желанию.
Output: Обновлённый tour.py с 5 шагами + кнопка в header.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Extracted from app/components/onboarding/tour.py -->
```python
# Сигнатура render_tour — НЕ МЕНЯТЬ (вызывается из registry.py)
def render_tour(on_complete: Callable) -> None:
    # Hidden NiceGUI button — JS вызывает его click() когда тур завершён/пропущен
    # ui.html(tour_html) — один вызов в конце

# TOUR_STEPS — список dict с ключами: target, title, body, position
# position values: "center-top" | "below-left" | "below-right"
TOUR_STEPS = [...]  # 3 шага сейчас → 5 шагов после

# HEX из styles.py:
HEX = {
    "indigo_600": "#4f46e5",
    "slate_400": "#94a3b8",
    "slate_500": "#64748b",
    "slate_900": "#0f172a",
}
```

<!-- Extracted from app/components/header.py -->
```python
# render_header сигнатура — НЕ МЕНЯТЬ
def render_header(state: AppState, on_upload: Optional[Callable] = None) -> None:

# Структура header (справа налево):
# [лого] [Реестр · Шаблоны · ⚙] [+ Загрузить документы] [📁 client_dropdown]
# Добавить «? Гид» между upload_btn и client dropdown row

# save_setting из config:
from config import load_settings, save_setting
save_setting("tour_completed", False)  # сброс флага тура
```

<!-- CSS-классы целей тура в DOM -->
# target ".ag-root-wrapper" — AG Grid таблица
# target ".search-row" — строка поиска+фильтров
# target "#upload-btn" — кнопка загрузки (props назначается в registry.py)
# target ".q-header" — header целиком (для шага навигации)
# Новые targets для шагов 4-5 (client dropdown и calendar toggle):
# target ".q-header .shrink-0:last-child" — client dropdown row
# target "#calendar-toggle" — обёртка calendar toggle (нужно добавить id в registry.py)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Обновить TOUR_STEPS до 5 шагов, улучшить визуал tooltip</name>
  <files>app/components/onboarding/tour.py</files>
  <action>
Обновить TOUR_STEPS — расширить с 3 до 5 шагов per CONTEXT.md (ONBR-01):
1. Upload button — «Загрузка документов» (target: "#upload-btn", position: "below-right")
2. Navigation tabs — «Навигация» (target: ".q-header", position: "center-top"), body: «Реестр, Шаблоны и Настройки»
3. Search + filters — «Фильтры и поиск» (target: ".search-row", position: "below-left")
4. Calendar toggle — «Вид календаря» (target: "#calendar-toggle", position: "below-right"), body: «Переключите на вид календаря для просмотра дат платежей и окончания договоров»
5. Client dropdown — «Рабочие пространства» (target: ".q-header .shrink-0:last-child", position: "below-right"), body: «Создавайте отдельные пространства для разных клиентов»

Улучшить tooltip визуал (делать КРАСИВО — per user: «как в лучших SaaS»):
- Добавить box-shadow: 0 20px 60px rgba(0,0,0,0.15), 0 4px 16px rgba(0,0,0,0.1) на tooltip
- Увеличить max-width: 300px (было 256px)
- Добавить border-radius: 12px (было 8px)
- Добавить прогресс-бар под счётчиком шага: thin indigo bar width proportional to step
- Кнопка «Далее» — увеличить padding: 10px 28px
- overlay z-index: 9000, tooltip z-index: 9001 (чтобы не конфликтовать с Quasar dialogs)
- Добавить step indicator dots внизу карточки (inactive = slate-200, active = indigo-600)

НЕ менять сигнатуру render_tour(on_complete) — registry.py вызывает без изменений.
НЕ менять паттерн hidden button для on_complete callback.

В registry.py нужно будет добавить id="calendar-toggle" на обёртку calendar toggle — но это в следующем таске (или после). В этом таске просто пишем target "#calendar-toggle" в steps — он найдёт элемент после Task 2 добавит id.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from app.components.onboarding.tour import TOUR_STEPS, render_tour; assert len(TOUR_STEPS) == 5, f'Expected 5 steps, got {len(TOUR_STEPS)}'; print('OK: 5 steps')"</automated>
  </verify>
  <done>TOUR_STEPS содержит ровно 5 шагов, каждый с target/title/body/position. render_tour импортируется без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: Добавить кнопку «? Гид» в header + id=calendar-toggle в registry</name>
  <files>app/components/header.py, app/pages/registry.py</files>
  <action>
В app/components/header.py — добавить кнопку «? Гид» (ONBR-02):

Добавить импорт: `from config import load_settings, save_setting`

В render_header(), между upload_btn и блоком client dropdown, добавить:

```python
# «? Гид» — маленькая, subtle, не конкурирует с upload CTA
ui.button(
    "? Гид",
    on_click=_restart_tour,
).props('flat no-caps id=tour-guide-btn aria-label="Запустить тур по приложению"').classes(
    "text-xs text-slate-400 hover:text-slate-200 transition-colors duration-150 px-2"
)
```

Добавить функцию _restart_tour как closure внутри render_header (или как отдельную nested def):
```python
def _restart_tour() -> None:
    save_setting("tour_completed", False)
    ui.navigate.to("/")  # перезагружает реестр, там _init() проверит флаг и запустит тур
```

В app/pages/registry.py — добавить id="calendar-toggle" на обёртку calendar toggle (строка ~145):
Найти: `with ui.row().classes("items-center gap-1 bg-slate-100 p-1 rounded-lg"):`
Изменить на: `with ui.row().classes("items-center gap-1 bg-slate-100 p-1 rounded-lg").props("id=calendar-toggle"):`

Это позволяет туру найти элемент по селектору "#calendar-toggle".
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import ast, pathlib
src = pathlib.Path('app/components/header.py').read_text()
assert 'tour-guide-btn' in src, 'Missing tour-guide-btn'
assert 'tour_completed' in src, 'Missing tour_completed reset'
src2 = pathlib.Path('app/pages/registry.py').read_text()
assert 'calendar-toggle' in src2, 'Missing calendar-toggle id'
print('OK: tour button and calendar-toggle id present')
"</automated>
  </verify>
  <done>
  - В header между upload CTA и client dropdown появилась кнопка «? Гид» (flat, text-xs, slate-400)
  - Клик сбрасывает tour_completed=False и делает navigate.to("/")
  - В registry.py calendar toggle wrapper имеет id="calendar-toggle"
  </done>
</task>

</tasks>

<verification>
python -c "from app.components.onboarding.tour import TOUR_STEPS; assert len(TOUR_STEPS)==5"
python -c "from app.components.header import render_header" — импорт без ошибок
grep -n "tour-guide-btn" app/components/header.py
grep -n "calendar-toggle" app/pages/registry.py
</verification>

<success_criteria>
- 5 шагов тура: upload → nav → search/filters → calendar → client
- Tooltip: box-shadow, max-w 300px, radius 12px, step dots, прогресс-линейка
- Кнопка «? Гид» в header (flat, subtle, text-xs)
- id="calendar-toggle" на обёртке toggle
- render_tour(on_complete) сигнатура не изменена
</success_criteria>

<output>
После завершения создать `.planning/phases/19-onboarding-demo/19-01-SUMMARY.md`
</output>
