---
phase: 28-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/llama_server.py
  - modules/ai_extractor.py
autonomous: true
requirements: [STAB-01, STAB-02]

must_haves:
  truths:
    - "atexit.register(self.stop) вызывается ровно 1 раз — вне retry-цикла"
    - "При тексте длиннее 30K символов logging.warning вызывается с сообщением об обрезке"
    - "При тексте короче 30K символов warning не вызывается"
    - "python -c 'from services.llama_server import LlamaServerManager' работает"
  artifacts:
    - path: "services/llama_server.py"
      provides: "LlamaServerManager.start() с atexit вне retry-цикла"
    - path: "modules/ai_extractor.py"
      provides: "extract_metadata с truncation warning при >30K символов"
  key_links:
    - from: "services/llama_server.py"
      to: "atexit.register"
      via: "вызов в start() — ДОЛЖЕН быть вне for-цикла"
      pattern: "atexit.register"
---

<objective>
Два точечных stability-фикса: atexit регистрируется один раз, пользователь видит предупреждение при обрезке документа.

Purpose: atexit в retry-цикле регистрирует stop() до 3 раз — при выходе llama-server останавливается трижды (ошибки). Обрезка текста молчаливая — юрист не знает что 15% документа не проанализировано.
Output: services/llama_server.py с atexit вне цикла; modules/ai_extractor.py с logging.warning при обрезке.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/ROADMAP.md
@.planning/workstreams/backend/REQUIREMENTS.md
@.planning/workstreams/backend/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: atexit.register() вынести из retry-цикла в llama_server.py</name>
  <read_first>
    - services/llama_server.py (строки 197-288 — метод start(), особенно строки 241-252: for attempt in range(3) и atexit.register(self.stop) на строке 252)
  </read_first>
  <files>services/llama_server.py</files>
  <behavior>
    - Тест 1: atexit.register вызывается ровно 1 раз при успешном старте на первой попытке
    - Тест 2: atexit.register вызывается ровно 1 раз при успешном старте на второй попытке (первая упала)
    - Тест 3: atexit.register НЕ вызывается если сервер не запустился ни на одном порту
  </behavior>
  <action>
    Текущая структура start():
    ```python
    for attempt in range(3):
        port = self._port + attempt
        cmd = ...
        try:
            self._process = subprocess.Popen(...)
            atexit.register(self.stop)   # ← ПРОБЛЕМА: вызывается каждую попытку
            ...
            if started:
                self._port = port
                return
            else:
                self.stop()
        except OSError:
            ...
    ```

    Исправление — вынести atexit ПОСЛЕ успешного старта, прямо перед return:
    ```python
    for attempt in range(3):
        port = self._port + attempt
        cmd = ...
        try:
            self._process = subprocess.Popen(...)
            # НЕТ atexit.register здесь

            # Ожидание готовности
            ...

            if started:
                self._port = port
                atexit.register(self.stop)   # ← ОДИН РАЗ, только при успехе
                logger.info("llama-server запущен на порту %d (PID %d)", port, self._process.pid)
                return
            else:
                ...
                self.stop()
        except OSError:
            ...
    ```

    Убедиться что `atexit.register(self.stop)` встречается в методе start() ровно ОДИН раз и только внутри блока `if started:`.

    Написать тест в новый файл `tests/test_llama_server_atexit.py`:
    ```python
    from unittest.mock import patch, MagicMock, call
    import atexit as _atexit

    def test_atexit_registered_once_on_first_attempt():
        from services.llama_server import LlamaServerManager
        mgr = LlamaServerManager(port=19090)
        with patch("subprocess.Popen") as mock_popen, \
             patch("urllib.request.urlopen") as mock_url, \
             patch("atexit.register") as mock_atexit, \
             patch.object(mgr, "is_running", return_value=False):
            mock_proc = MagicMock()
            mock_proc.poll.return_value = None
            mock_popen.return_value = mock_proc
            mock_resp = MagicMock()
            mock_resp.__enter__ = MagicMock(return_value=mock_resp)
            mock_resp.__exit__ = MagicMock(return_value=False)
            mock_resp.status = 200
            mock_url.return_value = mock_resp
            mgr.start()
            assert mock_atexit.call_count == 1

    def test_atexit_not_registered_on_failure():
        from services.llama_server import LlamaServerManager
        mgr = LlamaServerManager(port=19091)
        with patch("subprocess.Popen") as mock_popen, \
             patch("urllib.request.urlopen", side_effect=Exception("refused")), \
             patch("atexit.register") as mock_atexit, \
             patch.object(mgr, "is_running", return_value=False), \
             patch("time.sleep"):
            mock_proc = MagicMock()
            mock_proc.poll.return_value = None
            mock_popen.return_value = mock_proc
            mgr.start()
            assert mock_atexit.call_count == 0
    ```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from services.llama_server import LlamaServerManager; print('OK')"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "atexit.register" services/llama_server.py</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_llama_server_atexit.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "atexit.register" services/llama_server.py` — ровно одна строка (count=1)
    - Эта строка находится ВНУТРИ блока `if started:` — проверить контекст через grep -A2 -B2
    - `pytest tests/test_llama_server_atexit.py -x` — код 0, оба теста PASSED
    - `python -c "from services.llama_server import LlamaServerManager"` — код 0
  </acceptance_criteria>
  <done>atexit регистрируется ровно 1 раз при успешном старте; тесты подтверждают</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Добавить logging.warning при обрезке текста >30K символов</name>
  <read_first>
    - modules/ai_extractor.py (строки 259-263 — начало extract_metadata, строка с `text = anonymized_text[:30_000]`)
  </read_first>
  <files>modules/ai_extractor.py</files>
  <behavior>
    - Тест 1: extract_metadata с текстом 31000 символов — logging.warning вызывается с сообщением содержащим "обрезан" и длину
    - Тест 2: extract_metadata с текстом 5000 символов — logging.warning НЕ вызывается
    - Тест 3: extract_metadata с текстом ровно 30000 символов — logging.warning НЕ вызывается (граница не включается в предупреждение)
  </behavior>
  <action>
    Найти строку в extract_metadata:
    ```python
    # Обрезать текст если слишком длинный (30K достаточно для 95% документов)
    text = anonymized_text[:30_000]
    ```

    Заменить на:
    ```python
    # Обрезать текст если слишком длинный (30K достаточно для 95% документов)
    original_len = len(anonymized_text)
    text = anonymized_text[:30_000]
    if original_len > 30_000:
        logger.warning(
            "Текст документа обрезан до 30 000 символов (оригинал: %d символов). "
            "Часть документа не будет проанализирована.",
            original_len,
        )
    ```

    Написать тест в `tests/test_truncation_warning.py`:
    ```python
    import logging
    from unittest.mock import patch, MagicMock

    def test_warning_on_long_text(caplog):
        from modules.ai_extractor import extract_metadata
        from config import Config
        cfg = Config()
        long_text = "А" * 31_000
        mock_provider = MagicMock()
        mock_provider.name = "test"
        mock_provider.complete.return_value = '{"document_type":"Договор","counterparty":"ООО Тест","subject":"Тест","confidence":0.9,"parties":[],"special_conditions":[]}'
        with caplog.at_level(logging.WARNING, logger="modules.ai_extractor"):
            extract_metadata(long_text, cfg, provider=mock_provider)
        assert any("обрезан" in r.message for r in caplog.records), \
            f"Expected truncation warning, got: {[r.message for r in caplog.records]}"

    def test_no_warning_on_short_text(caplog):
        from modules.ai_extractor import extract_metadata
        from config import Config
        cfg = Config()
        short_text = "А" * 5_000
        mock_provider = MagicMock()
        mock_provider.name = "test"
        mock_provider.complete.return_value = '{"document_type":"Договор","counterparty":"ООО Тест","subject":"Тест","confidence":0.9,"parties":[],"special_conditions":[]}'
        with caplog.at_level(logging.WARNING, logger="modules.ai_extractor"):
            extract_metadata(short_text, cfg, provider=mock_provider)
        assert not any("обрезан" in r.message for r in caplog.records)
    ```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from modules.ai_extractor import extract_metadata; print('OK')"</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "original_len\|обрезан" modules/ai_extractor.py</automated>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_truncation_warning.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "original_len" modules/ai_extractor.py` — находит строку `original_len = len(anonymized_text)`
    - `grep "logger.warning" modules/ai_extractor.py | grep "обрезан"` — находит строку с предупреждением
    - `pytest tests/test_truncation_warning.py -x` — код 0, оба теста PASSED
    - `python -c "from modules.ai_extractor import extract_metadata"` — код 0
  </acceptance_criteria>
  <done>extract_metadata логирует warning при обрезке >30K; тесты подтверждают оба случая</done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# Atexit ровно 1 раз
grep -c "atexit.register" services/llama_server.py   # должно быть 1

# Truncation warning присутствует
grep "обрезан" modules/ai_extractor.py

# Все новые тесты проходят
python -m pytest tests/test_llama_server_atexit.py tests/test_truncation_warning.py -v 2>&1 | tail -15
```
</verification>

<success_criteria>
- atexit.register(self.stop) вызывается ровно 1 раз в методе start(), внутри блока if started
- extract_metadata логирует warning при тексте длиннее 30K символов с указанием оригинальной длины
- Оба новых тестовых файла существуют и проходят
</success_criteria>

<output>
После выполнения создать `.planning/phases/28-cleanup/28-03-SUMMARY.md`
</output>
