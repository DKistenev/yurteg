---
phase: 08-registry-view
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/pages/registry.py
  - app/components/registry_table.py
  - app/components/header.py
autonomous: true
requirements: [REG-02, REG-03, REG-05]

must_haves:
  truths:
    - "Сегментированный фильтр «Все · Истекают ⚠ · Требуют внимания» переключает данные таблицы"
    - "Текстовый поиск с debounce сужает результаты в реальном времени (fuzzy, AND-logic)"
    - "Клик по строке навигирует на /document/{doc_id}"
    - "Переключение клиента в header перезагружает таблицу данными нового клиента"
  artifacts:
    - path: "app/pages/registry.py"
      provides: "Search input, segmented filter, grid event wiring"
      contains: "search_input"
    - path: "app/components/header.py"
      provides: "Client dropdown in profile area"
      contains: "client_menu"
  key_links:
    - from: "app/pages/registry.py"
      to: "app/components/registry_table.py"
      via: "load_table_data() on segment change and search input"
      pattern: "load_table_data"
    - from: "app/components/header.py"
      to: "services/client_manager.py"
      via: "ClientManager.list_clients() for dropdown"
      pattern: "list_clients"
    - from: "app/pages/registry.py"
      to: "/document/{doc_id}"
      via: "ui.navigate.to on cellClicked"
      pattern: "navigate.to"
---

<objective>
Добавить поисковую строку с debounce, сегментированный фильтр, навигацию по клику и переключение клиента.

Purpose: Юрист должен мгновенно находить нужный документ и переключаться между клиентами — это основные рабочие сценарии реестра.
Output: Полнофункциональная страница реестра с поиском, фильтрами, навигацией и client switching.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-registry-view/08-CONTEXT.md
@.planning/phases/08-registry-view/08-RESEARCH.md
@.planning/phases/08-registry-view/08-01-SUMMARY.md

@app/state.py
@app/pages/registry.py
@app/components/registry_table.py
@app/components/header.py
@services/client_manager.py

<interfaces>
From app/components/registry_table.py (created in 08-01):
```python
COLUMN_DEFS: list[dict]  # AG Grid column definitions
async def render_registry_table(state: AppState) -> ui.aggrid: ...
async def load_table_data(grid: ui.aggrid, state: AppState, segment: str = "all") -> None: ...
def _fetch_rows(client_name: str, segment: str, search: str, warning_days: int) -> list[dict]: ...
def _fuzzy_filter(rows: list[dict], query: str) -> list[dict]: ...
```

From app/state.py:
```python
class AppState:
    current_client: str = "Основной реестр"
    filter_search: str = ""
    warning_days_threshold: int = 30
```

From services/client_manager.py:
```python
class ClientManager:
    DEFAULT_CLIENT = "Основной реестр"
    def list_clients(self) -> list[str]: ...
    def get_db(self, name: str) -> Database: ...
    def add_client(self, name: str) -> Path: ...
```

From app/components/header.py:
```python
def render_header(state: AppState) -> None: ...
# Right side has placeholder: ui.label('👤▾').classes('text-gray-500 text-sm')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search bar, segmented filter, row click navigation</name>
  <files>app/pages/registry.py, app/components/registry_table.py</files>
  <read_first>
    - app/pages/registry.py (current implementation from 08-01)
    - app/components/registry_table.py (COLUMN_DEFS, render_registry_table, load_table_data)
    - app/state.py (AppState fields)
    - .planning/phases/08-registry-view/08-RESEARCH.md (Pattern 8 segments, Pitfall 6 debounce, Pattern 5 cellClicked)
  </read_first>
  <action>
    1. Update `app/pages/registry.py` to add search bar and segmented filter (per D-04, D-07, D-18):

       ```python
       from nicegui import ui
       from app.state import get_state
       from app.components.registry_table import render_registry_table, load_table_data

       # Segment styling — literal classes per D-24
       _SEG_ACTIVE = "px-4 py-1.5 text-sm font-medium rounded-md bg-gray-900 text-white"
       _SEG_INACTIVE = "px-4 py-1.5 text-sm font-medium rounded-md text-gray-600 hover:bg-gray-100"

       def build() -> None:
           state = get_state()
           active_segment = {"value": "all"}
           grid_ref = {"grid": None}
           seg_buttons = {}

           with ui.column().classes('w-full'):
               # Search + Segments row
               with ui.row().classes('w-full px-6 pt-4 pb-2 items-center gap-4'):
                   search_input = ui.input(placeholder="Поиск по реестру...").props('outlined dense').classes('flex-1 max-w-md')

                   with ui.row().classes('gap-1 bg-gray-100 p-1 rounded-lg'):
                       for key, label in [("all", "Все"), ("expiring", "Истекают ⚠"), ("attention", "Требуют внимания")]:
                           btn = ui.button(label, on_click=lambda k=key: _switch_segment(k)).props('flat unelevated no-caps')
                           btn.classes(_SEG_ACTIVE if key == "all" else _SEG_INACTIVE)
                           seg_buttons[key] = btn

               # AG Grid
               grid = await render_registry_table(state)
               grid_ref["grid"] = grid

               # Row click → navigate to document (D-18)
               async def _on_cell_clicked(e):
                   col_id = e.args.get("colId", "")
                   if col_id == "actions":
                       return  # D-19: don't navigate on action clicks
                   data = e.args.get("data", {})
                   doc_id = data.get("id")
                   if doc_id:
                       ui.navigate.to(f"/document/{doc_id}")

               grid.on("cellClicked", _on_cell_clicked)

               # Initial data load
               await load_table_data(grid, state, "all")
       ```

       The `build()` function should be `async def build()` if NiceGUI sub_pages supports it; if not, wrap async work in `ui.timer(0, _init, once=True)` as shown in RESEARCH code example.

    2. Implement `_switch_segment(key)`:
       - Update `active_segment["value"] = key`
       - Update button classes: active segment gets _SEG_ACTIVE, others get _SEG_INACTIVE
       - Call `await load_table_data(grid, state, key)` to reload with new segment

    3. Implement debounced search (per Pitfall 6 from RESEARCH, 300ms debounce):
       ```python
       _timer = [None]
       def _on_search(e):
           state.filter_search = e.value if hasattr(e, 'value') else str(e.args)
           if _timer[0]:
               _timer[0].cancel()
           _timer[0] = ui.timer(0.3, lambda: _do_search(), once=True)

       async def _do_search():
           if grid_ref["grid"]:
               await load_table_data(grid_ref["grid"], state, active_segment["value"])

       search_input.on("update:model-value", _on_search)
       ```

    4. Update `load_table_data` in `app/components/registry_table.py` if needed — ensure it passes `state.filter_search` to `_fetch_rows`.

    Per D-04: Three segments — Все · Истекают ⚠ · Требуют внимания.
    Per D-05: "Истекают" filters by computed_status == "expiring".
    Per D-06: "Требуют внимания" filters by validation_score < 0.7 OR non-empty validation_warnings.
    Per D-07, D-08, D-09: Fuzzy search with 80% threshold, multi-word AND-logic (already in _fuzzy_filter).
    Per D-10, D-11: AG Grid column filters work alongside Python filters (AND logic).
    Per D-18: Row click navigates to /document/{doc_id}.
    Per D-19: Action column clicks don't trigger navigation (checked by colId).
  </action>
  <verify>
    <automated>python -c "from app.pages.registry import build; print('OK')" && grep -q "Истекают" app/pages/registry.py && grep -q "navigate.to" app/pages/registry.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Поиск по реестру" app/pages/registry.py
    - grep -q "Истекают" app/pages/registry.py
    - grep -q "Требуют внимания" app/pages/registry.py
    - grep -q "navigate.to" app/pages/registry.py
    - grep -q "timer.*0.3\|timer.*300" app/pages/registry.py (debounce)
    - grep -q "cellClicked" app/pages/registry.py
    - python -m pytest tests/test_registry_view.py -x -q passes
  </acceptance_criteria>
  <done>Search bar with 300ms debounce filters table via rapidfuzz. Three segments (Все/Истекают/Требуют внимания) toggle data. Row click navigates to /document/{id}. Column filters (floatingFilter) work alongside Python-side filters.</done>
</task>

<task type="auto">
  <name>Task 2: Client switching dropdown in header</name>
  <files>app/components/header.py</files>
  <read_first>
    - app/components/header.py (current implementation)
    - services/client_manager.py (list_clients, get_db, add_client)
    - app/state.py (AppState.current_client)
    - .planning/phases/08-registry-view/08-RESEARCH.md (Pattern 7 client dropdown)
  </read_first>
  <action>
    Update `app/components/header.py` to replace the `👤▾` placeholder with a functional client dropdown (per D-20, D-21, D-22):

    1. Import `ClientManager` and `ui` dialog:
       ```python
       from services.client_manager import ClientManager
       ```

    2. Replace the right-side placeholder in `render_header()`:
       ```python
       # Right: client dropdown (D-20)
       with ui.row().classes('shrink-0 items-center gap-1'):
           profile_btn = ui.button(
               f'👤 {state.current_client}',
               on_click=lambda: client_menu.open()
           ).props('flat no-caps').classes('text-sm text-gray-600')

           with ui.menu() as client_menu:
               cm = ClientManager()
               for name in cm.list_clients():
                   ui.menu_item(
                       name,
                       on_click=lambda n=name: _switch_client(state, n, profile_btn, client_menu)
                   )
               ui.separator()
               ui.menu_item('+ Добавить клиента', on_click=lambda: _show_add_dialog(state, cm, profile_btn, client_menu))
       ```

    3. Implement `_switch_client` (per D-21):
       ```python
       def _switch_client(state: AppState, name: str, btn, menu) -> None:
           state.current_client = name
           state.filter_search = ""  # reset filters on switch
           btn.text = f'👤 {name}'
           menu.close()
           ui.navigate.to("/")  # reload registry with new client data
       ```

    4. Implement `_show_add_dialog` — simple `ui.dialog` with `ui.input` for new client name. On confirm:
       ```python
       def _show_add_dialog(state, cm, btn, menu):
           menu.close()
           with ui.dialog() as dlg, ui.card():
               ui.label('Новый клиент').classes('text-lg font-medium')
               name_input = ui.input('Название клиента').props('outlined')
               with ui.row().classes('gap-2 justify-end w-full'):
                   ui.button('Отмена', on_click=dlg.close).props('flat')
                   async def _add():
                       n = name_input.value.strip()
                       if n:
                           await run.io_bound(cm.add_client, n)
                           _switch_client(state, n, btn, None)
                           dlg.close()
                   ui.button('Добавить', on_click=_add).props('flat color=primary')
           dlg.open()
       ```

    Per D-20: Profile icon 👤▾ in header → dropdown with clients + "Добавить клиента".
    Per D-21: On switch — table reloads with new client data, filters reset, stay on registry.
    Per D-22: Use ClientManager.list_clients() for list, ClientManager.get_db() for DB access.
  </action>
  <verify>
    <automated>python -c "from app.components.header import render_header; print('OK')" && grep -q "list_clients\|ClientManager" app/components/header.py && grep -q "client_menu\|ui.menu" app/components/header.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ClientManager" app/components/header.py
    - grep -q "list_clients" app/components/header.py
    - grep -q "ui.menu" app/components/header.py
    - grep -q "Добавить клиента" app/components/header.py
    - grep -q "_switch_client\|switch_client" app/components/header.py
    - grep -q "navigate.to" app/components/header.py
    - python -m pytest tests/test_registry_view.py -x -q passes
  </acceptance_criteria>
  <done>Client dropdown in header shows all clients from ClientManager. Selecting a client reloads registry. "Добавить клиента" opens dialog to create new client. Filters reset on switch.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_registry_view.py -x -q` — all tests still pass
- `python -c "from app.pages.registry import build"` — no import errors
- `grep -q "navigate.to" app/pages/registry.py` — row click wired
- `grep -q "Истекают" app/pages/registry.py` — segments present
- `grep -q "ClientManager" app/components/header.py` — client switching wired
</verification>

<success_criteria>
1. Сегментированный фильтр переключает данные таблицы между Все/Истекают/Требуют внимания (REG-05)
2. Поиск с debounce 300ms фильтрует результаты через rapidfuzz (REG-03)
3. Клик по строке навигирует на /document/{doc_id} (REG-02)
4. Переключение клиента в header перезагружает таблицу (prep for SETT-05)
5. AG Grid column filters работают совместно с Python-фильтрами
</success_criteria>

<output>
After completion, create `.planning/phases/08-registry-view/08-02-SUMMARY.md`
</output>
