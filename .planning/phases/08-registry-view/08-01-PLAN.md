---
phase: 08-registry-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_registry_view.py
  - app/components/registry_table.py
  - app/pages/registry.py
  - app/main.py
autonomous: true
requirements: [REG-01, REG-04]

must_haves:
  truths:
    - "Таблица реестра отображает все документы из БД с колонками: тип, контрагент, статус, сумма"
    - "Статус-бейджи визуально различимы по цвету (зелёный/жёлтый/красный/серый)"
    - "Скрытые колонки (date_end, validation_score, filename, processed_at) не видны по умолчанию"
    - "Данные отсортированы по дате обработки (новейшие сверху)"
  artifacts:
    - path: "app/components/registry_table.py"
      provides: "AG Grid table component, COLUMN_DEFS, _fetch_rows, _fuzzy_filter"
      exports: ["COLUMN_DEFS", "render_registry_table", "_fetch_rows", "_fuzzy_filter"]
    - path: "app/pages/registry.py"
      provides: "Registry page build() calling registry_table"
    - path: "tests/test_registry_view.py"
      provides: "Unit tests for data layer and fuzzy filter"
  key_links:
    - from: "app/pages/registry.py"
      to: "app/components/registry_table.py"
      via: "render_registry_table() call"
      pattern: "render_registry_table"
    - from: "app/components/registry_table.py"
      to: "services/lifecycle_service.py"
      via: "get_computed_status_sql import"
      pattern: "get_computed_status_sql"
    - from: "app/components/registry_table.py"
      to: "services/client_manager.py"
      via: "ClientManager.get_db() for data fetch"
      pattern: "ClientManager"
---

<objective>
Создать data layer реестра и базовую AG Grid таблицу с живыми данными из SQLite и статус-бейджами.

Purpose: Это фундамент для всех последующих фич реестра (поиск, сегменты, hover-actions). Без рабочей таблицы с данными ничего не визуально.
Output: Работающая таблица документов на странице «Документы» с 4 видимыми колонками, статус-бейджами и тестами.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-registry-view/08-CONTEXT.md
@.planning/phases/08-registry-view/08-RESEARCH.md

@app/state.py
@app/main.py
@app/pages/registry.py
@app/components/header.py
@services/lifecycle_service.py
@services/client_manager.py
@modules/database.py

<interfaces>
From app/state.py:
```python
@dataclass
class AppState:
    current_client: str = "Основной реестр"
    selected_doc_id: Optional[int] = None
    filter_type: str = ""
    filter_status: str = ""
    filter_search: str = ""
    warning_days_threshold: int = 30

def get_state() -> AppState: ...
```

From services/lifecycle_service.py:
```python
MANUAL_STATUSES = frozenset({"terminated", "extended", "negotiation", "suspended"})
STATUS_LABELS = {
    "active": ("✔", "Действует", "#22c55e"),
    "expiring": ("⚠", "Скоро истекает", "#f59e0b"),
    "expired": ("✗", "Истёк", "#ef4444"),
    "unknown": ("?", "Нет даты", "#9ca3af"),
    "terminated": ("✖", "Расторгнут", "#6b7280"),
    "extended": ("↻", "Продлён", "#3b82f6"),
    "negotiation": ("~", "На согласовании", "#8b5cf6"),
    "suspended": ("⏸", "Приостановлен", "#f97316"),
}
def get_computed_status_sql(warning_days: int) -> str: ...
def set_manual_status(db: Database, contract_id: int, status: str) -> None: ...
```

From services/client_manager.py:
```python
class ClientManager:
    DEFAULT_CLIENT = "Основной реестр"
    def list_clients(self) -> list[str]: ...
    def get_db(self, name: str) -> Database: ...
    def add_client(self, name: str) -> Path: ...
```

From modules/database.py:
```python
class Database:
    def __init__(self, db_path: Path) -> None: ...
    conn: sqlite3.Connection  # row_factory = sqlite3.Row
    def get_all_results(self) -> list[dict]: ...
```

From app/main.py:
```python
@ui.page("/")
def root() -> None:
    ui.dark_mode(value=False)
    state = get_state()
    render_header(state)
    ui.sub_pages({
        "/": registry.build,
        "/document/{doc_id}": document.build,
        ...
    })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold + data layer (_fetch_rows, _fuzzy_filter)</name>
  <files>tests/test_registry_view.py, app/components/registry_table.py</files>
  <read_first>
    - services/lifecycle_service.py (get_computed_status_sql, STATUS_LABELS)
    - services/client_manager.py (ClientManager API)
    - modules/database.py (Database class, get_all_results, conn, _SCHEMA)
    - app/state.py (AppState fields)
    - .planning/phases/08-registry-view/08-RESEARCH.md (Pattern 1, Pattern 2)
  </read_first>
  <behavior>
    - test_fetch_rows_returns_dicts_with_computed_status: _fetch_rows returns list[dict] where each dict has 'computed_status' key
    - test_fetch_rows_orders_by_processed_at_desc: first row has most recent processed_at
    - test_fuzzy_filter_single_word: _fuzzy_filter("аренда", rows) returns only rows where contract_type/counterparty/subject/filename/amount fuzzy-matches "аренда" at ≥80%
    - test_fuzzy_filter_multi_word_and_logic: _fuzzy_filter("аренда Ромашка", rows) requires BOTH words to match (AND logic per D-09)
    - test_fuzzy_filter_empty_query: _fuzzy_filter("", rows) returns all rows
    - test_fetch_rows_segment_expiring: with segment="expiring", returns only rows with computed_status="expiring" (per D-05)
    - test_fetch_rows_segment_attention: with segment="attention", returns rows with validation_score<0.7 or non-empty validation_warnings (per D-06)
  </behavior>
  <action>
    1. Create `tests/test_registry_view.py` with pytest fixtures:
       - `tmp_db` fixture: creates in-memory SQLite DB with `modules.database.Database` using a temp path. Inserts 5-10 test contracts with varied fields:
         - 2 active (date_end in future, validation_score=0.9)
         - 1 expiring (date_end within 30 days)
         - 1 expired (date_end in past)
         - 1 low-quality (validation_score=0.5, validation_warnings='["missing field"]')
         - 1 with manual_status="terminated"
       - Insert via `db.conn.execute("INSERT INTO contracts ...")`

    2. Create `app/components/registry_table.py` with:
       - `_client_manager = ClientManager()` at module level (singleton, per Pitfall 5 in RESEARCH)
       - `_SEARCH_FIELDS = ("contract_type", "counterparty", "subject", "filename", "amount")`
       - `_THRESHOLD = 80` (per D-08)

       - `def _fuzzy_filter(rows: list[dict], query: str) -> list[dict]:`
         Per D-07, D-08, D-09: split query into words, for each row join all _SEARCH_FIELDS into haystack, check `fuzz.partial_ratio(word, haystack) >= _THRESHOLD` for ALL words. Use `from rapidfuzz import fuzz`.

       - `def _fetch_rows(client_name: str, segment: str, search: str, warning_days: int) -> list[dict]:`
         Synchronous function (called via run.io_bound). Per Pattern 1 from RESEARCH:
         ```python
         db = _client_manager.get_db(client_name)
         sql = f"""
             SELECT id, contract_type, counterparty, amount, filename, subject,
                    date_end, validation_score, validation_warnings, processed_at,
                    manual_status,
                    {get_computed_status_sql(warning_days)} AS computed_status
             FROM contracts
             WHERE status = 'done'
             ORDER BY processed_at DESC
         """
         rows = [dict(r) for r in db.conn.execute(sql, {"warning_days": warning_days}).fetchall()]
         ```
         Then apply segment filter (per D-05, D-06):
         - "expiring": filter rows where computed_status == "expiring"
         - "attention": filter rows where validation_score < 0.7 OR json.loads(validation_warnings or "[]") is non-empty
         Then apply fuzzy filter if search is non-empty.
         Return rows.

    3. Write tests that directly test _fuzzy_filter and _fetch_rows (importing from app.components.registry_table). For _fetch_rows tests, mock ClientManager.get_db to return the tmp_db fixture.

    Note: _fetch_rows tests need to mock `_client_manager` module variable. Use `monkeypatch.setattr("app.components.registry_table._client_manager", mock_cm)`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_registry_view.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "_fuzzy_filter" app/components/registry_table.py
    - grep -q "_fetch_rows" app/components/registry_table.py
    - grep -q "partial_ratio" app/components/registry_table.py
    - grep -q "get_computed_status_sql" app/components/registry_table.py
    - grep -q "test_fuzzy_filter" tests/test_registry_view.py
    - grep -q "test_fetch_rows" tests/test_registry_view.py
    - python -m pytest tests/test_registry_view.py -x -q passes
  </acceptance_criteria>
  <done>_fetch_rows returns filtered dicts with computed_status from real DB. _fuzzy_filter implements multi-word AND-logic with 80% threshold via rapidfuzz. All 7 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: AG Grid table with status badges on registry page</name>
  <files>app/components/registry_table.py, app/pages/registry.py, app/main.py</files>
  <read_first>
    - app/components/registry_table.py (just created — _fetch_rows, _fuzzy_filter)
    - app/pages/registry.py (current stub)
    - app/main.py (ui.run, sub_pages, head_html location)
    - .planning/phases/08-registry-view/08-RESEARCH.md (Pattern 3 columnDefs, Pattern 4 status CSS)
  </read_first>
  <action>
    1. Add to `app/components/registry_table.py`:

       `COLUMN_DEFS` list (per D-01, D-02, D-03, Pattern 3 from RESEARCH):
       ```python
       COLUMN_DEFS = [
           {
               "headerName": "Тип документа",
               "field": "contract_type",
               "flex": 2,
               "filter": "agTextColumnFilter",
               "floatingFilter": True,
               "sortable": True,
           },
           {
               "headerName": "Контрагент",
               "field": "counterparty",
               "flex": 2,
               "filter": "agTextColumnFilter",
               "floatingFilter": True,
               "sortable": True,
           },
           {
               "headerName": "Статус",
               "field": "computed_status",
               "width": 160,
               "sortable": True,
               "filter": "agTextColumnFilter",
               "cellRenderer": STATUS_CELL_RENDERER,  # JS string — see below
           },
           {
               "headerName": "Сумма",
               "field": "amount",
               "width": 140,
               "sortable": True,
               "filter": "agTextColumnFilter",
           },
           # Hidden columns (D-02)
           {"field": "date_end", "hide": True},
           {"field": "validation_score", "hide": True},
           {"field": "filename", "hide": True},
           {"field": "processed_at", "hide": True, "sort": "desc"},
           {"field": "id", "hide": True},
       ]
       ```

       `STATUS_CELL_RENDERER` — JS string for cellRenderer (per D-23, Pattern 3 from RESEARCH):
       ```python
       STATUS_CELL_RENDERER = """(params) => {
           const labels = {
               active: ['✔', 'Действует', 'status-active'],
               expiring: ['⚠', 'Скоро истекает', 'status-expiring'],
               expired: ['✗', 'Истёк', 'status-expired'],
               unknown: ['?', 'Нет даты', 'status-unknown'],
               terminated: ['✖', 'Расторгнут', 'status-terminated'],
               extended: ['↻', 'Продлён', 'status-extended'],
               negotiation: ['~', 'На согласовании', 'status-negotiation'],
               suspended: ['⏸', 'Приостановлен', 'status-suspended'],
           };
           const [icon, label, cls] = labels[params.value] || ['?', params.value, 'status-unknown'];
           return `<span class="${cls}">${icon} ${label}</span>`;
       }"""
       ```

       `async def render_registry_table(state: AppState) -> ui.aggrid:`
       - Creates ui.aggrid with COLUMN_DEFS, empty rowData, domLayout="autoHeight", defaultColDef with sortable/resizable.
       - Returns the grid element (for later event binding in registry.py).

       `async def load_table_data(grid: ui.aggrid, state: AppState, segment: str = "all") -> None:`
       - Calls `rows = await run.io_bound(_fetch_rows, state.current_client, segment, state.filter_search, state.warning_days_threshold)`
       - Sets `grid.options["rowData"] = rows; grid.update()` (per Pitfall 2 in RESEARCH)

    2. Add status badge CSS to `app/main.py` — add `ui.add_head_html(STATUS_CSS)` call right before `@ui.page("/")`.
       STATUS_CSS per Pattern 4 from RESEARCH — `@layer components` with literal Tailwind-like classes for each status.

    3. Replace `app/pages/registry.py` stub with real implementation:
       ```python
       from nicegui import ui
       from app.state import get_state
       from app.components.registry_table import render_registry_table, load_table_data

       def build() -> None:
           state = get_state()
           # Title
           with ui.column().classes('w-full px-6 pt-4'):
               ui.label('Документы').classes('text-2xl font-semibold text-gray-900')

           # Grid (async — uses ui.timer for initial load)
           grid_container = ui.column().classes('w-full')

           async def _init():
               with grid_container:
                   grid = await render_registry_table(state)
                   await load_table_data(grid, state)

           ui.timer(0, _init, once=True)
       ```

    Per D-03: Default sort by processed_at DESC — handled via `{"field": "processed_at", "hide": True, "sort": "desc"}` in COLUMN_DEFS.
    Per D-10: AG Grid column filters embedded via `floatingFilter: True` in visible columns.
    Per D-24: Tailwind literal classes via @layer components CSS, not dynamic.
  </action>
  <verify>
    <automated>python -c "from app.components.registry_table import COLUMN_DEFS, render_registry_table, load_table_data; print(f'OK: {len(COLUMN_DEFS)} columns')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "COLUMN_DEFS" app/components/registry_table.py
    - grep -q "render_registry_table" app/components/registry_table.py
    - grep -q "load_table_data" app/components/registry_table.py
    - grep -q "cellRenderer" app/components/registry_table.py
    - grep -q "status-active" app/main.py
    - grep -q "status-expiring" app/main.py
    - grep -q "render_registry_table\|load_table_data" app/pages/registry.py
    - grep -q "floatingFilter" app/components/registry_table.py
    - python -m pytest tests/test_registry_view.py -x -q still passes
  </acceptance_criteria>
  <done>Registry page shows AG Grid table with 4 visible columns (type, counterparty, status badge, amount), hidden columns, default sort by processed_at DESC, and floating column filters. Status badges use CSS @layer components for Tailwind-safe styling.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_registry_view.py -x -q` — all data layer tests pass
- `python -c "from app.components.registry_table import COLUMN_DEFS, render_registry_table"` — imports work
- `python -c "from app.pages.registry import build"` — page builds without errors
- `grep -c "status-" app/main.py` returns ≥8 (all status CSS classes present)
</verification>

<success_criteria>
1. Таблица рендерится с 4 видимыми колонками и скрытыми колонками (REG-01)
2. Статус-бейджи имеют разные цвета по статусу: зелёный/жёлтый/красный/серый (REG-04)
3. _fetch_rows возвращает данные с computed_status через SQL (REG-01)
4. _fuzzy_filter реализует multi-word AND-logic с порогом 80% (prep for REG-03)
5. Все unit-тесты проходят
</success_criteria>

<output>
After completion, create `.planning/phases/08-registry-view/08-01-SUMMARY.md`
</output>
