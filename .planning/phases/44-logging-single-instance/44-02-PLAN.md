---
phase: 44-logging-single-instance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/instance_lock.py
  - app/main.py
autonomous: true
requirements: [LOG-05]

must_haves:
  truths:
    - "При попытке запустить второй экземпляр появляется сообщение 'ЮрТэг уже запущен' и процесс завершается"
    - "При нормальном закрытии приложения lock-файл освобождается"
    - "При крэше приложения lock-файл не блокирует следующий запуск (stale lock detection)"
  artifacts:
    - path: "services/instance_lock.py"
      provides: "Single instance lock mechanism"
      exports: ["acquire_instance_lock"]
  key_links:
    - from: "app/main.py"
      to: "services/instance_lock.py"
      via: "acquire_instance_lock() call before ui.run()"
      pattern: "from services.instance_lock import acquire_instance_lock"
---

<objective>
Prevent multiple app instances from running simultaneously via file lock.

Purpose: Two instances = two llama-server processes on same port = crash. File lock at ~/.yurteg/app.lock prevents this with a user-friendly message.
Output: services/instance_lock.py module, wired into app/main.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-logging-single-instance/44-CONTEXT.md
@.planning/notes/2026-03-29-desktop-runtime-safety.md

<interfaces>
<!-- From app/main.py — end of file structure -->

Lines 64-69 (startup/shutdown hooks):
```python
app.on_startup(_start_llama)
app.on_shutdown(_stop_llama)
app.on_disconnect(_stop_llama)
atexit.register(_stop_llama)
```

Lines 265-273 (entry point):
```python
ui.run(
    native=True,
    dark=False,
    reload=False,
    host="127.0.0.1",
    title="ЮрТэг",
    window_size=(1400, 900),
    storage_secret='yurteg-desktop-secret',  # will be replaced by Plan 01
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create services/instance_lock.py with cross-platform file lock</name>
  <files>services/instance_lock.py</files>
  <read_first>
    - app/main.py (understand startup flow, atexit pattern)
    - config.py (see _SETTINGS_FILE path for ~/.yurteg/ convention)
  </read_first>
  <action>
Create `services/instance_lock.py` with a function `acquire_instance_lock()` that:

**Lock file:** `~/.yurteg/app.lock`

**Cross-platform locking:**
- Unix: `fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)` — non-blocking exclusive lock
- Windows: `msvcrt.locking(fd, msvcrt.LK_NBLCK, 1)` — non-blocking lock

**Implementation:**

```python
"""Single-instance lock — prevents running two copies of the app."""

from __future__ import annotations

import atexit
import logging
import os
import sys
from pathlib import Path

logger = logging.getLogger(__name__)

_LOCK_PATH = Path.home() / ".yurteg" / "app.lock"
_lock_fd: int | None = None


def acquire_instance_lock() -> None:
    """Acquire exclusive file lock or exit with user-friendly message.

    Must be called before ui.run(). On failure, prints message to stderr
    and calls sys.exit(1).
    """
    global _lock_fd
    _LOCK_PATH.parent.mkdir(parents=True, exist_ok=True)

    try:
        fd = os.open(str(_LOCK_PATH), os.O_CREAT | os.O_RDWR)
    except OSError as e:
        logger.error("Не удалось создать lock-файл: %s", e)
        sys.exit(1)

    try:
        if sys.platform == "win32":
            import msvcrt
            msvcrt.locking(fd, msvcrt.LK_NBLCK, 1)
        else:
            import fcntl
            fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
    except (OSError, IOError):
        os.close(fd)
        print("ЮрТэг уже запущен.", file=sys.stderr)
        logger.warning("Попытка повторного запуска заблокирована (lock busy)")
        sys.exit(1)

    # Write PID for debugging
    os.ftruncate(fd, 0)
    os.write(fd, str(os.getpid()).encode())

    _lock_fd = fd
    atexit.register(_release_lock)
    logger.info("Instance lock acquired (PID %d)", os.getpid())


def _release_lock() -> None:
    """Release file lock on normal exit."""
    global _lock_fd
    if _lock_fd is not None:
        try:
            os.close(_lock_fd)
        except OSError:
            pass
        _lock_fd = None
        logger.debug("Instance lock released")
```

Key design choices:
- Uses OS-level file locking (not PID file) — automatically released on crash (OS cleans up fd)
- `fcntl.flock` on Unix releases when process dies, no stale lock problem
- `atexit.register` for clean exit path
- Writes PID to lock file for debugging (which process holds the lock)
- `sys.exit(1)` on lock failure — process terminates before NiceGUI starts
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from services.instance_lock import acquire_instance_lock, _release_lock, _LOCK_PATH
acquire_instance_lock()
assert _LOCK_PATH.exists(), 'lock file not created'
pid = _LOCK_PATH.read_text()
import os
assert pid == str(os.getpid()), f'PID mismatch: {pid} vs {os.getpid()}'
_release_lock()
print('OK: lock acquired and released')
"</automated>
  </verify>
  <acceptance_criteria>
    - File services/instance_lock.py exists
    - grep "def acquire_instance_lock" services/instance_lock.py returns a match
    - grep "fcntl.flock" services/instance_lock.py returns a match
    - grep "msvcrt.locking" services/instance_lock.py returns a match
    - grep "ЮрТэг уже запущен" services/instance_lock.py returns a match
    - grep "atexit.register" services/instance_lock.py returns a match
    - grep "app.lock" services/instance_lock.py returns a match
  </acceptance_criteria>
  <done>services/instance_lock.py exists with cross-platform file lock, prints "ЮрТэг уже запущен" on duplicate launch, auto-releases on crash via OS fd cleanup</done>
</task>

<task type="auto">
  <name>Task 2: Wire instance lock into app/main.py before ui.run()</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (full file — see import block, module-level code order, ui.run() location)
    - services/instance_lock.py (just created — verify export name)
  </read_first>
  <action>
**In app/main.py:**

1. Add import after existing imports (near the top, with other service imports):
```python
from services.instance_lock import acquire_instance_lock
```

2. Add `acquire_instance_lock()` call BEFORE the `ui.run()` call (line ~265). It must be before ui.run() because:
   - If lock fails, sys.exit(1) prevents NiceGUI from starting
   - If lock succeeds, app proceeds normally

Place it right before the ui.run() block:
```python
# ── Entry point ────────────────────────────────────────────────────────────────
acquire_instance_lock()

ui.run(
    ...
)
```

**Important:** This call is at module level (same as ui.run()). It runs before any NiceGUI window appears.

**Note:** If Plan 01 has already modified main.py (setup_logging), make sure acquire_instance_lock() goes AFTER setup_logging() (so the lock failure warning can be logged) but BEFORE ui.run().
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && grep -n "acquire_instance_lock" app/main.py && python -c "import ast; ast.parse(open('app/main.py').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "from services.instance_lock import acquire_instance_lock" app/main.py returns a match
    - grep "acquire_instance_lock()" app/main.py returns a match (the call)
    - acquire_instance_lock() appears BEFORE ui.run() in the file (lower line number)
    - python -c "import ast; ast.parse(open('app/main.py').read())" exits 0
  </acceptance_criteria>
  <done>app/main.py calls acquire_instance_lock() before ui.run(), preventing duplicate instances</done>
</task>

</tasks>

<verification>
1. `python -c "from services.instance_lock import acquire_instance_lock"` — imports without error
2. `grep "ЮрТэг уже запущен" services/instance_lock.py` — user message present
3. `grep "acquire_instance_lock" app/main.py` — wired into entry point
4. `python -c "import ast; ast.parse(open('app/main.py').read())"` — valid syntax
</verification>

<success_criteria>
- Single instance lock prevents duplicate launches
- User sees "ЮрТэг уже запущен" when trying to start a second instance
- Lock auto-releases on normal exit (atexit) and on crash (OS fd cleanup)
- Lock file location: ~/.yurteg/app.lock
- Cross-platform: fcntl on Unix, msvcrt on Windows
</success_criteria>

<output>
After completion, create `.planning/phases/44-logging-single-instance/44-02-SUMMARY.md`
</output>
