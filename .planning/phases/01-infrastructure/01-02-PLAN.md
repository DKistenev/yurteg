---
phase: 01-infrastructure
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - providers/__init__.py
  - providers/base.py
  - providers/zai.py
  - providers/openrouter.py
  - providers/ollama.py
  - config.py
  - tests/test_providers.py
autonomous: true
requirements:
  - FUND-03

must_haves:
  truths:
    - "Переключение с GLM на OpenRouter происходит заменой одной строки в config.py (active_provider)"
    - "ZAI extra_body thinking:disabled передаётся только в ZAI-запросах — не утекает в OpenRouter/Ollama"
    - "OpenRouter-провайдер склеивает system-роль в user-сообщение для совместимости с бесплатными моделями"
    - "get_provider('unknown') поднимает ValueError немедленно — не молчит"
  artifacts:
    - path: "providers/base.py"
      provides: "LLMProvider ABC с complete() и verify_key()"
      contains: "class LLMProvider"
    - path: "providers/__init__.py"
      provides: "get_provider(config) factory + get_fallback_provider(config)"
      exports: ["get_provider", "get_fallback_provider"]
    - path: "providers/zai.py"
      provides: "ZAIProvider с extra_body thinking:disabled"
      contains: "class ZAIProvider"
    - path: "providers/openrouter.py"
      provides: "OpenRouterProvider с _merge_system_into_user"
      contains: "class OpenRouterProvider"
    - path: "providers/ollama.py"
      provides: "OllamaProvider stub для Вехи 3"
      contains: "class OllamaProvider"
    - path: "config.py"
      provides: "active_provider и fallback_provider поля в Config"
      contains: "active_provider"
    - path: "tests/test_providers.py"
      provides: "6 unit-тестов"
      exports: ["test_factory_zai", "test_factory_openrouter", "test_factory_unknown_raises", "test_zai_thinking_disabled", "test_openrouter_system_merge", "test_ollama_stub"]
  key_links:
    - from: "config.py"
      to: "providers/__init__.py get_provider()"
      via: "config.active_provider string field"
      pattern: "active_provider"
    - from: "providers/zai.py ZAIProvider.complete()"
      to: "openai SDK"
      via: "extra_body={'thinking': {'type': 'disabled'}} только если config.ai_disable_thinking"
      pattern: "extra_body"
---

<objective>
Создать пакет `providers/` с абстракцией LLM-провайдеров: ABC базовый класс, ZAI/OpenRouter/Ollama реализации, фабричная функция. Расширить `config.py` полями `active_provider` и `fallback_provider`. Провайдер переключается одной строкой конфига.

Purpose: FUND-03 — AI-провайдер переключается через конфигурацию (GLM / OpenRouter / будущая Ollama) без изменения кода. Текущий `_create_client(use_fallback=bool)` — ZAI-специфичная логика смешана с роутингом.
Output: `providers/` пакет (5 файлов), расширенный `config.py`, `tests/test_providers.py` с 6 тестами.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Текущий config.py — нужно добавить два поля в Config dataclass -->

Из config.py (текущее состояние):
```python
@dataclass
class Config:
    # AI — провайдеры и модели
    ai_base_url: str = "https://api.z.ai/api/coding/paas/v4"
    model_dev: str = "glm-4.7"
    model_prod: str = "glm-4.7"
    ai_fallback_base_url: str = "https://openrouter.ai/api/v1"
    model_fallback: str = "arcee-ai/trinity-large-preview:free"
    use_prod_model: bool = False
    ai_max_retries: int = 2
    ai_temperature: float = 0
    ai_max_tokens: int = 2000
    ai_disable_thinking: bool = True
    max_workers: int = 5
    # ... остальные поля
    @property
    def active_model(self) -> str:
        return self.model_prod if self.use_prod_model else self.model_dev
```

<!-- Текущий ai_extractor.py — _create_client и _merge_system_into_user которые будут ИЗВЛЕЧЕНЫ -->
<!-- providers/ дублирует их логику; ai_extractor.py изменяется в плане 01-03 -->

Из modules/ai_extractor.py (текущее состояние — паттерн для переноса в providers/):
```python
def _create_client(config: Config, use_fallback: bool = False) -> OpenAI:
    if use_fallback:
        api_key = os.environ.get("OPENROUTER_API_KEY", "")
        return OpenAI(base_url=config.ai_fallback_base_url, api_key=api_key,
                      default_headers={"HTTP-Referer": "...", "X-Title": "YurTeg"})
    api_key = os.environ.get("ZHIPU_API_KEY", "") or os.environ.get("ZAI_API_KEY", "") or os.environ.get("OPENROUTER_API_KEY", "")
    if api_key.startswith("sk-or-"):
        base_url = config.ai_fallback_base_url
        headers = {"HTTP-Referer": "...", "X-Title": "YurTeg"}
    else:
        base_url = config.ai_base_url
        headers = {}
    return OpenAI(base_url=base_url, api_key=api_key, default_headers=headers)

# ZAI extra_body (строка 278-279):
if config.ai_disable_thinking and not use_fallback:
    extra["extra_body"] = {"thinking": {"type": "disabled"}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать тест-файл для провайдеров (Wave 0 — тесты перед реализацией)</name>
  <files>tests/test_providers.py</files>
  <read_first>
    - tests/conftest.py (sys.path уже настроен)
    - config.py (знать поля Config)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 2 — сигнатуры ABC и провайдеров)
  </read_first>
  <behavior>
    - test_factory_zai: Config(active_provider="zai") → get_provider() возвращает ZAIProvider
    - test_factory_openrouter: Config(active_provider="openrouter") → get_provider() возвращает OpenRouterProvider
    - test_factory_unknown_raises: Config(active_provider="unknown") → get_provider() поднимает ValueError
    - test_zai_thinking_disabled: ZAIProvider.complete() с ai_disable_thinking=True передаёт extra_body={'thinking':{'type':'disabled'}} в вызов create() (через unittest.mock patch)
    - test_openrouter_system_merge: _merge_system_into_user([{system}, {user}]) → возвращает одно user-сообщение с вклеенным system-контентом
    - test_ollama_stub: OllamaProvider(config).complete([]) поднимает NotImplementedError
  </behavior>
  <action>
Создать `tests/test_providers.py`. Содержание:

```python
"""Тесты пакета providers/ — фабрика, ZAI, OpenRouter, Ollama (FUND-03)."""
import os
from unittest.mock import MagicMock, patch

import pytest

from config import Config


def _make_config(**kwargs) -> Config:
    """Конфиг с переопределёнными полями для тестов."""
    defaults = dict(active_provider="zai", fallback_provider="openrouter")
    defaults.update(kwargs)
    return Config(**defaults)


def test_factory_zai():
    """get_provider('zai') возвращает ZAIProvider."""
    from providers import get_provider
    from providers.zai import ZAIProvider
    cfg = _make_config(active_provider="zai")
    provider = get_provider(cfg)
    assert isinstance(provider, ZAIProvider)


def test_factory_openrouter():
    """get_provider('openrouter') возвращает OpenRouterProvider."""
    from providers import get_provider
    from providers.openrouter import OpenRouterProvider
    cfg = _make_config(active_provider="openrouter")
    provider = get_provider(cfg)
    assert isinstance(provider, OpenRouterProvider)


def test_factory_unknown_raises():
    """get_provider с неизвестным провайдером поднимает ValueError."""
    from providers import get_provider
    cfg = _make_config(active_provider="unknown_provider_xyz")
    with pytest.raises(ValueError, match="unknown_provider_xyz"):
        get_provider(cfg)


def test_zai_thinking_disabled():
    """ZAIProvider.complete() передаёт extra_body thinking:disabled когда ai_disable_thinking=True."""
    from providers.zai import ZAIProvider
    cfg = _make_config(ai_disable_thinking=True)
    provider = ZAIProvider(cfg)

    # Мокаем chat.completions.create
    mock_response = MagicMock()
    mock_response.choices[0].message.content = '{"ok": true}'
    provider._client = MagicMock()
    provider._client.chat.completions.create.return_value = mock_response

    messages = [{"role": "user", "content": "test"}]
    provider.complete(messages)

    call_kwargs = provider._client.chat.completions.create.call_args[1]
    assert "extra_body" in call_kwargs, "extra_body отсутствует в вызове ZAI"
    assert call_kwargs["extra_body"] == {"thinking": {"type": "disabled"}}


def test_openrouter_system_merge():
    """_merge_system_into_user вклеивает system-контент в первое user-сообщение."""
    from providers.openrouter import _merge_system_into_user
    messages = [
        {"role": "system", "content": "Ты юрист"},
        {"role": "user", "content": "Проанализируй договор"},
    ]
    merged = _merge_system_into_user(messages)
    assert len(merged) == 1
    assert merged[0]["role"] == "user"
    assert "Ты юрист" in merged[0]["content"]
    assert "Проанализируй договор" in merged[0]["content"]


def test_ollama_stub():
    """OllamaProvider.complete() поднимает NotImplementedError (stub для Вехи 3)."""
    from providers.ollama import OllamaProvider
    cfg = _make_config()
    provider = OllamaProvider(cfg)
    with pytest.raises(NotImplementedError):
        provider.complete([{"role": "user", "content": "test"}])
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_providers.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл `tests/test_providers.py` существует и содержит 6 test-функций
    - Все тесты ПАДАЮТ с ImportError (providers/ ещё не создан) — RED state до реализации
  </acceptance_criteria>
  <done>6 тестов в RED state.</done>
</task>

<task type="auto">
  <name>Task 2: Создать providers/ пакет и расширить config.py</name>
  <files>
    providers/__init__.py,
    providers/base.py,
    providers/zai.py,
    providers/openrouter.py,
    providers/ollama.py,
    config.py
  </files>
  <read_first>
    - config.py (ПОЛНОСТЬЮ — знать все существующие поля перед добавлением)
    - modules/ai_extractor.py (строки 105-184 — _merge_system_into_user и _create_client для переноса логики)
    - tests/test_providers.py (знать что именно проверяют тесты)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 2 — точные сигнатуры)
  </read_first>
  <action>
Создать директорию `providers/` и 5 файлов + изменить `config.py`:

**1. config.py** — добавить два поля в Config dataclass после `ai_disable_thinking`:
```python
active_provider: str = "zai"          # "zai" | "openrouter" | "ollama"
fallback_provider: str = "openrouter"  # автофallback при недоступности active_provider
```

**2. providers/base.py** — создать файл:
```python
"""Абстрактный базовый класс для AI-провайдеров."""
from abc import ABC, abstractmethod


class LLMProvider(ABC):
    """Контракт для всех LLM-провайдеров.

    Конкретный провайдер реализует complete() и verify_key().
    Retry-логика и fallback-роутинг живут в ai_extractor.py — не здесь.
    """

    @property
    @abstractmethod
    def name(self) -> str:
        """Идентификатор провайдера для логирования: 'zai', 'openrouter', 'ollama'."""
        ...

    @abstractmethod
    def complete(self, messages: list[dict], **kwargs) -> str:
        """Отправляет запрос к LLM, возвращает текстовый ответ.

        Args:
            messages: список сообщений в формате OpenAI [{role, content}, ...]
            **kwargs: провайдер-специфичные параметры

        Returns:
            Строка с ответом модели.

        Raises:
            RuntimeError: при неустранимой ошибке провайдера.
        """
        ...

    @abstractmethod
    def verify_key(self) -> bool:
        """Проверяет валидность API-ключа. Возвращает True/False, не поднимает исключений."""
        ...
```

**3. providers/zai.py** — создать файл:
```python
"""ZAI (GLM) провайдер — основной провайдер ЮрТэг."""
import logging
import os

from openai import OpenAI

from config import Config
from providers.base import LLMProvider

logger = logging.getLogger(__name__)


class ZAIProvider(LLMProvider):
    """Провайдер ZAI (api.z.ai) — GLM-4.7 и совместимые модели."""

    name = "zai"

    def __init__(self, config: Config) -> None:
        self._config = config
        api_key = (
            os.environ.get("ZAI_API_KEY", "")
            or os.environ.get("ZHIPU_API_KEY", "")
            or ""
        )
        self._client = OpenAI(
            base_url=config.ai_base_url,
            api_key=api_key,
        )

    def complete(self, messages: list[dict], **kwargs) -> str:
        """Вызов ZAI API. Добавляет extra_body thinking:disabled если включено в конфиге."""
        extra = {}
        if self._config.ai_disable_thinking:
            extra["extra_body"] = {"thinking": {"type": "disabled"}}

        response = self._client.chat.completions.create(
            model=self._config.active_model,
            temperature=self._config.ai_temperature,
            max_tokens=self._config.ai_max_tokens,
            messages=messages,
            **extra,
        )
        content = response.choices[0].message.content
        if not content:
            raise RuntimeError("ZAI вернул пустой ответ")
        return content

    def verify_key(self) -> bool:
        """Проверяет API-ключ дешёвым запросом."""
        try:
            self._client.models.list()
            return True
        except Exception:
            return False
```

**4. providers/openrouter.py** — создать файл:
```python
"""OpenRouter провайдер — запасной провайдер ЮрТэг."""
import logging
import os

from openai import OpenAI

from config import Config
from providers.base import LLMProvider

logger = logging.getLogger(__name__)


def _merge_system_into_user(messages: list[dict]) -> list[dict]:
    """Вклеивает system-сообщения в начало первого user-сообщения.

    Нужно для бесплатных моделей OpenRouter, не поддерживающих role='system'
    (gemma, некоторые llama-вариации).
    """
    system_parts: list[str] = []
    other: list[dict] = []
    for msg in messages:
        if msg["role"] == "system":
            system_parts.append(msg["content"])
        else:
            other.append(msg)

    if not system_parts or not other:
        return messages

    prefix = "\n\n".join(system_parts)
    merged = []
    injected = False
    for msg in other:
        if msg["role"] == "user" and not injected:
            merged.append({
                "role": "user",
                "content": f"[Инструкция]\n{prefix}\n\n[Задание]\n{msg['content']}",
            })
            injected = True
        else:
            merged.append(msg)
    return merged


class OpenRouterProvider(LLMProvider):
    """Провайдер OpenRouter — бесплатные и платные модели через openrouter.ai."""

    name = "openrouter"

    def __init__(self, config: Config) -> None:
        self._config = config
        api_key = os.environ.get("OPENROUTER_API_KEY", "")
        self._client = OpenAI(
            base_url=config.ai_fallback_base_url,
            api_key=api_key,
            default_headers={
                "HTTP-Referer": "https://github.com/yurteg",
                "X-Title": "YurTeg",
            },
        )

    def complete(self, messages: list[dict], **kwargs) -> str:
        """Вызов OpenRouter API. Автоматически склеивает system в user для совместимости."""
        merged = _merge_system_into_user(messages)
        response = self._client.chat.completions.create(
            model=self._config.model_fallback,
            temperature=self._config.ai_temperature,
            max_tokens=self._config.ai_max_tokens,
            messages=merged,
        )
        content = response.choices[0].message.content
        if not content:
            raise RuntimeError("OpenRouter вернул пустой ответ")
        return content

    def verify_key(self) -> bool:
        """Проверяет API-ключ."""
        try:
            self._client.models.list()
            return True
        except Exception:
            return False
```

**5. providers/ollama.py** — создать файл:
```python
"""Ollama провайдер — stub для локальной LLM (Веха 3).

Ollama поддерживает OpenAI-совместимый endpoint на localhost:11434.
Реализация запланирована на Веху 3 (локальная QWEN).
"""
from config import Config
from providers.base import LLMProvider


class OllamaProvider(LLMProvider):
    """Провайдер Ollama — локальная LLM через OpenAI-совместимый endpoint.

    STUB: полная реализация в Вехе 3.
    """

    name = "ollama"

    def __init__(self, config: Config) -> None:
        self._config = config

    def complete(self, messages: list[dict], **kwargs) -> str:
        raise NotImplementedError(
            "Поддержка Ollama запланирована на Веху 3. "
            "Используйте active_provider='zai' или 'openrouter'."
        )

    def verify_key(self) -> bool:
        raise NotImplementedError(
            "Поддержка Ollama запланирована на Веху 3."
        )
```

**6. providers/__init__.py** — создать файл:
```python
"""Пакет providers/ — фабрика LLM-провайдеров.

Использование:
    from providers import get_provider, get_fallback_provider
    provider = get_provider(config)          # активный провайдер
    fallback = get_fallback_provider(config) # запасной или None
"""
from config import Config
from providers.base import LLMProvider
from providers.zai import ZAIProvider
from providers.openrouter import OpenRouterProvider
from providers.ollama import OllamaProvider


def get_provider(config: Config) -> LLMProvider:
    """Создаёт провайдер по config.active_provider.

    Args:
        config: конфиг приложения

    Returns:
        Экземпляр LLMProvider для активного провайдера.

    Raises:
        ValueError: если active_provider неизвестен.
    """
    match config.active_provider:
        case "zai":
            return ZAIProvider(config)
        case "openrouter":
            return OpenRouterProvider(config)
        case "ollama":
            return OllamaProvider(config)
        case _:
            raise ValueError(
                f"Неизвестный active_provider: {config.active_provider!r}. "
                f"Допустимые значения: 'zai', 'openrouter', 'ollama'."
            )


def get_fallback_provider(config: Config) -> LLMProvider | None:
    """Создаёт запасной провайдер по config.fallback_provider.

    Returns:
        Экземпляр LLMProvider или None если fallback не задан.
    """
    if not config.fallback_provider:
        return None
    match config.fallback_provider:
        case "openrouter":
            return OpenRouterProvider(config)
        case "ollama":
            return OllamaProvider(config)
        case "zai":
            return ZAIProvider(config)
        case _:
            return None
```

ВАЖНО по структуре providers/:
- `extra_body` с thinking mode — ТОЛЬКО в `ZAIProvider.complete()`, нигде больше
- `_merge_system_into_user` — ТОЛЬКО в `providers/openrouter.py`
- Retry-логика и fallback-роутинг остаются в `ai_extractor.py` (изменяется в плане 01-03)
- `ai_extractor.py` в этом плане НЕ изменяется — он продолжает работать через старый `_create_client()`
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_providers.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `ls providers/` выдаёт: `__init__.py base.py zai.py openrouter.py ollama.py`
    - `grep "active_provider" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/config.py` выдаёт строку с определением поля
    - `grep "fallback_provider" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/config.py` выдаёт строку с определением поля
    - `grep "extra_body" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/providers/base.py` — НЕ выдаёт ничего (extra_body не должно быть в base)
    - `grep "extra_body" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/providers/openrouter.py` — НЕ выдаёт ничего
    - `python -m pytest tests/test_providers.py -v` — все 6 тестов GREEN
    - `python -c "from providers import get_provider; from config import Config; print(get_provider(Config()).name)"` выводит `zai`
  </acceptance_criteria>
  <done>
    - test_factory_zai PASS
    - test_factory_openrouter PASS
    - test_factory_unknown_raises PASS
    - test_zai_thinking_disabled PASS
    - test_openrouter_system_merge PASS
    - test_ollama_stub PASS
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg
python -m pytest tests/test_providers.py -v
python -c "from providers import get_provider, get_fallback_provider; from config import Config; c=Config(); print(get_provider(c).name, get_fallback_provider(c).name)"
```
Выводит: `zai openrouter`
</verification>

<success_criteria>
- `python -m pytest tests/test_providers.py -v` — 6/6 PASSED
- providers/ содержит 5 файлов: __init__.py, base.py, zai.py, openrouter.py, ollama.py
- config.py содержит поля active_provider и fallback_provider
- extra_body присутствует ТОЛЬКО в providers/zai.py
- Существующий ai_extractor.py не затронут (продолжает работать)
</success_criteria>

<output>
После завершения создать `.planning/phases/01-infrastructure/01-02-SUMMARY.md`
</output>
