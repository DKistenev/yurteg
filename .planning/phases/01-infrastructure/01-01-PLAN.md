---
phase: 01-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/database.py
  - tests/test_migrations.py
autonomous: true
requirements:
  - FUND-01

must_haves:
  truths:
    - "После обновления с v0.4 существующая база открывается без ошибок и все данные на месте"
    - "При повторном запуске приложения миграции не выполняются дважды (идемпотентность)"
    - "Перед любой миграцией на непустой БД создаётся timestamped backup-файл"
    - "Будущие миграции добавляются как новые пронумерованные функции — без изменения предыдущих"
  artifacts:
    - path: "modules/database.py"
      provides: "schema_migrations table + _run_migrations() + _migrate_v1_review_columns()"
      contains: "schema_migrations"
    - path: "tests/test_migrations.py"
      provides: "4 unit-теста: fresh_db, v04_upgrade, backup_created, idempotent"
      exports: ["test_fresh_db", "test_v04_upgrade_preserves_rows", "test_backup_created", "test_idempotent"]
  key_links:
    - from: "modules/database.py __init__"
      to: "_run_migrations()"
      via: "вызов после executescript(_SCHEMA)"
      pattern: "_run_migrations"
    - from: "_migrate_v1_review_columns"
      to: "schema_migrations"
      via: "_mark_migration_applied(conn, 1)"
      pattern: "_mark_migration_applied"
---

<objective>
Заменить хрупкий `try/except OperationalError` в `database.py` на версионированную систему миграций с таблицей `schema_migrations`. Добавить pre-migration backup для защиты от повреждения данных при обновлении.

Purpose: FUND-01 — обновление приложения не должно ломать существующую базу пользователя. Это первый и самый критичный блокирующий риск: любой пользователь с v0.4-базой должен безопасно обновиться.
Output: `modules/database.py` с миграционной системой; `tests/test_migrations.py` с 4 тестами.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Текущий код database.py, который нужно изменить. -->
<!-- Строки 57-75 — это зона изменений: __init__ и старый try/except паттерн. -->

Из modules/database.py (текущее состояние — строки, которые ЗАМЕНЯЮТСЯ):
```python
def __init__(self, db_path: Path) -> None:
    self.db_path = db_path
    self._lock = threading.Lock()
    db_path.parent.mkdir(parents=True, exist_ok=True)
    self.conn = sqlite3.connect(str(db_path), check_same_thread=False)
    self.conn.row_factory = sqlite3.Row
    self.conn.executescript(_SCHEMA)

    # Миграция v0.3: пометки юриста  ← ЭТОТ БЛОК ЗАМЕНЯЕТСЯ
    for col, default in (("review_status", "'not_reviewed'"), ("lawyer_comment", "''")):
        try:
            self.conn.execute(f"ALTER TABLE contracts ADD COLUMN {col} TEXT DEFAULT {default}")
            self.conn.commit()
        except sqlite3.OperationalError:
            pass  # Колонка уже существует

    logger.info("БД открыта: %s", db_path)
```

Импорты уже есть: `import sqlite3`, `import logging`, `from pathlib import Path`, `import threading`
Нужно добавить: `import shutil`, `import time`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать тест-файл для миграций (Wave 0 — тесты перед реализацией)</name>
  <files>tests/test_migrations.py</files>
  <read_first>
    - tests/conftest.py (sys.path настройка уже есть, не дублировать)
    - modules/database.py (понять текущую схему, import-пути)
  </read_first>
  <behavior>
    - test_fresh_db: создать Database на новой пустой БД — не должно быть исключений; таблица schema_migrations должна существовать
    - test_v04_upgrade_preserves_rows: создать v0.4-like БД (без schema_migrations, без review_status/lawyer_comment), добавить строку, открыть Database — строка должна остаться, столбцы добавлены
    - test_backup_created: создать непустую БД, открыть Database — файл вида `{stem}_backup_{timestamp}.sqlite` должен существовать рядом
    - test_idempotent: открыть Database дважды на той же БД — в schema_migrations ровно одна строка с version=1, не две
  </behavior>
  <action>
Создать `tests/test_migrations.py`. Содержание:

```python
"""Тесты версионированных миграций схемы БД (FUND-01)."""
import sqlite3
import time
from pathlib import Path

import pytest

from modules.database import Database


@pytest.fixture
def tmp_db(tmp_path) -> Path:
    """Путь к временной БД (файл не создан)."""
    return tmp_path / "test.db"


def test_fresh_db(tmp_db):
    """Создание на пустом месте — нет ошибок, schema_migrations существует."""
    db = Database(tmp_db)
    db.close()
    conn = sqlite3.connect(str(tmp_db))
    cursor = conn.execute(
        "SELECT name FROM sqlite_master WHERE type='table' AND name='schema_migrations'"
    )
    assert cursor.fetchone() is not None, "Таблица schema_migrations не создана"
    conn.close()


def test_v04_upgrade_preserves_rows(tmp_db):
    """Апгрейд с v0.4-базы: существующие строки сохраняются, новые столбцы добавлены."""
    # Создать v0.4-like базу: без schema_migrations, без review_status/lawyer_comment
    conn = sqlite3.connect(str(tmp_db))
    conn.execute("""
        CREATE TABLE contracts (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            original_path TEXT NOT NULL,
            filename TEXT NOT NULL,
            file_hash TEXT UNIQUE,
            status TEXT DEFAULT 'pending'
        )
    """)
    conn.execute(
        "INSERT INTO contracts (original_path, filename, file_hash, status) "
        "VALUES ('test.pdf', 'test.pdf', 'abc123', 'done')"
    )
    conn.commit()
    conn.close()

    # Открыть через Database — должна применить миграцию
    db = Database(tmp_db)
    db.close()

    conn = sqlite3.connect(str(tmp_db))
    row = conn.execute("SELECT * FROM contracts WHERE file_hash='abc123'").fetchone()
    assert row is not None, "Строка потеряна при миграции"

    # Новые столбцы добавлены
    cols = {d[1] for d in conn.execute("PRAGMA table_info(contracts)").fetchall()}
    assert "review_status" in cols, "review_status не добавлен"
    assert "lawyer_comment" in cols, "lawyer_comment не добавлен"
    conn.close()


def test_backup_created(tmp_db):
    """На непустой БД перед миграцией создаётся backup-файл."""
    # Создать непустую БД (без schema_migrations — триггер для backup)
    conn = sqlite3.connect(str(tmp_db))
    conn.execute("""
        CREATE TABLE contracts (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            original_path TEXT NOT NULL,
            filename TEXT NOT NULL,
            file_hash TEXT UNIQUE,
            status TEXT DEFAULT 'pending'
        )
    """)
    conn.execute(
        "INSERT INTO contracts (original_path, filename, file_hash, status) "
        "VALUES ('x.pdf', 'x.pdf', 'deadbeef', 'done')"
    )
    conn.commit()
    conn.close()

    db = Database(tmp_db)
    db.close()

    backups = list(tmp_db.parent.glob(f"{tmp_db.stem}_backup_*.sqlite"))
    assert len(backups) >= 1, f"Backup не создан. Файлы в папке: {list(tmp_db.parent.iterdir())}"


def test_idempotent(tmp_db):
    """Открыть БД дважды — в schema_migrations ровно одна запись version=1."""
    db = Database(tmp_db)
    db.close()
    db2 = Database(tmp_db)
    db2.close()

    conn = sqlite3.connect(str(tmp_db))
    count = conn.execute(
        "SELECT COUNT(*) FROM schema_migrations WHERE version=1"
    ).fetchone()[0]
    conn.close()
    assert count == 1, f"Ожидалась 1 запись v1 в schema_migrations, найдено {count}"
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_migrations.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл `tests/test_migrations.py` существует
    - Содержит ровно 4 функции: `test_fresh_db`, `test_v04_upgrade_preserves_rows`, `test_backup_created`, `test_idempotent`
    - Все 4 теста ПАДАЮТ (RED state) до реализации — проверить командой выше
  </acceptance_criteria>
  <done>4 теста в RED state. Реализация ещё не написана.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Реализовать версионированные миграции в database.py</name>
  <files>modules/database.py</files>
  <read_first>
    - modules/database.py (ПОЛНОСТЬЮ — понять текущую структуру до изменений)
    - tests/test_migrations.py (знать что именно проверяют тесты)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 1 — точные сигнатуры функций)
  </read_first>
  <behavior>
    - _backup_database(db_path, conn) создаёт файл `{stem}_backup_{int(time.time())}.sqlite` через shutil.copy2 — только если db_path.exists() и db_path.stat().st_size > 0
    - _ensure_migrations_table(conn) создаёт schema_migrations(version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL DEFAULT datetime('now'))
    - _is_migration_applied(conn, version) → bool — SELECT 1 FROM schema_migrations WHERE version = ?
    - _mark_migration_applied(conn, version) — INSERT OR IGNORE INTO schema_migrations (version) VALUES (?)
    - _migrate_v1_review_columns(conn) — ADD COLUMN review_status TEXT DEFAULT 'not_reviewed' и lawyer_comment TEXT DEFAULT '' через отдельные execute() с try/except OperationalError; затем _mark_migration_applied(conn, 1)
    - _run_migrations(db_path, conn) — вызывает _ensure_migrations_table, backup (если непустая БД И schema_migrations не существует), затем _migrate_v1_review_columns
    - Database.__init__ вызывает _run_migrations(self.db_path, self.conn) ПОСЛЕ self.conn.executescript(_SCHEMA)
  </behavior>
  <action>
Изменить `modules/database.py`:

1. Добавить в НАЧАЛО импортов (после `import logging`):
   ```python
   import shutil
   import time
   ```

2. Добавить 5 новых приватных функций ПЕРЕД классом Database (после _SCHEMA):
   ```python
   def _backup_database(db_path: Path) -> Path:
       """Создаёт timestamped backup перед миграцией. Возвращает путь к backup."""
       ts = int(time.time())
       backup = db_path.parent / f"{db_path.stem}_backup_{ts}.sqlite"
       shutil.copy2(db_path, backup)
       logger.info("DB backup создан: %s", backup)
       return backup


   def _ensure_migrations_table(conn: sqlite3.Connection) -> None:
       """Создаёт таблицу schema_migrations если не существует."""
       conn.execute("""
           CREATE TABLE IF NOT EXISTS schema_migrations (
               version INTEGER PRIMARY KEY,
               applied_at TEXT NOT NULL DEFAULT (datetime('now'))
           )
       """)
       conn.commit()


   def _is_migration_applied(conn: sqlite3.Connection, version: int) -> bool:
       """True если миграция с данным версионным номером уже применена."""
       row = conn.execute(
           "SELECT 1 FROM schema_migrations WHERE version = ?", (version,)
       ).fetchone()
       return row is not None


   def _mark_migration_applied(conn: sqlite3.Connection, version: int) -> None:
       """Записывает факт применения миграции."""
       conn.execute(
           "INSERT OR IGNORE INTO schema_migrations (version) VALUES (?)", (version,)
       )
       conn.commit()
       logger.info("Миграция v%d применена", version)


   def _migrate_v1_review_columns(conn: sqlite3.Connection) -> None:
       """v1: Добавить review_status и lawyer_comment (заменяет старый try/except паттерн)."""
       if _is_migration_applied(conn, 1):
           return
       # ALTER TABLE не работает внутри транзакции в SQLite.
       # Используем отдельные execute() для идемпотентности:
       for col, default in [("review_status", "'not_reviewed'"), ("lawyer_comment", "''")]:
           try:
               conn.execute(
                   f"ALTER TABLE contracts ADD COLUMN {col} TEXT DEFAULT {default}"
               )
           except sqlite3.OperationalError:
               pass  # Колонка уже существует — безопасно
       conn.commit()
       _mark_migration_applied(conn, 1)


   def _run_migrations(db_path: Path, conn: sqlite3.Connection) -> None:
       """Запускает все ожидающие миграции по порядку.
       Вызывается из Database.__init__ после создания основной схемы.
       Для добавления новой миграции — добавить _migrate_vN_* функцию сюда.
       """
       # Backup только если БД уже содержит данные И ещё не версионирована
       needs_backup = (
           db_path.exists()
           and db_path.stat().st_size > 0
           and not _table_exists(conn, "schema_migrations")
       )
       _ensure_migrations_table(conn)
       if needs_backup:
           _backup_database(db_path)
       _migrate_v1_review_columns(conn)


   def _table_exists(conn: sqlite3.Connection, table_name: str) -> bool:
       """Проверяет существование таблицы в БД."""
       row = conn.execute(
           "SELECT 1 FROM sqlite_master WHERE type='table' AND name=?", (table_name,)
       ).fetchone()
       return row is not None
   ```

3. В методе `Database.__init__` ЗАМЕНИТЬ блок "Миграция v0.3" (строки 67-73) на:
   ```python
   _run_migrations(self.db_path, self.conn)
   ```
   Убрать старые строки:
   ```python
   # Миграция v0.3: пометки юриста
   for col, default in (("review_status", "'not_reviewed'"), ("lawyer_comment", "''")):
       try:
           self.conn.execute(f"ALTER TABLE contracts ADD COLUMN {col} TEXT DEFAULT {default}")
           self.conn.commit()
       except sqlite3.OperationalError:
           pass  # Колонка уже существует
   ```

ВАЖНО: `executescript()` в SQLite делает неявный COMMIT перед выполнением — он используется для `_SCHEMA`, это нормально. Но для `ALTER TABLE` в миграциях использовать только `execute()`, не `executescript()`.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_migrations.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_run_migrations" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/modules/database.py` — выдаёт минимум 2 строки (определение функции + вызов в __init__)
    - `grep -n "schema_migrations" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/modules/database.py` — выдаёт минимум 4 строки (CREATE TABLE, SELECT, INSERT, вызов в _run_migrations)
    - `grep -n "try/except OperationalError\|# Миграция v0.3" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/modules/database.py` — НЕ выдаёт строк с блоком "Миграция v0.3" (старый паттерн удалён; try/except внутри _migrate_v1 — нормально)
    - `python -m pytest tests/test_migrations.py -v` — все 4 теста GREEN
    - `import shutil` и `import time` присутствуют в начале файла
  </acceptance_criteria>
  <done>
    - test_fresh_db PASS
    - test_v04_upgrade_preserves_rows PASS
    - test_backup_created PASS
    - test_idempotent PASS
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg
python -m pytest tests/test_migrations.py -v
```
Все 4 теста GREEN. Приложение запускается без ошибок:
```bash
python -c "from modules.database import Database; from pathlib import Path; import tempfile, os; d=Database(Path(tempfile.mkdtemp()) / 'v.db'); d.close(); print('OK')"
```
</verification>

<success_criteria>
- `python -m pytest tests/test_migrations.py -v` — 4/4 PASSED
- Нет строк с блоком "Миграция v0.3: пометки юриста" в database.py
- Функции `_run_migrations`, `_ensure_migrations_table`, `_backup_database`, `_is_migration_applied`, `_mark_migration_applied`, `_migrate_v1_review_columns` присутствуют в database.py
- `Database(fresh_path)` не падает
</success_criteria>

<output>
После завершения создать `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>
