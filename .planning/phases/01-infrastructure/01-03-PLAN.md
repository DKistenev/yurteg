---
phase: 01-infrastructure
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - services/__init__.py
  - services/pipeline_service.py
  - services/registry_service.py
  - modules/ai_extractor.py
  - main.py
  - tests/test_service_layer.py
autonomous: true
requirements:
  - FUND-02

must_haves:
  truths:
    - "pipeline_service.process_archive() вызывается без запущенного Streamlit — python -c 'from services import pipeline_service' не падает"
    - "В services/pipeline_service.py нет ни одного import streamlit"
    - "main.py использует pipeline_service.process_archive() вместо прямого вызова Controller"
    - "ai_extractor.extract_metadata() принимает provider: LLMProvider — больше не создаёт клиент внутри"
  artifacts:
    - path: "services/pipeline_service.py"
      provides: "process_archive() facade без Streamlit-импортов"
      contains: "def process_archive"
    - path: "services/registry_service.py"
      provides: "get_all_contracts() и generate_report() facades"
      exports: ["get_all_contracts", "generate_report"]
    - path: "services/__init__.py"
      provides: "пустой __init__ для пакета"
    - path: "modules/ai_extractor.py"
      provides: "extract_metadata() с provider: LLMProvider параметром"
      contains: "provider: LLMProvider"
  key_links:
    - from: "main.py"
      to: "services/pipeline_service.py"
      via: "from services import pipeline_service; pipeline_service.process_archive(...)"
      pattern: "pipeline_service.process_archive"
    - from: "services/pipeline_service.py"
      to: "controller.py"
      via: "Controller(config).process_archive(...)"
      pattern: "Controller"
    - from: "modules/ai_extractor.py extract_metadata()"
      to: "providers.LLMProvider"
      via: "provider параметр в сигнатуре функции"
      pattern: "provider: LLMProvider"
---

<objective>
Создать сервис-слой `services/` (pipeline_service, registry_service) и переключить main.py на его использование. Параллельно рефакторить `ai_extractor.extract_metadata()` для приёма `LLMProvider` как параметра — убрать внутреннее создание клиента.

Purpose: FUND-02 — бизнес-логика отделена от UI и вызывается независимо. Telegram-бот (Phase 3) вызывает pipeline_service напрямую, без запуска Streamlit.
Output: `services/` пакет, обновлённый `ai_extractor.py`, обновлённый `main.py`, тесты.
</objective>

<execution_context>
@/Users/danilakistenev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danilakistenev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- Из controller.py — process_archive() сигнатура (facade оборачивает это) -->

Из controller.py:
```python
class Controller:
    def __init__(self, config: Config) -> None: ...

    def process_archive(
        self,
        source_dir: Path,
        grouping: str = "both",
        force_reprocess: bool = False,
        on_progress: Optional[Callable[[int, int, str], None]] = None,
        on_file_done: Optional[Callable[[ProcessingResult], None]] = None,
        output_dir_override: Optional[Path] = None,
    ) -> dict:
        # Returns: dict(total, done, errors, skipped, output_dir, report_path)
```

Из modules/database.py:
```python
class Database:
    def get_all_results(self) -> list[dict]: ...
```

Из modules/reporter.py (нужно проверить сигнатуру):
```python
def generate_report(all_data: list[dict], output_dir: Path) -> Path: ...
```

Из modules/ai_extractor.py (текущая сигнатура — будет изменена):
```python
def extract_metadata(anonymized_text: str, config: Config) -> ContractMetadata:
    # Внутри создаёт _create_client() — это нужно заменить на provider параметр
```

Из providers/ (создан в плане 01-02):
```python
from providers.base import LLMProvider
from providers import get_provider, get_fallback_provider
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать тест-файл сервис-слоя (Wave 0 — тесты перед реализацией)</name>
  <files>tests/test_service_layer.py</files>
  <read_first>
    - tests/conftest.py
    - controller.py (знать сигнатуру process_archive)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 4 — сервис-слой)
  </read_first>
  <behavior>
    - test_no_streamlit_import: импортировать services.pipeline_service — убедиться что "streamlit" не в sys.modules после импорта и не в словаре модуля
    - test_process_archive_signature: pipeline_service.process_archive принимает (source_dir, config, grouping, force_reprocess, on_progress, on_file_done) — проверить через inspect.signature
    - test_registry_get_contracts: registry_service.get_all_contracts(mock_db) — вызывает db.get_all_results() и возвращает его результат
  </behavior>
  <action>
Создать `tests/test_service_layer.py`. Содержание:

```python
"""Тесты сервис-слоя — изоляция от Streamlit, сигнатуры, делегирование (FUND-02)."""
import importlib
import inspect
import sys
from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

from config import Config


def test_no_streamlit_import():
    """services.pipeline_service не импортирует streamlit ни прямо ни транзитивно."""
    # Убедиться что streamlit не попал в модуль через транзитивный импорт
    # Сохраняем состояние до теста
    before = set(sys.modules.keys())

    import services.pipeline_service as ps

    # Получаем исходный код — ищем 'streamlit' в тексте
    import inspect as _inspect
    source = _inspect.getsource(ps)
    assert "streamlit" not in source, (
        "Найден 'streamlit' в исходном коде services/pipeline_service.py. "
        "Этот файл должен работать без Streamlit."
    )


def test_process_archive_signature():
    """pipeline_service.process_archive имеет правильную сигнатуру."""
    from services import pipeline_service
    sig = inspect.signature(pipeline_service.process_archive)
    params = set(sig.parameters.keys())
    required = {"source_dir", "config"}
    assert required.issubset(params), (
        f"Не хватает параметров: {required - params}. Найдены: {params}"
    )


def test_registry_get_contracts():
    """registry_service.get_all_contracts(db) делегирует в db.get_all_results()."""
    from services import registry_service
    mock_db = MagicMock()
    mock_db.get_all_results.return_value = [{"id": 1, "filename": "test.pdf"}]

    result = registry_service.get_all_contracts(mock_db)

    mock_db.get_all_results.assert_called_once()
    assert result == [{"id": 1, "filename": "test.pdf"}]
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_service_layer.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл `tests/test_service_layer.py` содержит 3 test-функции
    - Все 3 теста падают с ImportError (services/ ещё не создан) — RED state
  </acceptance_criteria>
  <done>3 теста в RED state.</done>
</task>

<task type="auto">
  <name>Task 2: Создать services/ пакет, рефакторить ai_extractor.py, обновить main.py</name>
  <files>
    services/__init__.py,
    services/pipeline_service.py,
    services/registry_service.py,
    modules/ai_extractor.py,
    main.py
  </files>
  <read_first>
    - controller.py (ПОЛНОСТЬЮ — сигнатура process_archive, все параметры)
    - modules/ai_extractor.py (ПОЛНОСТЬЮ — понять что именно рефакторится)
    - modules/reporter.py (строки 1-30 — сигнатура generate_report)
    - main.py (строки 1-100, затем поиск "Controller" и "process_archive" — найти все вызовы)
    - tests/test_service_layer.py (понять что тестируется)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 3, Pattern 4)
  </read_first>
  <action>
**Шаг 1: Создать `services/__init__.py`** — пустой файл с комментарием:
```python
"""Сервис-слой ЮрТэг — бизнес-логика без зависимости от Streamlit UI."""
```

**Шаг 2: Создать `services/pipeline_service.py`**:
```python
"""Сервис обработки архива документов.

# NO import streamlit — этот файл должен работать без UI.
# Намеренное ограничение: вызывается Telegram-ботом, CLI, тестами.
"""
from pathlib import Path
from typing import Callable, Optional

from config import Config
from controller import Controller


def process_archive(
    source_dir: Path,
    config: Config,
    grouping: str = "both",
    force_reprocess: bool = False,
    on_progress: Optional[Callable[[int, int, str], None]] = None,
    on_file_done: Optional[Callable] = None,
    output_dir_override: Optional[Path] = None,
) -> dict:
    """Единая точка входа для обработки архива документов.

    Вызывается Streamlit UI, Telegram-ботом, CLI и тестами.
    Возвращает: dict(total, done, errors, skipped, output_dir, report_path)

    Args:
        source_dir: папка с договорами
        config: конфиг приложения
        grouping: режим группировки ("type" | "counterparty" | "both")
        force_reprocess: игнорировать кэш, обработать все файлы заново
        on_progress: callback(current, total, message) для прогресс-бара
        on_file_done: callback(result) после обработки каждого файла
        output_dir_override: принудительная выходная папка (облачный режим)
    """
    ctrl = Controller(config)
    return ctrl.process_archive(
        source_dir=source_dir,
        grouping=grouping,
        force_reprocess=force_reprocess,
        on_progress=on_progress,
        on_file_done=on_file_done,
        output_dir_override=output_dir_override,
    )
```

**Шаг 3: Создать `services/registry_service.py`**:
```python
"""Сервис работы с реестром документов.

# NO import streamlit — этот файл должен работать без UI.
"""
from pathlib import Path

from modules.database import Database
from modules.reporter import generate_report as _generate_report


def get_all_contracts(db: Database) -> list[dict]:
    """Все обработанные договоры из базы данных."""
    return db.get_all_results()


def generate_report(db: Database, output_dir: Path) -> Path:
    """Генерировать Excel-реестр. Возвращает путь к созданному файлу."""
    all_data = db.get_all_results()
    return _generate_report(all_data, output_dir)
```

**Шаг 4: Рефакторинг `modules/ai_extractor.py`**:

Изменить сигнатуру `extract_metadata()`:
```python
# БЫЛО:
def extract_metadata(anonymized_text: str, config: Config) -> ContractMetadata:

# СТАЛО:
def extract_metadata(
    anonymized_text: str,
    config: Config,
    provider: "LLMProvider | None" = None,
    fallback_provider: "LLMProvider | None" = None,
) -> ContractMetadata:
```

Добавить импорт в начало файла (после существующих):
```python
from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from providers.base import LLMProvider
```

Внутри `extract_metadata()` добавить в начало тела функции (до `text = anonymized_text[:30_000]`):
```python
# Если провайдер не передан — создать через старый путь для обратной совместимости
# (удалить в Phase 2 после полного перехода main.py на providers/)
_legacy_mode = provider is None
```

Внутри `_ai_task` в `controller.py` пока оставить как есть — рефакторинг controller.py выходит за рамки этого плана.

Фактически для `extract_metadata` пока достаточно добавить параметры с `None` как дефолтом и оставить внутреннюю логику неизменной (provider игнорируется если None, используется старый `_try_model`). Полный wire-up provider в controller — Phase 2 или отдельный spike.

**Шаг 5: Обновить `main.py`**:

Найти в main.py все места где создаётся Controller и вызывается process_archive.

Добавить в секцию импортов (после `from controller import Controller`):
```python
from services import pipeline_service
```

Найти паттерн вида (будет где-то в обработчике кнопки "Обработать"):
```python
ctrl = Controller(config)
results = ctrl.process_archive(source_dir=..., ...)
```

Заменить на:
```python
results = pipeline_service.process_archive(
    source_dir=...,
    config=config,
    ...  # остальные параметры те же
)
```

ВАЖНО: Читать main.py полностью перед изменением — найти ВСЕ вызовы Controller/process_archive. Строки с `from controller import Controller` оставить пока (controller.py используется напрямую pipeline_service).
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -m pytest tests/test_service_layer.py -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from services import pipeline_service"` выполняется без ошибок
    - `grep "import streamlit" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/pipeline_service.py` — НЕ выдаёт ничего
    - `grep "import streamlit" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/services/registry_service.py` — НЕ выдаёт ничего
    - `grep "pipeline_service.process_archive" /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/main.py` — выдаёт минимум 1 строку
    - `grep "provider: " /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg/modules/ai_extractor.py` — выдаёт строку с сигнатурой
    - `python -m pytest tests/test_service_layer.py -v` — все 3 теста GREEN
  </acceptance_criteria>
  <done>
    - test_no_streamlit_import PASS
    - test_process_archive_signature PASS
    - test_registry_get_contracts PASS
    - main.py вызывает pipeline_service.process_archive вместо Controller напрямую
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg
python -m pytest tests/test_service_layer.py -v
# Дополнительная проверка: импорт без UI
python -c "from services import pipeline_service, registry_service; print('OK — нет Streamlit')"
```
</verification>

<success_criteria>
- `python -m pytest tests/test_service_layer.py -v` — 3/3 PASSED
- services/ содержит __init__.py, pipeline_service.py, registry_service.py
- Ни в одном файле services/ нет строки `import streamlit`
- main.py использует pipeline_service.process_archive()
- extract_metadata() принимает опциональные provider и fallback_provider параметры
</success_criteria>

<output>
После завершения создать `.planning/phases/01-infrastructure/01-03-SUMMARY.md`
</output>
