---
phase: 38-cross-scope-config-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06

must_haves:
  truths:
    - "Config(llama_server_port=-1) исправляет порт до 8080, не падает"
    - "Config(active_provider='ollama').active_model возвращает 'local', не 'glm-4.7'"
    - "telegram_chat_id принимает None и 0 как 'не привязан'"
    - "Config(validation_mode='invalid') исправляет до 'off', не падает"
    - "load_settings() при повреждённом JSON логирует ошибку и возвращает {}"
    - "save_setting() защищена lock от гонки двух потоков"
    - "settings.json получает chmod 0o600 при записи (Unix)"
  artifacts:
    - path: "config.py"
      provides: "Полностью hardened Config dataclass"
      contains: "__post_init__"
    - path: "config.py"
      provides: "active_model property с provider-aware логикой"
      contains: "active_provider"
    - path: "config.py"
      provides: "Atomic save_setting с lock"
      contains: "_settings_lock"
  key_links:
    - from: "config.py Config.__post_init__"
      to: "load_settings() / __main__"
      via: "Config(**saved_settings) при старте"
      pattern: "__post_init__"
    - from: "config.py save_setting"
      to: "app/pages/settings.py"
      via: "save_setting(key, value) из UI"
      pattern: "_settings_lock"
---

<objective>
Сделать Config безопасным: добавить __post_init__ с graceful validation, исправить active_model property, сделать telegram_chat_id Optional, добавить Literal для validation_mode, заменить bare except в load_settings, добавить lock на save_setting, chmod 0o600 на settings.json.

Purpose: Config — единственный файл без защиты от невалидных данных. Старый settings.json или опечатка — и приложение падает при старте. Phase 39 (Provider Cleanup) зависит от корректного active_model.
Output: Полностью hardened config.py — Config validates gracefully, save_setting thread-safe, settings.json protected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-cross-scope-config-hardening/38-CONTEXT.md

<interfaces>
<!-- Текущий config.py — полное состояние перед изменениями -->

```python
# Текущий active_model (НЕПРАВИЛЬНО — hardcode):
@property
def active_model(self) -> str:
    return "glm-4.7"

# Текущий telegram_chat_id (нужно Optional):
telegram_chat_id: int = 0      # 0 = не привязан → нужно Optional[int] = None

# Текущий validation_mode (нет проверки):
validation_mode: str = "off"  # "off" | "selective" | "full" → нужно Literal + __post_init__

# Текущий load_settings (bare except):
def load_settings() -> dict:
    try:
        if _SETTINGS_FILE.exists():
            return json.loads(_SETTINGS_FILE.read_text(encoding="utf-8"))
    except Exception:    # ← bare except — нужно конкретные
        pass
    return {}

# Текущий save_setting (нет lock, нет chmod):
def save_setting(key: str, value) -> None:
    s = load_settings()
    s[key] = value
    _SETTINGS_FILE.parent.mkdir(parents=True, exist_ok=True)
    _SETTINGS_FILE.write_text(json.dumps(s, ...), encoding="utf-8")
    # ← нет lock, нет chmod
```

Импорты которые нужно добавить:
- `import threading` (для Lock)
- `import logging` (для logger в __post_init__)
- `import os` (для chmod)
- `from typing import Literal` (для validation_mode)

Импорты которые уже есть:
- `import json`
- `from dataclasses import dataclass, field`
- `from pathlib import Path`
- `from typing import Optional`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config.__post_init__ + active_model fix + telegram_chat_id + validation_mode</name>
  <read_first>
    - config.py (весь файл — текущее состояние dataclass)
  </read_first>
  <files>config.py</files>
  <behavior>
    - Config(active_provider="unknown_value") → active_provider == "ollama" (graceful fallback, не raise)
    - Config(active_provider="ollama").active_model == "local" (не "glm-4.7")
    - Config(active_provider="openrouter").active_model == config.model_fallback
    - Config(active_provider="zai").active_model == "glm-4.7"
    - Config(llama_server_port=-1) → llama_server_port == 8080 (graceful fix)
    - Config(llama_server_port=99999) → llama_server_port == 8080
    - Config(validation_mode="typo") → validation_mode == "off"
    - Config(confidence_high=0.3, confidence_low=0.7) → raises ValueError (логически невозможно)
    - Config().telegram_chat_id is None (дефолт Optional[int] = None)
  </behavior>
  <action>
**Шаг 1: Обновить импорты** — добавить в начало файла config.py (после `from typing import Optional`):

```python
import logging
import os
import threading
from typing import Literal, Optional

logger = logging.getLogger(__name__)
```

(Убрать `Optional` из уже существующего import если там конфликт — заменить на объединённый)

**Шаг 2: Изменить telegram_chat_id** с `int = 0` на `Optional[int] = None`:

```python
telegram_chat_id: Optional[int] = None  # привязанный Telegram chat_id (None = не привязан)
```

**Шаг 3: Изменить validation_mode** с `str = "off"` на `Literal["off", "selective", "full"] = "off"`:

```python
validation_mode: Literal["off", "selective", "full"] = "off"
```

**Шаг 4: Добавить `__post_init__` в класс Config** (после поля `output_folder_name`, перед `@property active_model`):

```python
def __post_init__(self) -> None:
    """Graceful validation — исправляет невалидные значения, не падает."""
    _VALID_PROVIDERS = {"zai", "openrouter", "ollama"}
    if self.active_provider not in _VALID_PROVIDERS:
        logger.warning("Неизвестный провайдер %r, использую ollama", self.active_provider)
        self.active_provider = "ollama"

    if self.fallback_provider not in _VALID_PROVIDERS:
        logger.warning("Неизвестный fallback провайдер %r, использую zai", self.fallback_provider)
        self.fallback_provider = "zai"

    if not (0 < self.llama_server_port <= 65535):
        logger.warning("Неверный порт %d, использую 8080", self.llama_server_port)
        self.llama_server_port = 8080

    if self.validation_mode not in {"off", "selective", "full"}:
        logger.warning("Неверный validation_mode %r, использую off", self.validation_mode)
        self.validation_mode = "off"

    if self.max_workers < 1:
        logger.warning("max_workers < 1, использую 1", )
        self.max_workers = 1

    # Только raise для логически невозможных состояний — не могут быть в реальном settings.json
    if self.confidence_high <= self.confidence_low:
        raise ValueError(
            f"confidence_high ({self.confidence_high}) должен быть > confidence_low ({self.confidence_low})"
        )
```

**Шаг 5: Исправить `active_model` property** — заменить хардкод на provider-aware логику:

```python
@property
def active_model(self) -> str:
    """Возвращает имя модели для текущего active_provider."""
    if self.active_provider == "ollama":
        return "local"
    elif self.active_provider == "openrouter":
        return self.model_fallback
    return "glm-4.7"
```
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
from config import Config
import traceback

# Тест 1: graceful fallback для неизвестного провайдера
c = Config(active_provider='legacy_local')
assert c.active_provider == 'ollama', f'Expected ollama, got {c.active_provider}'

# Тест 2: active_model по провайдеру
assert Config(active_provider='ollama').active_model == 'local', 'ollama должен давать local'
assert Config(active_provider='zai').active_model == 'glm-4.7', 'zai должен давать glm-4.7'

# Тест 3: graceful fallback для плохого порта
c = Config(llama_server_port=-1)
assert c.llama_server_port == 8080, f'Expected 8080, got {c.llama_server_port}'

# Тест 4: telegram_chat_id Optional
c = Config()
assert c.telegram_chat_id is None, f'Expected None, got {c.telegram_chat_id}'

# Тест 5: raise для confidence_high <= confidence_low
try:
    Config(confidence_high=0.3, confidence_low=0.7)
    assert False, 'должен был поднять ValueError'
except ValueError:
    pass

print('OK: все тесты Config прошли')
" && pytest tests/ -x -q -k "config" 2>/dev/null || pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Config(active_provider="unknown")` → не поднимает исключений, active_provider == "ollama"
    - `Config(active_provider="ollama").active_model` == "local" (не "glm-4.7")
    - `Config(active_provider="openrouter").active_model` == config.model_fallback строка
    - `Config(active_provider="zai").active_model` == "glm-4.7"
    - `Config(llama_server_port=-1).llama_server_port` == 8080
    - `Config().telegram_chat_id` is None
    - `Config(confidence_high=0.3, confidence_low=0.7)` raises ValueError
    - `Config(validation_mode="bad").validation_mode` == "off"
    - Существующие тесты зелёные
  </acceptance_criteria>
  <done>Config dataclass hardened: __post_init__ с graceful validation, active_model provider-aware, telegram_chat_id Optional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: load_settings bare except fix + save_setting lock + chmod 0o600</name>
  <read_first>
    - config.py (строки 149-172 — load_settings и save_setting текущие)
  </read_first>
  <files>config.py</files>
  <behavior>
    - load_settings() при JSONDecodeError — логирует предупреждение, возвращает {}
    - load_settings() при OSError — логирует предупреждение, возвращает {}
    - save_setting("key1", "v1") и save_setting("key2", "v2") из двух потоков одновременно — оба ключа присутствуют в файле после завершения
    - save_setting создаёт settings.json с правами 0o600 на Unix
  </behavior>
  <action>
**Шаг 1: Добавить module-level lock** (после строки `_SETTINGS_FILE = Path.home() / ...`):

```python
_settings_lock = threading.Lock()
```

**Шаг 2: Исправить load_settings** — заменить bare `except Exception` на конкретные:

```python
def load_settings() -> dict:
    """Загружает персистентные настройки из ~/.yurteg/settings.json."""
    try:
        if _SETTINGS_FILE.exists():
            return json.loads(_SETTINGS_FILE.read_text(encoding="utf-8"))
    except json.JSONDecodeError as e:
        logger.warning("settings.json повреждён: %s — используются дефолты", e)
    except OSError as e:
        logger.warning("Не удалось прочитать settings.json: %s — используются дефолты", e)
    return {}
```

**Шаг 3: Переписать save_setting** — добавить lock + chmod:

```python
def save_setting(key: str, value) -> None:
    """Сохраняет один ключ в ~/.yurteg/settings.json (merge, thread-safe)."""
    with _settings_lock:
        s = load_settings()
        s[key] = value
        _SETTINGS_FILE.parent.mkdir(parents=True, exist_ok=True)
        _SETTINGS_FILE.write_text(json.dumps(s, ensure_ascii=False, indent=2), encoding="utf-8")
        # Ограничить права на Unix: только владелец может читать/писать
        try:
            os.chmod(_SETTINGS_FILE, 0o600)
        except OSError:
            pass  # Windows не поддерживает POSIX chmod — игнорировать молча
```

Убедиться что `import os` и `import threading` и `logger = logging.getLogger(__name__)` добавлены в начале файла (из Task 1). Если Task 1 уже их добавил — не дублировать.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import tempfile, threading, json, os, sys
from pathlib import Path

# Патчим _SETTINGS_FILE на временный файл
import config as cfg

with tempfile.TemporaryDirectory() as tmp:
    test_file = Path(tmp) / 'settings.json'
    orig_file = cfg._SETTINGS_FILE
    cfg._SETTINGS_FILE = test_file

    try:
        # Тест 1: load_settings при отсутствии файла
        result = cfg.load_settings()
        assert result == {}, f'Expected {{}}, got {result}'

        # Тест 2: save_setting создаёт файл
        cfg.save_setting('provider', 'ollama')
        assert test_file.exists(), 'Файл не создан'
        data = json.loads(test_file.read_text())
        assert data['provider'] == 'ollama', f'Bad data: {data}'

        # Тест 3: chmod (только Unix)
        if sys.platform != 'win32':
            mode = oct(os.stat(test_file).st_mode)[-3:]
            assert mode == '600', f'Expected 600, got {mode}'

        # Тест 4: concurrent write — оба ключа присутствуют
        barrier = threading.Barrier(2)
        errors = []
        def write_key(k, v):
            barrier.wait()
            try:
                cfg.save_setting(k, v)
            except Exception as e:
                errors.append(e)

        t1 = threading.Thread(target=write_key, args=('key1', 'val1'))
        t2 = threading.Thread(target=write_key, args=('key2', 'val2'))
        t1.start(); t2.start()
        t1.join(); t2.join()

        assert not errors, f'Errors: {errors}'
        final = cfg.load_settings()
        assert 'key1' in final and 'key2' in final, f'Keys lost: {final}'

        print('OK: load_settings + save_setting thread-safe + chmod 600')
    finally:
        cfg._SETTINGS_FILE = orig_file
"</automated>
  </verify>
  <acceptance_criteria>
    - `load_settings()` при JSONDecodeError возвращает `{}` (не поднимает)
    - `load_settings()` при OSError возвращает `{}` (не поднимает)
    - `save_setting()` обёрнута в `with _settings_lock`
    - После `save_setting()` на Unix — `oct(os.stat(settings_file).st_mode)[-3:] == '600'`
    - Concurrent save двух ключей → оба ключа в файле (нет потери данных)
    - `grep "except Exception" config.py` возвращает 0 результатов (нет bare except)
    - `pytest tests/ -x -q` зелёный
  </acceptance_criteria>
  <done>config.py полностью hardened: bare except убран, save_setting thread-safe с lock, settings.json защищён chmod 0o600.</done>
</task>

</tasks>

<verification>
После обоих тасков запустить финальную проверку:

```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# Комплексная проверка всех CONF требований
python -c "
from config import Config, load_settings, save_setting

# CONF-01: __post_init__ валидация
c = Config(active_provider='bad', llama_server_port=-1, validation_mode='wrong')
assert c.active_provider == 'ollama'
assert c.llama_server_port == 8080
assert c.validation_mode == 'off'
print('CONF-01: __post_init__ graceful validation OK')

# CONF-02: active_model
assert Config(active_provider='ollama').active_model == 'local'
assert Config(active_provider='zai').active_model == 'glm-4.7'
print('CONF-02: active_model provider-aware OK')

# CONF-03: telegram_chat_id Optional
assert Config().telegram_chat_id is None
print('CONF-03: telegram_chat_id Optional[int] = None OK')

# CONF-04: validation_mode Literal
try:
    from typing import get_args, get_type_hints
    hints = get_type_hints(Config)
    print('CONF-04: validation_mode annotation:', hints.get('validation_mode'))
except Exception as e:
    print('CONF-04: annotation check skipped:', e)

# CONF-05: bare except
import subprocess
result = subprocess.run(['grep', '-n', 'except Exception:', 'config.py'], capture_output=True, text=True)
assert result.stdout.strip() == '', f'CONF-05 FAIL: bare except найден:\n{result.stdout}'
print('CONF-05: no bare except in load_settings OK')

# CONF-06: chmod проверить через наличие os.chmod в коде
result = subprocess.run(['grep', '-n', 'chmod', 'config.py'], capture_output=True, text=True)
assert 'chmod' in result.stdout, 'CONF-06 FAIL: chmod не найден в config.py'
print('CONF-06: chmod 0o600 present OK')

print()
print('All CONF requirements satisfied')
"

# Тесты
pytest tests/ -x -q
```
</verification>

<success_criteria>
1. `Config(active_provider="legacy")` не поднимает — исправляет до "ollama"
2. `Config(active_provider="ollama").active_model` == "local"
3. `Config().telegram_chat_id` is None
4. `Config(validation_mode="typo").validation_mode` == "off"
5. `grep "except Exception:" config.py` → 0 результатов
6. `save_setting()` содержит `with _settings_lock`
7. `os.chmod(_SETTINGS_FILE, 0o600)` вызывается при save_setting
8. `pytest tests/ -x -q` зелёный
</success_criteria>

<output>
После завершения создать `.planning/phases/38-cross-scope-config-hardening/38-02-SUMMARY.md`

Формат:
- CONF-01...CONF-06 статус каждого
- Конкретные изменения в config.py (какие строки изменены)
- Результат pytest

Уведомить через mcp_agent_mail Phase 39 (Provider Cleanup) о готовности Config — Phase 39 зависит от active_model и активного провайдера.
</output>
