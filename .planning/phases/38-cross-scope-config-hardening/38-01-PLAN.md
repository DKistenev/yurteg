---
phase: 38-cross-scope-config-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - services/lifecycle_service.py
  - modules/database.py
autonomous: true
requirements:
  - XSCOPE-04
  - XSCOPE-05
  - XSCOPE-06

must_haves:
  truths:
    - "from config import APP_VERSION работает, возвращает строку вида '0.9.x'"
    - "STATUS_LABELS['active'] содержит 4-й элемент — строку Tailwind-классов"
    - "database.get_contract_by_id() и get_all_results() задокументированы как dict-only в аннотациях"
  artifacts:
    - path: "config.py"
      provides: "APP_VERSION константа"
      contains: "APP_VERSION"
    - path: "services/lifecycle_service.py"
      provides: "STATUS_LABELS с css_class"
      contains: "css_class"
    - path: "modules/database.py"
      provides: "dict-only типы возврата"
      contains: "list[dict]"
  key_links:
    - from: "config.py"
      to: "app/components/header.py (footer)"
      via: "APP_VERSION import"
      pattern: "APP_VERSION"
    - from: "services/lifecycle_service.py"
      to: "app/components/split_panel.py"
      via: "STATUS_LABELS import"
      pattern: "STATUS_LABELS"
---

<objective>
Разблокировать VioletRiver Phase 36: добавить APP_VERSION в config.py, расширить STATUS_LABELS в lifecycle_service.py четвёртым элементом css_class, верифицировать dict-only возврат из database.py.

Purpose: Phase 36 (VioletRiver) заблокирована на этих трёх контрактах. После этого плана VioletRiver может подключить STATUS_LABELS, APP_VERSION и убрать dict(doc) cast.
Output: APP_VERSION = "1.0.0-beta", STATUS_LABELS с Tailwind css_class, подтверждение dict-only в database.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-cross-scope-config-hardening/38-CONTEXT.md

<interfaces>
<!-- Текущее состояние контрактов, которые нужно изменить -->

Из config.py (строки 1-172):
```python
# Нет APP_VERSION — нужно добавить

@property
def active_model(self) -> str:
    return "glm-4.7"   # хардкод — будет исправлено в Plan 02

def load_settings() -> dict:   # bare except — будет исправлено в Plan 02
def save_setting(key: str, value) -> None:  # нет lock — будет исправлено в Plan 02
```

Из lifecycle_service.py (строки 17-23):
```python
STATUS_LABELS = {
    "active":      ("\u2714", "Действует",         "#22c55e"),
    "expiring":    ("\u26a0", "Заканчивается",     "#f59e0b"),
    "expired":     ("\u2717", "Закончился",        "#ef4444"),
    "unknown":     ("?", "Нет даты",               "#9ca3af"),
    "negotiation": ("~", "На согласовании",        "#8b5cf6"),
}
# Нужно добавить 4-й элемент css_class
```

Из modules/database.py:
```python
def get_contract_by_id(self, contract_id: int) -> dict | None:
    # Уже возвращает dict — row_factory=sqlite3.Row + dict(row)

def get_all_results(self) -> list[dict]:
    # Уже возвращает list[dict] — dict(row) для каждой строки

def get_stats(self) -> dict:
    # Уже возвращает dict — явное {total:, done:, ...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить APP_VERSION в config.py и расширить STATUS_LABELS css_class</name>
  <read_first>
    - config.py (весь файл — понять структуру и где вставить константу)
    - services/lifecycle_service.py (строки 1-25 — текущий STATUS_LABELS)
  </read_first>
  <files>config.py, services/lifecycle_service.py</files>
  <action>
**config.py:** Добавить константу `APP_VERSION` в самое начало файла, до класса `Config`, после импортов:

```python
APP_VERSION = "1.0.0-beta"
```

Это строка, не часть dataclass — просто module-level константа. VioletRiver будет делать `from config import APP_VERSION`.

**services/lifecycle_service.py:** Расширить каждый элемент STATUS_LABELS четвёртым значением — строкой Tailwind CSS-классов для badge-компонентов:

```python
STATUS_LABELS = {
    "active":      ("\u2714", "Действует",      "#22c55e", "bg-green-100 text-green-800"),
    "expiring":    ("\u26a0", "Заканчивается",  "#f59e0b", "bg-amber-100 text-amber-800"),
    "expired":     ("\u2717", "Закончился",     "#ef4444", "bg-red-100 text-red-800"),
    "unknown":     ("?",      "Нет даты",       "#9ca3af", "bg-gray-100 text-gray-600"),
    "negotiation": ("~",      "На согласовании","#8b5cf6", "bg-violet-100 text-violet-800"),
}
```

Четвёртый элемент — Tailwind-строка для background + text цвета badge. Hex в третьем элементе оставить — используется в AG Grid (не Tailwind-среда).

Не трогать ничего другого в lifecycle_service.py — только STATUS_LABELS dict.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "from config import APP_VERSION; assert isinstance(APP_VERSION, str) and APP_VERSION.startswith('1.'), f'Bad APP_VERSION: {APP_VERSION}'; from services.lifecycle_service import STATUS_LABELS; assert len(STATUS_LABELS['active']) == 4, 'active missing css_class'; assert 'bg-' in STATUS_LABELS['active'][3], 'css_class not Tailwind'; print('OK: APP_VERSION =', APP_VERSION, '| STATUS_LABELS[active][3] =', STATUS_LABELS['active'][3])"</automated>
  </verify>
  <acceptance_criteria>
    - `from config import APP_VERSION` работает, возвращает строку начинающуюся с "1."
    - `STATUS_LABELS["active"]` — кортеж длиной 4
    - `STATUS_LABELS["active"][3]` содержит "bg-" (Tailwind-класс)
    - Все 5 статусов имеют 4-й элемент: active, expiring, expired, unknown, negotiation
    - Hex в [2] не изменён (не ломает AG Grid)
    - `pytest -x -q` зелёный после изменений
  </acceptance_criteria>
  <done>APP_VERSION импортируется из config, STATUS_LABELS содержит css_class для всех 5 статусов.</done>
</task>

<task type="auto">
  <name>Task 2: Верифицировать dict-only возврат database.py и добавить type hints</name>
  <read_first>
    - modules/database.py (строки 420-520 — get_contract_by_id, get_all_results, get_stats)
  </read_first>
  <files>modules/database.py</files>
  <action>
Проверить что все публичные read-методы возвращают `dict`, а не `sqlite3.Row`. По анализу уже так, но нужно убедиться и явно задокументировать.

**Проверить следующие методы:**

1. `get_contract_by_id(self, contract_id: int) -> dict | None` — уже есть `dict(row)`, возвращает dict. Убедиться что тип аннотации `dict | None` стоит.

2. `get_all_results(self) -> list[dict]` — уже есть `dict(row)` в цикле. Убедиться что тип аннотации `list[dict]` стоит.

3. `get_stats(self) -> dict` — уже возвращает literal dict с именованными ключами. Аннотация `dict` уже стоит.

**Если тип аннотации уже правильный — не трогать код.** Если annotation `sqlite3.Row` или отсутствует — добавить `dict | None` / `list[dict]`.

**Добавить docstring-уточнение в get_all_results**, если его нет:
```python
def get_all_results(self) -> list[dict]:
    """Возвращает все записи как list[dict] с десериализованными JSON-полями.
    Никогда не возвращает sqlite3.Row — safe для прямого использования в UI."""
```

Не добавлять lock на read-методы — это Phase 41 (Thread Safety). Задача только про тип возврата.
  </action>
  <verify>
    <automated>cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg && python -c "
import sqlite3, tempfile
from pathlib import Path
from modules.database import Database

with tempfile.TemporaryDirectory() as tmp:
    db = Database(Path(tmp) / 'test.db')
    # get_stats возвращает dict
    stats = db.get_stats()
    assert isinstance(stats, dict), f'get_stats returned {type(stats)}'
    # get_all_results возвращает list[dict]
    rows = db.get_all_results()
    assert isinstance(rows, list), f'get_all_results returned {type(rows)}'
    # get_contract_by_id возвращает None (пустая БД)
    result = db.get_contract_by_id(999)
    assert result is None, f'Expected None, got {result}'
    print('OK: all read methods return dict/list[dict]/None')
"</automated>
  </verify>
  <acceptance_criteria>
    - `get_contract_by_id()` — аннотация `dict | None`, тело возвращает `dict(row)` или `None`
    - `get_all_results()` — аннотация `list[dict]`, тело возвращает list с `dict(row)` элементами
    - `get_stats()` — аннотация `dict`, тело возвращает literal dict
    - Нигде не возвращается голый `sqlite3.Row`
    - `pytest tests/test_database.py -x -q` зелёный
  </acceptance_criteria>
  <done>database.py задокументирован как dict-only. VioletRiver может убрать защитный dict(doc) cast в registry.py.</done>
</task>

</tasks>

<verification>
После выполнения плана запустить:

```bash
cd /Users/danilakistenev/Downloads/Личное/ЮР\ тэг/yurteg

# Верификация всех трёх XSCOPE требований
python -c "
from config import APP_VERSION
print('XSCOPE-04 APP_VERSION:', APP_VERSION)

from services.lifecycle_service import STATUS_LABELS
for k, v in STATUS_LABELS.items():
    assert len(v) == 4, f'XSCOPE-05 FAIL: {k} has {len(v)} elements'
print('XSCOPE-05 STATUS_LABELS css_class: OK')

from modules.database import Database
import inspect
sig = inspect.signature(Database.get_all_results)
print('XSCOPE-06 get_all_results return annotation:', sig.return_annotation)
"

pytest -x -q
```
</verification>

<success_criteria>
1. `from config import APP_VERSION` → строка вида "1.0.0-beta"
2. `STATUS_LABELS["terminated"]` не существует — но все 5 статусов имеют 4-й элемент css_class с Tailwind-классами
3. `db.get_contract_by_id(id)` и `db.get_all_results()` никогда не возвращают `sqlite3.Row`
4. `pytest -x -q` зелёный (252+ тестов)
</success_criteria>

<output>
После завершения создать `.planning/phases/38-cross-scope-config-hardening/38-01-SUMMARY.md`

Формат:
- Что сделано (конкретные изменения построчно)
- APP_VERSION значение
- STATUS_LABELS css_class значения для всех 5 статусов
- Статус pytest
- Готовность для VioletRiver Phase 36: можно ли подключать STATUS_LABELS и APP_VERSION

Уведомить через mcp_agent_mail VioletRiver о готовности XSCOPE-04, XSCOPE-05, XSCOPE-06 после коммита.
</output>
