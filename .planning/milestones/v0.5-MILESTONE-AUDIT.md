---
milestone: v0.5
audited: 2026-03-21
status: tech_debt
scores:
  requirements: 9/9
  phases: 3/3
  integration: 11/12
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 05-pipeline-local-llm
    items:
      - "SRVR-03 sequencing: _get_llama_manager() fires before persisted active_provider restored from settings.json — llama-server may start unnecessarily when user saved a cloud provider"
      - "verify_api_key() uses legacy _create_client path, does not call OllamaProvider.verify_key() — blocks local-model processing unless cloud API key present (pre-existing)"
---

# Milestone v0.5 — Re-Audit Report

## Phase Verification Status

| Phase | Status | Score | Requirements |
|-------|--------|-------|-------------|
| 4. Сервер и провайдер | ✓ Passed | 12/12 | SRVR-01..04, PROV-01..02 |
| 5. Пайплайн с локальной моделью | ✓ Passed | 3/3 | PROV-03, PROC-01, PROC-02 |
| 6. Проводка ai_extractor (gap closure) | ✓ Passed | 5/5 | SRVR-01, SRVR-02, PROV-01, PROC-01 |

All phases passed verification. Phase 6 gap closure confirmed — both critical bugs fixed.

## Cross-Phase Integration

### Bug Fixes Confirmed

**Bug 1 (provider routing):** FIXED — `_try_provider()` exists at ai_extractor.py:379, called when `provider is not None` (line 309). `OllamaProvider.complete()` is now invoked. Legacy `_try_model` path preserved for `provider=None`.

**Bug 2 (sanitize_metadata):** FIXED — `asdict(result)` converts ContractMetadata → dict (line 321), return value captured and rebuilt via `_json_to_metadata(sanitized)` (line 322). Applied in both primary and fallback paths.

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | VERIFICATION.md | SUMMARY | REQUIREMENTS.md | Integration | Final Status |
|--------|----------------|---------|-----------------|-------------|-------------|
| SRVR-01 | passed | listed | [x] Complete | WIRED — requests route to llama-server via _try_provider | **satisfied** |
| SRVR-02 | passed | listed | [x] Complete | WIRED — grammar at server startup via --grammar-file | **satisfied** |
| SRVR-03 | passed | listed | [x] Complete | PARTIAL — autostart uses hardcoded default, not persisted choice | **satisfied** (tech debt) |
| SRVR-04 | passed | listed | [x] Complete | WIRED | **satisfied** |
| PROV-01 | passed | listed | [x] Complete | WIRED — OllamaProvider.complete() called via _try_provider | **satisfied** |
| PROV-02 | passed | listed | [x] Complete | WIRED | **satisfied** |
| PROV-03 | passed | listed | [x] Complete | WIRED — sidebar toggle with file persistence | **satisfied** |
| PROC-01 | passed | listed | [x] Complete | WIRED — sanitize_metadata(asdict(result)) with return captured | **satisfied** |
| PROC-02 | passed | listed | [x] Complete | WIRED — AnonymizedText(replacements={}) | **satisfied** |

**Satisfied:** 9/9 | **Partial:** 0/9 | **Unsatisfied:** 0/9

## E2E Flow Status

| Flow | Status | Notes |
|------|--------|-------|
| Local LLM extraction | ✓ OK | provider routing fixed in Phase 6 |
| Anonymization skip | ✓ OK | controller.py conditional |
| Provider toggle | ✓ OK | sidebar + file persistence |
| Cloud fallback | ✓ OK | selectbox → zai/openrouter |

## Tech Debt

### Phase 5: Pipeline
1. **SRVR-03 autostart sequencing** — `_get_llama_manager()` uses compile-time `Config()` default, not persisted `settings.json`. Llama-server may start unnecessarily when user saved a cloud provider. Fix: read settings.json before autostart decision.
2. **verify_api_key legacy path** — `verify_api_key(config)` uses `_create_client()` (ZAI/OpenRouter only), doesn't check `OllamaProvider.verify_key()`. Blocks local-model processing without cloud API key. Pre-existing issue.

### Total: 2 items across 1 phase
